# R2-b2b2-DEP-02 (phase-146): the Gradle distribution is integrity-pinned here.
# The wrapper downloads ONLY this exact URL and refuses to use it unless its
# SHA-256 matches `distributionSha256Sum`. This is the committed source of truth
# for Gradle provisioning integrity; CI currently provisions 8.13 via
# `gradle/actions/setup-gradle` `gradle-version` (which overrides the wrapper) —
# passing `distribution-sha256-sum` to setup-gradle / switching CI to
# `./gradlew` (wrapper mode) is DEFERRED to phase-147 (workflow edits require
# user approval). Do NOT bump the version without regenerating the sum.
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.13-bin.zip
distributionSha256Sum=20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists