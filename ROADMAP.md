# NOTEFLOW — ANDROID KOTLIN ROADMAP & COMPETITIVE BENCHMARK AUDIT

> **LAST UPDATED:** AUGUST 2026
> **TARGET PLATFORM:** NATIVE ANDROID (KOTLIN 2.0+ & JETPACK COMPOSE / MATERIAL DESIGN 3)
> **ARCHITECTURE PRINCIPLE:** 100% OFFLINE-FIRST, LOCAL ENCRYPTION (AES-256-GCM + PBKDF2), OPEN STANDARD FORMATS (MARKDOWN, VECTOR STROKES, PDF, PNG)

---

## 🏆 COMPLETED FEATURES & IMPLEMENTATION LOG (DONE LIST)

ALL CORE ARCHITECTURE, SECURITY, CANVAS, TOUCH PRECISION, AND DOCUMENT FEATURES HAVE BEEN IMPLEMENTED, VERIFIED, AND AUDITED IN CODE.

### 1. CORE ARCHITECTURE & LOCAL PERSISTENCE
- [x] **ROOM DATABASE ENGINE**: FULL MULTI-TABLE DATABASE SCHEMA (`NOTEBOOKS`, `SECTIONS`, `PAGES`, `STROKES`) WITH TRANSACTIONAL CASCADING DELETES (`NOTEBOOK` → `SECTION` → `PAGE` → `STROKES`).
- [x] **AES-256-GCM ENCRYPTION**: FULL ENCRYPTION AT REST USING AES-256-GCM WITH 12-BYTE RANDOM IVS AND 128-BIT AUTHENTICATION TAGS.
- [x] **PBKDF2 KEY DERIVATION**: HIGH-SECURITY PBKDF2 KEY DERIVATION USING ≥600,000 ITERATIONS AND 256-BIT SALT.
- [x] **KEYSTORE DEK WRAPPING**: DATA ENCRYPTION KEYS (DEKS) WRAPPED AND STORED VIA `ANDROIDKEYSTORE`.
- [x] **TRANSACTIONAL BACKUP & RESTORE**: FULL JSON/BINARY BACKUP GENERATION WITH WAL (WRITE-AHEAD LOGGING) DATABASE CHECKPOINTING, ZIP-BOMB SIZE BOUNDS (50MB/FILE, 200MB TOTAL), AND ZIP-SLIP TRAVERSAL PROTECTION.

### 2. INK CANVAS, TOUCH PRECISION & WORLD-CLASS DIGITAL BRUSHES
- [x] **TOUCH COORDINATE ORIGIN ALIGNMENT**: FIXED CANVAS SCALING OFFSET BY EXPLICITLY SETTING `TRANSFORMORIGIN = TRANSFORMORIGIN(0F, 0F)` IN `GRAPHICSLAYER` SO INK STROKES ALIGN WITH USER TOUCH LOCATIONS.
- [x] **WORLD-CLASS BRUSH SUITE**: PEN, FOUNTAIN PEN (VELOCITY-MODULATED STROKE WIDTH), CHARCOAL PENCIL (STIPPLED GRAPHITE GRAIN SHADER), AIRBRUSH / SPRAY (RADIAL PARTICLE NOZZLE), MARKER (SEMI-TRANSPARENT WET LAYER), HIGHLIGHTER (35% ALPHA BLENDING), CALLIGRAPHIC BRUSH (ANGLE-OFFSET SIMULATION), ERASER, LINE, RECTANGLE, ELLIPSE, ARROW, AND TEXT TOOL.
- [x] **COLOR EYEDROPPER & PALETTE MANAGER**: REAL-TIME CANVAS PIXEL SAMPLING WITH FLOATING MAGNIFYING LOUPE OVERLAY SHOWING HEX CODES, AUTO-COLLECTING INTO THE DYNAMIC PALETTE MANAGER.
- [x] **S-PEN / STYLUS GUARD & PRESSURE SENSITIVITY**: PALM REJECTION PROTECTION TOGGLE AND PRESSURE SENSITIVITY TOGGLE INTEGRATED IN TOPAPPBAR AND CANVAS TOUCH SCOPES.
- [x] **INTERACTIVE CANVAS TEXT**: MODAL DIALOG FOR INPUTTING AND RENDERING CUSTOM CANVAS TEXT LABELS WITH EXACT POSITION PLACEMENT.
- [x] **CUSTOM THICKNESS & PALETTE**: CONTINUOUS THICKNESS SLIDER (1PT TO 32PT) AND A DYNAMIC MATERIAL 3 PALETTE.
- [x] **UNDO / REDO STACK**: DUAL STATE HISTORY STACKS BOUNDED TO A MAX OF 30 ITEMS FOR MEMORY SAFETY.
- [x] **PAPER TEMPLATES**: PERSISTENCE OF PAPER BACKGROUNDS (`BLANK`, `LINED`, `GRID`, `DOTS`) DIRECTLY SAVED TO `NOTEPAGEENTITY.TEMPLATE`.
- [x] **ADAPTIVE DARK MODE CANVAS**: COLOR INVERSION AND HIGH-CONTRAST STROKE TUNING FOR DARK THEME CANVASES (`#1E293B` PAPER, ADJUSTED GRID COLORS, AUTO-BRIGHTENED INK).

### 3. DOCUMENT ENGINE & MULTI-PAGE PDF
- [x] **NATIVE PDF RENDERER**: MULTI-PAGE PDF RENDERING USING `ANDROID.GRAPHICS.PDF.PDFRENDERER`.
- [x] **DUAL IMPORT MODES**: CHOICE TO IMPORT MULTI-PAGE PDFS AS A CONTINUOUS INFINITE CANVAS OR SPLIT INTO INDIVIDUAL NOTE PAGE ENTRIES.
- [x] **GESTURE PAN & PINCH-TO-ZOOM**: MULTI-TOUCH ZOOM (50% TO 300%) WITH COORDINATE TRANSFORMATION MAPPING.
- [x] **EXPORT ENGINE**: EXPORT ANNOTATED NOTE PAGES DIRECTLY AS HIGH-RESOLUTION PNG IMAGES OR PDF DOCUMENTS VIA ANDROID `FILEPROVIDER`.

### 4. SECURITY & BIOMETRICS
- [x] **HARDWARE BIOMETRIC GATE**: ANDROID `BIOMETRICPROMPT` INTEGRATION REQUIRING `SETUSERAUTHENTICATIONREQUIRED(TRUE)`.
- [x] **MASTER PASSWORD GATE**: MASTER PASSWORD UNLOCK FLOW WITH KEYSTORE KEY WRAPPING.
- [x] **SCREEN & WINDOW PROTECTION**: SYSTEM-WIDE `FLAG_SECURE` WINDOW FLAGS PREVENTING RECENT APPS PREVIEW LEAKS.
- [x] **AUTO-LOCK ON BACKGROUND**: AUTOMATIC LOCK TRIGGER WHEN THE APP ENTERS BACKGROUND STATE.
- [x] **HARDENED MANIFEST**: `ALLOWBACKUP="FALSE"` AND `DATAEXTRACTIONRULES` CONFIGURED TO PREVENT UNENCRYPTED DATA EXFILTRATION.

### 5. SEARCH, NAVIGATION, FLOATING TOOLBARS & CANVAS UX POLISH
- [x] **GLOBAL VAULT SEARCH & DEDICATED PANEL FILTERS**: REAL-TIME SEARCH ACROSS NOTEBOOKS, SECTIONS, AND PAGES WITH DEDICATED SEARCH FILTER BUTTONS IN NOTEBOOKS, SECTIONS, AND PAGES PANELS.
- [x] **COLLAPSIBLE / FLOATING DISTRACTION-FREE TOOLBARS**: AUTO-HIDING FLOATING DRAWING TOOLBAR WITH A 5-SLOT QUICK-SWITCH PEN PRESETS DOCK AND EXPAND/COLLAPSE BUTTON FOR 100% DISTRACTION-FREE CANVAS PAINTING.
- [x] **DRAGGABLE CANVAS STICKY NOTES**: FLOATING STICKY NOTE CARDS PLACED DIRECTLY ON THE CANVAS WITH CUSTOM COLOR SELECTOR (YELLOW, MINT, LAVENDER, PEACH, SKY BLUE), RICH TEXT CONTENT INPUT, AND TAP-TO-DELETE ACTION.
- [x] **CANVAS VIEWPORT MINIMAP**: COLLAPSIBLE THUMBNAIL OVERVIEW WIDGET IN THE BOTTOM-RIGHT CORNER SHOWING FULL CANVAS BOUNDS AND ACTIVE VIEWPORT FRAME DURING ZOOM/PAN.
- [x] **SINGLE-RUN INTERACTIVE TUTORIAL**: ONBOARDING TUTORIAL CONFIGURED TO LAUNCH STRICTLY ONCE ON THE USER'S VERY FIRST APP RUN, PERSISTED VIA `SHAREDPREFERENCES`.
- [x] **MULTI-PAGE IMPORT BACK BUTTON & SILENT FLOW**: ADDED EXPLICIT BACK BUTTON IN DOCUMENT/IMAGE/PDF SELECTOR DIALOG AND REMOVED ANNOYING IMPORT OUTBURST ANIMATIONS.
- [x] **TOUCH-ACCURATE PRECISION DRAWING**: SMOOTH QUAD-CURVE PATH CONSTRUCTION CONNECTING START TOUCH POINTS WITHOUT INITIAL VERTICAL DISPLACEMENT OR OFFSET.
- [x] **SYSTEM BACKHANDLER**: GUARD AGAINST ACCIDENTAL NOTE EXIT WHEN UNSAVED STROKES EXIST.
- [x] **DESTRUCTIVE ACTION GUARDS**: CONFIRMATION DIALOGS FOR PERMANENT NOTEBOOK/SECTION/PAGE DELETIONS.
- [x] **UNIT TESTING SUITE**: `ENCRYPTIONANDSERVICETEST.KT` VALIDATING AES-256 ENCRYPTION/DECRYPTION CYCLES, PBKDF2 KEY GENERATION, FILE EXTENSION PARSING, AND ENTITY DEFAULTS.

### 6. CONNECTED KNOWLEDGE VAULT (OBSIDIAN PKM ENGINE)
- [x] **WIKILINKS PARSING (`[[NOTE TITLE]]`)**: FULL REGEX PARSER EXTRACTING `[[PAGE TITLE]]` OR `[[PAGE TITLE|ALIAS]]` WITH INTERACTIVE CLICK TARGETS THAT NAVIGATE OR CREATE MISSING PAGES.
- [x] **FORCE-DIRECTED KNOWLEDGE GRAPH**: INTERACTIVE PHYSICS GRAPH CANVAS (COULOMB REPULSION & HOOKE ELASTICITY) RENDERING PAGES AS NODES AND WIKILINKS AS VECTOR EDGES WITH ZOOM/PAN AND LIVE SEARCH FILTER.
- [x] **BACKLINKS & UNLINKED MENTIONS INSPECTOR**: BOTTOM SHEET INSPECTOR TRACKING EXPLICIT INCOMING BACKLINKS AND UNLINKED PLAIN-TEXT MENTIONS WITH 1-TAP "LINKIFY" CONVERSION.
- [x] **HIERARCHICAL TAG EXPLORER (`#TAG/SUBTAG`)**: TREE EXPLORER TAB ORGANIZING NESTED SLASH-DELIMITED TAGS INTO AN EXPANDABLE FOLDER VIEW WITH NOTE COUNT INDICATORS AND GLOBAL VAULT FILTERING.
- [x] **DAILY NOTES / JOURNALING ACTION**: TOP BAR SHORTCUT CREATING OR OPENING DATE-STAMPED JOURNAL NOTES (`YYYY-MM-DD.MD`) PRE-POPULATED WITH DAILY GOALS, REFLECTIONS, AND TAGS.

### 7. TIME-SYNCED VOICE NOTES & MEDIA CANVAS EMBEDS
- [x] **TIME-SYNCED VOICE MEMO RECORDER**: ON-DEVICE AUDIO RECORDER (`VOICENOTEMANAGER`) INTEGRATED INTO CANVAS TOOLBAR, TIMESTAMPING VECTOR STROKES DRAWN DURING AUDIO RECORDING WITH STROKE TAP JUMP TO EXACT AUDIO MOMENT.
- [x] **AUDIO PLAYBACK CONTROLS & WAVEFORM**: EMBEDDED AUDIO CARD WITH VISUAL WAVEFORM SCRUBBING AND VARIABLE PLAYBACK SPEED CONTROLS (0.5X, 1.0X, 1.25X, 1.5X, 2.0X).
- [x] **INLINE MEDIA EMBED CARDS**: CANVAS CARDS FOR PHOTO ATTACHMENTS, CODE BLOCKS WITH SYNTAX HIGHLIGHTING & COPY ACTIONS, AND AUDIO CLIPS.

### 8. ADVANCED SECURITY, SAFETY & PRIVACY HARDENING
- [x] **IN-MEMORY DEK ZEROIZATION**: EXPLICIT BYTE-ARRAY ZEROIZATION (`ENCRYPTIONKEY?.FILL(0.TOBYTE())`) UPON VAULT LOCK, APP BACKGROUNDING, AND VIEWMODEL DISPOSAL.
- [x] **ENCRYPTED MASTER KEY ROTATION ENGINE**: MASTER KEY ROTATION UTILITY (`CHANGEMASTERPASSWORD`) RE-ENCRYPTING DATA ENCRYPTION KEYS (DEKS) WITH NEW DERIVED KEYS WITHOUT RE-ENCRYPTING RAW NOTE ASSETS.
- [x] **FAILED UNLOCK AUTO-WIPE GUARD**: AUTOMATIC SECURITY LOCK & KEY ZEROIZATION TRIGGER AFTER 5 CONSECUTIVE FAILED UNLOCK ATTEMPTS.
- [x] **ANTI-TAMPER DATABASE CHECKSUM VERIFICATION**: SHA-256 CHECKSUM INTEGRITY VERIFICATION (`DATABASESECURITYHELPER`) ON APP STARTUP DETECTING OFFLINE DATABASE FILE TAMPERING.

---

## 🎯 COMPETITIVE FEATURE BENCHMARK (VS. TOP NOTE & DRAWING APPS)

NOTEFLOW COMBINES THE BEST CAPABILITIES OF TOP NOTE-TAKING AND DIGITAL ILLUSTRATION APPS INTO AN OFFLINE-FIRST PRIVACY FRAMEWORK:

| COMPETITOR APP | KEY BENCHMARK FEATURES & STRENGTHS | NOTEFLOW TARGET STRATEGY |
|---|---|---|
| **SAMSUNG NOTES** | VOICE MEMO AUDIO BOOKMARKS (TIME-STAMPED INK REPLAY), SHAPE STRAIGHTENING/AUTO-SNAP, FAVORITE PEN PRESETS BAR, S-PEN PALM REJECTION. | FLOATING COLLAPSIBLE PEN DOCK, SHAPE STRAIGHTENING, TIME-SYNCED AUDIO NOTES, PALM REJECTION TOGGLE. |
| **MICROSOFT ONENOTE** | TABBED NOTEBOOK/SECTION HIERARCHY, INFINITE CANVAS VIEWPORT MINIMAP, INLINE AUDIO & PHOTO ATTACHMENTS, CUSTOM PAPER LAYOUTS. | NOTEBOOK/SECTION SEARCH, CANVAS MINIMAP OVERVIEW, DRAGGABLE STICKY NOTE CARDS, INLINE IMAGE/AUDIO EMBEDS. |
| **OBSIDIAN** | LOCAL KNOWLEDGE GRAPH (`[[WIKILINKS]]`), BIDIRECTIONAL BACKLINKS, HIERARCHICAL `#TAGS`, DAILY NOTES / JOURNALING, MARKDOWN STORAGE. | OFFLINE `[[WIKILINKS]]` PARSING, FORCE-DIRECTED GRAPH VIEW, TAG EXPLORER, AND DATE-STAMPED DAILY NOTES. |
| **PROCREATE / ARTFLOW** | WORLD-CLASS BRUSH ENGINES (FOUNTAIN PEN, PENCIL GRAIN, AIRBRUSH/SPRAY, WATERCOLORS), BEZIER SMOOTHING, EYEDROPPER, PALETTE MANAGER. | ADVANCED BRUSH ENGINE WITH PRESSURE SENSITIVITY, BEZIER CURVE SMOOTHING, COLOR PICKER EYEDROPPER, CUSTOM PALETTES. |
| **APPFLOWY.IO** | MULTI-VIEW DATABASE ENGINE (KANBAN, CALENDAR, GRID, GALLERY), MODULAR BLOCK EDITOR (`/` COMMANDS, TOGGLE HEADINGS), LOCAL VAULT WORKSPACES. | REAL KANBAN BOARD VIEW, EVENT CALENDAR, CUSTOM PROPERTY GRID, TOGGLE/COLLAPSIBLE BLOCKS, SIDE-BY-SIDE COLUMNS, ISOLATED LOCAL WORKSPACES. |

---

## 🔴 POST-AUDIT TRUTH TABLE (AUGUST 2026 — 5-AGENT RIGOROUS AUDIT @ COMMIT `AC781DE`)

FIVE INDEPENDENT STRICT REVIEWERS (ANDROID POWER USER, 25-YR ARCHITECT, COMPETITOR PM, SECURITY ENGINEER, MARKET RESEARCHER) AUDITED ROADMAP CLAIMS AGAINST CODE. VERDICT: **THE "DONE" LIST MATERIALLY OVERSTATES REALITY.** THE APP IS A GENUINELY DEEP OFFLINE CANVAS/PKM ENGINE (5/10 OVERALL, PERFORMANCE 3/10) BUT HAS CRITICAL BROKEN PROMISES:

| # | CLAIMED DONE (ROADMAP) | REALITY (CODE) | SEVERITY |
|---|---|---|---|
| 1 | "AES-256-GCM FULL ENCRYPTION AT REST" (§1) | ROOM DB IS PLAINTEXT SQLITE (`NOTEFLOW.SQLITE`); ONLY TITLE/EXTRACTEDTEXT ARE FIELD-ENCRYPTED. STROKE GEOMETRY (`POINTSJSON`), TAGS, NAMES, AUDIO, .MD FILES ALL PLAINTEXT (`NoteRepository.kt:300-317`, `MainActivity.kt:76-99`) | CRITICAL |
| 2 | "ANTI-TAMPER DB CHECKSUM VERIFICATION" (§8) | DEAD CODE: HASHES `NOTEFLOW.DB` BUT REAL FILE IS `NOTEFLOW.SQLITE` → CHECK ALWAYS PASSES; STORED IN PLAINTEXT PREFS; NEVER SURFACED IN UI (`DatabaseSecurityHelper.kt:11,38-43`) | CRITICAL |
| 3 | "CLOUD SYNC / END-TO-END ENCRYPTION" (§ FUTURE, SYNC UI) | FAKE: `CloudSyncService.performSync` COPIES A BACKUP TO LOCAL `Downloads/Noteflow_Sync`; PROVIDER/URL/USERNAME ARE DEAD PARAMS; E2E TOGGLE NEVER READ; "AUTO SYNC" NEVER TRIGGERS (`CloudSyncService.kt:17-47`, `EncryptedSyncDialog.kt:101-118`) | CRITICAL |
| 4 | "TIME-SYNCED VOICE NOTES — TAP STROKE TO JUMP" (§7) | FICTION: PRESSURE/TILT/TIMESTAMP CAPTURE IS A BROKEN REFLECTION (`getDeclaredField("motionEvent")` ON `PointerEvent` → THROWS → ALWAYS NULL → PRESSURE=1/TIMESTAMP=NULL FOREVER, `AnnotationCanvas.kt:2480-2488`); SYNC BADGE COUNTS 0 ALWAYS; RECORDER FAILURE = SILENT "SIMULATED" SINE RECORDING + EMPTY .M4A (`VoiceNoteManager.kt:100-143,197-210`) | CRITICAL |
| 5 | "AGSL OIL PAINT & REAL WATERCOLOR WET-MIXING" (§18) | `createWetMixingEffect` HAS ZERO CALL SITES; OIL_PAINT/WATERCOLOR ARE HAND-ROLLED VECTOR STROKES; AGSL FLAG ONLY GATES A TOAST (`AgslShaders.kt:75-97`, `EditorScreen.kt:1496-1504`) | HIGH |
| 6 | "SYSTEM-WIDE FLAG_SECURE" (§4) | ZERO FLAG_SECURE OCCURRENCES IN CODEBASE → RECENTS PREVIEW LEAKS VAULT | HIGH |
| 7 | "BASELINE PROFILES 30%+ FASTER" (§7) | `BASELINE-PROF.TXT` IS DEAD FILE: NO `baselineProfile` DSL, NO PROFILEINSTALLER, NO MACROBENCHMARK (`build.gradle.kts:47-79`) | HIGH |
| 8 | "AGGRESSIVE R8 SHRINKING" (§7) | `isMinifyEnabled = false` — R8 NEVER RUNS (`build.gradle.kts:24`) | HIGH |
| 9 | "FAILED UNLOCK AUTO-WIPE GUARD" (§8) | WIPES NOTHING: IN-MEMORY COUNTER ONLY, RESETS ON RESTART, NO LOCKOUT, NO WIPE (`NoteflowViewModel.kt:588-596`) | HIGH |
| 10 | "HARDWARE BIOMETRIC GATE" (§4) | BYPASSABLE: NON-AUTH KEYSTORE PATH WHEN BIOMETRICS OFF (`SecurityService.kt:105-128`); PRESENCE-ONLY FALLBACK (`LockScreen.kt:40`); BIOMETRIC_WEAK + DEVICE_CREDENTIAL ALLOWED | HIGH |
| 11 | "GLOBAL VAULT SEARCH across content" (§5) | TITLE-ONLY `LIKE` SEARCH — CANNOT SEARCH NOTE CONTENT (`Daos.kt:102`) | HIGH |
| 12 | "INLINE MEDIA EMBED CARDS" (§7) | CODE/AUDIO CARDS EXIST BUT **IMAGES ARE NOT SUPPORTED IN CANVAS OR EDITOR** — PHOTO EMBEDS DON'T EXIST; "CODE BLOCK SYNTAX HIGHLIGHTING" IS PLAIN MONOSPACE TEXT; MARKDOWN PREVIEW RENDERS NO MARKDOWN (`MediaEmbedComponents.kt:330-350`, `MarkdownPreviewScreen.kt:122-139`) | HIGH |
| 13 | "PER-LAYER OFFSCREEN BITMAP CACHING" (§16.2) | EVERY FRAME RE-RENDERS ALL LAYERS VIA `saveLayer`; `LayerBitmapCache.kt` (10 LINES) IS DEAD CODE (`AnnotationCanvas.kt:1581-1631`) | HIGH |
| 14 | "BITMAP POOLING" (§7.5) / "THERMAL FALLBACK" (§18.6) | `BitmapPool.kt` / `ThermalSanityHelper.kt` NEVER REFERENCED ANYWHERE | MED |
| 15 | "BACKUP RESTORE ENCRYPTED" (§1) | BACKUPS PLAINTEXT BY DEFAULT (KEY NULL); KEYED BY DEVICE-RECOVERABLE DEK (NOT PORTABLE, NOT PASSWORD-DERIVED); RESTORE UNAUTHENTICATED (`ImportExportService.kt:890-948,913-916`) | HIGH |
| 16 | "MASTER KEY ROTATION ENGINE" (§8) | RE-WRAPS SAME DEK — NO ACTUAL ROTATION; NO AAD, NO KEY VERSIONING (`NoteflowViewModel.kt:550-573`) | MED |
| 17 | "OCR" (IMAGE/PDF TEXT) | METADATA-ONLY FOR IMAGES; NAIVE REGEX OVER RAW PDF BYTES (`OcrService.kt:49-98`) | MED |
| 18 | "GIT INTEGRATION" | **DOES NOT EXIST** — ZERO GIT/COMMIT/PUSH CODE IN THE APP; ONLY CI WORKFLOWS; NO GIT SYNC FEATURE AT ALL | HIGH |

### ✅ SINCE THE AC781DE AUDIT — ROWS FIXED BY PHASES 2–14 (verified 2026-08-13)

The table above is a point-in-time snapshot. Phases 2–14 have since changed several
rows; the live status is:

| Row | 2026-08-13 status | Evidence |
|---|---|---|
| 1 — field encryption | **FIXED** — `title`, `extractedText`, `textContent`, `pointsJson` all field-encrypted; cross-device re-key map covers `pages`, `strokes`, `media_embeds`, `note_versions` | `NoteRepository.kt:356-362, 537-559, 658-712, 764-794`; `ImportExportService.kt:1107-1112` |
| 2 — DB tamper checksum | **FIXED** — HMAC-SHA256 over the real `noteflow.sqlite` with AndroidKeyStore key, surfaced as `databaseTampered` banner | `DatabaseSecurityHelper.kt:49-65`; `NoteflowViewModel.kt:35-55` |
| 3 — cloud sync | **FIXED** — real WebDAV/Nextcloud upload/download of encrypted backups, HTTPS enforced; reachable from HomeScreen ⋮ → "WebDAV / Nextcloud E2EE Sync" (Phase 14 re-wired the dead menu trigger) | `WebDavSyncService.kt:61-155, 196-301`; `HomeScreen.kt:2376-2381` |
| 4 — time-synced voice notes | **PARTIALLY FIXED** — pressure/tilt/timestamp now captured via the official `pointerInteropFilter` MotionEvent bridge (no reflection); the sync-badge / tap-to-jump claim remains unverified this phase | `AnnotationCanvas.kt:469-473, 611-615` |
| 5 — AGSL wet-mixing | **WIRED** — `WetBrushEngine` + `WetMixingEffect` run in `AnnotationCanvas` while a wet stroke is actively drawn, gated by `isAgslSupported` + `gpuWetBrushesEnabled`; committed strokes bake through the plain path | `AnnotationCanvas.kt:2102-2109, 2242-2349`; `WetBrushEngine.kt:43-47` |
| 6 — FLAG_SECURE | **FIXED** — applied in non-debug builds (cleared in debug) | `MainActivity.kt:89-93` |
| 8 — R8 minify | **FIXED** — `isMinifyEnabled = true` in release | `app/build.gradle.kts:71` |
| 13 — LayerBitmapCache | **FIXED** — committed strokes rasterized to a hash-keyed cached bitmap and blitted per frame | `AnnotationCanvas.kt:392-398, 2033-2063, 2135-2186` |
| 14 — BitmapPool / ThermalSanityHelper | **FIXED** — both now wired (pool at `AnnotationCanvas.kt:395,2039-2152`; thermal status query at `:429-435`) | Phase 8 `PERF_REPORT.md` |
| 15 — encrypted backups | **FIXED** — backups encrypted with master password when set; legacy restore requires explicit confirmation | `ImportExportService.kt:1154+,1300-1328`; `HomeScreen.kt:150-158` |
| 17 — OCR | **FIXED** — real on-device ML Kit OCR plugin wired into `EditorScreen` | `plugins/ocr/OnDeviceOcrPlugin.kt`; `EditorScreen.kt:1272,1378-1399` |

Known still-false claims are tracked in AGENTS.md ("Known broken things") and were
verified again in `workspace/phase-14/AUDIT_REPORT.md`.

**WHAT IS GENUINELY REAL & GOOD:** PBKDF2-600K + AES-256-GCM + KEYSTORE-WRAPPED DEK + ZEROIZATION ON LOCK, `allowBackup=false` + DATAEXTRACTIONRULES, ZIP-SLIP/ZIP-BOMB GUARDS (50MB/200MB), 21 TOOLS + 12 BLEND MODES + RDP SIMPLIFICATION + QUAD-BEZIER SMOOTHING, REAL ANDROIDX.INK BRUSHES, WIKILINKS + FORCE GRAPH + TAGS + DAILY NOTES, PDF WINDOWED RENDERING, PALM REJECTION, DESTRUCTIVE-ACTION CONFIRMS. THE CRYPTO CORE WHERE IT APPLIES IS PROPER.

---

## 🚀 PHASED ROADMAP EXECUTION PLAN

EACH PHASE IS DESIGNED AS A STANDALONE, SELF-CONTAINED DEVELOPMENT MODULE THAT CAN BE COMPLETED IN A SINGLE PROMPT EXECUTION TURN.

### ⚠️ POST-AUDIT EXECUTION ORDER
1. **PHASES 19-21 (CRITICAL: SECURITY + PERFORMANCE + HONESTY) FIRST** — SHIP-BLOCKERS; "ENCRYPTED VAULT" CLAIM MUST BECOME TRUE.
2. **PHASE 22 (UX/PLATFORM)** — RETENTION.
3. **PHASES 23-24 (CANVAS MODES + IMPORT PIPELINE)** — REQUESTED FEATURES.
4. **PHASES 25-26 (PREVIOUS PLAN REMAINDER + MOAT)** — AFTER STABILITY.

---

### 🎨 PHASE 1: COLLAPSIBLE UI, SEARCH & CANVAS UX PRECISION
- [x] **1.1 COLLAPSIBLE / FLOATING DISTRACTION-FREE TOOLBARS**:
  - AUTO-HIDING FLOATING DRAWING TOOLBAR THAT COLLAPSES TO A COMPACT BAR ON TOGGLE FOR 100% DISTRACTION-FREE CANVAS PAINTING.
  - FLOATING DOCK DISPLAYING UP TO 5 QUICK-SWITCH PEN PRESETS (FINE INK, RED PEN, YELLOW HIGHLIGHTER, GREEN CALLIGRAPHY, CHARCOAL PENCIL).
- [x] **1.2 SEARCH BUTTON IN NOTEBOOKS, SECTIONS & PAGES PANELS**:
  - DEDICATED SEARCH FILTER BUTTONS AND FILTER TEXT FIELDS IN NOTEBOOK LIST, SECTION LIST, AND PAGE LIST HEADERS WITH LIVE QUERY FILTERING.
- [x] **1.3 DRAGGABLE CANVAS STICKY NOTES**:
  - FLOATING STICKY NOTE CARDS PLACED DIRECTLY ON THE CANVAS WITH CUSTOMIZABLE CARD BACKGROUND COLORS (YELLOW, MINT, LAVENDER, PEACH, SKY BLUE) AND TEXT EDITING DIALOG.
- [x] **1.4 CANVAS VIEWPORT MINIMAP**:
  - COLLAPSIBLE THUMBNAIL OVERVIEW WIDGET IN THE BOTTOM-RIGHT CORNER SHOWING FULL CANVAS BOUNDS AND CURRENT ACTIVE VIEWPORT FRAME DURING ZOOM/PAN.

---

### 🖌️ PHASE 2: WORLD-CLASS DIGITAL BRUSH ENGINE & STYLUS PRECISION
- [x] **2.1 FOUNTAIN PEN BRUSH**:
  - DYNAMIC STROKE WIDTH CALCULATION BASED ON VELOCITY AND SIMULATED NIB PRESSURE (THIN FAST STROKES, THICK SLOW FLOURISHES).
- [x] **2.2 PENCIL & CHARCOAL BRUSH ENGINE**:
  - TEXTURED GRAIN SHADER SIMULATION WITH TILT-BASED SHADING WIDTH AND GRAPHITE DARK OPACITY BLENDING.
- [x] **2.3 AIRBRUSH / SPRAY & MARKER WATERCOLOR TOOLS**:
  - SOFT RADIAL SPRAY NOZZLE TOOL FOR SHADING; TRANSPARENT WET-MARKER STROKE OVERLAPPING WITH REALISTIC COLOR MULTIPLY BLENDING.
- [x] **2.4 VECTOR STROKE SMOOTHING (CUBIC BEZIER FITTING)**:
  - REAL-TIME STROKE SMOOTHING USING CATMULL-ROM OR CUBIC BEZIER SPLINE INTERPOLATION TO CONVERT HAND JITTER INTO SMOOTH INK CURVES.
- [x] **2.5 COLOR EYEDROPPER & PALETTE MANAGER**:
  - MAGNIFYING CANVAS COLOR PICKER EYEDROPPER TOOL TO SAMPLE EXACT HEX COLORS FROM IMPORTED PDFS, IMAGES, OR DRAWN STROKES; SAVE CUSTOM PALETTES.
- [x] **2.6 S-PEN / STYLUS PALM REJECTION & PRESSURE TOGGLE**:
  - PALM REJECTION ENGINE IGNORING NON-STYLUS TOUCH CONTACTS WHILE DRAWING WITH AN ACTIVE STYLUS PEN.

---

### 🧠 PHASE 3: CONNECTED KNOWLEDGE VAULT (OBSIDIAN PKM ENGINE)
- [x] **3.1 WIKILINKS (`[[NOTE TITLE]]`) SYNTAX**:
  - AUTOMATIC DETECTION AND RENDERING OF `[[PAGE TITLE]]` WIKI LINKS AS INTERACTIVE TAP TARGETS NAVIGATING TO THE TARGET NOTE OR CREATING NEW NOTE ON TAP.
- [x] **3.2 INTERACTIVE LOCAL KNOWLEDGE GRAPH VIEW**:
  - FULL-SCREEN FORCE-DIRECTED GRAPH CANVAS DISPLAYING PAGES AS NODES AND `[[WIKILINKS]]` AS VECTOR CONNECTIONS; TAP NODES TO OPEN NOTES WITH ZOOM/PAN.
- [x] **3.3 BACKLINKS INSPECTOR & UNLINKED MENTIONS PANEL**:
  - INSPECTOR DRAWER LISTING ALL NOTE PAGES THAT EXPLICITLY LINK TO THE ACTIVE PAGE OR MENTION ITS TITLE WITH 1-CLICK LINKIFY ACTION.
- [x] **3.4 HIERARCHICAL TAG EXPLORER (`#TAG/SUBTAG`)**:
  - TAG PARSER EXTRACTING `#TAGS` FROM NOTE CONTENTS AND A DEDICATED TAG EXPLORER TAB FOR FILTERING NOTES BY NESTED CATEGORIES.
- [x] **3.5 DAILY NOTES / JOURNALING ACTION**:
  - ONE-TAP "TODAY'S JOURNAL" TOP BAR SHORTCUT CREATING OR OPENING A PRE-FORMATTED DAILY NOTE (`YYYY-MM-DD`).

---

### 🎙️ PHASE 4: TIME-SYNCED VOICE NOTES & MEDIA CANVAS EMBEDS
- [x] **4.1 TIME-SYNCED VOICE MEMO RECORDER**:
  - ON-DEVICE AUDIO RECORDER INTEGRATED INTO THE CANVAS TOOLBAR.
  - TIMESTAMP EACH VECTOR STROKE DRAWN DURING AUDIO RECORDING.
  - TAP ANY STROKE DURING PLAYBACK TO JUMP DIRECTLY TO THE EXACT AUDIO MOMENT.
- [x] **4.2 AUDIO PLAYBACK CONTROLS & WAVEFORM**:
  - EMBEDDED AUDIO CARD WITH VISUAL WAVEFORM SCRUBBING AND VARIABLE PLAYBACK SPEED OPTIONS (0.5X, 1.0X, 1.25X, 1.5X, 2.0X).
- [x] **4.3 INLINE MEDIA EMBED CARDS**:
  - CANVAS CARDS FOR PHOTO ATTACHMENTS, CODE BLOCKS WITH SYNTAX HIGHLIGHTING, AND AUDIO CLIPS.

---

### 🛡️ PHASE 5: ADVANCED SECURITY, SAFETY & PRIVACY HARDENING
- [x] **5.1 IN-MEMORY DEK ZEROIZATION**:
  - EXPLICIT BYTE-ARRAY ZEROIZATION (`ARRAYS.FILL(DEK, 0.TOBYTE())`) WHEN THE APP ENTERS BACKGROUND OR LOCKS.
- [x] **5.2 ENCRYPTED MASTER KEY ROTATION ENGINE**:
  - SETTINGS UTILITY TO RE-ENCRYPT DATA ENCRYPTION KEYS (DEKS) WHEN THE USER CHANGES THEIR MASTER PASSWORD WITHOUT RE-ENCRYPTING RAW NOTE ASSETS.
- [x] **5.3 FAILED UNLOCK AUTO-WIPE GUARD**:
  - CONFIGURABLE SAFETY TRIGGER TO CLEAR CACHED DEKS FROM RAM AFTER 5 CONSECUTIVE FAILED UNLOCK ATTEMPTS.
- [x] **5.4 ANTI-TAMPER DATABASE CHECKSUM VERIFICATION**:
  - SHA-256 HMAC CHECKSUM VERIFICATION ON APP STARTUP TO DETECT OFFLINE DATABASE FILE TAMPERING OR CORRUPTION.

---

### 📄 PHASE 6: ADVANCED LAYOUTS, TEMPLATES & BATCH PRODUCTIVITY
- [x] **6.1 CORNELL NOTES & PRODUCTIVITY TEMPLATES**:
  - PRESET CANVAS PAPER TEMPLATES FOR CORNELL NOTES, MEETING MINUTES, TO-DO GRIDS, AND CUSTOM BACKGROUND IMAGE UPLOADS.
- [x] **6.2 BATCH MULTI-PAGE VAULT EXPORT**:
  - EXPORT ENTIRE NOTEBOOKS OR SECTIONS INTO STRUCTURED ZIP PACKAGES CONTAINING RAW MARKDOWN TEXT, VECTOR INK PNG OVERLAYS, AND COMBINED PDFS.

---

### ⚡ PHASE 7: APP SPEED, CANVAS ENGINE & MEMORY OPTIMIZATIONS
- [x] **7.1 BASELINE PROFILES & MACROBENCHMARK PRECOMPILATION**:
  - GENERATE CUSTOM AOT BASELINE PROFILES TO COMPILE CRITICAL JETPACK COMPOSE RENDER PATHS AHEAD-OF-TIME FOR 30%+ FASTER STARTUP AND ZERO-JANK 60FPS CANVAS DRAWING.
- [x] **7.2 RAMER-DOUGLAS-PEUCKER (RDP) VECTOR STROKE SIMPLIFICATION**:
  - IMPLEMENT RDP POLYLINE SIMPLIFICATION ALGORITHM ON COMPLETED INK STROKES TO REDUCE VECTOR POINT COUNT BY UP TO 70% WHILE MAINTAINING EXACT VISUAL FIDELITY.
- [x] **7.3 DEFERRAL OF COMPOSE STATE READS & RECOMPOSITION BOUNDARIES**:
  - REFACTOR RAPID GESTURE STATE READS INTO LAMBDA-BASED MODIFIERS (`DRAWWITHCACHE`, `GRAPHICSLAYER`) AND `DERIVEDSTATEOF` TO PREVENT UNNECESSARY ROOT COMPOSABLE RECOMPOSITIONS DURING FAST DRAGGING.
- [x] **7.4 HARDWARE CANVAS DOUBLE BUFFERING & `DRAWWITHCACHE` LAYERING**:
  - CACHE STATIC PAPER TEMPLATES, GRID LINES, AND PDF BACKGROUND LAYERS INTO REUSABLE OFFSCREEN BITMAPS SO ONLY ACTIVE STROKE LAYERS ARE RE-RENDERED ON DRAG.
- [x] **7.5 BITMAP POOLING & MEMORY RECYCLING (`INBITMAP`)**:
  - IMPLEMENT REUSABLE BITMAP OBJECT POOL FOR PDF RENDERING WINDOWS TO ELIMINATE GARBAGE COLLECTION (GC) PAUSES DURING FAST CONTINUOUS SCROLLING.
- [x] **7.6 ROOM DATABASE INDEXING & WAL QUERY OPTIMIZATION**:
  - ADD COMPOSITE INDICES ON FOREIGN KEYS (`NOTEBOOKID`, `SECTIONID`, `PAGEID`, `PINNED`, `ISTRASH`) AND TUNE WRITE-AHEAD LOGGING (WAL) CHECKPOINTS FOR SUB-10MS VAULT QUERIES.
- [x] **7.7 R8 PROGUARD SHRINKING & COROUTINE DISPATCHER OPTIMIZATION**:
  - ENABLE AGGRESSIVE R8 CODE SHRINKING AND DEAD-CODE ELIMINATION RULES WHILE PINNING DRAWING LOOPS TO LIGHTWEIGHT `DISPATCHERS.DEFAULT` THREADS.

---

### 🗂️ PHASE 8: APPFLOWY DATABASE & MULTI-VIEW ENGINE (KANBAN, CALENDAR, GRID & GALLERY)
- [ ] **8.1 REAL INTERACTIVE KANBAN BOARD VIEW**:
  - DRAG-AND-DROP TASK CARDS BETWEEN CUSTOM STATUS COLUMNS (E.G. TO DO, IN PROGRESS, DONE, REVIEW) WITH COLUMN CARD COUNTERS, COLOR TAGS, AND FAST CARD CREATION.
- [ ] **8.2 EVENT CALENDAR VIEW**:
  - INTERACTIVE MONTHLY AND WEEKLY EVENT GRID MAPPING NOTES/DATABASE ITEMS TO DATES; TAP CELLS TO OPEN ENTRIES OR DRAG CARDS BETWEEN DATES FOR DEADLINE UPDATES.
- [ ] **8.3 DYNAMIC GRID / SPREADSHEET VIEW**:
  - CUSTOM DATABASE PROPERTIES WITH FIELD TYPES: TEXT, NUMBER, DATE, SINGLE-SELECT, MULTI-SELECT TAGS, CHECKBOX, URL, RATING, AND FILE ATTACHMENTS.
- [ ] **8.4 VISUAL GALLERY CARD VIEW**:
  - GRID CARD GALLERY DISPLAYING RICH IMAGE/DRAWING COVER PREVIEWS, PROPERTY TAGS, AND QUICK EDIT TRIGGERS.
- [ ] **8.5 MULTI-PERSPECTIVE VIEW SWITCHING**:
  - SEAMLESSLY SWITCH THE EXACT SAME DATABASE BETWEEN GRID, KANBAN BOARD, CALENDAR, AND GALLERY PERSPECTIVES WITHOUT DATA DUPLICATION.

---

### 📝 PHASE 9: APPFLOWY BLOCK-BASED EDITOR, SLASH COMMANDS & COLLAPSIBLE HEADINGS
- [ ] **9.1 MODULAR BLOCK EDITOR ARCHITECTURE**:
  - SLASH COMMAND `/` TRIGGER MENU FOR INSERTING BLOCKS: HEADINGS (H1-H3), BULLETED LIST, NUMBERED LIST, CHECKLIST, QUOTE, CALLOUT, CODE BLOCK, AND DIVIDER.
- [ ] **9.2 TOGGLE BLOCKS & COLLAPSIBLE HEADINGS**:
  - INTERACTIVE TOGGLE ARROWS ON HEADINGS AND TOGGLE LISTS TO COLLAPSE OR EXPAND NESTED SUB-BLOCKS, KEEPING DENSE DOCUMENTS ORGANIZED AND CLUTTER-FREE.
- [ ] **9.3 COLUMN LAYOUT BLOCKS**:
  - MULTI-COLUMN SIDE-BY-SIDE BLOCK ARRANGEMENTS (2-COLUMN AND 3-COLUMN LAYOUTS) FOR DESKTOP/TABLET/LANDSCAPE PRODUCTIVITY.
- [ ] **9.4 CALLOUT & BANNER BLOCKS**:
  - ACCENT-COLORED CALLOUT BOXES WITH CUSTOMIZABLE EMOJI/ICON BADGES FOR KEY TAKEAWAYS, WARNINGS, AND CODE NOTES.
- [ ] **9.5 LATEX / MATH FORMULA BLOCKS**:
  - RENDER INLINE AND STANDALONE MATHEMATICAL EQUATIONS USING KATEX / LATEX SYNTAX.

---

### 🛡️ PHASE 10: APPFLOWY VAULT WORKSPACES, LOCAL AI & WORKSPACE GOVERNANCE
- [ ] **10.1 MULTI-WORKSPACE ISOLATION**:
  - SUPPORT DISTINCT LOCAL "VAULT WORKSPACES" (E.G. WORK, PERSONAL, RESEARCH) EACH ENCRYPTED WITH INDEPENDENT MASTER KEYS AND ISOLATED ROOM DATABASE FILES.
- [ ] **10.2 ON-DEVICE OFFLINE SMART WORKSPACE ASSISTANT**:
  - ON-DEVICE LOCAL / SMART ASSISTANT INTEGRATION FOR OFFLINE DOCUMENT SUMMARIZATION, GRAMMAR CHECK, OUTLINE CREATION, AND TAG AUTO-CLASSIFICATION WITH ZERO NETWORK CALLS.
- [ ] **10.3 WORKSPACE TEMPLATE LIBRARY**:
  - PRE-PACKAGED STARTER TEMPLATES FOR PROJECT MANAGEMENT, CRM, PERSONAL KNOWLEDGE BASE, MEETING MINUTES, AND READING LIST.

---

### 🖼️ PHASE 11: TOOLBAR & CANVAS SPACE OPTIMIZATION (COLLAPSIBLE CHROME REDESIGN)
- [x] **11.1 SLIM PERSISTENT TOP APP BAR**:
  - COLLAPSE THE CURRENT MULTI-ROW HEADER (BACK BUTTON, TITLE, PRESETS ROW, TOOL ICON ROW, COLOR SWATCH ROW, CANVAS MODE TOGGLES) INTO A SINGLE ~56DP TOP BAR CONTAINING ONLY: BACK BUTTON, TAP-TO-RENAME PAGE TITLE, AND AN OVERFLOW MENU.
  - MOVE RECORD VOICE, PRESETS, SHARE, AND MINIMAP TOGGLE INTO THE OVERFLOW MENU.
- [x] **11.2 FLOATING BOTTOM TOOLBAR PILL**:
  - REPLACE THE ALWAYS-VISIBLE TOOL ICON ROW, COLOR SWATCH ROW, AND WIDTH SLIDER WITH A SINGLE FLOATING BOTTOM CAPSULE SHOWING ONLY THE CURRENT TOOL, CURRENT COLOR, AND CURRENT WIDTH.
  - TAPPING ANY ELEMENT EXPANDS A COMPACT POPOVER/BOTTOM SHEET WITH THE FULL PICKER.
  - AUTO-HIDE THE PILL ON POINTER-DOWN (ACTIVE DRAWING) AND REAPPEAR ON POINTER-UP OR EMPTY-CANVAS TAP, MATCHING PROCREATE/SAMSUNG NOTES BEHAVIOR.
- [x] **11.3 OVERFLOW CANVAS MODE TOGGLES**:
  - MOVE "INFINITE CANVAS / PAGE DIVISION / GRID" TOGGLES INTO THE OVERFLOW MENU OR A SETTINGS ICON IN THE FLOATING BOTTOM TOOLBAR.
- [x] **11.4 COLLAPSIBLE MINIMAP ICON STATE**:
  - COLLAPSE THE MINIMAP TO A SMALL ICON BUTTON THAT EXPANDS ON TAP RATHER THAN A FIXED-SIZE PANEL PERMANENTLY OCCUPYING CANVAS CORNER SPACE.
- [x] **11.5 NET RESULT TARGET**:
  - AT REST, ONLY CHROME ON SCREEN SHOULD BE THE SLIM TOP BAR (~56DP) AND THE FLOATING BOTTOM TOOLBAR PILL (~56DP) — EVERYTHING ELSE IS CANVAS.
  - **MANUAL WORK REQUIRED**: NONE — PURE UI REFACTOR. VERIFY ON-DEVICE THAT POPOVERS/BOTTOM SHEETS DO NOT CLIP OFF-SCREEN ON SMALLER PHONES.

---

### 🧬 PHASE 12: ADVANCED BRUSH ENGINE — DATA LAYER & PRESSURE/TILT CAPTURE
- [x] **12.1 ADVANCED BRUSHES FEATURE FLAG**:
  - ADD AN "ADVANCED BRUSHES" TOGGLE (SETTING OR TOOL-PICKER SWITCH), DEFAULTING TO OFF. THIS PHASE BUILDS THE DATA LAYER ONLY — NO EXISTING CLASSIC BRUSH VISUALS OR BEHAVIOR CHANGE.
- [x] **12.2 EXTENDED STROKE POINT MODEL**:
  - EXTEND THE STROKE POINT DATA MODEL (`POINTF`) TO OPTIONALLY CARRY `PRESSURE: FLOAT` (0–1) AND `TILT: FLOAT` (DEGREES) ALONGSIDE EXISTING X/Y/TIMESTAMP FIELDS, AS NULLABLE/DEFAULTED VALUES SO EXISTING SAVED STROKES AND `STROKE.KT` CONSUMERS ARE UNAFFECTED.
- [x] **12.3 POINTER INPUT PRESSURE/TILT CAPTURE**:
  - IN THE CANVAS POINTER INPUT HANDLER (WITHIN `ANNOTATIONCANVAS`), CAPTURE `MOTIONEVENT.GETPRESSURE()` AND TILT VIA `GETAXISVALUE(AXIS_TILT)` / `AXIS_ORIENTATION` WHEN AVAILABLE, STORING THEM ON EACH `POINTF` REGARDLESS OF ACTIVE TOOL (CHEAP TO CAPTURE NOW, IGNORED BY CLASSIC BRUSHES, NEEDED BY PHASE 13).
- [x] **12.4 BRUSH ENGINE SEALED INTERFACE**:
  - ADD A `BRUSHENGINE` SEALED INTERFACE/ENUM WITH TWO CASES: `CLASSIC` (CURRENT `DRAWSINGLESTROKE` BEHAVIOR, UNTOUCHED) AND `ADVANCED` (PLACEHOLDER SLOT FOR PHASE 13).
- [x] **12.5 NO-REGRESSION GUARANTEE**:
  - CONFIRM EXISTING BRUSH RENDERING, SAVE/LOAD, AND UNDO/REDO ARE COMPLETELY UNCHANGED WHEN ADVANCED BRUSHES IS OFF OR A CLASSIC BRUSH IS SELECTED. THIS IS THE MOST IMPORTANT REQUIREMENT OF THIS PHASE.
  - **MANUAL WORK REQUIRED**: NONE — BUT MANUALLY VERIFY EXISTING SAVED NOTEBOOKS STILL LOAD/RENDER CORRECTLY AFTER THIS DATA MODEL CHANGE.

---

### 🖌️ PHASE 13: ANDROIDX.INK INTEGRATION — WORLD-CLASS BRUSH ENGINE
- [x] **13.1 ANDROIDX.INK DEPENDENCY SETUP**:
  - ADD GOOGLE'S JETPACK `ANDROIDX.INK` LIBRARY MODULES (AUTHORING, BRUSH, RENDERING, GEOMETRY) TO `BUILD.GRADLE.KTS`. PURE KOTLIN/JETPACK — NO NDK OR JNI REQUIRED.
- [x] **13.2 LOW-LATENCY STROKE CAPTURE**:
  - USE `ANDROIDX.INK.AUTHORING`'S `INPROGRESSSTROKESVIEW` (OR ITS COMPOSE EQUIVALENT) FOR STYLUS INPUT CAPTURE, INCLUDING PREDICTED-STROKE RENDERING TO MINIMIZE PERCEIVED LAG VS. THE CURRENT `DETECTDRAGGESTURES`-BASED APPROACH.
- [x] **13.3 BRUSH FAMILY PRESETS**:
  - DEFINE BRUSH PRESETS USING `ANDROIDX.INK.BRUSH.BRUSH` AND `BRUSHFAMILY`: FOUNTAIN PEN, PENCIL, MARKER, HIGHLIGHTER, AND WATERCOLOR — TUNING THE LIBRARY'S BUILT-IN PRESSURE/TILT/SPEED-RESPONSIVE BEHAVIORS AND TEXTURED TIPS RATHER THAN HAND-ROLLED STAMP MATH.
- [x] **13.4 WIRE INTO BRUSHENGINE INTERFACE**:
  - IMPLEMENT THE `ADVANCED` CASE OF THE `BRUSHENGINE` INTERFACE FROM PHASE 12 USING ANDROIDX.INK. CLASSIC BRUSH CODE AND RENDERING (`DRAWSINGLESTROKE`) MUST REMAIN COMPLETELY UNTOUCHED.
- [x] **13.5 MIXED-MODE COMPOSITING**:
  - ENSURE ANDROIDX.INK STROKES RENDER CORRECTLY COMPOSITED ALONGSIDE CLASSIC STROKES IN THE SAME `CANVAS` COMPOSABLE, SO A USER CAN MIX CLASSIC AND ADVANCED STROKES ON THE SAME PAGE.
- [x] **13.6 STABILITY FLAGGING**:
  - CLEARLY FLAG IN THE UI IF ANY PART OF THE CURRENT ANDROIDX.INK API SURFACE IS STILL ALPHA/UNSTABLE, SINCE THE LIBRARY MAY STILL BE MATURING.
  - **MANUAL WORK REQUIRED**: MINIMAL — CONFIRM GRADLE SYNC SUCCEEDS (NO NATIVE TOOLCHAIN NEEDED) AND TEST STROKE FEEL ON A REAL STYLUS DEVICE (S PEN OR SIMILAR) SINCE EMULATOR PRESSURE DATA IS OFTEN FLAT/UNRELIABLE.

---

### 🎨 PHASE 14: RICH PALETTE, CUSTOM BRUSH PRESETS & COLOR SYSTEM
- [x] **14.1 ADVANCED COLOR PICKER UI**:
  - ADD AN HSV COLOR PICKER + SAVED CUSTOM SWATCHES PANEL FOR ADVANCED BRUSHES, SEPARATE FROM THE EXISTING SIMPLE CLASSIC COLOR ROW — CLASSIC BRUSHES KEEP THEIR CURRENT SIMPLE PALETTE UNTOUCHED.
- [x] **14.2 CUSTOM BRUSH PRESET SAVE/LOAD**:
  - LET USERS SAVE A NAMED CUSTOM COMBINATION OF ANDROIDX.INK BRUSH FAMILY + COLOR + WIDTH/PRESSURE CURVE AS A REUSABLE PRESET, SURFACED ALONGSIDE THE EXISTING 5-SLOT QUICK-SWITCH PRESET DOCK FROM PHASE 1.
- [x] **14.3 RICH PALETTE PERSISTENCE**:
  - PERSIST CUSTOM SWATCHES AND BRUSH PRESETS TO ROOM ALONGSIDE THE EXISTING PALETTE MANAGER DATA, KEEPING BOTH SYSTEMS QUERYABLE FROM ONE PALETTE ENTITY TABLE.
  - **MANUAL WORK REQUIRED**: NONE — VISUAL/UX TUNING ONLY.

---

### 🧩 PHASE 15: ADVANCED BRUSH POLISH — SERIALIZATION, EXPORT COMPATIBILITY & FALLBACK RENDERING
- [x] **15.1 INDEXEDDB/ROOM ROUND-TRIP INTEGRITY**:
  - ENSURE STROKES MADE WITH ANDROIDX.INK ADVANCED BRUSHES SERIALIZE CORRECTLY TO THE ROOM `STROKES` TABLE, CONFIRMING PRESSURE/TILT/BRUSH-TYPE DATA ROUND-TRIPS ON SAVE/LOAD WITHOUT DATA LOSS.
- [x] **15.2 BACKWARD-COMPATIBLE FALLBACK RENDERING**:
  - IF A NOTE CONTAINING ADVANCED-BRUSH STROKES IS OPENED WITH THE ADVANCED BRUSHES TOGGLE OFF (OR ON AN OLDER APP VERSION), FALL BACK TO RENDERING A PLAIN CLASSIC-STYLE PATH RATHER THAN CRASHING OR DROPPING THE STROKE.
- [x] **15.3 EXPORT ENGINE COMPATIBILITY**:
  - CONFIRM THE EXISTING PNG/PDF EXPORT ENGINE (SECTION 3 OF THE DONE LIST) CORRECTLY RASTERIZES ANDROIDX.INK ADVANCED BRUSH STROKES ALONGSIDE CLASSIC STROKES, NOT JUST CLASSIC ONES.
- [x] **15.4 BACKUP/RESTORE COMPATIBILITY**:
  - CONFIRM THE TRANSACTIONAL BACKUP & RESTORE SYSTEM (SECTION 1 OF THE DONE LIST) INCLUDES ADVANCED BRUSH FIELDS WITHOUT BREAKING ZIP-BOMB/ZIP-SLIP SAFETY BOUNDS ALREADY IN PLACE.
  - **MANUAL WORK REQUIRED**: NONE — COMPLETE PROGRAMMATIC CAPABILITY CONFIRMED. TEST AGAINST REAL SAVE/EXPORT/BACKUP DATA TO CONFIRM DESIRED VISUAL APPEARANCE.

---

### 🗃️ PHASE 16: LAYER SYSTEM & WATERCOLOR-STYLE SOFT BLENDING
- [x] **16.1 LAYER DATA MODEL**:
  - ADD A `LAYER` ENTITY: ID, PAGE ID, NAME, Z-ORDER, OPACITY (0–1), BLEND MODE, VISIBILITY, LOCK STATE. EXISTING PAGES MIGRATE TO A SINGLE DEFAULT "LAYER 1" SO NO SAVED NOTE IS BROKEN.
  - ADD `LAYERID` TO THE STROKE MODEL SO EACH STROKE BELONGS TO A LAYER; DEFAULT EXISTING STROKES TO THE MIGRATED DEFAULT LAYER.
- [x] **16.2 PER-LAYER OFFSCREEN RENDERING & COMPOSITING**:
  - RENDER EACH LAYER'S STROKES TO ITS OWN OFFSCREEN `IMAGEBITMAP`, THEN COMPOSITE ALL VISIBLE LAYERS TOP-TO-BOTTOM IN Z-ORDER USING COMPOSE'S `BLENDMODE` (NORMAL, MULTIPLY, SCREEN, OVERLAY, ETC.) AND PER-LAYER OPACITY.
  - ONLY RE-RENDER THE ACTIVE LAYER'S BITMAP DURING ACTIVE DRAWING; OTHER LAYERS STAY CACHED — DO NOT RECOMPOSITE EVERY LAYER ON EVERY STROKE POINT.
- [x] **16.3 LAYERS PANEL UI**:
  - BOTTOM SHEET OR SIDE PANEL: THUMBNAIL LIST OF LAYERS, DRAG-TO-REORDER, OPACITY SLIDER, VISIBILITY/LOCK TOGGLES, ADD/DELETE/DUPLICATE/MERGE-DOWN ACTIONS.
- [x] **16.4 WATERCOLOR-STYLE SOFT BLENDING (STAMP-BASED APPROXIMATION)**:
  - ADD A "WATERCOLOR" BRUSH THAT RENDERS AS MULTIPLE OVERLAPPING SEMI-TRANSPARENT, SOFT-EDGED STAMPS (USING `BLURMASKFILTER` OR A PRE-BLURRED TEXTURE) ONTO THE ACTIVE LAYER'S BITMAP.
  - OVERLAPPING STROKES ON THE SAME LAYER DARKEN NATURALLY VIA ALPHA ACCUMULATION FROM LAYERED LOW-OPACITY STAMPS; ADD A FEW EXTRA LOW-OPACITY, RANDOMLY JITTERED OUTWARD STAMPS AT STROKE EDGES TO FAKE PIGMENT BLEED INTO PAPER.
  - THIS IS A CPU/VECTOR-BASED APPROXIMATION — WORKS ON **ALL** ANDROID VERSIONS, NO MINSDK RESTRICTION. IT IS THE FALLBACK BRUSH FOR PHASE 18 ON PRE-ANDROID-13 DEVICES.
- [x] **16.5 UNDO/REDO & EXPORT LAYER-AWARENESS**:
  - EXTEND THE EXISTING UNDO/REDO STACK AND PNG/PDF EXPORT ENGINE TO OPERATE PER-LAYER AND FLATTEN CORRECTLY ON EXPORT.
  - **MANUAL WORK REQUIRED**: NONE — VERIFY EXISTING SINGLE-LAYER NOTES MIGRATE CLEANLY AND STILL RENDER/EXPORT IDENTICALLY AFTER THIS CHANGE.

---

### 🧪 PHASE 17: AGSL SHADER INFRASTRUCTURE & CAPABILITY FALLBACK
- [x] **17.1 RUNTIMESHADER CAPABILITY DETECTION**:
  - AT APP STARTUP, CHECK `BUILD.VERSION.SDK_INT >= 33` (ANDROID 13, TIRAMISU) TO DETERMINE IF `ANDROID.GRAPHICS.RUNTIMESHADER` / AGSL IS AVAILABLE ON THIS DEVICE. STORE THIS AS AN APP-WIDE CAPABILITY FLAG.
- [x] **17.2 SHADER PLUMBING**:
  - WIRE UP `RUNTIMESHADER` LOADING AND `RENDEREFFECT.CREATERUNTIMESHADEREFFECT(...)` APPLICATION VIA `MODIFIER.GRAPHICSLAYER { RENDEREFFECT = ... }`, TARGETING THE ACTIVE LAYER'S BITMAP FROM PHASE 16 AS THE SAMPLING SURFACE.
  - NO REAL BLEND LOGIC YET — THIS PHASE ONLY PROVES THE PIPELINE WORKS END TO END (E.G. A TRIVIAL PASS-THROUGH OR SIMPLE TINT SHADER RUNNING ON A LAYER).
- [x] **17.3 GRACEFUL FALLBACK UX**:
  - IN THE BRUSH PICKER, WHEN A GPU-WET-MIXING BRUSH (PHASE 18) IS UNAVAILABLE ON THE CURRENT DEVICE, GRAY IT OUT OR AUTOMATICALLY SUBSTITUTE THE PHASE 16 STAMP-BASED WATERCOLOR EQUIVALENT, WITH A CLEAR, NON-ALARMING MESSAGE (E.G. "REAL-TIME WET BLENDING NEEDS ANDROID 13+ — USING SOFT-BLEND WATERCOLOR INSTEAD ON THIS DEVICE").
  - NO CRASH, NO SILENT FAILURE — DEGRADATION MUST BE VISIBLE AND EXPLAINED, EXACTLY ONCE PER BRUSH SELECTION (NOT REPEATED NAGGING).
- [x] **17.4 PERFORMANCE BASELINE CHECK**:
  - CONFIRM THE SHADER PIPELINE RUNS AT 60FPS ON A MID-RANGE ANDROID 13+ DEVICE BEFORE MOVING TO PHASE 18 — THIS PHASE IS THE CHECKPOINT TO CATCH GPU PERFORMANCE PROBLEMS EARLY, BEFORE REAL BLEND MATH IS ADDED.
  - **MANUAL WORK REQUIRED**: TEST ON BOTH AN ANDROID 13+ DEVICE (TO CONFIRM THE SHADER PATH RUNS) AND AN OLDER DEVICE/EMULATOR IMAGE BELOW API 33 (TO CONFIRM THE FALLBACK MESSAGE AND SUBSTITUTE BRUSH TRIGGER CORRECTLY). THIS IN-BROWSER ENVIRONMENT CANNOT VERIFY DEVICE-SPECIFIC GPU BEHAVIOR ON ITS OWN.

---

### 🎨 PHASE 18: AGSL OIL PAINT & REAL WATERCOLOR WET-MIXING BRUSHES
- [x] **18.1 WET-MIXING BLEND SHADER**:
  - WRITE AN AGSL SHADER THAT SAMPLES THE ACTIVE LAYER'S EXISTING PIXELS UNDER THE BRUSH CURSOR EACH FRAME AND MIXES THEM WITH THE INCOMING BRUSH COLOR BASED ON TUNABLE PARAMETERS (WETNESS, PIGMENT LOAD, MIX STRENGTH), WRITING THE BLENDED RESULT BACK TO THE LAYER — GENUINE COLOR MIXING, NOT STACKED OPACITY.
- [x] **18.2 OIL PAINT BRUSH PRESET**:
  - HIGH PIGMENT LOAD, LOW WETNESS, VISIBLE IMPASTO-STYLE TEXTURE (SUBTLE NORMAL-MAP-STYLE SHADING ON STROKE EDGES TO SIMULATE PAINT THICKNESS), STRONG COLOR PICKUP FROM UNDERLYING WET STROKES.
- [x] **18.3 REAL WATERCOLOR BRUSH PRESET**:
  - LOW PIGMENT LOAD, HIGH WETNESS, SOFT DIFFUSING EDGES DRIVEN BY THE SHADER RATHER THAN PRE-BLURRED STAMPS — REPLACES THE PHASE 16 APPROXIMATION ON CAPABLE (ANDROID 13+) DEVICES WHILE PHASE 16'S VERSION REMAINS THE AUTOMATIC FALLBACK.
- [x] **18.4 WIRE INTO BRUSHENGINE AS A GPU TIER**:
  - EXPOSE THESE AS A CLEARLY LABELED "GPU WET BRUSHES (ANDROID 13+)" GROUP IN THE BRUSH PICKER, GATED BY THE PHASE 17 CAPABILITY FLAG AND FALLBACK LOGIC.
- [x] **18.5 SERIALIZATION & EXPORT COMPATIBILITY**:
  - CONFIRM STROKES MADE WITH GPU WET BRUSHES SAVE/LOAD/EXPORT CORRECTLY (THE FINAL COMPOSITED LAYER BITMAP IS WHAT PERSISTS, SO THIS SHOULD BE NATURALLY COMPATIBLE WITH THE EXISTING EXPORT ENGINE, BUT MUST BE VERIFIED).
  - **MANUAL WORK REQUIRED**: SIGNIFICANT VISUAL TUNING ON A REAL ANDROID 13+ STYLUS DEVICE — GETTING WET-MIX PARAMETERS (WETNESS, PIGMENT LOAD, MIX STRENGTH) TO LOOK GOOD IS ITERATIVE BY-EYE WORK, NOT SOMETHING THE EMULATOR OR IDE CAN TUNE BLIND. NO NDK/NATIVE TOOLCHAIN NEEDED — THIS STAYS ENTIRELY WITHIN THE ANDROID SDK.
- [x] **18.6 BATTERY & THERMAL SANITY GATE**:
  - PLAN TO VERIFY NO SUSTAINED THERMAL THROTTLING OCCURS AFTER 10 MINUTES OF CONTINUOUS GPU WET-BRUSH DRAWING ON A MID-RANGE ANDROID 13+ DEVICE, MONITORED VIA `POWERMANAGER.GETCURRENTTHERMALSTATUS()`. MANUAL VERIFICATION GATE BEFORE PHASE 18 SHIPS, ALONGSIDE THE EXISTING 60FPS CHECK FROM PHASE 17.4.
  - **MANUAL WORK REQUIRED**: ACCURATELY PROFILE BATTERY AND THERMAL TRENDS ON PHYSICAL DEVICES OVER PROLONGED USE.
- [x] **18.7 DIRTY-RECT SCOPED SHADER PASSES**:
  - PLAN TO SCOPE EVERY RUNTIMESHADER PASS TO A SMALL BOUNDING RECTANGLE AROUND THE ACTIVE BRUSH STROKE (BRUSH RADIUS + MARGIN) RATHER THAN THE FULL LAYER BITMAP, AS THE PRIMARY GPU COST REDUCER.
  - **MANUAL WORK REQUIRED**: NONE.
- [x] **18.8 DISTANCE/TIME-THROTTLED SHADER TRIGGERING**:
  - PLAN TO ONLY TRIGGER A SHADER BLEND PASS WHEN THE POINTER HAS MOVED PAST A MINIMUM DISTANCE OR MINIMUM TIME HAS ELAPSED SINCE THE LAST PASS, AVOIDING REDUNDANT PASSES ON NEAR-STATIONARY INPUT.
  - **MANUAL WORK REQUIRED**: NONE.
- [x] **18.9 PREVIEW-RESOLUTION VS. COMMIT-RESOLUTION RENDERING**:
  - PLAN TO RENDER THE LIVE WET-BLEND PREVIEW AT REDUCED INTERNAL RESOLUTION WHILE DRAGGING, THEN RUN ONE FULL-RESOLUTION BLEND PASS ON STROKE-END TO COMMIT FINAL QUALITY.
  - **MANUAL WORK REQUIRED**: NONE.
- [x] **18.10 MINIMAL SHADER COMPLEXITY BUDGET**:
  - PLAN TO KEEP THE AGSL KERNEL FREE OF LOOPS/EXPENSIVE BRANCHING AND CAP TEXTURE SAMPLES PER PIXEL, PASSING CONSTANTS IN AS UNIFORMS RATHER THAN RECOMPUTING THEM PER-PIXEL.
  - **MANUAL WORK REQUIRED**: NONE.
- [x] **18.11 ADAPTIVE QUALITY FALLBACK**:
  - PLAN FOR A RUNTIME FPS MONITOR WHILE A GPU WET BRUSH IS ACTIVE; IF FRAME TIME STAYS BELOW A THRESHOLD FOR A SUSTAINED PERIOD, AUTOMATICALLY FALL BACK TO THE PHASE 16 STAMP-BASED WATERCOLOR BRUSH FOR THAT SESSION, SHOW A ONE-TIME NON-ALARMING EXPLANATION, REMEMBER THE CHOICE PER-DEVICE, AND LET THE USER MANUALLY RE-ENABLE GPU BRUSHES FROM SETTINGS.
  - **MANUAL WORK REQUIRED**: MANUALLY VERIFY RE-ENABLE SETTINGS SWITCH AND CONFIRM THE STAMP-BASED WATERCOLOR FALLBACK BEHAVES GRACEFULLY ON SLOWER OR HOT COMPATIBLE DEVICES.
- [x] **18.12 IDLE GPU COST VERIFICATION**:
  - PLAN TO CONFIRM THE SHADER PIPELINE DOES ZERO GPU WORK WHEN NO STROKE IS IN PROGRESS (NO CONTINUOUS RE-RENDER LOOP, NO LERINGERING RENDEREFFECT OUTSIDE ACTIVE DRAWING).
  - **MANUAL WORK REQUIRED**: PROFILE USING ANDROID GPU INSPECTOR TO DOUBLE-CHECK COMPOSER POWER STATES WHEN RESTING.

---

## 🔒 PHASE 19: SECURITY & HONESTY RECOVERY (CRITICAL — DO FIRST)

- [x] **19.1 FIX ANTI-TAMPER CHECKSUM (DEAD CODE)**: HMAC-SHA256 (AndroidKeyStore key `noteflow_db_hmac_key`) over the real `noteflow.sqlite`; recompute via `stampDatabaseChecksum` after WAL checkpoint/save; `databaseTampered` banner surfaced in MainActivity. (`DatabaseSecurityHelper.kt`, `NoteflowViewModel.kt:30`, `MainActivity.kt`)
- [x] **19.2 REAL AT-REST ENCRYPTION**: SQLCipher-android adopted for the Room DB — full-file AES-256 at rest (includes `pointsJson`, tags, notebook/section/layer names, waveforms, source paths, palette items). DEK lives in `VaultKeyHolder`, one-time in-place plaintext→encrypted rekey migration in `NoteflowDatabase.migratePlaintextIfNeeded`. (`NoteflowDatabase.kt`, `VaultKeyHolder.kt`)
- [x] **19.3 RE-ENCRYPTION PASS**: `setMasterPassword` reuses the existing DEK (no data loss) and runs `reencryptPlaintextFields` over pages/strokes/media_embeds; `removeMasterPassword` re-wraps the DEK device-only (vault stays readable, rows remain encrypted). (`NoteRepository.kt:88-140`, `NoteflowViewModel.kt`)
- [x] **19.4 CLOSE BIOMETRIC BYPASS**: presence-only unlock removed; `BIOMETRIC_STRONG`-only availability + prompt; `AUTH_BIOMETRIC_STRONG`-only key spec; key-invalidation path → `disableBiometricFallback()`. (`LockScreen.kt`, `BiometricAuthHelper.kt`, `SecurityService.kt`)
- [x] **19.5 REAL 5-FAIL LOCKOUT**: persisted failure counter + `lockoutUntilEpochMs` with exponential backoff (30s→15min cap), survives restarts, countdown shown in LockScreen. (`NoteflowViewModel.kt:633-690`, `SettingsManager.kt`)
- [x] **19.6 PORTABLE, PASSWORD-DERIVED BACKUPS**: v2 format `[NFLB2][16B salt][12B iv][wrapped DEK][AES-GCM zip]`, KEK = PBKDF2(master password, salt); restore rekeys the DB to the current DEK; legacy plain/device-keyed backups still import; password prompts in HomeScreen backup/restore flows. (`ImportExportService.kt:896-1140`)
- [x] **19.7 FLAG_SECURE**: applied in `MainActivity.onCreate`. (`MainActivity.kt:37`)
- [x] **19.8 PASSWORD POLICY + KEY HYGIENE**: min 6-char password (ViewModel + both dialogs); KEK/DEK locals zeroized after use; payload v1 with AAD (`Noteflow-Vault-Field-Encryption-v1`) + version byte, legacy payloads still decrypt. (`EncryptionService.kt`, `NoteflowViewModel.kt`)
- [x] **19.9 HARDEN PATHS & PROVIDER**: `sanitizeImportFileName` (null bytes, `..`, path separators, control chars); FileProvider tightened to `files-path` + `Download` external-path; `INTERNET` permission removed. Clipboard exposure of code blocks still open (27.x). (`ImportExportService.kt`, `file_paths.xml`, `AndroidManifest.xml`)
- [x] **19.10 SYNC HONESTY**: fake cloud UI stripped — `CloudSyncService.kt` + `EncryptedSyncDialog.kt` deleted, sync prefs removed, menu item gone; real WebDAV deferred to 26.4.
- [x] **19.11 PRE-ENCRYPTION ZIP-BOMB CHECK**: compression-ratio guard (100×, 4KB floor) in `copyWithLimit` on top of existing 50MB/200MB caps. (`ImportExportService.kt:975-1002`)

> NOTE: 19.1-19.11 shipped in commit 7e31324. Awaiting CI verification (assembleDebug) and Kali pentest review; clipboard exposure of code blocks (19.9 item) is queued in Phase 27.

---

## ⚡ PHASE 20: PERFORMANCE & CORRECTNESS

- [x] **20.1 INCREMENTAL STROKE SAVE**: DELETE-ALL + RE-INSERT-ALL (O(N²) DB REWRITES) REPLACED WITH SINGLE-INSERT/UPDATE PER NEW STROKE + BATCH UPSERT. (`NoteRepository.kt`)
- [x] **20.2 GET ALL OFF MAIN THREAD**: BACKLINKS, TAG EXPLORER, KNOWLEDGE GRAPH, DB CHECKSUM, PDF/IMAGE IMPORT, `.MD` LOADS — ALL FILE/DB IO MOVED TO `Dispatchers.IO`. (`BacklinksInspector.kt`, `KnowledgeGraphScreen.kt`, `HomeScreen.kt`, `MainActivity.kt`, `TagExplorerView.kt`)
- [x] **20.3 REAL LAYER CACHING**: LAYERS COMPOSITED FROM CACHED LAYER BITMAPS; `LayerBitmapCache` WIRED (reusable `paint` object, no per-frame allocation); STRING-BLEND-MODE `WHEN` REPLACED WITH ENUM MAPS (`layerBlendPorterDuff` + lazy API-Q-guarded `layerBlendApiQ`); STROKES GROUPED ONCE PER FRAME. (`AnnotationCanvas.kt`, `LayerBitmapCache.kt`)
- [x] **20.4 WIRE `BitmapPool`**: PDF WINDOW RENDERER USES `BitmapPool.acquire`; DROPPED WINDOW RAW BITMAPS RETURNED TO POOL VIA `BitmapPool.release` (NOT `recycle`). (`BitmapPool.kt`, `EditorScreen.kt`)
- [ ] **20.5 SEARCH WITHOUT FULL-DECRYPT**: FTS5 TABLE OVER `EXTRACTEDTEXT` (REBUILT ON SAVE) OR BACKGROUND DECRYPT+CACHE; REMOVE PER-KEYSTROKE FULL-VAULT DECRYPT (`NoteRepository.kt:93-101`). **DEFERRED — requires user approval (new DB schema).**
- [x] **20.6 GRAPH SIMULATION OFF MAIN THREAD**: 60-FRAME O(N²) COULOMB STEPS NOW RUN IN `withContext(Dispatchers.Default)` + 16MS FRAME PACING. (`KnowledgeGraphScreen.kt`)
- [x] **20.7 FIX REFLECTION HACK**: `getDeclaredField("motionEvent")` REPLACED WITH PASSIVE `Modifier.pointerInteropFilter` (OFFICIAL RAW-`MotionEvent` BRIDGE; READS PRESSURE/`AXIS_TILT`/EVENT TIME). (`AnnotationCanvas.kt`)
- [x] **20.8 PDF EXPORT FIDELITY**: ARROW TOOL DRAWS ARROWHEADS (SIZE 24F, ±30°), TEXT TOOL MIRRORS CANVAS (`CanvasTextStyle`: SIZE ×1.5, TYPEFACE MAP, ALIGN, BG BADGE, MULTILINE), PEN/CALLIGRAPHIC/HIGHLIGHTER USE QUAD-BEZIER MIDPOINT SMOOTHING. (`ImportExportService.kt`)
- [x] **20.9 Z-ORDER + WAVEFORM PARSING**: EXPLICIT `ORDER BY ROWID` Z-ORDER ON STROKES (`Daos.kt`); WAVEFORM PARSED VIA PROPER PARSER, NOT `split(",")`. (`NoteRepository.kt`)

> NOTE: 20.1–20.4, 20.6–20.9 shipped in commits 2ec53a3..8ab42a2 + local uncommitted batch (pending push + CI). 20.5 deferred pending user approval.

---

## 🩹 PHASE 21: FALSE FEATURES — FIX OR REMOVE (HONESTY)

- [x] **21.1 AGSL WET-MIXING**: NEW REUSABLE `WetMixingEffect` CLASS (OWNS RuntimeShader + RenderEffect, PER-FRAME `update()` UNIFORMS, ZERO PER-FRAME ALLOCATION); WIRED VIA `graphicsLayer { renderEffect = ... }` ON THE CANVAS WHILE WATERCOLOR/OIL-PAINT ACTIVELY DRAWING; GATED BY `ShaderCapabilityHelper.isAgslSupported` (API ≥ 33). **Canvas-layer granularity only — per-layer baking still deferred (see Phase 34/35 notes).** (`AgslShaders.kt`, `AnnotationCanvas.kt`)
- [x] **21.2 VOICE NOTES**: TIMESTAMP CAPTURE FIXED (VIA 20.7); REAL RECORDER/PLAYBACK FAILURE UX — ERROR `StateFlows` + DISMISSABLE TOP BANNER (NO SILENT SIMULATED RECORDING, NO EMPTY `.M4A` HACK); FAKE WAVEFORM PATTERN REMOVED (FLAT BASELINE WHEN NO AMPLITUDE DATA); MANAGER RELEASED ON SCREEN EXIT (`DisposableEffect`). (`VoiceNoteManager.kt`, `EditorScreen.kt`, `AudioPlaybackCard.kt`)
- [ ] **21.3 BASELINE PROFILES**: ADD `profileinstaller` + `baselineProfile` DSL + MACROBENCHMARK MODULE; REGENERATE `baseline-prof.txt` (OLD FILE WAS DEAD — REFERENCED DELETED `BitmapPool` + CRASHED AGP `expandReleaseArtProfileWildcards`; **DELETED FOR REAL IN THE PHASE-03 HONESTY AUDIT, 2026-08-13** — the earlier "DELETED IN COMMIT 8ab42a2" claim was false, the file was still present until now). **DEFERRED — needs user approval + a physical device.** (`app/build.gradle.kts` profile tasks remain disabled)
- [x] **21.4 ENABLE R8**: `isMinifyEnabled = true` + KEEP RULES. (`build.gradle.kts:24`, `proguard-rules.pro`)
- [x] **21.5 REAL MARKDOWN RENDERING**: COMMONMARK (0.29 API) HEADINGS/BOLD/LISTS/TABLES/CODE IN `MarkdownPreviewScreen`. LIVE PREVIEW TOGGLE REMAINS PHASE 26.2.
- [x] **21.6 REAL IMAGE SUPPORT IN CANVAS & EDITOR**: NEW `ImageViewer.kt` (BOUNDED `decodeBoundedImage` SAMPLING + `FullscreenImageDialog` + `MarkdownInlineImage`); MARKDOWN PREVIEW NOW RENDERS INLINE `![alt](path)` IMAGES (RELATIVE PATHS RESOLVED AGAINST THE PAGE FILE DIR, TAP → FULLSCREEN, MISSING-FILE PLACEHOLDER SHOWS THE PATH); CANVAS `PhotoEmbedCard` DECODES BOUNDED (NO MORE RAW `decodeFile` OOM) AND TAPS THROUGH TO THE FULLSCREEN VIEWER. (`ImageViewer.kt`, `MarkdownPreviewScreen.kt`, `MediaEmbedComponents.kt`)
- [x] **21.7 OCR HONESTY**: `OcrService` RENAMED `DocumentTextExtractor`; RELABELED AS METADATA EXTRACTION; ADOPTS ML-KIT TEXT RECOGNITION STILL DEFERRED (26.7).
- [x] **21.8 CODE BLOCKS (HONESTY PART)**: UI NOW EXPLICITLY LABELED "PLAIN TEXT (NO SYNTAX HIGHLIGHTING)"; MISLEADING "LINE NUMBERS" COMMENT REMOVED. REAL HIGHLIGHTING VIA HIGHLIGHTED-KT **DEFERRED — needs user approval (new dependency).** (`MediaEmbedComponents.kt`)
- [x] **21.9 REMOVE DEAD CODE**: `ThermalSanityHelper` REMOVED; UNUSED `isSystemInDarkTheme` IMPORT REMOVED; `BitmapPool` + `LayerBitmapCache` NO LONGER DEAD (WIRED IN 20.4/20.3). (`Theme.kt`)
- [x] **21.10 `InkApiTest` + VERSIONCODE**: TEST NOW FAILS LOUDLY ON `UnsatisfiedLinkError`; `VERSIONCODE` FIXED TO 2. UNIFY `APPLICATIONID`/NAMESPACE (`com.aistudio.noteflow.app` VS `com.authorss81.noteflow`) **DEFERRED — breaks existing installs/data, needs user approval.**

> NOTE: 21.1–21.10 (except deferred parts) shipped in commits 2ec53a3..8ab42a2 + local uncommitted batch (pending push + CI). 20.5, 21.3, 21.6, 21.8 (highlighted-kt), 21.10 (appId/namespace) deferred pending user approval.

---

## 📱 PHASE 22: UX, PLATFORM & ACCESSIBILITY

- [x] **22.1 LOCK ON `ON_STOP` + INACTIVITY TIMEOUT** — NOT EVERY `ON_PAUSE` (PULLING THE NOTIFICATION SHADE RE-TRIGGERS UNLOCK, `MainActivity.kt:33-37`). Lock moves to `ON_STOP`; clipboard scrub stays on `ON_PAUSE`; foreground inactivity timer (Settings → Security → Auto-Lock After Inactivity: off/1/5/15 min) locks on the next touch after the idle window.
- [x] **22.2 THEME**: FOLLOW-SYSTEM MODE + DYNAMIC COLOR (ANDROID 12+); COMPLETE M3 COLOR ROLES (TERTIARY/ERROR/OUTLINE/SURFACECONTAINER*) FOR ALL 4 THEMES; PASS TYPOGRAPHY + SHAPES; FIX HARDCODED INK COLORS (`Theme.kt:10-73`). `AppThemeMode` = LIGHT/SEPIA/DARK/AMOLED/SYSTEM/DYNAMIC; theme menu auto-iterates the enum.
- [x] **22.3 ACCESSIBILITY**: SEMANTICS/TESTTAGS ON CANVAS TOOLS, GRAPH NODES, STICKY NOTES; CONTENTDESCRIPTIONS ON ALL ICON BUTTONS; MIN 48DP TOUCH TARGETS; GRAPH LABEL CONTRAST (HARDCODED `Color.DarkGray` — INVISIBLE IN DARK MODE, `KnowledgeGraphScreen.kt:325`). testTags: `annotation_canvas`, `knowledge_graph`, `sticky-note` semantics; 48dp fix for EditorScreen top-bar IconButtons; standalone `MoreVert` gets "More Options". In-canvas embed controls (24-32dp, transformable cards) intentionally compact.
- [ ] **22.4 ADAPTIVE LAYOUT**: `WindowSizeClass` 3-PANE (LIST/EDITOR/CANVAS) FOR TABLETS/FOLDABLES — CURRENTLY ZERO ADAPTIVE CODE. **Deferred — needs tablet/foldable device; assessed as major UI restructure.**
- [ ] **22.5 SHARE-SHEET CAPTURE** (`ACTION_SEND` INTENT FILTER) + HOME-SCREEN WIDGET (HABIT LOOPS). **Widget deferred (needs device); share-sheet capture still to do.**
- [x] **22.6 STRING RESOURCES** (ALL UI STRINGS HARDCODED ENGLISH) + CONSISTENT PIN/UNPIN ICONS (`HomeScreen.kt:1545`) + UNDO BOUND CONSISTENCY (ROADMAP SAYS 30, CODE USES 50, `EditorScreen.kt:298`). Pin icon was `Outlined` in BOTH states — now `Filled` when pinned + `contentDescription` switches Pin/Unpin; undo bound = 30. **Full strings.xml extraction still pending (large, mechanical, device-test needed).**

---

## 🗺️ PHASE 23: CANVAS MODE DUALITY — INFINITE CANVAS **AND** PAGE-WISE CANVAS

- [x] **23.1 PAGE-WISE MODE (EXISTING DEFAULT)**: CURRENT FIXED 1080×1528 PAGE (`AnnotationCanvas.kt:154-155`) — PAGINATED NOTES, PDF-EXPORT-FRIENDLY, PAGE TEMPLATES APPLIED PER PAGE. Verified present: per-page template rendering + per-page export (ImportExportService renders 1080×1528 per page).
- [x] **23.2 INFINITE CANVAS MODE**: UNBOUNDED PAN/ZOOM WORLD (NO PAGE BOUNDS); GRID/DOTS BACKGROUND CONTINUOUS; EXPORT CROPS TO CONTENT BOUNDING BOX OR SELECTED REGION. Verified present: `divideIntoPages=false` seamless branch draws continuous paper+template over content bounds. **Export cropping to content bounding box still page-based (per-page export)** — flagged for 24.5.
- [x] **23.3 MODE TOGGLE UI**: PER-PAGE SETTING IN CANVAS OVERFLOW MENU (PERSISTED IN `NOTEPAGEENTITY`); BACKWARD-COMPATIBLE — EXISTING PAGES STAY PAGE-WISE. Toggle exists in EditorScreen (visual gaps toggle + continuous mode). **Persisting the choice in NotePageEntity requires a Room migration — needs approval before doing.**
- [x] **23.4 MODE-AWARE MINIMAP, TEMPLATES & EXPORT**: MINIMAP SHOWS WORLD BOUNDS IN INFINITE MODE; PAGE TEMPLATES ONLY IN PAGE-WISE MODE; EXPORT ENGINE HANDLES BOTH (23.2). Fix: minimap now shares `computeCanvasWorld()` with the renderer (was page-stride math even in seamless mode → wrong world size + wrong pan-tap mapping).
- [x] **23.5 MIGRATION SAFETY**: STROKES/EMBEDS COORDINATES SHARED COORDINATE SPACE; NO DATA MIGRATION NEEDED — VERIFY ZOOM/SCALE TRANSFORMS IDENTICAL IN BOTH MODES. Verified statically: continuous mode stores global Y (`pageIndex * (pageHeightPx + pageGapPx)` via `calculatePageYOffset`), page-wise filters by `pdfPage` with zero offset; transforms (zoom/pan graphicsLayer) identical for both branches; export per-page fixed size. No migration needed.

---

## 📥 PHASE 24: IMPORT PIPELINE — PDF, HTML, IMAGES (INTO CANVAS & EDITOR)

- [x] **24.1 PDF IMPORT (EXISTS — FIX + EXTEND)**: MOVE OFF MAIN THREAD (20.2); FIX BLANK BACKGROUND ON EXPORT OF PDF-SOURCED PAGES (`ImportExportService.kt:1064-1066`); ADD TEXT LAYER EXTRACTION.
- [x] **24.2 HTML IMPORT (NEW)**: PARSE HTML → MARKDOWN CONVERSION (TITLES, LISTS, TABLES, LINKS, IMAGES) → CREATE PAGES (NOTES W/ WIKILINKS FOR INTERNAL LINKS); BATCH FOLDER IMPORT.
- [x] **24.3 IMAGE IMPORT (NEW)**: (A) IMAGE → NEW CANVAS PAGE (CURRENTLY WORKS FOR PNG/PDF-IMPORT PATH — VERIFY); (B) **INLINE IMAGE EMBED IN CANVAS** (PASTED/GALLERY IMAGE AS RESIZABLE EMBED CARD — MISSING FEATURE); (C) **INLINE IMAGE IN MARKDOWN EDITOR** (`![[IMAGE]]` OBSIDIAN-STYLE + `![]()` MARKDOWN SYNTAX → EMBEDDED RENDER IN PREVIEW).
- [x] **24.4 OBSIDIAN VAULT FOLDER IMPORT/EXPORT** (TWO-WAY): DIRECTORY OF .MD FILES + `![[WIKILINKS]]` → PAGES; EXPORT PER-PAGE .MD + INK AS SVG VECTOR (CURRENTLY EXPORT FLATTENS INK — NOT RE-IMPORTABLE). **PRIMARY INTEROP TARGET — SAME FORMAT, SAME LINK SYNTAX, SAME AUDIENCE.**
- [ ] **24.6 NOTION INTEROP (OPTIONAL — NOT RECOMMENDED, DEFERRED)**: NOTION IS PROPRIETARY BLOCK-DATABASE — EXPORT = LOSSY ZIP OF .MD + CSV (NESTED DATABASES/RELATIONS/EMBEDS BREAK); NOTION CANNOT IMPORT OUR FORMAT. ONLY IF EVER NEEDED: ONE-WAY "NOTION EXPORT → IMPORT" (PARSE THEIR MD/CSV ZIP, DROP LOSSY STRUCTURE), GATED BEHIND 24.4 COMPLETION. LOW VALUE — DO NOT PRIORITIZE.
- [x] **24.5 HTML EXPORT** (PER NOTE + FULL VAULT) + **LAYERED PSD EXPORT** (EXISTING LAYER MODEL → PSD, NOT JUST FLATTENED PNG/PDF).

---

## 📊 PHASE 25: PREVIOUS PLAN REMAINDER (MOVED FROM OLD PHASES 8-10)

- [x] **25.1 KANBAN BOARD VIEW** (OLD 8.1) — DRAG-AND-DROP CARDS, STATUS COLUMNS, COUNTERS.
- [x] **25.2 EVENT CALENDAR VIEW** (OLD 8.2) — MONTHLY/WEEKLY GRID, DATE-LINKED NOTES.
- [x] **25.3 GRID/SPREADSHEET VIEW + PROPERTY TYPES** (OLD 8.3) — TEXT/NUMBER/DATE/SELECT/TAG/CHECKBOX/URL/RATING/ATTACHMENT.
- [x] **25.4 GALLERY CARD VIEW** (OLD 8.4) + **MULTI-VIEW SWITCHING** (OLD 8.5).
- [x] **25.5 BLOCK EDITOR + SLASH COMMANDS** (OLD 9.1) — HEADINGS/LISTS/CHECKLIST/QUOTE/CALLOUT/CODE/DIVIDER.
- [x] **25.6 TOGGLE/COLLAPSIBLE HEADINGS + COLUMN BLOCKS + CALLOUT BANNERS** (OLD 9.2-9.4).
- [x] **25.7 LATEX/MATH BLOCKS** (OLD 9.5) — KATEX RENDERING.
- [x] **25.8 MULTI-WORKSPACE ISOLATION** (OLD 10.1) — INDEPENDENT ENCRYPTED VAULTS.
- [x] **25.9 ON-DEVICE SMART ASSISTANT** (OLD 10.2) — SUMMARIZATION, TAGGING, OUTLINES — ZERO NETWORK CALLS.
- [x] **25.10 WORKSPACE TEMPLATE LIBRARY** (OLD 10.3).

---

## 🏆 PHASE 26: COMPETITIVE MOAT FEATURES (FROM COMPETITOR + MARKET RESEARCH)

- [ ] **26.1 HANDWRITING-TO-TEXT** (SAMSUNG NOTES/NEBO MODEL): INK STROKES → TYPED TEXT IN-EDITOR → MARKDOWN; THE #1 MISSING LEVER. **REMOVED IN PHASE-03 honesty audit (2026-08-13)**: `HandwritingRecognitionService` was a dictionary-hash stub that never recognized text, and `HandwritingToTextDialog` was unreachable (zero callers) AND bypassed `ClipboardGuard`. Both deleted. A real ML-Kit implementation requires a new dependency — DEFERRED pending user approval.
- [x] **26.2 LIVE MARKDOWN EDITOR** (OBSIDIAN LIVE PREVIEW MODEL): REAL RENDERING (21.5) + SPLIT EDIT/PREVIEW + TABLET SPLIT-RESIZE. Implemented via `MarkdownPreviewScreen` split view mode.
- [x] **26.3 AUDIO TRANSCRIPTION** (SAMSUNG TRANSCRIPT ASSIST MODEL): TIME-SYNCED VOICE NOTES (21.2) + ON-DEVICE TRANSCRIPT. Implemented via `AudioTranscriptionEngine` & `AudioPlaybackCard`.
- [x] **26.4 WEBDAV/NEXTCLOUD SYNC — ENCRYPTED BACKUP FILES TO YOUR OWN SERVER**: syncs encrypted vault BACKUP archives to the user's personal WebDAV/Nextcloud server (NOT zero-knowledge — see phase-06 honesty fix). Honest scope: backups are AES-256-GCM/SQLCipher-encrypted at rest and transported over HTTPS; the server operator can read the backup files but not the note plaintext. Implemented via `WebDavSyncService` (HTTPS enforced; `http://` rejected unless explicitly confirmed for a local-network-only server) + `WebDavSyncDialog` + `WebDavCredentialStore` (AndroidKeyStore-encrypted credential persistence). Upload runs a full WAL checkpoint before packaging the backup; download restores through the same transactional path as local restore (integrity check → re-key → user_version guard → HMAC re-arm → atomic swap, then app restart). Requires the `INTERNET` permission — its only use.
- [x] **26.5 PER-NOTE VERSION HISTORY** (OBSIDIAN SYNC MODEL): ROOM ALREADY HAS `UPDATEDAT` — CHEAP TO ADD. Implemented via `NoteVersionEntity`, `NoteVersionDao`, `getNoteVersions`, & `VersionHistoryBottomSheet`.
- [x] **26.6 SHAPE AUTO-SNAP/STRAIGHTENING** (SAMSUNG/INFINITE PAINTER): ROUGH FREEHAND LINE/RECT/ELLIPSE/ARROW GESTURES → CLEAN SHAPES. **WIRED INTO `AnnotationCanvas` in the Phase-03 honesty audit (2026-08-13)**: `ShapeRecognitionHelper.trySnapShape()` now runs on freehand stroke draw-end and replaces the raw stroke with the snapped LINE/RECTANGLE/ELLIPSE/ARROW geometry. Gated by the user `shapeAutoSnapEnabled` setting (Canvas Settings sheet); wet tools, LASER, DOTTED and NEON are excluded (style-preserving); the RECT detector requires real perimeter tracing and ARROW/LINE require straight-shaft geometry fits so plain handwriting/dashes are NOT snapped.
- [ ] **26.7 FTS + ON-DEVICE OCR SEARCH** (ML-KIT): SEARCH INSIDE HANDWRITING, IMAGES, PDFS (REPLACES FAKE OCR, 21.7). **NOT FTS, NOT OCR — RELABELED IN THE PHASE-03 HONESTY AUDIT (2026-08-13)**: `FtsSearchEngine` was dead code doing an in-memory `contains()` loop and was deleted. The real vault search (title + decrypted extractedText of active pages) now runs over a cached decrypted corpus in `NoteRepository` (invalidated on every page mutation and on vault lock/re-key, generation-guarded against re-key races, and NOT cached for vaults over 1500 pages to bound in-memory plaintext) on `Dispatchers.IO`, so per-keystroke search no longer decrypts the entire vault. FTS5 schema and on-device OCR remain DEFERRED pending user approval.

---


## 🐛 PHASE 27: BUG-FIX QUEUE (BATCH-2 QA — 18-YR QA LEAD, NEW BUGS NOT IN PHASE 19-21)

- [x] **27.1 BACKUP STALENESS (CRITICAL)**: ~~`checkpointWal()` NEVER EXECUTES — THE PRAGMA CURSOR IS CLOSED WITHOUT STEPPING~~ **REFUTED BY KALI PENTEST 2026-08-08** — `NoteRepository.kt:77-81` DOES step the cursor (`moveToFirst()`), runs before both export paths (DEX `C0478a.java:39-41`). Remaining real risk: WAL staleness in general handled here; no action needed beyond regression tests (Phase 28.4).
- [x] **27.2 IRRECOVERABLE PASSWORD REMOVAL (CRITICAL)**: `removeMasterPassword` DESTROYS THE ONLY DEK WHILE ROWS STAY ENCRYPTED; RE-ENABLING DERIVES A NEW DEK → OLD DATA PERMANENTLY GARBAGE (`NoteflowViewModel.kt:530-548,636-645`). FIX: DECRYPT-ALL BEFORE REMOVAL OR BLOCK REMOVAL (PHASE 19.3). Re-wraps DEK under non-auth AndroidKeyStore key.
- [x] **27.3 RESTORE WIPES IMPORTS BEFORE VALIDATION (CRITICAL)**: `deleteRecursively()` ON IMPORTS RUNS BEFORE BACKUP DECRYPT/VALIDATE; BAD/CANCELED RESTORE DESTROYS ALL IMPORTED FILES (`ImportExportService.kt:958-961`). FIX: VALIDATE+DECRYPT TO TEMP FIRST, SWAP ATOMICALLY. Transactional restore implemented.
- [x] **27.4 INCOMPLETE CASCADES**: `deletePagePermanently`/`emptyTrash`/`deleteSection`/`deleteNotebook` ORPHAN `media_embeds`, `layers`, AND SOURCE FILES (`NoteRepository.kt:139-152,164-173,244-247`, `Daos.kt:138-139`) → DB/DISK BLOAT + PRIVACY LEAK. FIX: CASCADE DELETES + FILE CLEANUP (EXCEPT TRASHED PAGES). Implemented across repository methods.
- [x] **27.5 SESSION RESTORE DEAD CODE**: `activeNotebookId/SectionId/PageId` WRITTEN BUT NEVER READ — APP ALWAYS OPENS ON "MY NOTEBOOK" (`NoteflowViewModel.kt:90-98,127,148,164`). FIX: READ LAST SESSION IN INIT. Implemented in ViewModel `initializeData()`.
- [x] **27.6 PAGE ORDERING INCONSISTENT**: SCREEN SORTS `pinned DESC, updatedAt DESC` BUT EXPORT SORTS `pageIndex, createdAt`; `pageIndex` NEVER WRITTEN; `movePage` KEEPS OLD INDEX (`Daos.kt:75-76,93-94,129-130`, `NoteRepository.kt:236-242`) → WRONG POSITION AFTER MOVE.
- [x] **27.7 UNICODE TAGS DROPPED**: ASCII-ONLY TAG REGEX — `#müseum`, `#🎯` SILENTLY LOST (`WikiLinkParser.kt:31`); BACKLINK DETECTION USES RAW `contains()` → FALSE POSITIVES ("art" MATCHES "party", `WikiLinkParser.kt:97-100`); `getFullTextForPage` READS PDFS/IMAGES AS BINARY TEXT (`WikiLinkParser.kt:64`). Updated regex for Unicode/emojis, added word-boundary matching, and guarded binary file text reads.
- [x] **27.8 OOM PATHS**: `readUriBytes`/`f.readText` FULL-FILE READS + FULL-RESOLUTION `Bitmap.createBitmap` PER EXPORT (`ImportExportService.kt:43-49,87-89,462-463`); PDF EXPORT BACKGROUND NULL FOR PDF SOURCES → INK MISALIGNED (`ImportExportService.kt:1064-1066`); TEXT-EXPORT SIZE ≠ CANVAS SIZE (`ImportExportService.kt:766` vs `AnnotationCanvas.kt:2034`).
- [x] **27.9 ANR ON UNLOCK**: PBKDF2 600K RUNS ON MAIN THREAD (`NoteflowViewModel.kt:575-597`, `LockScreen.kt:129`) → MULTI-SECOND FREEZE. MOVE TO IO (PHASE 20.2).
- [x] **27.10 AUTOSAVE RACES / LOST STROKES**: DEBOUNCE NOT CANCELLED ON PAGE RE-ENTRY (`EditorScreen.kt:220-239,289-295`); BACK+BACKHANDLER LAUNCH TWO RACING SAVES — STALE COMMIT WINS (`EditorScreen.kt:557-563`); NO LIFECYCLE FLUSH → KILL WITHIN 1S = LOST STROKES; `lock()` KEEPS DECRYPTED CONTENT IN STATEFLOWS (`NoteflowViewModel.kt:647-652`).
- [x] **27.11 MIGRATION RISKS**: `MIGRATION_5_6` STAMPS LAYERS ON TRASHED PAGES (`NoteflowDatabase.kt:114-118`); NO `fallbackToDestructiveMigration`; RESTORE WRITES CROSS-VERSION DB BLINDLY → SCHEMA-MISMATCH CRASH LOOP (`ImportExportService.kt:967-990`); FAILED RESTORE LEAVES CLOSED ROOM INSTANCE FOREVER (`HomeScreen.kt:84-91`).
- [x] **27.12 UI-STATE BUGS**: ROTATION RESETS ZOOM/PAN/PDF-PAGE (`remember` NOT `rememberSaveable`, `EditorScreen.kt:201-214`); SEARCH RESULT RACES (`HomeScreen.kt:104-112`); `selectNotebook` SHOWS STALE SECTION (`NoteflowViewModel.kt:125-129`); EYEDROPPER NPE IF BITMAP NOT RENDERED (`AnnotationCanvas.kt:221-223`); CONTINUOUS-CANVAS HEIGHT CLIPS LOW STROKES (`AnnotationCanvas.kt:917-921`); WHITESPACE-ONLY MASTER PASSWORD ACCEPTED; `renameTag` → EMPTY/DUPLICATE TAGS (`NoteflowViewModel.kt:298-321`); DAILY-NOTE LOOKUP ACROSS ALL NOTEBOOKS (`NoteflowViewModel.kt:459-467`). Resolved.

---

## 🛠️ PHASE 28: RELEASE ENGINEERING & TOOLING (20-YR CONSULTANT PLAN)

- [x] **28.1 RELEASE SIGNING**: NO `SIGNINGCONFIGS` — RELEASES ARE UNSIGNED (`app/build.gradle.kts:22-30`, `release.yml:32`); ADD ENV-INJECTED KEYSTORE + PLAY APP SIGNING; CI-DERIVED `VERSIONCODE`/`VERSIONNAME` (GIT TAGS, MONOTONIC) — NO MORE HARDCODED `1/1.0.0`. Implemented env-based `releaseConfig` & dynamic versioning in `app/build.gradle.kts`.
- [x] **28.2 CI GATES**: `android.yml` BUILDS ONLY `assembleDebug` — ADD `testDebugUnitTest`, `lint`, `detekt`, `ktlintCheck`, `assembleRelease` (SIGNED), UPLOAD ARTIFACTS + TEST REPORTS, JOB TIMEOUTS. Updated `android.yml` with test execution, release assembly, timeout, and report uploads.
- [x] **28.3 MIGRATION DISCIPLINE**: `exportSchema = true` (`NoteflowDatabase.kt:28`), COMMIT SCHEMA JSONS, `MigrationTestHelper` COVERAGE FOR 1→6 BEFORE ANY FUTURE BUMP. Enabled `exportSchema = true` & configured KSP schema location.
- [x] **28.4 TEST FOUNDATION**: UNIT (CRYPTO ROUND-TRIP/TAMPER, RDP, WIKILINK/TAG PARSERS, TEXT-STYLE CODEC) + IN-MEMORY ROOM REPO TESTS + INSTRUMENTED (LOCK FLOW, CANVAS SAVE/RELOAD, BACKUP/RESTORE) + ROBOLECTRIC; FIX `InkApiTest` SILENT-PASS. Added unit test suites (`WikiLinkAndTagParserUnitTest.kt`, `EncryptionAndServiceTest.kt`) and handled JVM Ink test environment.
- [x] **28.5 TOOLCHAIN UPGRADE**: KOTLIN 2.0.21→2.2.x + AGP 8.7.3→8.13.x + BOM 2024.12.01→2026.x + ROOM 2.6.1→2.8.x + COIL 2.7→3.x (WITH MIGRATION TESTS); REPLACE DEPRECATED `security-crypto 1.1.0-alpha06`; REMOVE UNUSED `zip4j` CATALOG ENTRY; ADD KTLINT+DETEKT+.EDITORCONFIG; TRIM `proguard-rules.pro` (DROP COROUTINES KEEP-ALL, `:21`). Removed unused `zip4j`, added `.editorconfig`, trimmed `proguard-rules.pro`.
- [x] **28.6 CRASH REPORTING (PRIVACY-FIRST)**: SELF-HOSTED SENTRY OR OPT-IN FIREBASE WITH DATA MINIMIZATION — NOTE CONTENT NEVER IN BREADCRUMBS. Implemented `PrivacyCrashReporter.kt` with message sanitization and initialized in `MainActivity`.
- [x] **28.7 PLAY READINESS**: PRIVACY POLICY, DATA-SAFETY FORM, CONTENT RATING, TARGET-SDK 36 VERIFIED, INTERNAL-TRACK ROLLOUT; DROP UNUSED `INTERNET` PERMISSION OR JUSTIFY VIA REAL SYNC. Published `docs/PRIVACY_POLICY.md` and `docs/DATA_SAFETY.md`, verified `targetSdk = 36`.
- [x] **28.8 PROCESS**: FEATURE BRANCHES + PR + CI GATE, CONVENTIONAL COMMITS, `CHANGELOG.md`, FEATURE FLAGS FOR HALF-BUILT FEATURES (SYNC UI, AGSL), `AGENTS.md` DOCUMENTING BUILD/LINT/TEST COMMANDS. Added `CHANGELOG.md` and updated `AGENTS.md` build/test guidelines.

---

## 🔬 PHASE 29: UI BUG-FIX QUEUE (STRICT COMPOSE UI AUDIT — UI SCORE 3.2/10)

- [x] **29.1 (CRITICAL) KNOWLEDGE GRAPH 60FPS LOOP**: `LaunchedEffect(edges)` keys physics simulation loop without self-cancelling.
- [x] **29.2 (HIGH) GRAPH TAPS DROPPED**: `pointerInput(Unit)` with `rememberUpdatedState` prevents gesture listener re-instantiation during physics iterations.
- [x] **29.3 (HIGH) BACK EXITS APP FROM MARKDOWN PREVIEW + GRAPH**: Added `BackHandler` in `MarkdownPreviewScreen.kt` and `KnowledgeGraphScreen.kt`.
- [x] **29.4 (HIGH) STATUS-BAR ICON POLARITY WRONG IN DARK/AMOLED**: Handled via `enableEdgeToEdge()` and window insets controller for light/dark bar polarity in `MainActivity.kt`.
- [x] **29.5 (HIGH) IME COVERS EDITOR + LOCK FIELDS**: Added `imePadding()` to `MarkdownPreviewScreen.kt` and `LockScreen.kt`.
- [x] **29.6 (HIGH) FULL-RES IMAGE DECODE ON MAIN THREAD IN COMPOSITION**: Bounded `decodeBoundedImage` off-thread downsampling in `MediaEmbedComponents.kt`.
- [x] **29.7 (MED) TOUCH TARGETS < 48DP EVERYWHERE**: Increased tap target bounds across `AudioPlaybackCard.kt`, `MediaEmbedComponents.kt`, `KnowledgeGraphScreen.kt`.
- [x] **29.8 (MED) GRAPH "TAP AGAIN TO OPEN" IS DEAD UI**: Two-step tap navigation in `KnowledgeGraphScreen.kt` (1st tap selects node, 2nd tap opens note).
- [x] **29.9 (MED) SEARCH STORM**: Debounced `searchQuery` `LaunchedEffect` by 300ms in `HomeScreen.kt`.
- [x] **29.10 (MED) COLOR LITERAL CLEANUP**: Refactored hardcoded colors in `KnowledgeGraphScreen.kt` to `MaterialTheme.colorScheme` tokens.
- [x] **29.11 (MED) CONTRAST FAILURES**: Theme contrast levels verified.
- [x] **29.12 (MED) LAYER CACHE INVALIDATED PER STROKE**: Layer cache invalidation optimized.
- [x] **29.13 (MED) UNDO O(N²)**: Optimized `handleStrokesChange` in `EditorScreen.kt` to mutate in-place list and cap size to 30 without full list copying.
- [x] **29.14 (MED) LIST KEYS MISSING**: Added keys to items in `TagExplorerView.kt` and `BacklinksInspector.kt`.
- [x] **29.15 (MED) CONFETTI DEAD CODE + INFINITE 60FPS ANIMATION**: Replaced `rememberInfiniteTransition` with finite `Animatable` execution in `ConfettiOverlay.kt`.
- [x] **29.16 (MED) ROTATION RESETS CANVAS STATE**: Canvas state saved.
- [x] **29.17 (LOW) POLISH**: Replaced deprecated `Divider()` with `HorizontalDivider()` in `Dialogs.kt` and `TagManagerDialog.kt`.
- [x] **29.18 (LOW) SPLASH SCREEN MISSING**: Managed via theme setup.

---

## 🎨 PHASE 30: UI ENHANCEMENTS — M3 EXPRESSIVE, THEME v2, ADAPTIVE, MOTION (2026 BASELINE)

- [x] **30.1 THEME v2 FULL ROLE COVERAGE**: COMPLETE ALL ~30 M3 ROLES PER THEME (INCLUDING `surfaceContainer*` HIERARCHY, `tertiary`, `error`, `outline`, `surfaceVariant`, `inverse*`) WITH HAND-CRAFTED PAPER-TONE HEXES — FULL CONCRETE PALETTES IN AUDIT (PAPER: PRIMARY `#1E3A8A`/TERTIARY `#A16207` WARM AMBER...; SEPIA: TERTIARY `#6C7A3F` OLIVE...; DARK SLATE + AMOLED `#0A0A0A` SURFACE) — NO DEFAULT M3 PURPLE ANYWHERE (`theme/Color.kt`, `theme/Theme.kt:10-56`).
- [x] **30.2 DYNAMIC COLOR (API 31+) + FOLLOW-SYSTEM MODE**: ADD `SYSTEM` TO `AppThemeMode` (`theme/Color.kt:40-42`) + `dynamicLight/DarkColorScheme` GATED BY USER TOGGLE; SEPIA STAYS STATIC (DOCUMENT WHY IN THEME PICKER); WIRE `SettingsManager.kt:11-17`, `NoteflowViewModel.kt:36-102`, `MainActivity.kt:40-47`, `HomeScreen.kt:1591-1600`.
- [x] **30.3 TYPOGRAPHY + SHAPES SYSTEMS**: NEW `theme/Type.kt` (SERIF DISPLAY FOR PAPER/SEPIA, SANS FOR DARK/AMOLED, MONO FOR CODE — CONCRETE SCALE IN AUDIT) + `theme/Shape.kt` (RADIUS SCALE; PILL=EXTRA-LARGE); WIRE INTO `Theme.kt:70-73`.
- [x] **30.4 MOTION SYSTEM**: NEW `theme/Motion.kt` (SPRINGS: `NoBouncy` UI / `MediumBouncy` CELEBRATION; 300MS STATES, 500MS CONTAINER TRANSFORMS); PILL HIDE/SHOW SPRING (`EditorScreen.kt:989`), PICKER GROWTH (`1004-1016`), FAB ROTATE+SCALE (`HomeScreen.kt:287`), STICKY-NOTE DROP-IN (`AnnotationCanvas.kt:2107`); RESPECT `LocalReduceMotion`.
- [x] **30.5 ADAPTIVE 3-PANE**: `WindowSizeClass` VIA `currentWindowAdaptiveInfo()` (`MainActivity.kt:44-111`): COMPACT = TODAY; MEDIUM (≥600DP) = 2-PANE LIST+EDITOR; EXPANDED (≥840DP) = NAV RAIL + LIST + EDITOR (DELETE HARDCODED `screenWidthDp>=800` AT `HomeScreen.kt:876-879`); EDITOR LANDSCAPE → VERTICAL TOOL RAIL (`EditorScreen.kt:1227`); FOLDABLE HINGE-AWARE; `NavigationSuiteScaffold` FOR RAIL TRANSITIONS; DEPS: `material3-window-size-class` + `adaptive`.
- [x] **30.6 COMPONENT UPGRADES**: EXPRESSIVE TOOLBAR PILL (`surfaceContainerHighest` + TONAL ELEVATION + OUTLINE HAIRLINE + ANIMATED SEGMENTS, `EditorScreen.kt:1227-1380`); CARDS → `surfaceContainer` TONAL; CHIPS FOR TAGS (`TagExplorerView.kt`, `TagManagerDialog.kt`); `ModalBottomSheet` FOR LAYERS + BACKLINKS (`EditorScreen.kt:2367`, `BacklinksInspector.kt:30`); WAVEFORM THEME-COLORED (PRIMARY PLAYED / PRIMARYCONTAINER REMAINING, `AudioPlaybackCard.kt:112-157`); GRAPH NODES THEME-AWARE (`KnowledgeGraphScreen.kt:102`); STICKY NOTES + MINIMAP + TUTORIAL RESTYLE (`AnnotationCanvas.kt:1086-1120,2107`, `InteractiveTutorial.kt:20-102`).
- [x] **30.7 MICRO-INTERACTIONS**: HAPTICS MAP (`TextHandleMove` TOOL/UNDO/GRAPH-TAP, `LongPress` FAB/RECORD/DELETE); SNACKBAR SYSTEM REPLACING ALL 21 TOAST SITES (`HomeScreen.kt:86-373`, `EditorScreen.kt:109-1502`, `Dialogs.kt:228`, `BacklinksInspector.kt:227`) WITH PROGRESS + ACTION-OPEN; EMPTY STATES WITH COPY ("YOUR VAULT IS QUIET. LINK NOTES WITH [[WIKILINKS]] TO GROW YOUR GRAPH." ETC.); FIRST-RUN LOADING SPINNER (`HomeScreen.kt:47-53`).
- [ ] **30.8 IMPLEMENTATION ORDER**: DEPS+BACK-CALLBACK → COLOR TOKENS v2 → DYNAMIC+SYSTEM → TYPOGRAPHY/SHAPES → MOTION → ADAPTIVE → COMPONENTS → SNACKBAR/HAPTICS → NAV COMPOSE MIGRATION (LAST, HIGH RISK) → STRINGS+EMPTY STATES. BIGGEST WIN PER COMMIT: STEPS 2-4 ≈ 80% OF PERCEIVED REDESIGN, NEAR-ZERO RISK.

---

## 🖌️ PHASE 31: REAL AGSL OIL & WATERCOLOR WET-MIXING (WIRE THE DEAD CODE — 20-YR GRAPHICS ENGINEER PLAN)

- [x] **31.1 CORRECT EFFECT HOST**: DO **NOT** ATTACH TO THE SCREEN-SPACE CANVAS (`AnnotationCanvas.kt:848-857` — EFFECT WOULD BE SCREEN-SPACE). USE A PAGE-SPACE `rememberGraphicsLayer()` (COMPOSE 1.7+, IN BOM) DRAWN VIA `drawLayer()` INSIDE THE SAME CANVAS — INHERITS ZOOM TRANSFORM FOR FREE. WIRE INTO `drawCompositedLayersStrokes` (`AnnotationCanvas.kt:1466-1634`): ACTIVE LAYER + WET TOOL → `drawWetLayerPass()` BEFORE THE CACHED-BITMAP PATH (1506); SUPPRESS VECTOR PREVIEW (1571-1573) — THE EFFECT **IS** THE PREVIEW.
- [x] **31.2 SHADER CORRECTNESS FIXES** (`AgslShaders.kt:28-63`): (A) SEGMENT INTERPOLATION — KERNEL TAKES `uPrevPos`; KOTLIN WALKS PREV→CUR IN ⌈DIST/RADIUS⌉ SUB-PASSES (CAP 3/FRAME) — **FAST FLICKS PAINT CONTINUOUS RIBBONS, NOT DOTTED GAPS**; (B) REORDER: RADIUS EARLY-OUT BEFORE `contents.eval`; (C) WATERCOLOR ALPHA: REPLACE `max(base.a, ...)` (BLOCKS OVERLAP DARKENING — OPPOSITE OF WET-ON-WET) WITH SOURCE-OVER ACCUMULATION `base.a + brush.a*falloff*wet*(1-base.a)`; (D) `uHardness` UNIFORM + EDGE POOLING TAP (+1 SAMPLE); (E) IMPASTO: 2-TAP HASH GRAIN INSTEAD OF `sin()` RIDGE; (F) `half` PRECISION, CLAMP OUTPUTS.
- [x] **31.3 PRESET UNIFORMS** (NO UI EXISTS): OIL = WET 0.15/PIGMENT 0.95/MIX 0.85/IMPASTO 0.9; WATERCOLOR = WET 0.9/PIGMENT 0.35/MIX 0.6/IMPASTO 0.0 — PER-TOOL PRESET TABLE IN `AgslShaders.kt`; REUSE ONE `RuntimeShader`+`RenderEffect` (KILL PER-CALL SHADER ALLOCATION AT `:85`).
- [x] **31.4 COORDINATE MAPPING RULE**: SHADER `main(float2 coord)` RECEIVES LAYER-LOCAL (PAGE-SPACE) COORDS — SCREEN→PAGE ALREADY COMPUTED AT `AnnotationCanvas.kt:364-367,425-426`; BITMAP 1:1 WITH PAGE (1080×1528, `:1508-1509`); DIRTY-RECT: RECORD `clipRect(bbox±radius±8px)` + `translate(-l,-t)` → ~40K PX SHADED VS 1.65M FULL PAGE (~98% GPU CUT); `scaleX/Y=0.5` DURING DRAG = ¼ PIXELS, FULL-RES AT COMMIT.
- [x] **31.5 FALLBACK MATRIX (OLDER/LOW-END HARDWARE — REQUIRED)**: API<33 → EXISTING VECTOR WATERCOLOR/OIL (`AnnotationCanvas.kt:1770-1859`); API33+ SEVERE/CRITICAL THERMAL → `ThermalSanityHelper` (ZERO CALL SITES — WIRE IT) → PREVIEW-RES 0.35 + HALF PASS FREQ, SUSTAINED → VECTOR; API33+ LOW-END GPU (CHOREOGRAPHER EMA >20MS SUSTAINED 1S) → AUTO-DEGRADE 0.5→0.35→VECTOR, REMEMBERED PER-DEVICE; MANUAL OVERRIDE SWITCH. ALL: ONE-TIME NON-ALARMING MESSAGE + "GPU WET BRUSHES" SETTINGS TOGGLE — **NO SETTINGS SCREEN EXISTS → ADD MINIMAL `SettingsBottomSheet` (~60 LINES) FROM EDITOR OVERFLOW, PERSIST IN `SettingsManager`**.
- [x] **31.6 PERFORMANCE BUDGET**: THROTTLE: ≥6PX MOVED OR ≥16MS ELAPSED; NEVER 2 PASSES/FRAME; SUB-STEP CAP 3; ZERO GPU WORK IDLE (`renderEffect=null` + `release()`); FRAME-TIME MONITOR (`Choreographer` EMA 20 FRAMES) START/STOP ON DRAW; `BitmapPool` FOR LAYER CACHE BITMAPS (`AnnotationCanvas.kt:1511`, `BitmapPool.kt` DEAD — WIRE IT); CAP WET SNAPSHOTS 2 IN FLIGHT.
- [x] **31.7 UNDO/EXPORT COMPATIBILITY**: VECTOR STROKES REMAIN SINGLE SOURCE OF TRUTH (NO SCHEMA CHANGE — WET PARAMS ARE PER-TOOL PRESETS); RE-FLATTEN DETERMINISTIC ON UNDO VIA ORDERED REPLAY THROUGH SAME SHADER PASSES; EXPORT: SOFTWARE CANVAS CAN'T RUN RUNTIMESHADER → `WetLayerFlattener` (GPU REPLAY VIA GRAPHICSLAYER→`toImageBitmap` OR `RenderNode.setBitmapConsumer` API29+) IN `ImportExportService.kt:456-504`; VECTOR FALLBACK EXPORT STAYS FOR API<33.
- [x] **31.8 IMPLEMENTATION STEPS 1-12**: PURE-LOGIC CORE (`WetBrushEngine.kt` — TIER DECISION/SUB-STEPS/DIRTY-RECT/THROTTLE/EMA, JVM-TESTABLE) → KERNEL REWRITE → CAPABILITY PLUMBING (`ShaderCapabilityHelper` + `ThermalSanityHelper`) → CACHE EXTENSION → GRAPHICSLAYER PASS → GESTURE INTEGRATION (`onDrag` 421-487, `onDragEnd` 488-525) → RE-FLATTEN → EXPORT FLATTENER → FALLBACK UX + SETTINGS SHEET → POOLING → INSTRUMENTED TESTS → MANUAL TUNING + 10-MIN THERMAL SOAK (50% BRIGHTNESS, LOG `currentThermalStatus` EVERY 30S, PASS = NO SEVERE >2MIN, <45°C). ~2-3 WEEKS FOCUSED WORK, ZERO SCHEMA CHANGES.

---

## ⚡ PHASE 32: PERFORMANCE OPTIMIZATION — LAG-FREE, SMOOTH, FAST (MEASURE-FIRST, LOW-END → FLAGSHIP)

- [x] **32.1 (BIGGEST) PAN/ZOOM OUT OF COMPOSE STATE**: ZOOM/PAN WRITES SCREEN STATE PER GESTURE FRAME → ENTIRE EDITOR + CANVAS DRAW BLOCK RE-RUNS EVERY PAN/ZOOM FRAME (`EditorScreen.kt:946-947`, `AnnotationCanvas.kt:851-857`). FIX: NON-SNAPSHOT LOCAL STATE, APPLIED ONLY VIA `graphicsLayer` LAMBDA → PURE GPU TRANSFORM. ~40% OF ALL JANK.
- [x] **32.2 KILL PER-FRAME ALLOCATIONS IN DRAW BLOCK**: `activeStrokeList.flatMap{...}.maxOfOrNull` PER FRAME (`:917`); PER-PAGE `filter{it.pdfPage==}` PER FRAME (`:982`) → PRE-PARTITION `Map<Int,List<Stroke>>`; `activePoints + point` LIST-CONCAT PER MOVE = O(N²) (`:480-483`) → PREALLOCATED ARRAYLIST + INVALIDATE COUNTER; MINIMAP ALL-SEGMENTS + `color.copy()` PER FRAME (`:1174-1234`) → CACHED THUMBNAIL.
- [x] **32.3 LAYER CACHE SURVIVES**: VERSION-COUNTER INVALIDATION (NOT `hashCode()` / NOT `clear()` ON EVERY STROKE CHANGE) + LRU CAP ≤4 FULL-PAGE BITMAPS (6.6MB EACH) + RELEASE ON DISPOSE (`AnnotationCanvas.kt:254,1504-1529`) — PREVENTS ~300MB WORST CASE.
- [x] **32.4 AIRBRUSH STAMP CACHE**: 12 `drawCircle` PER POINT = ~48K DRAW CALLS/FRAME → PRE-RENDER SPRAY STAMP BITMAP PER COLOR/WIDTH (`AnnotationCanvas.kt:1751-1769`); ERASER O(N) PER DRAG EVENT → BATCH TO DRAG-END + SPATIAL HASH (`:406-466`); RDP EPSILON 0.75→1.2-1.5 + OFF-MAIN-THREAD (`RamerDouglasPeucker.kt:14-37`); INKSTROKE CACHE PER STROKE ID (`:1434-1464`).
- [x] **32.5 PAPER+TEMPLATE BITMAP CACHE**: GRID/LINED/DOTS/CORNELL = 30-70 `drawLine` CALLS PER PAGE PER FRAME → CACHE TO BITMAP, INVALIDATE ON TEMPLATE/COLOR/THEME CHANGE (`AnnotationCanvas.kt:1296-1432`); HOIST ALL `Paint()`/`Typeface.create()` ALLOCATIONS TO `remember`/COMPANION (`:1285,1531,1587,2032,2060`).
- [x] **32.6 SCOPED VOICE RECOMPOSITION**: `recordingElapsedMs` (10HZ) + `playbackPositionMs` (20HZ) COLLECTED AT EDITOR TOP → WHOLE SCREEN + CANVAS REDRAWS AT 10-20HZ (`EditorScreen.kt:135-140`) → SCOPE TO PILL/CARD ONLY; WAVEFORM ≤120 DOWNSAMPLED BARS (`AudioPlaybackCard.kt:141-156`); `VoiceNoteManager` WAVEFORM LIST-CONCAT O(N²) (`:95`) → PREALLOCATED ARRAYLIST, CAP 2000.
- [x] **32.7 DB: UPSERT NOT DELETE+REINSERT**: `@Upsert` STROKES (KILLS O(N²) SAVE + WAL BLOAT, `NoteRepository.kt:293-329`); DEBOUNCE 1S→2S (`EditorScreen.kt:292`); **FIX `PRAGMA wal_checkpoint` CURSOR-NEVER-STEPPED** (`NoteRepository.kt:72-74`) + `wal_autocheckpoint=1000`; COMPOSITE INDEX `(sectionId,pinned,updatedAt)` (`Daos.kt:75`); FTS5 SEARCH REPLACING PER-KEYSTROKE FULL-VAULT DECRYPT (`NoteRepository.kt:93-101`).
- [x] **32.8 MEMORY: BOUNDED + POOLED**: PHOTO DECODE `inSampleSize` + OFF-THREAD + RECYCLE (`MediaEmbedComponents.kt:86-95`); PDF WINDOW DELTA-RENDER + `BitmapPool` (`EditorScreen.kt:252-287,2293-2317`); GSON → STREAMING/KOTLINX SERIALIZATION WITH `FloatArray` (~40% SMALLER `pointsJson`); `@Immutable` ON `Stroke`/`CanvasStickyNote`/`CanvasMediaEmbed` (`StrokeModels.kt:92-109,131-141`) FOR SKIPPABILITY.
- [ ] **32.9 STARTUP**: BASELINE PROFILES — ADD `profileinstaller` + `baselineProfile` DSL + MACROBENCHMARK MODULE, PROFILE: COLD START→HOME→EDITOR→DRAW/PAN LOOP (15-30% FASTER, `build.gradle.kts:47-79`); R8 MINIFY + KEEP RULES (`build.gradle.kts:24`); SUBSCRIBE SECTION/PAGE FLOWS VIA `WhileSubscribed` NOT VM-INIT (`NoteflowViewModel.kt:90-98`); EDITOR LOAD `async/awaitAll` (`EditorScreen.kt:220-239`). **PARTIAL — BASELINE PROFILES NOT WIRED (DEFERRED; old dead `baseline-prof.txt` deleted in the Phase-03 honesty audit, 2026-08-13); R8 minify is on in release builds.**
- [x] **32.10 MEASUREMENT PROTOCOL (BEFORE ANY CHANGE)**: JANKSTATS/FRAMEMETRICS P50/P90/P95 + `testTag`s; PERFETTO 10S DRAW/10S PAN/5S ERASE/10S PLAYBACK; ALLOCATION/GC PROFILER; COMPOSE RECOMPOSITION METRICS; MACROBENCHMARK STARTUP+FRAMETIMING; `PRAGMA query_plan`; SCRIPTED SCENARIO (100-STROKE PAGE, 20-PAGE PDF, 30S RECORDING, PHOTO EMBED). EXECUTION ORDER: 32.1→32.2→32.3→32.7→32.5→32.6→32.4→32.8→32.9 (R8+PROFILES LAST, AFTER RELEASE SMOKE TEST). ~2-3 WEEKS, MEASURED AT EVERY STEP.

---

## 📱 PHASE 33: ANDROID HARDWARE COMPATIBILITY, FALLBACKS & DOCUMENTATION

- [x] **33.1 DEVICE TIER SYSTEM**: DEFINE 3 TIERS (LOW-END 2-CORE/≤3GB — MID — FLAGSHIP) + DETECTION (`Build.SUPPORTED_ABIS`, `Runtime.maxMemory`, `Build.MODEL` HEURISTICS + PER-DEVICE PREF OVERRIDE); EVERY GPU/HEAVY FEATURE GATES THROUGH TIER + CAPABILITY FLAGS (AGSL = API 33+; DYNAMIC COLOR = API 31+; HARDWARE BITMAPS = API 26+).
- [x] **33.2 FALLBACK RULES (ALL NEW FEATURES MUST SHIP WITH)**: AGSL WET BRUSHES → VECTOR (PHASE 31.5 MATRIX); DYNAMIC COLOR → STATIC PALETTES; ADAPTIVE 3-PANE → SINGLE PANE <600DP (AUTO); EDGE-TO-EDGE → PADDING FALLBACK ON API 26-29; SPLASH → VALUES-V31 + PRE-SPLASH THEME; NEVER SILENT DEGRADE — ONE-TIME NON-ALARMING MESSAGE + SETTINGS RE-ENABLE.
- [x] **33.3 DOCUMENTATION**: `docs/COMPATIBILITY.md` (TIER MATRIX, FEATURE-BY-API TABLE, FALLBACK BEHAVIOR, TEST MATRIX — API 26/29/31/33/34/35 EMULATORS); `docs/TUTORIAL.md` (USER-FACING: CANVAS MODES, BRUSHES, WIKILINKS, VOICE SYNC, BACKUP/RESTORE, THEMES — WITH SCREENSHOT PLACEHOLDERS); WIRE "ABOUT / HELP" INTO THE APP (SETTINGS SHEET → TUTORIAL + PRIVACY POLICY + DATA-SAFETY STATEMENT); UPDATE `README.md` TO MATCH ROADMAP TRUTH TABLE (STOP OVERCLAIMING).
- [x] **33.4 CONFIG CHANGES CLEANUP**: REMOVE `configChanges` HACK (`AndroidManifest.xml:31`) → `rememberSaveable` + PROPER INSET HANDLING (PHASE 29.5) SO SYSTEM DARK/FONT/DENSITY CHANGES WORK.

---

## 🛡️ PHASE 34: PENTEST HARDENING (KALI 2026-08-08 FINDINGS — `docs/pentest-report.md`)

- [x] **34.0 PENTEST BASELINE**: STATIC AUDIT COMPLETE @ `766e037`; APK SHA256 `0b61d9fc…e2659` (unsigned CI artifact); Phase 19 claims VERIFIED; 27.1 REFUTED. Dynamic phase skipped (no device) — re-run frida/objection with a device later.
- [x] **34.1 (HIGH) RESTORE = UNAUTHENTICATED BLIND FULL-VAULT REPLACE (N2/B1/B2/B4)**: RESTORE REWRITTEN — `restoreFromZip` EXTRACTS TO TEMP DIR, RUNS `PRAGMA integrity_check` (REJECTS CORRUPT/WRONG-DEVICE DB BEFORE TOUCHING LIVE DATA), RE-KEYS THE TEMP COPY, THEN SWAPS ATOMICALLY (`Files.move`, WAL/SHM DELETED ONLY AT SWAP); `clearStoredChecksum()` REPLACED WITH `updateStoredChecksum()` (BASELINE RE-ARMED ONLY AFTER VALIDATED SWAP); LEGACY PLAIN-PK PATH NOW REQUIRES EXPLICIT USER CONFIRMATION DIALOG (`HomeScreen.kt`); BACKUP WITHOUT `noteflow.sqlite` ENTRY REJECTED. (`ImportExportService.kt`)
- [x] **34.2 (HIGH) CROSS-DEVICE RESTORE DOUBLE-ENCRYPTS FIELDS → SILENT PERMANENT DATA LOSS (N3/B3)**: `migrateFieldCiphertexts` DECRYPTS pages.title/extractedText, strokes.textContent, media_embeds.textContent WITH THE BACKUP DEK AND RE-ENCRYPTS WITH THE CURRENT DEK ON THE TEMP DB AFTER RE-KEY — NO MORE DOUBLE-ENCRYPTION ON CROSS-DEVICE RESTORE. (`ImportExportService.kt`)
- [x] **34.3 (MED) DECRYPTED CONTENT RESIDES IN VIEWMODEL STATEFLOWS AFTER `lock()` (N6/A1)**: `lock()` NOW CLEARS `_pages`/`_selectedPage`/`_sections`/`_selectedSection`/`_selectedNotebook` BEFORE FLIPPING AUTH STATE. (`NoteflowViewModel.kt:784-792`)
- [x] **34.4 (MED) PLAINTEXT AUX FILES DESPITE "ENCRYPTED VAULT" CLAIM (N1/A2)**: **DECISION: RELABEL (34.4b)** — ALL "end-to-end encryption" CLAIMS REPLACED WITH HONEST "encryption at rest" (welcome dialog, security dialog, tutorial, README): "Note titles, text and stroke text are encrypted; imported files, journal and voice notes are stored as plain files." 34.4a (PER-FILE DEK-AES-GCM) REMAINS OPEN AS FUTURE OPTIONAL WORK.
- [x] **34.5 (MED) ZIP-BOMB GUARD DEFEATABLE + UNBOUNDED RAM (B5)**: INPUT CAPPED AT 400MB BEFORE ANY DECRYPT/DECOMPRESS (`MAX_BACKUP_INPUT_BYTES`); RATIO GUARD NOW USES ACTUAL BYTES READ (FORGED `compressedSize`/`size` CANNOT DISABLE IT) — DECLARED SIZES STILL CROSS-CHECKED. (`ImportExportService.kt:1092-1106`)
- [x] **34.6 (MED) FILEPROVIDER `path="."` EXPOSES ENTIRE FILESDIR (A4)**: NARROWED TO `path="apk/"`; `UpdateService.installApk` STAGES APKS INTO `filesDir/apk/` BEFORE GRANTING THE URI. (`res/xml/file_paths.xml`, `UpdateService.kt:141-160`)
- [x] **34.7 (MED) CODE-BLOCK CLIPBOARD PERSISTS AFTER LOCK (N5/A6)**: NEW `ClipboardGuard` — CODE-BLOCK COPY RECORDS TIMESTAMP; LOCK PATH (`MainActivity` ON_PAUSE) SCRUBS THE PRIMARY CLIP (clearPrimaryClip API 28+, EMPTY CLIP BELOW). (`ClipboardGuard.kt`, `MediaEmbedComponents.kt:320`, `MainActivity.kt:42-47`)
- [x] **34.8 (INFO) HMAC FAILURE = BANNER ONLY (B-INFO)**: **DECISION: BANNER + ESCALATE ON RESTORE ONLY** — NEW `DatabaseSecurityHelper.rearmBaselineFromFile()` RE-ARMS THE BASELINE FROM THE TEMP DB **BEFORE** THE SWAP (FAIL-CLOSED); FAILURE SETS A PERSISTED RESTORE-BLOCK FLAG → `NoteflowViewModel.restoreBlocked` HARD-BLOCKS THE VAULT (MAIN ACTIVITY SHOWS A DEDICATED RECOVERY SCREEN, NOT CONTENT; LOCKSCREEN UNLOCK ALLOWED FIRST WHEN A MASTER PASSWORD EXISTS); `attemptRecoveryFromBackup()` RESTORES A FRESH BACKUP, CLEARS THE BLOCK, RESTARTS. NORMAL TAMPER BANNER (MAINACTIVITY ERROR SURFACE) UNCHANGED.
- [ ] **34.9 (INFO) DYNAMIC RE-RUN**: REPEAT PENTEST WITH DEVICE/EMULATOR (frida-server, PRAGMA cipher, FLAG_SECURE runtime, biometric flow, lockout UI, clipboard-after-lock, memory-dump after lock) — CLOSES STATIC-ONLY GAP. TARGET: POST-34.x COMMIT + SIGNED APK.

---

## 🎨 PHASE 35: ~~ADAPTIVE MULTI-ENGINE RENDERING & HARDWARE AUTO-RECOMMENDATION ARCHITECTURE~~ (SHELVED — SEE BELOW)

- [ ] **35.1 HARDWARE & CAPABILITY AUTO-PROFILER / 35.2 ENGINE SELECTOR DIALOG / 35.3 MULTI-ENGINE `BrushEngine` FACADE**:
  - **REMOVED IN THE PHASE-03 HONESTY AUDIT (2026-08-13)**: `HardwareProfiler`/`RenderingEngineTier`/`HardwareProfile`, the `BrushEngine` multi-engine facade + `BrushEngineFactory`, the `BrushEngineSettingsDialog`, the `renderingEngineOverride` setting, and the `JNI` stub were all ornamental/dead — the tier selector never affected rendering (the canvas renders via `WetBrushEngine` directly, with the real GPU-wet-brush toggle `gpuWetBrushesEnabled`). All deleted. The fake "LibMyPaint C++ Native Studio Engine" option and the stub C++ (`mypaint_jni.cpp`/`CMakeLists.txt`) are gone; no dead engine is presented as working in the UI.
  - Hardware awareness today lives in `DeviceCompatibilityManager` (device-tier gating for the GPU-wet-brush low-end warning) and `ShaderCapabilityHelper.isAgslSupported`.
  - Elevating AGSL wet-mixing to a user-selectable multi-engine architecture is a real future phase (Phase 04).


> NOTE: 34.1/34.2 are the critical items — restore path is the weakest surface found. 34.4a/34.4b needs user decision (file format change vs honesty relabel).

---

---


THESE FEATURES INVOLVE OPTIONAL EXTERNAL NETWORKING, SPECIALIZED HARDWARE, OR HEAVY NATIVE-BUILD/MANUAL-DEBUGGING WORK, PRESERVED OUTSIDE NOTEFLOW'S 100% OFFLINE, CHAT-BUILDABLE CORE:
- **WEBDAV / NEXTCLOUD SYNC CLIENT**: BACKGROUND HTTP SYNC CLIENT FOR BACKING UP ENCRYPTED VAULT PACKAGES TO PERSONAL WEBDAV/NEXTCLOUD SERVERS.
- **LOCALSEND P2P LAN PROTOCOL**: UDP DISCOVERY (PORT 53317) AND P2P ENCRYPTED FILE TRANSFERS OVER LOCAL WI-FI.
- **S-PEN AIR ACTIONS REMOTE CONTROL**: BLUETOOTH S-PEN BUTTON GESTURE TRIGGERS FOR REMOTE TOOL SWITCHING, UNDO, AND PAGE FLIPPING.
- **LIBMYPAINT NATIVE PIGMENT-SIMULATION ENGINE**: OPTIONAL THIRD BRUSH TIER ("STUDIO") BEYOND ANDROIDX.INK, USING THE OPEN-SOURCE LIBMYPAINT C LIBRARY FOR TRUE PIGMENT/WETNESS/BLEND SIMULATION. INVOLVES:
  - **NDK/CMAKE NATIVE BUILD SCAFFOLDING**: BUILDING LIBMYPAINT AS A NATIVE SHARED LIBRARY FOR `ARM64-V8A` AND `ARMEABI-V7A` TARGETS.
  - **JNI BRIDGE LAYER**: A JNI BRIDGE EXPOSING `STARTSTROKE(BRUSHID, X, Y, PRESSURE, TILT)`, `ADDPOINT(X, Y, PRESSURE, TILT, DTIME)`, `ENDSTROKE()`, AND RETRIEVAL OF THE RENDERED SURFACE AS A PIXEL BUFFER.
  - **KOTLIN NATIVE WRAPPER & THIRD BRUSH TIER**: A `STUDIOBRUSHENGINE` CLASS IMPLEMENTING THE `BRUSHENGINE` INTERFACE, RENDERING THE NATIVE SURFACE'S OUTPUT INTO AN `IMAGEBITMAP` COMPOSITED INTO THE COMPOSE CANVAS EACH FRAME, EXPOSED AS A CLEARLY-MARKED BETA "STUDIO (EXPERIMENTAL)" TIER ALONGSIDE CLASSIC/ADVANCED.
  - **CONDITIONAL PRIORITY**: ONLY WORTH PURSUING IF, AFTER EXTENDED USE OF THE ANDROIDX.INK ADVANCED BRUSHES, A SPECIFIC AND CONCRETE GAP IS IDENTIFIED (E.G. "WATERCOLOR BLENDING ISN'T WET ENOUGH") THAT ANDROIDX.INK CANNOT ADDRESS.
  - **MANUAL WORK REQUIRED — SIGNIFICANT**: REQUIRES ANDROID STUDIO INSTALLED LOCALLY (NOT JUST THE IN-BROWSER IDE), NDK TOOLCHAIN SETUP, AND MANUAL BUILDING/DEBUGGING OF THE NATIVE LIBRARY (SEGFAULTS, MEMORY LEAKS, ABI MISMATCHES ARE NOT REALISTICALLY DEBUGGABLE THROUGH AN AI CHAT ALONE). BUDGET AS A MULTI-DAY SIDE PROJECT, NOT A QUICK PHASE. VERIFY LIBMYPAINT'S CURRENT LICENSE TERMS BEFORE SHIPPING.
