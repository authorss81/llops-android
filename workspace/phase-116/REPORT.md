# Phase 116 — Round-2 Security Audit (full source review, like Phase 30)

> **Status: `DONE`.** Re-executed the Phase-30 methodology (2 batches × 5 parallel
> subagents, source-code review only, `file:line` evidence) on the **post-fix,
> post-feature** tree. All findings appended incrementally to the shared round-2
> findings file `docs/security-report-round2.md` (single source of truth, also used
> by phase-118 for the dynamic/APK pass). **No app code was changed** — this is an
> audit; fixes are generated by the phase-119 pipeline.

## 1. Method

- Audited commit: `c813c99` (the same commit the phase-116 Batch-1 run used; HEAD at
  write time is `c3a1789` + the batch-1/batch-2 commits — **only CI/pipeline commits
  separate `c813c99` from the current app tree, no app-code delta**, so all findings
  reference the current tree).
- 2 batches × 5 parallel subagents, source review only, no dynamic tooling, no
  external services attacked.
- Every finding carries the round-1 schema:
  `ID (R2-xxx) | Severity | Area | Evidence (file:line) | Reproducer | Suggested fix`.
- Evidence spot-verified line-by-line during consolidation (sample below).

## 2. Findings

- **Batch 1** (committed earlier as `cc04ed1`): **20 unique findings**
  (5 MEDIUM · 12 LOW · 3 INFO) across crypto (2), data-at-rest/DB (5), network/
  plugins (5), platform (5), app logic/auth (3).
- **Batch 2** (this run, commit `3134927`): **23 unique findings**
  (7 MEDIUM · 14 LOW · 2 INFO) across Compose/UI+concurrency (6), dependencies/CVE
  (4), logging/info-disclosure (4), resource-exhaustion/DoS (3), phase-34-38 feature
  attack surface (6). One raw finding (FEA-07 minimap draw) folded into DOS-03.
- **Round-2 total: 43 findings** (0 CRITICAL · 0 HIGH · 12 MEDIUM · 26 LOW · 5 INFO).
  No CRITICAL/HIGH — consistent with ~76 fixes applied by phases 39-115.

### Batch-2 highlights (top MEDIUMs)

| ID | Finding |
|----|---------|
| R2-b2b1-UI-01 | Composition-scoped READ-side vault loads crash on lock (`LaunchedEffect` full-corpus decrypts hit the disposed SQLCipher pool with no catch — read-side gap next to R2-B1A-01/02) |
| R2-b2b2-DEP-01 | Release-signing CI runs unpinned mutable-tag Actions with keystore secrets in job env |
| R2-b2b4-DOS-01 | `note_versions` unbounded — full-body snapshot per save, no retention, wholesale in-heap decrypt on history-open/backup/restore |
| R2-b2b4-DOS-02 | Live-canvas layer count + per-layer full-page ARGB bitmaps unbounded (export capped at 16, render path is not) |
| R2-b2b5-FEA-01 | Knowledge graph iterates ~10⁶ unculled edges per frame (nodes culled, edges not) |
| R2-b2b5-FEA-02 | `MarkdownInlineMath` O(n²) scans on uncloseable backtick runs + O(n·R) `insideAny` |
| R2-b2b5-FEA-03 | Hybrid markdown editor re-tokenizes the whole document twice per keystroke on the main thread |

## 3. Verification (audit is read-only — prove no accidental change)

- `gradle testDebugUnitTest` — **BUILD SUCCESSFUL** (57 tasks; full suite green).
- `gradle assembleDebug` — **BUILD SUCCESSFUL** (one first-invocation transient
  failure, retry fully green — the repo's documented transient-build pattern);
  debug APK `app/build/outputs/apk/debug/app-debug.apk` (173,838,046 B,
  SHA-256 `ceff4627…`) on disk.
- `git diff` vs the batch-1 commit contains only `docs/security-report-round2.md`
  (+ the pipeline `logs/` files) — **zero app-code delta**.
- `.github/workflows/` untouched; no schema change, no migration, no new deps.

## 4. Deliverables

- `docs/security-report-round2.md` — header + severity legend + living summary
  table + Batch-1 (20) + Batch-2 (23) quick-lookup tables and full detail blocks;
  final totals: **43 findings**. Phase-118 appends its dynamic/APK findings to the
  same file.
- Commits: `cc04ed1` (Batch 1, prior run) · `3134927` (Batch 2, this run) —
  both pushed to `main`.
