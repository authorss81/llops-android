# Phase 116: Round-2 Security Audit (full source review, like Phase 30) [NOT STARTED]

You are working on **InkFlow/Noteflow**. This phase repeats the Phase-30
methodology (the one that found 80 findings and generated the security-fix
phases) but on the **post-fix, post-feature codebase**:
- Read `docs/security-report.md` (round-1 findings + their fix phases),
  `docs/security-report-round2.md` (YOUR output - shared with phase-118),
  `docs/ARCHITECTURE.md`, `docs/phase-status.md`, `AGENTS.md` first.
- The code has since changed a LOT: phases 34-38 added features (glass theme,
  canvas ergonomics, micro-interactions, hybrid markdown editor, knowledge
  graph), phases 39-115 applied ~76 security fixes. Your job is a FRESH
  adversarial source audit of the CURRENT tree, not a re-check of round 1.

**Method (mirror Phase 30):** 2 batches × 5 parallel subagents, source-code
review only, `file:line` evidence, hacker mindset. Never attack external
services - this is source review.

## Step 1 - Batch 1 (5 parallel subagents)
Areas: 1) Cryptography & key management, 2) Data-at-rest & DB, 3) Data-in-
transit & network (incl. the downloadable-plugin runtime: pinned-cert download,
DexClassLoader, capability facade, update engine), 4) Android platform surface,
5) App logic & auth.

## Step 2 - Batch 2 (5 parallel subagents)
Areas: 1) Compose/UI + concurrency/races/TOCTOU, 2) Dependencies/CVE review,
3) Logging/telemetry/info disclosure, 4) Resource-exhaustion/DoS, 5) The NEW
phase 34-38 features (glass surfaces, brush/nib preview math, minimap/HUD,
markdown hybrid editor + callouts + waveform scrubbing, knowledge-graph layout)
as attack surface.

## Step 3 - Single shared findings file (with phase-118)
- Append ALL findings to **`docs/security-report-round2.md`** (create if missing)
  using the round-1 schema: `ID (R2-xxx) | Severity | Area | Evidence (file:line)
  | Reproducer | Suggested fix`. Do NOT create a separate file - phase-118 appends
  to the SAME file, so one md is the round-2 single source of truth.
- Add a header block with the audited commit sha, date, method, and an "APK
  target" note pointing at phase-117's release build (or the command to get it).
- Write findings INCREMENTALLY (append each agent batch's results as they
  finish) so a timeout/context truncation never loses completed work. Commit +
  push after each batch.

## Definition of done
- `docs/security-report-round2.md` contains the complete round-2 finding set
  (both batches), each with severity + `file:line` evidence + reproducer.
- Findings reference the CURRENT tree (post phase-38 features, post phase-39-115
  fixes) - no round-1 stale rows copied as-is.
- No finding references un-audited lines; every row is evidence-backed.
- Build/tests green (audit is read-only; run `gradle testDebugUnitTest` +
  `gradle assembleDebug` to prove no accidental change).
- `workspace/phase-116/REPORT.md` + commit + push.

## Constraints
- Source review ONLY - do NOT attack external services, do NOT run dynamic
  tooling (that is phase-118's job).
- Do NOT edit `.github/workflows/`. Do NOT change app code to "fix" findings -
  findings go to the shared md; fixes are generated by phase-119.
- Never log keys/passwords/decrypted content in the findings file beyond
  evidence already in the code. Keep honest-claims discipline.