# Phase 162 — Build & code failure fixes + verify decryption-safety edge cases

Status: **DONE** (2026-08-19)

Scope: Part A fixes the three real build/code failures (duplicate methods,
deprecated `project.exec`, missing `trusted-artifacts`), Part B audits the six
decryption-failure / data-integrity safeguards already implemented and fixes any
FALSE claim.

## Verification environment

- System `gradle` 8.13 (no wrapper jar in repo), JDK 21 (Temurin), Linux CI.
- `gradle :plugins:llm:packagePlugin :app:assembleDebug` → **BUILD SUCCESSFUL**
  (72 tasks executed, zero `e:` errors).
- Plugin-signing positive path proven with a throwaway `/tmp` keystore:
  `gradle :plugins:llm:verifyPluginSignature :plugins:llm:pluginMetadata` →
  **BUILD SUCCESSFUL** (signPlugin signed the artifact, verifyPluginSignature ran
  `jarsigner -verify`, pluginMetadata emitted the pin). Throwaway keystore + env
  vars scoped to the run; never committed. Without the env vars the signing tasks
  still FAIL CLOSED at the phase-76 `whenReady` gate (`keystore set=false`).
- `gradle testDebugUnitTest` → **2240 tests completed, 1 failed**: the single
  failure is the pre-existing, documented
  `Phase148UiFailureTextScrubTest:234` UNC-path assertion (`scrubForUi … redacts
  absolute paths`) — reproduced identically on a clean stash in phases 149/153/158
  and untouched by this diff.

## Part A — fix the build & code failures

| # | Item | Status | Evidence |
|---|------|--------|----------|
| A1 | Duplicate `renameTag` / `deleteTag` in `NoteflowViewModel.kt` | **VERIFIED-OK** (no duplicates exist) | The merge-conflict duplicates diagnosed in field are NOT present in the current tree: `grep -oE '^\s*(private\s+\|internal\s+\|public\s+)?(suspend\s+)?fun\s+\w+'` across all 178 top-level declarations is unique (no repeated function name). The two dispatched functions exist ONCE each: `renameTag(oldTag, newTag)` at `NoteflowViewModel.kt:2312` and `deleteTag(tag)` at `NoteflowViewModel.kt:2345`, both `viewModelScope.launch` + `writeGuardedAgainstLock` (the phase-134 lock-safe dispatch the prompt asked to keep). Braces: `awk` count = open 744 / close 744 (diff 0). `assembleDebug` compiles the file cleanly. No code change required. |
| A2 | Deprecated `project.exec` in `plugins/llm/build.gradle.kts` | **FIXED** | Both `signPlugin` (`was :250`) and `verifyPluginSignature` (`was :273`) `project.exec { … }.rethrowFailure()` blocks replaced with a `ProcessBuilder` invocation inside `doLast`: new private helper `runExternal(cmd, failureMessage)` (`plugins/llm/build.gradle.kts:283-292`) starts a list-form `ProcessBuilder` (no shell — args kept exact, paths with spaces safe = the "Windows-safe equivalent"), `redirectErrorStream(true)`, streams stdout/stderr into a discarded buffer so keystore paths/passwords never reach the build log, and throws `GradleException("$failureMessage (exit code $exitCode)")` on non-zero. Command semantics preserved: `jarsigner -keystore … -storepass … -keypass … -sigalg SHA256withRSA -digestalg SHA-256 -signedjar … <jar> plugin-signing` and `jarsigner -verify <signed>`. Added `import java.io.ByteArrayOutputStream`. `grep -rn "project.exec" plugins/llm/` → only a comment. Positive + fail-closed paths both exercised (see Verification environment). |
| A3 | `<trusted-artifacts>` in `gradle/verification-metadata.xml` | **FIXED** | Inserted AFTER the verify flags, before `<ignored-keys>` (`gradle/verification-metadata.xml:6-14`): `<trust group="com.android.tools.build" />`, `<trust group="org.jetbrains.kotlin" />`, `<trust group="androidx.databinding" />`. This allow-verifies official vendor groups without disabling verification (`verify-metadata=true` / `verify-signatures=true` remain untouched — note the prompt's "`verify-signatures=false`" premise is stale; the file has it `true`). |

## Part B — verify the decryption-failure & integrity safeguards

| # | Item | Status | Evidence |
|---|------|--------|----------|
| B1 | Fail-closed "Unreadable" marker + persistent threshold → CorruptionRecoveryScreen | **VERIFIED-OK** (one constant caveat) | `DecryptFailurePolicy.UNREADABLE_MARKER = "Unreadable (decryption failed)"` (`services/DecryptFailurePolicy.kt:42`). `NoteRepository.decryptFieldForDisplay` (`data/repository/NoteRepository.kt:134-158`) runs every stored display field through the ONE `DecryptFailurePolicy.render` decision (`:157`); an auth-failed genuine ciphertext yields the marker, never raw Base64 (the old `catch { rawText }` fallbacks are gone). Wiring for all four sinks: `getStrokesForPage` (`:991`, geometry via `decryptStoredGeometryOrBlank` `:168` → empty payload on failure), `getMediaEmbedsForPage` (`:1254`), `getNoteVersions` (`:1541/:1546`), `decryptPageIfNeeded` (`:1565-1567`). Persistent escalation: deduped per-note ledger `recordDecryptFailure("note:<pageId>")` (`:108-113`), `decryptFailuresPersistent` = `DecryptFailurePolicy.isPersistent(size)` (`:97-98`), listener wired in `NoteflowViewModel.initializeDataCore` (`NoteflowViewModel.kt:1705-1712`) fires ONCE → `DatabaseSecurityHelper.setCorruptionDetected(appContext)` `:1708` + `_corruptionBlocked.value = true` `:1709` → `MainActivity.kt:555-560` renders `CorruptionRecoveryScreen`. **Caveat**: `PERSISTENT_FAILURE_THRESHOLD` is **10** distinct records (`DecryptFailurePolicy.kt:51`), not "~3"; the threshold (an ID-count, deliberately not an attempt-count — one corrupted note read by many flows can't false-trigger) is real and trips the recovery screen as claimed. |
| B2 | AndroidKeyStore invalidation → DEK loss | **VERIFIED-OK** | `services/DekReadResult.kt:16-43` declares sealed `DekReadResult` = `NoBlob` / `Unlocked(dek)` / `AuthRequired` / `KeyLost(wrapperAlias)`. `SecurityService.readDekResult()` (`SecurityService.kt:200-224`): no blob → `NoBlob`; auth blob → `AuthRequired`; decrypt failure (`KeyPermanentlyInvalidatedException`/any decrypt exception) → `KeyLost(wrapperAlias)` `:217-223`. `getOrCreateDek` (`:244-274`) on `KeyLost` THROWS `KeystoreKeyLostException` (`:254-259`) — it can ONLY mint from `NoBlob` + the passwordless gate, so a lost keystore key can never silently re-key over the stored wrapper (phase-43 quarantiner root cause closed). ViewModel mapping: `NoteflowViewModel` reads via `security.readDekResult()` (`:1837-1863`) and sets `_keystoreKeyLost.value = true` on `KeyLost` (`:1677`, `:1855`, `:1863`); `dbGate` combines `_authenticated && !_corruptionBlocked && !_keystoreKeyLost` (`:1369`); `MainActivity.kt:561-568` renders `KeystoreKeyLostScreen` (recovery exits `attemptKeystoreKeyLostRecoveryFromBackup` `:2618`, `startFreshAfterKeystoreKeyLoss` `:2709`). No mint-over-KeyLost anywhere. |
| B3 | AAD context on copy/move/reparent | **VERIFIED-OK** | AAD = `FIELD_AAD_V2_PREFIX + table + "\|" + recordId + "\|" + fieldName` (`EncryptionService.kt:340-341` `fieldAad` — semantic twins with the prompt's `computeAad`). Every field write binds FRESH ciphertext to the target entity's own id: `createPage` (`NoteRepository.kt:785/:790`, new `pageId`), `createNoteVersion` (`:1478-1480`, NEW `versionId` minted at `:1473`), `saveStrokesForPage` (`:1183/:1187`, per-`stroke.id`), `saveMediaEmbedsForPage` (`:1356`, per-`embed.id`). The mutation paths: `movePage` only rewrites `sectionId` (`Daos.kt:158-159`), which is NOT part of the AAD, so per-record ciphertext stays valid (`page.id` and child record ids unchanged); the layer-duplicate path (`EditorScreen.kt:861-884`) copies strokes in MEMORY with fresh UUIDs and a subsequent `saveStrokesForPage` re-encrypts each with the new id. The cross-device re-key sweep `migrateFieldRecordAad` re-encrypts rows under their OWN ids (`NoteRepository.kt:444-509`). No path copies stored ciphertext bytes into an entity with a different id — nothing stale survives a pageId mutation (and no pageId mutation of encrypted records exists). |
| B4 | SQLite WAL vs checksum-baseline race | **VERIFIED-OK** | `firstDataInitDone = CompletableDeferred<Unit>()` (`NoteflowViewModel.kt:172`) buffers the FIRST tamper verification of a passwordless vault until the initial data open settles: the verify launch `firstDataInitDone.await(); verifyDatabaseIntegrityNow()` (`:1239-1242`), released in `initializeData` `.finally` (`:1687`) and the key-lost/anomalous branch endpoints (`:1859`, `:1866`). Locked vaults verify immediately (`:1236-1237`) — their at-rest file is byte-identical until unlock, so no false positive. The WAL-resident-vault probe `vaultFilePresentAtStart` (`:156-160`) treats a populated `-wal` as an existing vault so a 0-length main can never be silently re-baselined. The verify hash covers `main + -wal` (`DatabaseHmacPolicy.streamDbAndWal`, `DatabaseHmacPolicy.kt:42-66`) and the session-end funnel `NoteflowDatabase.dispose()` FULL-checkpoints the WAL (true `wal_checkpoint(FULL)` fully stepped, `NoteflowDatabase.kt:518-526`) then re-arms the baseline against the quiescent file (`:532-536`) — so at cold start after a clean session end the WAL is empty and the gate has settled init; a mid-read checkpoint can't flip a stable file into a false `Mismatch` (the phase-136 cadence + phase-91 gate jointly close the prompt's race). `Mismatch` is what drives the TAMPERED banner (`DatabaseIntegrityPolicy.verdictFor` `:52-60`; `NoteflowViewModel.kt:1305-1308`). |
| B5 | OOM guards (stroke geometry + attachments) | **VERIFIED-OK** (constant values caveat) | `StrokeGeometryPolicy` owns `MAX_POINTS_PER_STROKE` / `MAX_POINTS_PER_PAGE` / `MAX_STROKES_PER_PAGE` / plaintext + stored char caps (`StrokeGeometryPolicy.kt:46-57`) with `applySaveGate` (`:92-131` per-stroke truncation + page total envelope + stroke-row cap), `gateStroke` (`:78`), `capLoadedPoints` (`:137`). Applied at INGESTION: `saveStrokesForPage` routes every write through `applySaveGate` (`NoteRepository.kt:1155`). Applied at LOAD/RENDER: `getStrokesForPage` pages via `StrokeDao.getStrokesForPageBounded` (`Daos.kt` `WHERE length(pointsJson) <= :maxStoredChars … LIMIT/OFFSET`, `NoteRepository.kt:969-976`), refuses over-budget plaintext pre-Gson (`:1005-1008`), caps legacy points (`:1012`), honors the per-page point envelope (`:1016-1019`, `:970`), and `EncryptionService.deserializeStrokes` refuses an oversized payload BEFORE any object-graph allocation (`EncryptionService.kt:498-506`). `AttachmentIngestPolicy.MAX_ATTACHMENT_BYTES = 25 MB` + `boundedReadBytes` (mid-stream cap) + `readTextHead` (head-bounded text) (`AttachmentIngestPolicy.kt:40-120`) are applied at the four picker sites (`EditorScreen.kt:318/:348/:480/:1093`), text sources (`WikiLinkParser.kt:291`, `NoteBodyVaultPolicy.kt:70-71`, `NoteRepository.kt:658/:683`). **Caveat**: the actual constants are `MAX_POINTS_PER_STROKE = 20_000` and `MAX_POINTS_PER_PAGE = 200_000` (10× the prompt's "2000 / 50000"); the mechanism and every enforcement hop the prompt lists exist. |
| B6 | Concurrent save collisions | **VERIFIED-OK** | `pageSaveLocks = ConcurrentHashMap<String, Mutex>` (`NoteRepository.kt:1113`, fair FIFO per page), acquired by ALL three full-page writes via `lock.withLock { withTransaction { … } }`: `saveStrokesForPage` (`:1156-1157`), `saveMediaEmbedsForPage` (`:1345-1346`), `saveLayersForPage` (`:1455-1456`). `saveCanvasItemsForPage` (`:1316-1334`) funnels stickies+embeds into `saveMediaEmbedsForPage` so the full page flush holds the SAME mutex across strokes→items→layers. The ViewModel writer set all lands on those same functions: autosave `autosaveStrokes` → `persistEditorSaveSuspend` → `repo.saveStrokesForPage` (`NoteflowViewModel.kt:3664-3676`), on-pause/dispose `disposeEditorPageFlush` = cancel+join debounce → `flushEditorPageSave` → `saveStrokesForPage` + `saveCanvasItemsForPage` + `saveLayersForPage` (`:3632-3648`, `:3601-3616`), background/deferred unlock flush `flushPendingEditorSaves` → same `saveStrokesForPage`/`saveLayersForPage` (`:3836-3839`). Marked `kotlinx.coroutines` Mutex = FAIR (FIFO), so a later-issued snapshot commits last (newest-wins). |

## Constraints honored

- `.github/workflows/` untouched.
- Security model unchanged (fail-closed, DEK handling, threshold values untouched).
- Dependency verification NOT weakened — only the vendor allow-list added.
- No keystore password / DEK / decrypted content ever printed or logged (the
  ProcessBuilder helper discards process output; the throwaway keystore lives in
  `/tmp/opencode` only, never in git).
- Base-APK-size rule respected: no new dependencies.

## Files changed

- `plugins/llm/build.gradle.kts` — `project.exec` → `ProcessBuilder` (A2).
- `gradle/verification-metadata.xml` — `trusted-artifacts` allow-list (A3).
- `docs/phase-status.md`, `docs/ARCHITECTURE.md` — status/documentation updates.
- `workspace/phase-162/REPORT.md` — this report.