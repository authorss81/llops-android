# AGENTS.md — InkFlow (InkFlow)

Guidance for AI agents (opencode, Antigravity, etc.) working on this repo. Read before editing.

## Project
- Android note-taking app: markdown + ink canvas + knowledge graph (wikilinks/tags/daily notes) + time-synced voice notes + layers + AGSL brushes.
- Kotlin 2.0.21, Jetpack Compose (BOM 2024.12.01), Room 2.6.1, Material 3, androidx.ink 1.0.0.
- `applicationId` = `com.aistudio.inkflow.app.bkxjrz` (matches `app/build.gradle.kts:15` + repo-root `metadata.json`); namespace = `com.authorss81.noteflow` (known mismatch, Phase 21.10).
- Source: `app/src/main/kotlin/com/authorss81/noteflow/` (`data/`, `services/`, `theme/`, `ui/`, `utils/`).
- Single `NoteflowViewModel` + `MainActivity` with `mutableStateOf` navigation (not Navigation Compose yet).

## Commands
- **Do NOT build or run Gradle locally on the Windows dev machine** — no Android SDK installed (AMD Athlon 200GE, 7.5 GB RAM). Builds happen in GitHub Actions (`gradle assembleDebug` on main push).
- Verification commands on Linux/CI (there is NO gradle wrapper jar in this repo — use system `gradle`, not `./gradlew`):
  - Unit tests: `gradle testDebugUnitTest`
  - Debug build: `gradle assembleDebug`
  - Signed Release build: `gradle assembleRelease`
- Editor formatting: `.editorconfig` configured at project root.

## Git workflow
- GitHub is the single source of truth. All changes are committed and pushed to `main` (authorss81/Smooth-Notes).
- Repo was renamed from Noteflow-2; commit history and tags were preserved at rename time.
- Remote: `https://github.com/authorss81/Smooth-Notes.git`

## Hard rules
- **MAJOR ARCHITECTURAL CHANGE → ASK THE USER FIRST.** Any change that restructures packages, replaces the navigation model (e.g., migrating away from `mutableStateOf` navigation to Navigation Compose), adds a new DB schema/migration, introduces SQLCipher, adds new dependencies with broad impact, or rewrites a subsystem (canvas renderer, encryption layer) MUST be proposed to the user and get explicit approval BEFORE writing code. Tell the user what's changing, why, and the risk. Do not silently refactor architecture.
  - **USER APPROVED (2026-08-14): hybrid downloadable-plugin architecture** — the plugin model change (base-APK-size constraint, downloadable signature-verified plugins for heavy native features, compile-time for lightweight plugins, user-approved dynamic updates) was proposed to the user with risks and explicitly approved (commit `9292978`). Implementation proceeds per phases 22–26; the approved design doc is `docs/plugin-architecture.md`.
- **Android hardware reality**: this is a native Kotlin app targeting API 26+. Every feature must ship with a fallback for older/lower-end devices (API 33- for AGSL, API 31- for dynamic color, low-RAM behavior for 2-core devices). Never silent degradation — one-time non-alarming message + settings re-enable. See ROADMAP Phase 33 + `docs/COMPATIBILITY.md` (to be created).
- **Kali Linux pentest findings → write to a FILE immediately, not just the agent reply.** Security/pentest agents (running in opencode on the Kali VM) MUST append findings incrementally to `docs/pentest-findings-<YYYY-MM-DD>.md` as they work (one block per finding: severity, evidence, file/line or command output, reproducer), and commit/push after each agent batch. This prevents detail loss when a report grows long or context is truncated. The pentest plan lives in `docs/pentest-plan.md`; the summary report goes to `docs/pentest-report.md`.
- `allowBackup="false"` + `data_extraction_rules.xml` — never re-enable backup.
- **Base-APK size is a hard constraint.** Heavy/native features (ML Kit OCR, ML Kit
  barcode/QR, the MediaPipe `tasks-genai` LLM) MUST NOT be baked into the base APK.
  They ship as **downloadable, signature-verified plugins** (Phase 23 runtime:
  HTTPS download → pinned-cert-hash verify → `DexClassLoader` load → capability
  facade, never direct DB/keystore handles). Lightweight pure-JVM plugins stay
  compile-time. Do NOT add large native deps to the base app; use a separate
  plugin module instead. Plan: Phase 22 = architecture skeleton, Phase 23 =
  downloadable runtime, Phase 24 = user-approved updates, Phase 25 = ink→shape,
  Phase 26 = lightweight ecosystem, Phase 29 = move the LLM out of the base APK
  into a downloadable plugin.
- PBKDF2WithHmacSHA256 600k iterations, AES-256-GCM (12-byte IV, 128-bit tag), AndroidKeyStore-wrapped DEK, in-memory zeroization on lock (`NoteRepository.kt`, `EncryptionService.kt`, `SecurityService.kt`).
- Never log keys, passwords, or decrypted note content. Never add `INTERNET` usage without a real feature.
- Known broken things (do NOT trust ROADMAP [x] claims — see ROADMAP.md "POST-AUDIT TRUTH TABLE" @ commit ac781de):
  - ~~Checksum hashes `noteflow.db`~~ — FIXED: `DatabaseSecurityHelper` HMACs the real `noteflow.sqlite` (`DatabaseSecurityHelper.kt:26`) and the result is surfaced as the `databaseTampered` banner in MainActivity (`NoteflowViewModel.kt:35-55`). This stale claim was corrected in Phase 09.
  - `pointsJson` (stroke geometry) is FIELD-ENCRYPTED at rest now — title/extractedText/textContent/pointsJson are all encrypted fields (`saveStrokesForPage`, NoteRepository.kt); the Phase 02 restore re-key gap is closed.
  - "Sync" = WebDAV upload/download of ENCRYPTED backup archives to/from the user's own server (`WebDavSyncService.kt`, HTTPS enforced, HTTP only with explicit opt-in for local-network hosts), reachable from HomeScreen's ⋮ menu via `WebDavSyncDialog` — Phase 14 FIXED the previously-dead menu trigger; AGSL wet-mixing is WIRED through `WetBrushEngine` in `AnnotationCanvas` (gated by `ShaderCapabilityHelper.isAgslSupported` + the `gpuWetBrushesEnabled` setting, verified in the Phase 14 audit); pressure/tilt/timestamp are captured via the official raw-MotionEvent bridge (`pointerInteropFilter`, no reflection); FLAG_SECURE is applied in non-debug builds; R8 minify is ON in release; baseline-profile TOOLCHAIN is WIRED since phase-199 (`:baselineprofile` producer + consumer plugin + pinned profileinstaller + R8 fullMode + shrinkResources) but no profile is generated/committed yet — the guarded `compileArtProfile` stopgap stays active until a real profile lands (see `workspace/phase-199/REPORT.md`; the old dead `baseline-prof.txt` was deleted in the Phase 03 honesty audit). Phase-199 second review-fix round (2026-08-28) completed the producer's lockfile entries and wired `app/build.gradle.kts` `baselineProfile { from(project(":baselineprofile")) }` so `gradle :app:generateBaselineProfile` plans the real producer chain — run it (and any `:baselineprofile:*` producer task) with `--no-configuration-cache`, since the repo sets `org.gradle.configuration-cache=true` and AGP's `CheckAarMetadataTask`/`checkTestedAppObfuscation` fields aren't config-cache-serializable.
  - WAL checkpoint DOES execute — `checkpointWal()` fully steps the cursor (`NoteRepository.kt:136-144`).
  - **Phase 14 (2026-08-13) production readiness + LocalSend**: full per-phase audit lives in `workspace/phase-14/AUDIT_REPORT.md`. LocalSend sender (Protocol v2.2, sender-only, local-network only, no new permissions) is implemented in `services/localsend/` (`LocalSendProtocol.kt` pure-JVM + `LocalSendSender.kt` network) and reachable from HomeScreen's ⋮ menu → "Send to Nearby Device (LocalSend)". It sends a single note as HTML, the encrypted vault backup, or the Obsidian/HTML vault archives; the receiving device must human-accept before bytes move. Unit tests: `LocalSendProtocolTest` (18 tests, no network). **Release signing (updated 2026-08-17, phase-57/B1-PLAT-1): the release build FAILS CLOSED when `RELEASE_KEYSTORE_B64` (or `KEYSTORE_FILE`) is unset — the old debug-keystore fallback is gone; see `docs/RELEASE.md` + `B1Plat01ReleaseSigningTest`.**
  - **Phase 21 (2026-08-14) added the Plugin Store + verified the execution path** (see `docs/PLUGINS.md` "Plugin Store — install/uninstall lifecycle"): `PluginStoreCatalog` (bundled catalog, no network) + `PluginStoreController` (pure-JVM download/delete) + `PluginRegistry.installPlugin/uninstallPlugin` (install-aware registry; `compiledPlugins` = full shipped set, `allPlugins` = installed set) + `SettingsPluginInstallStore`/`SettingsManager.plugin_uninstalled_<id>` (default installed = backward compatible). Delete wipes opt-in + ever-enabled + all `plugins.<id>.*` settings and deletes downloaded assets (`NoteflowPlugin.deleteDownloadedAssets`); Disable keeps data. `CaseChangePlugin` is the store's OPTIONAL plugin (not in `defaultPlugins()`). UI: `PluginStoreDialog` reachable from HomeScreen ⋮ menu → "Plugin Store". Tests: `PluginStoreLifecycleTest` (8), `PluginExecutionSimulationTest` (4). Also fixed a pre-existing `PaletteCatalog.familyFor` hue-band bug (curated swatches misclassified) — `PaletteCatalogTest` is green again.
  - **Phase 10 (2026-08-13) added a REAL plugin framework** (`app/src/main/kotlin/com/authorss81/noteflow/plugins/`): `NoteflowPlugin` + `PluginCapability` sealed type (`TextTransform`, `OCR`, `WebSearch`, `FileTransfer`, `Assistant`, `Export`), compile-time `PluginRegistry` (no dynamic APK loading), `PluginManager` routing that fails loudly via `PluginResult.Failure`, per-plugin opt-in persisted in `SettingsManager` (`plugin_enabled_<id>`, off by default), a Settings → Plugins dialog (`PluginSettingsDialog`, reachable from HomeScreen's ⋮ menu), and `Rot13TransformPlugin` served through the `TextTransformPlugin` interface and wired into the Markdown editor's Plugins menu (`MarkdownPreviewScreen`). Covered by `PluginFrameworkTest` (10 tests). **Phase 12 made OCR + Web Search real**: `OnDeviceOcrPlugin` (ML Kit text-recognition, offline, no API key) and `DuckDuckGoWebSearchPlugin` (HTTPS, keyless) are registered (`PluginRegistry.kt`) and wired into `EditorScreen` (OCR result dialog) and `MarkdownPreviewScreen` (web-search dialog). The still-unserved capabilities are `FileTransfer`, `Assistant`, `Export` — requests for those fail loudly with `NO_PLUGIN_INSTALLED`. See `docs/PLUGINS.md`.
  - **Phase 09 (2026-08-13) fixed the two Phase-02 data-loss defects that were still live** (see workspace/phase-09/REPORT.md): (1) H2 — a corrupt/wrong-key DB open no longer DELETES the vault files; `SafeSupportSQLiteOpenHelper` now quarantines them as `*.corrupt-<timestamp>` (bytes preserved), sets a persistent corruption flag, and MainActivity shows `CorruptionRecoveryScreen` (restore-from-backup or explicit start-fresh) before any normal vault UI (`NoteflowDatabase.kt` `quarantineCorruptDatabase`, `DatabaseSecurityHelper.kt` corruption flags, `MainActivity.kt` `CorruptionRecoveryScreen`). (2) H1 — a failed restore no longer bricks the app: the backup password is validated BEFORE `closeDatabase()` (`ImportExportService.validateBackupPassword` now called from `HomeScreen.performRestore` and `NoteflowViewModel.attemptRecoveryFromBackup`), and on any post-close failure the Room instance is reopened (`NoteflowDatabase.dispose()` + `NoteRepository.reopenDatabase`) and the process restarts via the existing restart dialog. (3) C1 — `reencryptPlaintextFields` now also re-encrypts `note_versions.{title,extractedText}`, matching the cross-device migrate map.
  - **Phase 03 (2026-08-13) removed these dead/fake features**: the LibMyPaint C++ stub + whole engine-selector stack (`mypaint_jni.cpp`/`CMakeLists.txt`, `LibMyPaintJni`, `BrushEngine`/`BrushEngineFactory`, `HardwareProfiler`/`RenderingEngineTier`, `BrushEngineSettingsDialog`, `SettingsManager.renderingEngineOverride` — all deleted; the canvas renders via `WetBrushEngine`); `HandwritingRecognitionService` + `HandwritingToTextDialog` (dictionary-hash stub, unreachable — deleted); `FtsSearchEngine` (misnamed in-memory loop — deleted; vault search now uses a cached decrypted corpus in `NoteRepository`). Shape auto-snap is now WIRED (`ShapeRecognitionHelper.trySnapShape` runs on freehand draw-end in `AnnotationCanvas`, gated by the `shapeAutoSnapEnabled` setting; wet tools, LASER, DOTTED and NEON are excluded, and the RECT/ARROW/LINE detectors require real geometry fits so plain handwriting isn't snapped).

## Roadmap state (August 2026)
- Phases 1–18 marked done in ROADMAP.md are NOT all true — verified facts in "POST-AUDIT TRUTH TABLE" section.
- Actual work plan: Phases 19 (security recovery) → 20 (performance) → 21 (false-feature fix/remove) → 22 (UX/platform) → 23 (canvas modes: infinite + page-wise) → 24 (import pipeline: PDF/HTML/images) → 25 (old Phases 8–10 remainder) → 26 (moat features) → 27 (bug-fix queue) → 28 (release engineering).
- Fix bugs in Phase 27 before building new features; each fix needs `file:line` verification, not vibes.
- **Authoritative security-fix status: `docs/security-report.md`** (every finding marked with its fixing phase or `resolved at triage`, top-risks section rewritten to the post-fix state on 2026-08-17) + the fix-phase mapping in `workspace/SECURITY_FIX_PLAN.md` + the per-phase verification matrix in `workspace/phase-115/REPORT.md`. All phase-39..114 findings are FIXED except **`B2-DEPS-05` (phase-77, OPEN — the LLM model download is neither hash-pinned nor signature-verified; the phase-77 `.done` was a false completion removed on 2026-08-17, so the pipeline re-selects it for a real fix).** **Phase 149 (2026-08-18) closed `R2-b2b4-DOS-01` (MEDIUM — `note_versions` retention + paged decrypt, see `workspace/phase-149/REPORT.md`): newest-20-per-page cap pruned inside the `createNoteVersion` transaction, paged/lazy history decrypt, capped backup writer + restore sanitizer. `gradle assembleDebug` green; `gradle testDebugUnitTest` 2026 green / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (reproduced on a clean stash — untouched by phase-149).** **Phase 153 (2026-08-19) closed `R2-b2b1-UI-04` + `R2-b2b1-UI-05` (LOW×2 — post-lock UI channels, see `workspace/phase-153/REPORT.md`): the snackbar channel is a bounded `StateFlow` FIFO gated by pure-JVM `SnackbarLockPolicy.mayBufferWhileLocked` (locked vaults DROP every vault-content message), `lock()` clears the queue, the `MainActivity` root collector is re-keyed on `authenticated` (boundary dismiss + FIFO drain), and a lock-during-voice-recording now re-surfaces an honest fixed discard notice through the persistent pipeline (`VoiceNoteManager.release(): Boolean` + `EditorScreen` teardown) instead of the dead per-editor error banner. `gradle assembleDebug` green; `gradle testDebugUnitTest` 2117 green / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched).**
- **Phase 158 (2026-08-19) shipped deferred ROADMAP 22.5** (see `workspace/phase-158/REPORT.md`): (1) **reader/focus mode** for `MarkdownPreviewScreen` — pure-JVM `services/ReaderModePolicy.kt` (680dp cap, 1.15× leading applied to the style's OWN already-scaled line height — always WIDER than the default, never an absolute `.sp`; phase-158 review-fix corrected the original 1.35×-of-type-size that actually tightened body leading below Material's default 24sp), instant no-animation toggle, read-only path that never composes the hybrid editor (long-press-safe; editing chrome stripped; version-RESTORE disabled in reader mode — `VersionHistoryBottomSheet readOnly` review-fix), captured notes open in reader mode by default. (2) **share-sheet capture polish** — `ShareCaptureMode` new-vs-append radio choice in the confirm dialog (append disabled when no active note / image clip), 10-min hold expiry, honest defer/drop (applies only on authenticated frame; password-vault `lock()` drops states + clears the marker), NON-SECRET captured marker (`capturedSharePending`/`AtMs` — flag + stamp, never content). Review-fixes: the marker is now READ (honest "previous capture wasn't captured" notice after a process death mid-capture, cleared once shown); an append pushes the combined body into the open editor via `MarkdownPreviewScreen.externalBodyUpdate` so the text shows immediately and a stale pre-append snapshot can't be saved back over it; the dialog/snackbar strings moved to `strings.xml`. (3) **home widget quick-capture** — LIGHTWEIGHT in-base launcher-only AppWidget (`ui/widget/QuickCaptureWidget.kt` + `services/WidgetLaunchPolicy.kt`), `updatePeriodMillis=0`, no vault in the widget process, no permission, `exported="false"`; `MainActivity` fires `addPage` only once authenticated (the pending flag survives rotation via `onSaveInstanceState` — review-fix). `gradle assembleDebug` green; `gradle testDebugUnitTest` 2240 green / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched). No schema change, no new base-APK deps.

- **Phase 166 (2026-08-19) compact-screen overflow pass** (see `workspace/phase-166/REPORT.md`): at 360dp portrait, six fixed-width Row sites that CLIPPED their children were fixed and pinned by a new source-pinning test suite `Phase166LayoutOverflowTest` (8 tests). (1) `WebDavSyncDialog` primary actions moved from the `AlertDialog` confirm row to stacked full-width body buttons (`WebDavSyncDialog.kt:305-388`; `confirmButton` = Close only, `:391`). (2) `CalendarView` stacks the date summary above the "New Note for Date" button (`CalendarView.kt:188-212`). (3) HomeScreen import orientation chip row is horizontally scrollable (`HomeScreen.kt:1711-1714`). (4) HomeScreen filtered-by-tag banner text is `weight` + `maxLines` + ellipsis (`HomeScreen.kt:1233-1245`). (5) `InteractiveTutorial` puts "Skip Tutorial" on its own line and end-aligns the Back/Skip-step/Next row (`InteractiveTutorial.kt:338-402`). (6) `MarkdownPreviewScreen` app bar decluttered: the title row holds only the title + Reader chip; the view-mode / split-orientation / Serif chips moved to a full-width horizontally scrollable sub-bar beneath the app bar (`MarkdownPreviewScreen.kt:564-635`; Serif stays reachable in reader/preview modes — phase-158 behavior preserved). Also the KnowledgeGraph selected-node card title wraps/ellipsizes (`KnowledgeGraphScreen.kt:674-680`). Review-fixes (2026-08-19): the WebDav regression pin was strengthened — it now asserts the actions are full-width BODY buttons with the confirm slot holding only Close (the phase-166-authored assertions passed on the pre-fix code), and the Serif toggle kept its phase-158 gate, reachable in reader/preview/split modes and hidden in the plain editor. `gradle assembleDebug` green; `gradle testDebugUnitTest` 2268 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched, reproduced on a clean stash). No schema change, no new deps, `.github/workflows/` untouched.
- **Phase 177 (2026-08-20) plugin ecosystem full review** (see `workspace/phase-177/REPORT.md`): complete file:line inventory committed first (`workspace/phase-177/INVENTORY.md` — `plugins/` core, `plugins/store/`, UI dialogs, `SettingsManager` plugin keys, SDK contract in `plugin-sdk/plugins/FrameworkPlugin.kt:58`). **Off-by-default re-verified + pinned**: `plugin_enabled_<id>` defaults false (`SettingsManager.kt:447-448`), only the user-facing toggles write it, `installPlugin` flips install-only (`PluginRegistry.kt:428`), capability routing requires `enabled == true` (`PluginManager.kt:197`), `onProcessStart`/`resumeLifecycle`/`refreshAvailability` never auto-enable; store + settings show accurate on/off from the SAME enable store the router reads (off ⇔ REGISTERED/DISABLED, on ⇔ ENABLED/AVAILABLE/UNAVAILABLE, never both/neither). **Delete** is confirmation-gated on the ONE delete path (`PluginStoreDialog.kt:535-558`); rejected plugins show Delete/no-Enable; `PluginStoreController.delete` runs `deleteDownloadedAssets` (`PluginStoreController.kt:227`) before `uninstallPlugin` wipes opt-in/ever-enabled/journal/consent/entry. **Fix found**: phase-146 R2-b2b2-DEP-03 pin had gone stale after phase-175 made `:plugins:mlkit` resolve `mlkit:translate` → okhttp-3.0.0/okio-1.6.0 as VERIFIED jars — updated the pin to require a verified jar (sha256) or a retained POM-only entry (`Phase146BuildIntegrityTest.kt`) + the `settings.gradle.kts` comment; no security control weakened. New pins: `Phase177PluginEcosystemReviewTest` (3). `gradle assembleDebug` green; `gradle testDebugUnitTest` 2384 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched; `Phase151MarkdownMainThreadPerfTest` + `WikiLinkParserCacheUnitTest` timing/concurrency flakes pass in isolation). No schema change, no new deps, `.github/workflows/` untouched, base-APK-size rule intact.

## Docs
- `docs/pentest-plan.md` — Kali Linux pentest plan for the built APK (jadx/apktool/frida/objection/MobSF). Run findings into `docs/pentest-report.md`.
- `README.md` — user-facing overview (may contain stale claims; ROADMAP truth table supersedes).
- `docs/ARCHITECTURE.md` — **living architecture map** (package layout, core subsystem `file:line` anchors, build/CI essentials, gotchas). It is auto-injected at the start of every pipeline phase by `scripts/phase_runner.sh`. After implementing something, append a short "Implemented in <phase>:" note to the relevant section and update `docs/phase-status.md`.
