# InkFlow/Noteflow — Security Audit Report — Round 2 (Phase 116 + 117)

> Round-2 adversarial SOURCE-CODE review of the post-fix, post-feature tree (the
> Phase-30 methodology repeated on the codebase after phases 34-38 added features
> and phases 39-115 applied ~76 security fixes). Shared with phase-117 (dynamic/
> APK review) — BOTH phases append to this ONE file. Fixes are generated by the
> phase-118 pipeline; this file is the single source of truth for round-2
> findings. Round-1 findings + fix statuses live in `docs/security-report.md`
> (do NOT copy round-1 rows here as-is).

## Audit metadata
- Phase: 116 (source audit, phase-117 appends dynamic findings)
- Date: 2026-08-17
- Commit audited: `c813c99` (`llops: phase-77 (B2-DEPS-05) REAL fix — LLM GGUF download is now a PINNED identity`)
- Method: 2 batches x 5 parallel subagents, source-code review only, `file:line`
  evidence, hacker mindset. No external services attacked, no dynamic tooling
  (that is phase-117's job).
- Batch 1 areas: 1) Cryptography & key management, 2) Data-at-rest & DB,
  3) Data-in-transit & network (incl. the downloadable-plugin runtime), 4) Android
  platform surface, 5) App logic & auth.
- Batch 2 areas: 1) Compose/UI + concurrency/races/TOCTOU, 2) Dependencies/CVE
  review, 3) Logging/telemetry/info disclosure, 4) Resource-exhaustion/DoS,
  5) NEW phase 34-38 features (glass surfaces, brush/nib preview math, minimap/HUD,
  markdown hybrid editor + callouts + waveform scrubbing, knowledge-graph layout).
- APK target: phase-115's release build. Regenerate with the documented command
  (`RELEASE_KEYSTORE_B64=<base64> gradle assembleRelease`; the build FAILS CLOSED
  without the keystore env — see `docs/RELEASE.md` + `B1Plat01ReleaseSigningTest`),
  or audit the last phase-32 local artifacts (release 142.0 MB R8-minified,
  SHA-256 `d7cdbebe…`).

## Severity legend (matches round 1)
- CRITICAL — remote or local-privilege-free compromise of confidentiality/
  integrity/availability; data exfiltration or vault unlock without credentials.
- HIGH — significant confidentiality/integrity impact under realistic local
  attacker or man-in-the-middle scenarios.
- MEDIUM — moderate impact, requires non-default config, physical access, or
  co-located attacker.
- LOW — limited impact / hardening gap.
- INFO — informational, best-practice note.

---

# Findings summary (living — updated per batch; final totals after phase-117)

| Severity | Batch 1 | Batch 2 | Total |
|----------|---------|---------|-------|
| CRITICAL | 0       |         |       |
| HIGH     | 0       |         |       |
| MEDIUM   | 6       |         |       |
| LOW      | 11      |         |       |
| INFO     | 2       |         |       |
| **Total**| **19**  |         |       |

> Batch-1 subtotal = 19 unique findings (21 raw agent findings minus the
> R2-B1C-01 duplicate folded into R2-B1D-02).
> Per area (Batch 1): crypto (`b1-crypto`): 2 · data-at-rest/DB (`b1-datarest`): 5 ·
> network/plugins (`b1-net`): 5 · platform (`b1-platform`): 5 · app logic/auth (`b1-applogic`): 3.

---

# Batch 1 — findings (5 parallel subagents, c813c99)

## Quick lookup

| ID | Severity | Area | Status |
|----|----------|------|--------|
| R2-B1D-02 | MEDIUM | data-at-rest/DB | open |
| R2-B1A-01 | MEDIUM | app logic & auth | open |
| R2-B1A-02 | MEDIUM | app logic & auth | open |
| R2-B1D-01 | MEDIUM | data-at-rest/DB (B1-DB-6 regression-of-design) | open |
| R2-B1P-01 | MEDIUM | android platform surface | open |
| R2-B1D-05 | LOW | data-at-rest/DB | open |
| R2-B1D-03 | LOW | data-at-rest/DB | open |
| R2-B1D-04 | LOW | data-at-rest/DB | open |
| R2-B1A-03 | LOW | app logic & auth | open |
| R2-B1N-01 | LOW | network (plugin runtime) | open |
| R2-B1N-02 | LOW | network (B1-NET-04 residual) | open |
| R2-B1N-03 | LOW | network (B1-AUTH-01 residual) | open |
| R2-B1N-04 | LOW | network | open |
| R2-B1N-05 | INFO | network | open |
| R2-B1C-02 | LOW | crypto (credential key mgmt) | open |
| R2-B1C-03 | INFO | crypto (in-memory hygiene) | open |
| R2-B1P-02 | LOW | android platform surface | open |
| R2-B1P-03 | LOW | android platform surface | open |
| R2-B1P-04 | LOW | android platform surface | open |
| R2-B1P-05 | INFO | android platform surface | open |

## Detail

### [R2-B1D-02] Restore accepts a structurally-invalid or EMPTY SQLCipher database and swaps it over the live vault — irrecoverable data loss with the app's own HMAC re-armed on attacker data (independent crypto duplicate folded: R2-B1C-01)
- **Status:** OPEN
- **Severity:** MEDIUM
- **Area:** Batch 1 · Data-at-rest & DB (also reported by the crypto agent as
  R2-B1C-01 — same root cause, merged here)
- **Agent:** b1-datarest (+ b1-crypto duplicate)
- **Evidence:** `ImportExportService.kt:1867-1932` (`validateAndPrepareRestoredDb`),
  `:1281-1289` (`checkRestoredSchemaNotNewer` only rejects NEWER `user_version`,
  so a 0 < 9 DB passes), `:1816-1860` (`extractBackupEntriesTo` — no minimum
  size/content check; a crafted 0-byte `noteflow.sqlite` sets `sawDatabase = true`),
  `:1979-1982`/`:2017-2022` (delete/update sanitizers swallow "no such table" errors),
  `:1711-1722` (`rekeySqlcipherDb`), `:1801-1808` (`rearmBaselineFromFile` HMACs the
  restored copy — re-blessing attacker data), `:2086-2125` (`commitRestoredFiles` swaps
  + `deleteRecursively()` deletes the only surviving copy); `NoteflowDatabase.kt:416-417`
  (`fallbackToDestructiveMigration()` — a restored DB with a mismatched Room identity is
  silently DROPPED on the next open). The only pre-swap gates are SQLCipher candidate-key
  open + `PRAGMA integrity_check == ok` + `user_version <= SCHEMA_VERSION`, all of which
  a freshly-initialized EMPTY SQLCipher DB satisfies (`integrity_check` = ok, `user_version`
  = 0, `PRAGMA rekey` materializes the header so the file is non-empty by re-arm time).
- **Exploit scenario / Reproducer:** An attacker who knows the vault master password
  (the backup password — household/coworker share or an ex-partner) mints a valid NFLB3
  archive containing an empty/zero-schema `noteflow.sqlite`; the victim restores it via
  the normal restore flow or from a craftable WebDAV folder. Every note/stroke/
  voice-note/version is irreversibly replaced by an empty vault — no "this backup
  contains no data" warning, no quarantine of the live file. Less maliciously: restoring
  a backup exported from a fresh install silently wipes a populated vault. A wrong-schema
  (not version 9) restored DB additionally hits `fallbackToDestructiveMigration()`.
- **Fix:** Before the swap, open the restored DB under the successful candidate key and
  (a) require the expected Room schema (`pages`/`strokes`/`note_versions`/`media_embeds`
  tables exist in `sqlite_master`), (b) refuse `user_version` outside an accepted range
  (absence = empty backup, require explicit confirmation), and (c) warn/refuse on a zero
  rowcount vault unless the user confirms "start fresh". Abort the restore (quarantine the
  incoming file) rather than re-arm + swap on any failure.

### [R2-B1A-01] ~18 page/notebook/section/tag/pin/trash/template mutations bypass the lock-race guard — in-flight DAO call on the disposed SQLCipher pool crashes the process
- **Status:** OPEN
- **Severity:** MEDIUM
- **Area:** Batch 1 · App logic & auth (write paths vs `lock()` disposal)
- **Agent:** b1-applogic
- **Evidence:** the guard `writeGuardedAgainstLock`/`isLockRacedWrite` exists but is used
  only at 3 sites (`NoteflowViewModel.kt:3177` util def, `:3187`, comment `:3170-3176`
  scopes it to "the non-flush page writes (create/rename/tag/daily/wiki/template)").
  Bare `viewModelScope.launch { repository.X() }` with no catch: `updateNotebookTags`
  `:1728`, `deleteNotebook` `:1736`, `addSection` `:1750`, `renameSection` `:1758`,
  `deleteSection` `:1767`, `updatePageTags` `:1971`, `renameTag` `:1980`, `deleteTag`
  `:2007` (vault-wide scans `:1984-2024`), `togglePinPage` `:2032`, `trashPage` `:2038`,
  `updatePageTemplate` `:2047`, `restorePage` `:2260`, `deletePagePermanently` `:2266`
  (also FS-deletes audio embeds, `NoteRepository.kt:843-849`), `movePage` `:2278`,
  `emptyTrash` `:2284`, palette ops `:2290-2306`. `lock()` disposes the pool
  (`NoteflowViewModel.kt:3668-3673`, comment `:3657-3660` "fail closed ('connection pool
  has been closed')").
- **Exploit scenario / Reproducer:** Default auto-lock fires (1 s idle poll,
  `MainActivity.kt:248-263`) or the user taps "Lock Vault Now" racing a just-tapped
  destructive/iterate-heavy action (`renameTag`/`deleteTag` scan the whole vault); the
  in-flight DAO call hits the disposed pool and throws an uncaught
  `IllegalStateException("connection pool has been closed")` into the `viewModelScope`
  default handler on the main dispatcher → process crash. Wider window on 2-core/slow
  devices; `deletePagePermanently` adds filesystem deletes.
- **Fix:** Route all of these through the existing `writeGuardedAgainstLock` (catch +
  classify via `isLockRacedWrite`), or cancel a tracked mutations job inside `lock()`
  the way the search job is handled (see R2-B1A-02). Phase-49's comment already promises
  this delta; the tag/trash/pin/template/notebook/section paths were left out.

### [R2-B1A-02] In-flight vault search is never cancelled or fail-closed on lock — same pool-closed crash, and a completed search can still deliver decrypted results after auth dropped
- **Status:** OPEN
- **Severity:** MEDIUM
- **Area:** Batch 1 · App logic & auth (search job vs `lock()`)
- **Agent:** b1-applogic
- **Evidence:** shared `searchVaultJob` `NoteflowViewModel.kt:2098`; cancelled only on a
  new keystroke (`:2101`, `:2116`), NEVER in `lock()` (`:3638-3694` — no
  `searchVaultJob?.cancel()`). `deepSearchPages` (`NoteRepository.kt:550-564`) pages the
  DAO on `Dispatchers.IO`; `searchPages` re-loads the corpus (a `zeroizeKey()` →
  `invalidateSearchCorpus()` path) — both hit the pool `lock()` disposed, throwing into an
  uncaught launch. `onResult` (`:2105`, `:2120`) is guarded only by `ensureActive()`,
  which returns normally if the job just isn't cancelled, so a completed search can still
  publish a decrypted `List<NotePageEntity>` after the auth gate dropped (consumed behind
  the LockScreen — low secrecy impact, but a post-auth write to UI state).
- **Exploit scenario / Reproducer:** Fast typist + 5-min auto-lock: the polling lock fires
  while a deep search decrypts on `Dispatchers.IO`; the page-batch DAO call throws on the
  closed pool → crash, or (with a faster finish) decrypted results land in `_searchResults`
  after the vault locked.
- **Fix:** `lock()` should `searchVaultJob?.cancel()` + clear `_searchResults`; the search
  launch should re-check `_authenticated` before invoking `onResult`.

### [R2-B1D-01] Startup tamper-verify is structurally guaranteed `Mismatch` after any write since the last baseline stamp — the WAL-aware HMAC tripwire can no longer distinguish real tampering from an ordinary note edit (regression-of-design on B1-DB-6 / phase-87)
- **Status:** OPEN
- **Severity:** MEDIUM
- **Area:** Batch 1 · Data-at-rest & DB (B1-DB-6 regression-of-design)
- **Agent:** b1-datarest
- **Evidence:** WAL journal mode `NoteflowDatabase.kt:415`; HMAC streams main + `-wal`
  (`DatabaseHmacPolicy.kt:42-66`, `DatabaseSecurityHelper.kt:49-72`); verify runs once per
  process at init (`NoteflowViewModel.kt:1138-1150` → `1202-1223` → banner
  `MainActivity.kt:321-332`); the ONLY re-arm sites are event-driven — `NoteflowViewModel.kt:1116,1490,1517,1561,2634,3348`, `HomeScreen.kt:586,1398` — none on the normal write path; `lock()` (`NoteflowViewModel.kt:3638-3694`) and `NoteflowDatabase.dispose()`
  (`:431-436`) never checkpoint + re-arm. A vault used since its last backup/migration/
  password-armed baseline therefore fails the next-start verify whether a last-connection
  close checkpoint merged the WAL (main differs) or not (wal bytes differ).
- **Exploit scenario / Reproducer:** Create master password (baseline armed) → write/edit a
  note → next process start: the persistent "Database integrity check failed" banner shows,
  and `DatabaseIntegrityVerdict.Mismatch` can never be trusted as a tamper signal again —
  the same banner a real attacker would trigger is now routine. Per-session dismissal is
  the only relief; the door it closes (B1-DB-6) is effectively half-open (a real tamper
  hides in the noise).
- **Fix:** Checkpoint + re-arm the baseline at every session end (`lock()` / dispose) or
  verify only at a quiescent checkpointed state (fresh unlock, post-checkpoint, before
  writes), so a verified pass has meaning again; alternatively drive the baseline from a
  stable per-record construction (e.g. HMAC over a snapshot header) instead of raw main+wal
  bytes. Re-assert the B1-DB-6 coverage tests against the new cadence.

### [R2-B1P-01] Clipboard-scrub on lock covers only 2 of the app's copy surfaces — the primary note-editor copy is untracked and survives every lock (B2-UI-2 gap)
- **Status:** OPEN
- **Severity:** MEDIUM
- **Area:** Batch 1 · Android platform surface (B2-UI-2 gap)
- **Agent:** b1-platform
- **Evidence:** `ClipboardGuard.recordCopy()` is stamped at exactly two sites —
  `OcrResultDialog.kt:149` and `MediaEmbedComponents.kt:353`. `lock()` scrubs via
  `scrubIfOwnCopy` (`NoteflowViewModel.kt:3649`) which denies when `mostRecentCopyAtMs
  == 0L` (`ClipboardGuard.kt:50-54`, `ClipboardScrubPolicy.kt:32-33`). The markdown editor
  is a platform text field (`HybridMarkdownEditor.kt:219`) whose long-press → Select-all →
  Copy writes decrypted note body to the system clipboard with NO `recordCopy()` stamp; the
  OCR dialog's own `SelectionContainer` (`OcrResultDialog.kt:121`) has the same untracked
  native Copy next to the tracked button.
- **Exploit scenario / Reproducer:** User copies a note body via the normal editor selection
  menu → taps "Lock Vault Now" (or auto-lock/screen-off fires). `scrubIfOwnCopy` sees no
  stamp and wipes nothing; the LockScreen shows but the decrypted note text is still on the
  shared clipboard — a bystander pastes it in any app. Unlike the 60 s window caveat of
  B2-UI-2, these copies have NO window at all.
- **Fix:** Route every note-content copy through ONE shared stamping helper (intercept the
  editor selection via `LocalTextToolbar`/custom `SelectionContainer`), or — given this
  app's threat model — clear the primary clip unconditionally in `lock()` instead of only
  "own copy within window". Grep-pin that every clipboard write in `ui/` is preceded by
  `ClipboardGuard.recordCopy()`.

### [R2-B1D-05] `exportBackup` copies the live main DB file with a plain unbounded FileInputStream — a concurrent auto-checkpoint can produce a torn/corrupt archive
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-at-rest & DB
- **Agent:** b1-datarest
- **Evidence:** `ImportExportService.kt:1314-1318` (`exportBackup` copies
  `noteflow.sqlite` with a plain `FileInputStream` copy; only `-wal`/`-shm` also captured),
  vs the checkpoint-first producers `HomeScreen.kt:584-588` / `NoteflowViewModel.kt:3346-3350`
  (`checkpointWal()` BEFORE the backup). The WAL auto-checkpoint threshold (1000 pages)
  can fire DURING the copy.
- **Exploit scenario / Reproducer:** A large active vault where the WAL auto-checkpoints
  (reclaiming frames + rewriting main) mid-copy yields a main-file snapshot that never
  existed — a torn archive that only surfaces at restore time (`integrity_check` fails →
  the user's backup is quietly broken). Even `home`/`viewModel` checkpoint-first calls only
  checkpoint immediately before, racing the copy itself.
- **Fix:** Read the DB file under the same `getPagePermanently`-safe discipline — e.g. a
  full `checkpointWal(FULL)` + open a live SQLCipher backup source (`PRAGMA backup_to`,
  `SQLiteDatabase.database.copyToFile`) so the snapshot is consistent; or open the file for
  copy while holding a read lock after a fresh FULL checkpoint and verify size+HMAC on the
  staged copy.

### [R2-B1D-03] LocalSend `VAULT_BACKUP` builds the archive from the live main DB file WITHOUT the pre-export `wal_checkpoint(FULL)` every other producer does
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-at-rest & DB
- **Agent:** b1-datarest
- **Evidence:** `LocalSendSendDialog.kt:142` calls `exportBackup` directly (no checkpoint
  before); contrast `HomeScreen.kt:584-588` / `NoteflowViewModel.kt:3346-3350`
  (checkpoint first). `exportBackup` copies only `noteflow.sqlite`
  (`ImportExportService.kt:1314-1318`).
- **Exploit scenario / Reproducer:** User sends "Encrypted vault backup" to a paired device
  while the WAL holds recent writes not yet checkpointed; the receiving archive is silently
  missing those last note edits. (No confidentiality issue — the target is explicitly chosen
  and the archive is encrypted — stale data integrity only.)
- **Fix:** Call `repository.checkpointWal()` + re-stamp the HMAC before the LocalSend backup
  path, exactly like the HomeScreen/WebDAV producers.

### [R2-B1D-04] v2/v3 restore decrypt holds the <=400MB encrypted input AND <=400MB decrypted zip concurrently (~800MB peak), then extraction hits a LOWER 200MB cap — legitimate large vaults are unrestorable and the path OOMs mid-restore leaving the DB closed
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-at-rest & DB
- **Agent:** b1-datarest
- **Evidence:** `ImportExportService.kt:1563-1636` (in-heap decrypt: encrypted
  `ByteArrayOutputStream` + decrypted zip both materialized), caps `MAX_BACKUP_INPUT_BYTES`
  = 400MB `:1134`, extraction cap 200MB total / 50MB single `:1816-1856`; `performRestore`
  does `closeDatabase()` first (`HomeScreen.kt:132-164`), so an exception mid-restore leaves
  the vault closed with no automatic reopen (phase-09 H1 path handles the post-close case
  only for restore failures that return).
- **Exploit scenario / Reproducer:** A legitimately large (300-400MB) vault backup export is
  fine, but restoring it: ~400MB ciphertext in heap + ~400MB plaintext zip + extraction →
  OOM on 512MB-limit devices (2-core class explicitly supported). A DB copy whose
  decompressed size exceeds the 200MB extraction cap is rejected even though export allowed
  it — asymmetric export/restore budgets.
- **Fix:** Stream the decrypt file-to-file (mirror `BackupExportPolicy.encryptStreamGcm`),
  align the export and restore caps (or document the mismatch), and guarantee the
  post-restore-failure reopen/recovery path regardless of exception type.

### [R2-B1A-03] Decrypted content stays on screen across ON_PAUSE-only covers (overlays / in-call UI / PIP); lock only fires on ON_STOP, SCREEN_OFF, or foreground-idle
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · App logic & auth (lifecycle lock hooks)
- **Agent:** b1-applogic
- **Evidence:** `MainActivity.kt:145-156` — ON_PAUSE only scrubs the clipboard, ON_STOP calls
  `viewModel.lock()`; `:122-128` ACTION_SCREEN_OFF lock; `:248-263` foreground-only idle poll,
  restarted with a FRESH `lastActivityAtMs = now` on every `LaunchedEffect(
  autoLockTimeoutSeconds, authenticated)` restart (`:250`), so re-entering composition resets
  the idle clock. `FLAG_SECURE` (`:141`) blocks capture, not visual reading.
- **Exploit scenario / Reproducer:** A `SYSTEM_ALERT_WINDOW` overlay, an OEM in-call UI, or a
  translucent anti-theft app is drawn OVER the unlocked vault, which stays at ON_PAUSE
  indefinitely. On a no-keyguard device (or after the user already unlocked), the last
  decrypted note remains on screen beneath the cover — the exact threat the B1-PLAT-4 /
  B2-UI-2 fixes targeted without completing.
- **Fix:** For has-master-password vaults, lock (or render an opaque cover) on ON_PAUSE; the
  screen-away window is currently open until ON_STOP. Cost is a few frames; matches the
  stated threat model.

### [R2-B1N-01] LocalSend sender reads LAN-peer response bodies unboundedly before truncating (`readText().take()`)
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-in-transit & network (incl. plugin runtime)
- **Agent:** b1-net
- **Evidence:** `services/localsend/LocalSendSender.kt:260` (``
  `…use { it.readText() }.take(2048)` ``, register-probe response),
  `:445` (`…use { it.readText() }.take(8192)`), `:447`
  (`errorStream…use { it.readText() }?.take(512)`). Every other network client uses a bounded
  `readText(limit)` loop (`DuckDuckGoClient.kt:211-225`, `HttpsTitleFetcher.kt:128-140`,
  `WeatherClient.kt:148-160`, `DictionaryClient.kt:118-130`); LocalSend is the only
  slurp-then-truncate.
- **Exploit scenario / Reproducer:** A same-Wi-Fi host answers the broadcast register probe
  (`LocalSendSender.kt:258-260` — plain HTTP, not pre-gated by pairing) or a device claiming
  a paired device's IP answers `/prepare-upload`/`/upload` with an endless body; the app
  buffers the full response (up to 500 ms legacy-scan / 60-180 s payload read timeouts) before
  discarding. Repeated probes drain memory/bandwidth; no confidentiality impact.
- **Fix:** Apply the same capped mid-stream reader (`readText(limit)` / `FacadeHttpGetPolicy.
  readCapped`) to `inputStream` + `errorStream` in `httpPost` and the register probe.

### [R2-B1N-02] SSRF host guard is textual-only — the DNS-rebinding window is not mitigated (B1-NET-04 residual)
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-in-transit & network (B1-NET-04 residual)
- **Agent:** b1-net
- **Evidence:** `services/SsrfHostPolicy.kt:24-29` (KDoc: "no DNS resolution happens here …
  out of scope"). No reachable transport implements resolve-and-pin: `HttpsTitleFetcher.kt:65`
  → `:70`, `WebPageFetcher.kt:28` → `:32`, `AppFacadeHost.kt:61` → `:62`,
  `DuckDuckGoClient.kt:155-156`, `WeatherClient.kt:96-97`, `DictionaryClient.kt:61-62` — all
  validate the host STRING then let the OS resolve at connect time.
- **Exploit scenario / Reproducer:** User pastes `https://attacker.example/` into Web Capture
  or Citation; `blockedReason("attacker.example")` returns null (literal blocklist only). A
  DNS-rebinding domain answers the first query with a public IP and the connect-time query
  with `127.0.0.1`/`169.254.169.254`/LAN IP — the app's connection reaches an internal host
  the textual check never saw (where a device-trusted enterprise/MDM CA is present, the fetch
  can even succeed; otherwise the app acts as a blind internal scanner).
- **Fix:** Per hop, resolve the host and validate EVERY returned A/AAAA against the internal
  ranges, then pin the connect to the checked addresses (custom `SSLSocketFactory` +
  `HostnameVerifier`), or refuse any name whose resolution contains an internal address.

### [R2-B1N-03] Plugin egress/exec static-scan gate is bypassable by trivial string obfuscation — the scoped classloader deliberately delegates `java.*`/`javax.*` (B1-AUTH-01 residual)
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-in-transit & network (B1-AUTH-01 residual; inert until a rogue
  signer's artifact passes the operator gate)
- **Agent:** b1-net
- **Evidence:** `ArtifactStaticScan.kt:243-263` matches exact tokens from extracted constant
  pools + DEX string/type tables (`:333-404`, `:413-441`); `PluginFrameworkClassLoader.kt:27-29`
  + `:62` delegate everything outside the app namespace — INCLUDING `java.*`/`javax.*` — to the
  app classloader, so nothing at load time prevents `new java.net.Socket(...)`; the scan is the
  only gate. Caveat making it LOW: the compile-time pin table is empty
  (`CompileTimePluginPins.RELEASES = []`, `GeneratedLlmPluginPin.kt:22` `llmPluginSeedRelease =
  null`) so nothing unpinned downloads, and the verifier refuses everything unpinned
  (`PluginUpdateEngine.kt:110-116`, `DownloadablePluginInstaller.kt:78-82`,
  `PluginDownloader.kt:147-151`).
- **Exploit scenario / Reproducer:** A rogue artifact (stolen/leaked signing key) builds net
  egress via `Class.forName("java.net." + "Sock" + "et")` / fragmented constants — the scan sees
  only fragments, none equal to a forbidden token, so `verify()` returns pass and the plugin
  opens raw sockets bypassing `FacadeHost.httpGet` (no TLS/SSRF hop checks, no 10MB cap, no
  throttling).
- **Fix:** Treat the scan as advisory only and harden it (structural DEX analysis rejecting
  string-built `Class.forName`/reflection of `java.net`/`java.lang.Runtime`/`ProcessBuilder`),
  and/or have `PluginFrameworkClassLoader` refuse `java.net.*`/`java.lang.Runtime` resolution so
  ALL I/O must flow through the facade.

### [R2-B1N-04] Web Capture still fetches plain-HTTP entry URLs over cleartext — the sole remaining user-driven cleartext fetch
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Data-in-transit & network
- **Agent:** b1-net
- **Evidence:** `plugins/webcapture/WebPageFetchPolicy.kt:18` (`ALLOWED_SCHEMES =
  setOf("http","https")`), `:46` (explicit `http://` accepted), enforced per hop in
  `plugins/webcapture/WebPageFetcher.kt:28`/`:49`. Every other transport is HTTPS-only
  (WebDAV `requireSecureUrl` `WebDavSyncService.kt:134-142`; citation/content clients refuse
  HTTP by default `HttpsTitleFetcher.kt:51`).
- **Exploit scenario / Reproducer:** On open/guest Wi-Fi a user captures
  `http://example.com/article`; an on-path attacker rewrites the page body mid-stream and the
  Markdown (potentially embedding `[[wikilinks]]`, tags, images, raw HTML rendered by
  `MarkdownPreviewScreen`) is derived from attacker bytes (`WebCaptureEngine.kt:65-77`). No
  credentials ride the request — content-integrity/note-integrity only.
- **Fix:** Remove `http` from `ALLOWED_SCHEMES` (default bare/host input to `https://`, refuse
  explicit `http://`) or require an explicit per-fetch cleartext opt-in like the WebDAV
  `allowInsecureHttp` model.

### [R2-B1N-05] Plugin-manifest/artifact host allow-lists match host only, ignoring the URL port
- **Status:** OPEN
- **Severity:** INFO
- **Area:** Batch 1 · Data-in-transit & network (incl. plugin runtime)
- **Agent:** b1-net
- **Evidence:** `CompileTimePluginPinStore.kt:205-211` (`isHostAllowListed` uses `URL(url).host`
  — port folded away), gate call `:147`; `HttpsManifestTransport.kt:125` (host-only compare);
  `PluginDownloader.kt:147`. `https://<allowed-host>:8443/…` passes every allow-list.
- **Exploit scenario / Reproducer:** A (compile-time-constant today) URL on the allowed host at
  an arbitrary port is accepted; impact is effectively null while URLs are constants and
  redirects are refused (`PinnedTlsConnector.kt:72`, `HttpsManifestTransport.kt:146-149`) — the
  pinned-leaf handshake would also need to terminate at the pinned leaf on that port. The
  "only this host" invariant is not expressed at port level.
- **Fix:** Normalize allow-lists to `(scheme, host, effective-port)` triples (reuse the
  `WebDavHrefResolver.Origin` shape, `WebDavHrefResolver.kt:30-55`) and compare ports too.

### [R2-B1C-02] Auth-bound WebDAV credentials are silently DELETED on any `load()` exception — the biometric re-auth path the design implies does not exist
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Cryptography & key management (credential key mgmt)
- **Agent:** b1-crypto
- **Evidence:** `WebDavCredentialStore.kt:203-231` (`load()` with a generic
  `catch (e: Exception) { clear(); null }` at `:227-230`), `:126-140`
  (`setUserAuthenticationRequired(authBound)` + `AUTH_VALIDITY_WINDOW_MS` = 10 min),
  `WebDavSyncDialog.kt:56,77` (`authBound = biometricEnabled && isBiometricAvailable`),
  `WebDavSyncDialog.kt:62-69` (credentials loaded in a `LaunchedEffect`, NO
  `BiometricPrompt`/`CryptoObject` re-auth flow). Compare `SecurityService.getDecryptionCipher`
  (`SecurityService.kt:105-127`) which is wired to `BiometricPrompt.CryptoObject` — WebDAV is
  not.
- **Exploit scenario / Reproducer:** Save WebDAV creds with "remember me" while biometrics are
  on → close the device > 10 min → open the sync dialog. The auth-bound key's expiry throws
  `KeyStoreException: user not authenticated`; `load()`'s catch-all treats it as corruption and
  DELETES the remembered creds; the user must retype the password (and anyone who re-auths in
  the same session is the only audience left — the gate is effectively unexercisable).
- **Fix:** Route auth-bound loads through a `BiometricPrompt.CryptoObject` re-auth flow, or drop
  remember-me when the auth window cannot be satisfied; clear only on a true AEAD/tag failure,
  never on `UserNotAuthenticatedException`.

### [R2-B1C-03] DEK material lives in multiple immutable hex `String` copies during open and restore — unzeroizable heap residue
- **Status:** OPEN
- **Severity:** INFO
- **Area:** Batch 1 · Cryptography & key management (in-memory hygiene)
- **Agent:** b1-crypto
- **Evidence:** `NoteflowDatabase.kt:172,400` (`passphrase = dek?.toHexString()` fed to
  `SupportOpenHelperFactory(passphrase.toByteArray(...))`); restore path holds BOTH DEKs as hex
  strings simultaneously — `ImportExportService.kt:1724` (`toHexString`), `:1744`
  (`currentDek = key` → hex), `:1782` (`currentDekHex = key?.toHexString()`), `:1617`
  (`unwrappedDek.toHexString()`), `:1929`, `:2031-2033`, `:2128` (`fromHex` round-trips).
- **Exploit scenario / Reproducer:** Every SQLCipher open converts the 32-byte DEK into a 64-char
  immutable `String`; restore holds backup+current DEK hex strings across the whole
  `validateAndPrepareRestoredDb` pipeline. `String` is not zeroizable; bytes persist in heap /
  swap until GC — a process memory dump after lock can still sample them. Round-1 zeroized the
  ByteArray copies (KEKs, wrap keys, migrated DEKs) but not these hex strings.
- **Fix:** Where feasible keep passphrases in zeroizable buffers and bound hex round-trips; at
  minimum scope the hex copies to the smallest function, zero the byte arrays they came from,
  and avoid holding both DEKs as strings across an entire restore.

### [R2-B1P-02] A CANCELLED whole-vault PLAINTEXT export leaves the entire decrypted vault in app cache; the failed-write path deletes the staging copy instead
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Android platform surface
- **Agent:** b1-platform
- **Evidence:** `SaFExporter.kt:75-97` — staging file deleted only when
  `result.resultCode == RESULT_OK` (`:88`); the user-cancel (`:94-97`) and result-without-data
  (`:91-93`) branches call `done(false)` without touching `file`. Whole-vault plaintext archives
  are staged to `cacheDir/obsidian_exports` (`ImportExportService.kt:2552`), `html_vault_exports`
  (`:2430`), `vault_exports` (`:2164`), `html_exports` (`:2359`). Success path deletes the file
  even when the SAF `copyTo` FAILED (`:81-89`, `ok==false`) → the freshly generated export is
  silently destroyed on a transient I/O error.
- **Exploit scenario / Reproducer:** Export → Obsidian vault, picker → Cancel (repeatedly): every
  note's decrypted content accumulates in `cacheDir/*_exports` until the system purges cache —
  a quota-fill and a weakens-phase-59 "transfer-then-delete" regression; conversely a failing
  write destroys the export the user DID generate.
- **Fix:** Delete the staging file in cancel/no-data branches too (deletion in a `finally` after
  `done(...)`), and delete only on `ok == true`. Optionally sweep stale same-name stagings on
  export start.

### [R2-B1P-03] Export Engine launch uses ACTION_SEND WITHOUT a chooser; plaintext note files linger in the FileProvider-grantable cache root
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Android platform surface
- **Agent:** b1-platform
- **Evidence:** `EditorScreen.kt:1383-1387` takes the raw `ACTION_SEND` from
  `ExportShareHelper.shareFile` (`ExportEnginePlugin.kt:60-72`, `FLAG_GRANT_READ_URI_PERMISSION`
  + FileProvider URI) and `context.startActivity(shareIntent)` directly — no
  `Intent.createChooser`. `file_paths.xml:11` (`cache-path name="exports" path="exports/"`)
  grants URI access to the very directory the files live in (`ExportFileWriter.writeFile`,
  `ExportEnginePlugin.kt:32-33`).
- **Exploit scenario / Reproducer:** (a) With a device default handler for
  text/markdown/html/pdf SEND, tapping "Share via Export Engine…" pushes the decrypted note
  straight to that app with a read grant — no chooser, no per-send confirmation. (b) A dismissed
  share sheet leaves the plaintext export in `cacheDir/exports`, the grantable root.
- **Fix:** Route through `Intent.createChooser(...)` so the target is always user-chosen, and
  delete the export file once the share is delivered/dismissed (transfer-then-delete, as in
  `SaFExporter`).

### [R2-B1P-04] `BoundedStreamCopier.copyBounded` lacks the idle-read guard phase-81 added to `AttachmentIngestPolicy` — busy-spin on a progress-less malicious ContentProvider stream
- **Status:** OPEN
- **Severity:** LOW
- **Area:** Batch 1 · Android platform surface
- **Agent:** b1-platform
- **Evidence:** `BoundedStreamCopier.kt:40-51` keeps `if (read == 0) continue` with no progress
  counter; phase-81's own sibling fix is `AttachmentIngestPolicy.kt:96-109` (`if (++idleReads >
  16) throw IOException("Stream made no progress…")`). Shared content ingress runs here after the
  confirm dialog (`MainActivity.kt:761-772`).
- **Exploit scenario / Reproducer:** A malicious sending app (its own ContentProvider) returns an
  `InputStream` whose `read()` returns 0 forever (contract-legal); after the user confirms the
  clip on an unlocked vault, the IO thread hot-spins until the app is killed; repeated confirmed
  shares multiply busy threads. B1-PLAT-2 bounded bytes, not progress.
- **Fix:** Add the 16-consecutive-idle-read bailout to `copyBounded` + pin with
  `BoundedStreamCopierTest`.

### [R2-B1P-05] Share-confirmation state is activity-scoped and unpersisted: rotation re-prompts (or drops) a confirm, and the Clip dialog floats above the LockScreen after a screen-off lock
- **Status:** OPEN
- **Severity:** INFO
- **Area:** Batch 1 · Android platform surface
- **Agent:** b1-platform
- **Evidence:** `pendingShareConfirm`/`pendingShare` are activity `mutableStateOf` fields
  (`MainActivity.kt:109-114`); `*/*` ACTION_SEND filters (`AndroidManifest.xml:58-67`) keep the
  activity launchable; on rotation (no `configChanges`) the activity is recreated with the ORIGINAL
  SEND intent, so `readShareIntent` in `onCreate` (`MainActivity.kt:143,659-722`) re-parses and
  re-displays the dialog (in-flight confirms dropped). The AlertDialog renders OUTSIDE the lock
  branch (`MainActivity.kt:586-612`), so it stays composed above `LockScreen` after the screen-off
  receiver calls `lock()` (`:122-127`) — attacker-chosen preview text visible over the locked UI, and
  a "Clip" after a lock auto-applies at the NEXT unlock with no per-session expiry (`:268-293`).
- **Exploit scenario / Reproducer:** A malicious app that knows InkFlow is foreground fires repeated
  SEND intents; on rotation the confirm re-pops, and if the screen turns off mid-dialog the
  attacker-supplied preview remains over the locked vault while the deferred clip materializes at the
  next unlock.
- **Fix:** Make the share state `rememberSaveable` (or process confirm against
  `savedInstanceState`), gate the dialog render under `authenticated`, and drop `pendingShare` when
  `lock()` runs instead of deferring indefinitely.

---

*Batch 1 complete (2026-08-17). Batch 2 findings appended below as they finish.*