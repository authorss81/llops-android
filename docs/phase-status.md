# Phase Status — Verified Truth Table

> Generated by Phase 20 (doc-fix). Every workspace phase and every ROADMAP phase heading
> now carries a status marker verified against code/commits (5-parallel-subagent audit
> + direct cross-checks, 2026-08-14). Status vocabulary: `DONE` / `PARTIAL` /
> `BLOCKED` / `DEFERRED` / `NOT STARTED` / `CANCELLED`. `BLOCKED` and `DEFERRED` are
> reserved for whole-phase statuses and currently no phase heading carries either —
> deferred sub-items (20.5, 21.3, 21.8, 21.10, 22.4, 22.5, 23.3, 26.1, 26.7, 32.9, 34.9)
> are called out in prose below and keep their phase at `PARTIAL`.

## Workspace pipeline phases (`workspace/phase-NN`)

| Phase | Status | Verified evidence |
|-------|--------|-------------------|
| phase-01 | `DONE` | App scaffold = the InkFlow source; `.done` marker committed (d31c23d). |
| phase-02 | `PARTIAL` | Commit 338e457 delivered C1/H3/H4 (`validateBackupPassword`, `checkRestoredSchemaNotNewer`). H1 (failed-restore reopen+restart) and H2 (quarantine `*.corrupt-<ts>`) only landed later in phase-09 (89697ee). All five fixes present in the current tree. |
| phase-03 | `DONE` | 92b26b0 + review fixes d68fd21: removed libmypaint JNI stub + engine-selector stack, HandwritingRecognitionService stub, FtsSearchEngine; wired shape auto-snap; deleted dead baseline-prof.txt. |
| phase-04 | `DONE` | Commit 22cc99c was marker-only, but the real implementation shipped in 7854551 ("phase-04 review fixes"): `AgslShaders.kt` (+47), `WetMixingMath.kt` (new), `WetMixingMathTest.kt` (new), `AnnotationCanvas.kt` (+108), `WetCanvasEngine.kt` (+230). |
| phase-05 | `DONE` | 9e2aadc + d84c359: markdown back-save data-loss, 48dp touch targets, Snackbars, reduce-motion, haptics, status-bar polarity. |
| phase-06 | `DONE` | 9400725: real WebDAV/Nextcloud E2EE sync (HTTPS enforced, `WebDavSyncService.kt:61-155,196-301`), WAL checkpoint before upload, transactional restore; `INTERNET` permission added. |
| phase-07 | `DONE` | 41eb71a + 018736c: stabilizer, pressure curve, symmetry, color harmony, paper textures, WebP. Reference-image layer (encouraged item 5) NOT shipped — see `docs/phase-status-gaps.md`. |
| phase-08 | `DONE` | 3ede568 + `workspace/phase-08/PERF_REPORT.md` (10,252 B): RDP simplification, bitmap pooling, layer caching, thermal fallback, off-main-thread graph sim. |
| phase-09 | `PARTIAL` | 89697ee shipped the Phase-02 H1/H2 + C1 fixes and the corruption-recovery flow (`NoteflowDatabase.kt` quarantine, `CorruptionRecoveryScreen`), `.done` present — BUT the PROMPT-required deliverable `workspace/phase-09/REPORT.md` was never created (verified via `git log --all -- workspace/phase-09/REPORT.md` = empty). |
| phase-10 | `DONE` | 9d5df18 + 4ccac2c: real plugin framework (`plugins/`), `PluginRegistry`, `PluginManager` routing, per-plugin opt-in, `PluginSettingsDialog`, Rot13 plugin; `PluginFrameworkTest` (10 tests). |
| phase-11 | `DONE` | 3fc3aed + 20cb73b: plugin lifecycle + crash isolation; `docs/PLUGIN_SDK.md`. |
| phase-12 | `DONE` | 7483f2d + 8520449: real ML Kit OCR (`OnDeviceOcrPlugin`) + DuckDuckGo web search, wired into EditorScreen + MarkdownPreviewScreen. |
| phase-13 | `PARTIAL` | 1f0b6db shipped brush presets/stickers/sticky-note styling/rotation. Required androidTest "persist→load→render round-trip test" NOT delivered — `app/src/androidTest` does not exist (confirmed by `phase-14/AUDIT_REPORT.md`). |
| phase-14 | `DONE` | aff27cf + `workspace/phase-14/AUDIT_REPORT.md`: full production audit, LocalSend sender (`services/localsend/`, `LocalSendProtocolTest` 18 tests), release keystore fallback + `docs/RELEASE.md`. |
| phase-15 | `DONE` | d680d24: 5 pure-JVM plugins (Export, ClipShare, TextTools, LanguageDetection, WebCapture); Lingua+jsoup deps (`libs.versions.toml:27-28,72-73`, `app/build.gradle.kts:156-157`); ACTION_SEND filters (`AndroidManifest.xml:43-67`). |
| phase-16 | `DONE` | cc1ed8b: 5 keyless/on-device plugins (Dictation, ReadAloud, Translation, Assistant, Screenshot). |
| phase-17 | `CANCELLED` | libmypaint native engine. Rubber-stamped `.done` (942b5ed) → `.blocked`+REPORT (0fa97af) → **deleted** in commit 4112eb8 ("cancel phase-17 (libmypaint)"). No code exists (`app/src/main/cpp` absent, zero mypaint references). |
| phase-18 | `DONE` | 9194b42 + 8addded: six new brush tools + enhanced existing brushes (`docs/brush-styles.md`). |
| phase-19 | `DONE` | e624b6e + `docs/phase-19-erasers-vibrancy.md`: dual erasers, vibrant color palette. |
| phase-20 | `DONE` | Doc-fix phase — **this phase**. Commit 0c07c26 shipped `docs/phase-status.md` + `docs/phase-status-gaps.md`, appended status markers to every ROADMAP/PROMPT heading, and committed the `.done` marker. |
| phase-21 | `DONE` | Plugin Store UI + catalog/controller (see `workspace/phase-21/REPORT.md`). commit `af2591a` (+ review `0fb372b`). Implemented 2026-08-14 — earlier `[NOT STARTED]` row superseded by phase-115 verification (heading updated in `workspace/phase-21/PROMPT.md`). |
| phase-22 | `DONE` | Hybrid plugin architecture skeleton (see `workspace/phase-22/REPORT.md`). commit `02df5c6` (+ review `5f70805`): `PluginEntry/-Store`, `PluginRuntime`, `PluginContext`, `PluginVersion`, `plugin-architecture.md`. |
| phase-23 | `DONE` | Downloadable-plugin runtime — download, verify, load (see `workspace/phase-23/REPORT.md`). commit `86dab6e` (+ marker `519d8f4`): `ArtifactSignatureVerifier`, `HttpsPluginDownloadTransport`, `PinnedCertHash`, `PluginDownloader`, `RuntimePluginLoader`, `SignatureVerifiedPluginRuntime`, `DownloadablePluginInstaller`, `AppClassLoaderFactory`, `AppFacadeHost`. |
| phase-24 | `DONE` | Dynamic plugin updates — latest versions with user approval (see `workspace/phase-24/REPORT.md`). commit `43b286a` (+ marker `c16d2ea`): `HostedPluginManifest` (HTTPS keyless), `PluginUpdateChecker`, `PluginUpdateEngine` (approval → re-verify → smoke-test → atomic swap, auto-rollback), +43 tests. |
| phase-25 | `DONE` | InkStroke→Shape canvas plugin (see `workspace/phase-25/REPORT.md`). commit `00d9b35` (+ review `3ec168a`): `InkToShapeGeometry`, `InkToShapePlugin`, `InkToShapePluginTest` (445 lines). |
| phase-26 | `DONE` | Lightweight plugin ecosystem (see `workspace/phase-26/REPORT.md`). commit `552ad40` (+ review `93cc85d`): Citation, Dictionary, Outline, UnitConverter compile-time plugins + `CitationFormatterCore`/`HttpsTitleFetcher`. |
| phase-27 | `DONE` | Brush rendering — sharp edges, rainbow & unique colors, color picking (see `workspace/phase-27/REPORT.md`). commit `d916dde` (real fix) + marker `0839aa4`: `BrushColorModeMath` (rainbow/gradient/shimmer/complementary), edge feather ≥1.5px, CALLIGRAPHIC/CHISEL_MARKER ribbons via `RibbonJoinMath`, true-buffer eyedropper (`EyedropperSamplingMath`), `StrokeColorMode` round-trips through pointsJson — no DB schema change. |
| phase-28 | `DONE` | Sticker & emoji libraries + Glass (glassmorphism) theme (see `workspace/phase-28/REPORT.md`). commit `dfaa89b` (real fixes carried in review commit `a7e81de`): `StickerCatalog`, `GlassSurfaces`, `GlassThemeMath`, EditorScreen glass styling. |
| phase-29 | `DONE` | API-key integration + downloadable local-LLM plugin + APK build (see `workspace/phase-29/REPORT.md`). commit `c28057f` (+ review `6e96a73`): standalone `plugin-sdk` module (`FrameworkPlugin` API), base-APK-shrink groundwork for the LLM plugin. Phase-30 audit commit `d70ff6a` fixed 2 pre-existing phase-29 compile errors (Kotlin-DSL const val, non-suspend `assistantRewire` lambda). |
| phase-32 | `DONE` | APK attack (see `workspace/phase-32/PROMPT.md` + `workspace/phase-32/REPORT.md`). **Rerun 2026-08-15** after the 2026-08-15 review-fix reset (the prior `.done` at `6b17422` was a zero-work false completion and was removed). This run built both APKs locally (`:app:assembleDebug` 173.5 MB, SHA-256 `056c3a67…` + `:app:assembleRelease` 142.0 MB R8-minified, SHA-256 `d7cdbebe…`) and bombarded the release/debug artifacts with apktool, jadx 1.5.1, androguard 4.1.4, APKiD 3.1.0, `strings` (947k dex + 118k native), `apksigner`, `aapt`, `readelf`. NEW findings: Phase-32-NEW-01 (MEDIUM, `language-models/` n-gram pack = 80.2 MB packed = 56% of APK baked into base via the compile-time **lingua** language-detection lib, plus ML Kit OCR/translate natives — violates the downloadable-plugin hard rule; **2026-08-15 review corrected the attribution**: the pack is lingua's, not "ML Kit translation models" — ML Kit translate models are runtime-downloaded, `MlKitTranslatorEngine.kt:22-24`), -02 (LOW, no ABI splits), -03 (INFO, v2-only signing), -04 (INFO, placeholder manifest cert pin ⇒ plugin-update channel fail-closed until operator substitution). CONFIRMED on the APK: B1-PLAT-1 (debug-keystore release signing), B1-PLAT-2, B1-PLAT-6, B1-PLAT-7, B1-NET-02/06, B1-NET-03/B1-CRYPTO-01 (phase-39 fix wiring), B1-AUTH-01, B2-CRYPTO-06. Positives: release not debuggable, allowBackup=false, FLAG_SECURE wired, R8 ON, minimal perms, cleartext platform-deny, no tasks-genai in base, no hardcoded secrets, native RELRO/NX/PIE. Full evidence appended to `docs/security-report.md` "Phase 32 — APK dynamic/static analysis". No code/schema/workflow changes (gradle state unchanged). Dynamic/instrumented tooling deferred to PHASE 34.9 (no AVD on CI runner). |
| phase-35 | `DONE` | Canvas & scribe ergonomics (FloatingToolDock replacing FloatingBottomToolbarPill: draggable edge-snap pill via new `services/DockSnapMath.kt`, ≤2 taps for all tools, auto-tuck on pen); live nib previews (`services/NibPreviewMath.kt` driving enhanced `PenNibVisualPreview` + live sliders in the width picker); minimap spatial HUD (zoom%/layers/viewport, spring/snap, zoom controls, toggled from persisted `minimap_hud_enabled`); palette studio (`DesignerPalettes` — nordic/botanical/cyberpunk/terra, family-invariant preserved) + `services/HarmonicContrastMath.kt` contrast studio row + eyedropper magnifier loupe. `workspace/phase-35/REPORT.md`. New tests: NibPreviewMathTest (23), DockSnapMathTest (15), DesignerPalettesTest (10), HarmonicContrastMathTest (18); PaletteCatalogTest + ColorHarmonyHelperTest still green; `gradle testDebugUnitTest` 805 green, `gradle assembleDebug` green. |
| phase-37 | `DONE` | Markdown & hybrid editor slice. HybridMarkdownEditor (`ui/components/markdown/`) now REPLACES the raw OutlinedTextField in MarkdownPreviewScreen EDIT + split SPLIT panes (`MarkdownPreviewScreen.kt:544,583,631`): live inline rendering of headings/bold/italic/code/lists/links; tap-to-edit raw block; typed callout cards (NOTEWARNING/TIP/IMPORTANT/QUOTE); interactive checkboxes via `AnimatedCheckmark` (respects reduce-motion + haptics policy). Pure-JVM block tokenizer (`services/MarkdownBlockTokenizer.kt`, exact source round-trip) + inline math scanner (`services/MarkdownInlineMath.kt`, code-span aware) + `WaveformPeakMath.downsample` bounds AudioPlaybackCard's draw budget to 220 bars (B2-DOS-03). **Note: initial commit `eec811c` DID NOT COMPILE + was unwired; review fixes landed here (see `workspace/phase-37/REPORT.md`)**. Tests: MarkdownBlockTokenizerTest (12), MarkdownInlineMathTest (6), WaveformPeakMathTest (6); `gradle testDebugUnitTest` 838 green, `gradle assembleDebug` green. |
| phase-38 | `DONE` | Knowledge Graph & Spatial Navigation (see `workspace/phase-38/REPORT.md`). `services/graph/GraphLayoutMath.kt` (deterministic force layout + collision bounding + clusters via `assignClusters`, tier caps via `GraphTierSelector`) drives the rewritten `ui/screens/KnowledgeGraphScreen.kt` — cluster colouring, tag FilterChips (AND/OR), moving particle pulses on selected links (reduce-motion aware), push-apart collision + world-box bounds, and an explicit non-alarming low-RAM notice. Global Command Palette HUD (`ui/components/CommandPaletteOverlay.kt`) opens via two-finger swipe down (`MainActivity.detectTwoFingerSwipeDown`) or the Home-bar keyboard icon; searches the cached decrypted corpus (`NoteRepository.currentSearchCorpusGeneration`) + tag filter + plugin quick-actions via `NoteflowViewModel.commandPaletteSearch`/`runPaletteAction`; keyb'oard Arrow/Enter/Escape; 250ms debounce, ranking ≈2.9ms/1500-doc corpus. Tests: GraphLayoutMathTest (21) + CommandPaletteMathTest (16); `gradle testDebugUnitTest` 875 green, `gradle assembleDebug` green. |
| phase-39 | `DONE` | B1-CRYPTO-01 (CRITICAL) fixed (see `workspace/phase-39/REPORT.md` + `docs/security-report.md:813`). Plugin update manifest transport (`plugins/runtime/PluginManifestFetcher.kt` `HttpsManifestTransport` `:109`) now fetches over a chain-validating, **compile-time leaf-pinned** TLS connection via the new shared `plugins/runtime/PinnedTlsConnector.kt` (also refactored `HttpsPluginDownloadTransport.kt:55` onto it): host allow-list (`DEFAULT_MANIFEST_HOST`, `HostedPluginManifest.kt:197`), fail-closed when `PLUGIN_MANIFEST_CERT_PIN` (`:220`, placeholder until operator supplies the real host-leaf hash) is missing/malformed, `instanceFollowRedirects=false` + explicit 3xx refusal in both transports (3xx incl. HTTPS→HTTP downgrades are `Failed`, never followed), 256 KiB size cap, JDK-21 `SSLHandshakeException` cause-chain pin-failure surfacing. Plugins' `downloadUrl`/`sha256`/`pinnedCertHash` can no longer be redefined by a forged/unpinned manifest. Tests: HttpsManifestTransportTest (8, JDK-only local TLS over a private `TinyHttpsServer`); `gradle testDebugUnitTest` 884 green (0 failures), `gradle assembleDebug` green. |
| phase-40 | `DONE` | B1-NET-01 (HIGH) fixed (see `workspace/phase-40/REPORT.md` + `docs/security-report.md:814`). New pure-JVM `services/WebDavHrefResolver.kt` origin-resolves every server-supplied PROPFIND `href` (normalized scheme+host+port, default ports materialized, case + trailing-dot folded) and rejects anything that escapes the configured WebDAV server; `WebDavSyncService.kt:147` additionally gates EVERY connection through `requireConfiguredServerOrigin` before the `Authorization: Basic` header is attached (`:164`), and `:158` sets `instanceFollowRedirects=false` so 3xx never forwards credentials/backup bytes (closes the WebDAV slice of B1-NET-05 as well). Off-origin/private-IP/cleartext-downgrade hrefs are `SyncResult(false, "Sync refused: ...")` with a clear message; relative hrefs resolve per RFC 3986 under the request URL. `docs/security-report.md` B1-NET-01 row flipped to `FIXED`. Tests: `WebDavHrefResolverTest` (19, incl. end-to-end PROPFIND-XML cases); `gradle :app:testDebugUnitTest` green in isolation and 900/902 green in the full run (2 unrelated flaky failures — `WikiLinkParserCacheUnitTest` + `PluginUpdateEngineTest` timing/cancel cases that pass on re-run and are untouched by this diff), `gradle :app:assembleDebug` green. |
| phase-41 | `DONE` | B1-NET-02 (HIGH) fixed (see `workspace/phase-41/REPORT.md` + `docs/security-report.md:815`). Confirmed-pairing gate for LocalSend sends. New pure-JVM `services/localsend/LocalSendPairing.kt`: `LocalSendPairing.gate` refuses any receiver that announces `protocol:"http"` (or no TLS fingerprint) and any device whose fingerprint was never paired; `LocalSendPairing.startPairing` derives a deterministic 6-digit out-of-band code (`LocalSendPairingCodes.pairingCode`) + formatted fingerprint; `confirmPairing`/`pair` persist a TOFU anchor (constant-time code compare, `ConstantTime.stringEqual`) into a `LocalSendPairedDeviceStore` (`InMemoryLocalSendPairedDeviceStore` for tests; `SettingsLocalSendPairedDeviceStore` + `SettingsManager.localsend_paired_<fp>` for production). `LocalSendSender` now gates `sendFile` BEFORE any network I/O (`LocalSendSender.kt:313-326`) and pins every payload to the stored paired fingerprint, refuses non-`https` payload URLs in `openConnection`, and the announce never advertises `protocol:"http"` (`LocalSendMessages.senderIdentity` = `"https"`, `LocalSendProtocol.kt:107`). `LocalSendSendDialog` requires an explicit pairing (code typed from the receiver — verified constant-time, mismatch refuses — OR an explicit "fingerprints match" acknowledgement, `LocalSendSendDialog.kt:158-199`) before a device is sendable, and a per-send confirmation before every transmission — a `200` to `/prepare-upload` is documented + treated as ZERO evidence of human consent. Tests: `LocalSendPairingTest` (19) + `LocalSendProtocolTest` (25); `gradle testDebugUnitTest` 924 green + `gradle assembleDebug` green (1 pre-existing flaky `PluginUpdateEngineTest` timing failure that passes in isolation — unrelated to this diff, per phase-40 report). |
| phase-42 | `DONE` | B1-NET-03 (HIGH) fixed (see `workspace/phase-42/REPORT.md` + `docs/security-report.md` B1-NET-03 row). Plugin update trust anchor moved into the APK — the manifest can no longer set `downloadUrl`/`sha256`/`pinnedCertHash` out of nothing. New `plugins/runtime/CompileTimePluginPinStore.kt`: per-plugin compile-time release pins (`PinnedPluginRelease` id → version → {sha256, pinnedCertHash}), `PinVerdict`, download-host allow-list (`DEFAULT_DOWNLOAD_HOSTS = {DEFAULT_MANIFEST_HOST}`) with `isHostAllowListed` host gate (lowercase + trailing-dot folded, port ignored), and the production `CompileTimePluginPins.RELEASES` EMPTY ⇒ fail closed (no downloadable plugin shipped yet); all digest compares via `ConstantTime.hexEqual`. Wired as defense-in-depth at THREE independent gates: `PluginUpdateChecker.check` offers only compile-time-pinned values (`plugins/runtime/PluginUpdateChecker.kt`), `PluginUpdateEngine.update` re-verifies the persisted target against pins BEFORE any byte moves or rollback-root write (`plugins/runtime/PluginUpdateEngine.kt:110-116`), and `PluginDownloader` now refuses any artifact host not on the allow-list before connecting (`allowedDownloadHosts` param, `plugins/runtime/PluginDownloader.kt`). `PluginStoreController` threads pins into both check calls (`plugins/store/PluginStoreController.kt`). Surviving `PluginUpdateInfo` carries the compile-time pin digests, not the wire's casing/text. Tests: new `CompileTimePluginPinStoreTest` (10) + new B1-NET-03 cases in `PluginUpdateCheckerTest` (5), `PluginUpdateEngineTest` (1, unpinned/re-pinned refused before any byte moves), `PluginDownloaderTest` (2, host allow-list), `PluginUpdateStoreFlowTest` (1, forged offer refused); the documented flaky `PluginUpdateEngineTest#a hash mismatch…` test was made deterministic (tampered served payload can never match the pin — was zip-timestamp clock-tick dependent). `gradle testDebugUnitTest` 943 green + `gradle assembleDebug` green. |
| phase-43 | `DONE` | B1-DB-1 (HIGH) fixed (see `workspace/phase-43/REPORT.md` + `docs/security-report.md` B1-DB-1 row). Corrupt-open classifier narrowed to genuine corruption only: `NoteflowDatabase.kt` `isDatabaseCorruptException` matches `android.database.sqlite.SQLiteDatabaseCorruptException`, `net.zetetic.database.sqlcipher.SQLiteNotADatabaseException`, and the messages "file is not a database"/"malformed"/"database disk image is malformed" — a lock-contention/disk-I/O/ENOSPC `SQLiteException` subclass is NO LONGER classified as corruption, so a transient hiccup can never quarantine a healthy vault. Quarantine no longer auto-creates an empty replacement DB: `SafeSupportSQLiteOpenHelper` rethrows after quarantine and a `throwIfVaultQuarantined` guard fails every further open while the persistent flag is set, so an empty vault is created only after the user's explicit "start fresh". `NoteflowViewModel` surfaces `CorruptionRecoveryScreen` in-session (not just on the next launch), gates the six Room-backed note flows on `authenticated && !corruptionBlocked` (`.catch { emit(emptyList()) }` so a discovery race can't crash a collector), resets+re-runs initialization after start-fresh, and clears the corruption flag after a successful restore so the post-restart open isn't guard-blocked. Tests: new `CorruptionClassifierTest` (10); `gradle testDebugUnitTest` 953 green + `gradle assembleDebug` green. |
| phase-44 | `DONE` | B1-DB-4 (HIGH) + B1-AUTH-06 (MEDIUM) fixed — SAME root cause (see `workspace/phase-44/REPORT.md` + `docs/security-report.md` B1-DB-4/B1-AUTH-06 rows). Note bodies no longer exist as PLAINTEXT files: the field-encrypted `pages.extractedText` column is the ONLY body store. New pure-JVM `services/NoteBodyVaultPolicy.kt` classifies note-body sources (text/-typed + `.md`/`.txt` only — PDF/image/attachments untouched) and resolves/delete-legacy-file policy. New single write path `NotePageDao.updatePageBody` + `NoteRepository.updatePageBody` (AES-GCM per-record AAD). MainActivity (both layouts) now reads `resolveBodyForDisplay` and saves via new `viewModel.saveMarkdownNoteBody`; the `File.writeText` plaintext editor writes and their NonCancellable plumbing are deleted. Text imports (HomeScreen md/txt/docx, `ImportExportService` HTML/ZIP/Obsidian pass-2) and journal/daily/wiki page creation store `sourceFilePath = null` + body in `extractedText`. Readers (`WikiLinkParser.readFullText`, `BacklinksInspector` convert-to-wikilink, `DocumentTextExtractor`) follow the column. One-time `NoteRepository.migrateLegacyPlaintextNoteBodies` sweeps pre-fix file bodies into the encrypted column then deletes the files (undecryptable pages + failed deletes keep their file; flag `SettingsManager.noteBodyPlaintextMigrated` only set on completion); WAL checkpointed + DB HMAC re-stamped after mutation. `MarkdownPreviewScreen` baseDir falls back to the imports dir so relative markdown images keep resolving. Tests: new `NoteBodyVaultPolicyTest` (12); `gradle testDebugUnitTest` 965 green (0 failures) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-45 | `DONE` | B1-CRYPTO-02 (HIGH) fixed (see `workspace/phase-45/REPORT.md` + `docs/security-report.md` B1-CRYPTO-02 row). The vault DEK is no longer obtainable without the password: `SecurityService` (`services/SecurityService.kt`) now persists the device-wrapped DEK copy behind an internal `DekDeviceStore` seam (`SharedPrefsDekDeviceStore` = prefs `noteflow_keystore`/`noteflow_sec_dek`; `clear()` uses `commit()`, disk-acknowledged, and drops the stale `dek_auth_required` flag); `readDek()` fails closed (absent or `authRequired=true` blob ⇒ null) and `getOrCreateDek` never mints over an auth-gated blob. Pure-JVM `services/DekAtRestPolicy.kt` (decision table) is wired as `NoteflowViewModel.enforceDekAtRestPolicy()` into `setMasterPassword` (after the wrapped-DEK commit), `changeMasterPassword`, `verifyMasterPassword` (every password unlock), `verifyBiometricsAndUnlock`, and `setBiometricEnabled` — biometrics OFF ⇒ `clearDek()` so the ONLY at-rest wrapper is the password-derived KEK; biometrics ON ⇒ repersist ONLY `authRequired=true`. The pre-fix `setBiometricEnabled(false,…)` non-auth re-wrap is deleted. `SecurityService(context)` call sites → `SecurityService.forDevice(context)` (`NoteflowViewModel.kt:115`, `NoteflowDatabase.kt:345`). Tests: new `DekAtRestPolicyTest` (4) + `B1Crypto02DekAtRestTest` (9, incl. a source-level wiring pin); `gradle testDebugUnitTest` 978 green (0 failures) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. Related-but-out-of-scope: factory re-derive + locked-open gap = B1-AUTH-02 (phase-47), silent DEK re-mint = B1-CRYPTO-05 (phase-64), API 26-29 biometric downgrade = B1-CRYPTO-07 (phase-65) — none touched here. |
| phase-46 | `DONE` | B1-AUTH-01 (HIGH) fixed (see `workspace/phase-46/REPORT.md` + `docs/security-report.md` B1-AUTH-01 row). Downloadable-plugin bytecode can no longer reach the vault DEK in-process — two independent gates. (1) Runtime sandbox: pure-JVM `plugins/runtime/PluginFrameworkClassLoader.kt` becomes the plugin `DexClassLoader`'s parent (`services/AppClassLoaderFactory.kt:34`); `isAppPrivateForbidden` (`:70-71`) refuses EVERY `com.authorss81.noteflow.*` class outside the `plugins.*` framework surface (incl. `Class.forName` reach-through), while `java.*`/`kotlin.*`/`android.*`/third-party classes still delegate. (2) Verify-time content gate: pure-JVM `plugins/runtime/ArtifactStaticScan.kt` runs inside `ArtifactSignatureVerifier.verify` (`:76-81`), the single funnel for install / every load re-verify / update / rollback; it structurally parses `.class` constant pools + DEX `string_ids`/`type_ids` and byte-searches other entries, rejecting app-private prefixes (`services|data|ui|theme|utils`), bare secret-bearing names (`VaultKeyHolder`, `EncryptionService`, `NoteflowDatabase`, `SettingsManager`, `NoteRepository`, `SecurityService`) and raw `java.net`/`javax.net.ssl` egress primitives. Phase-46 review closed all 5 review findings in-tree: net-egress + `ProcessBuilder`/`Runtime` now matched in slash AND dot form (`Class.forName("java.net.HttpURLConnection")` literals refused), sensitive names matched ONLY as whole tokens (benign lookalike identifiers pass), and a source-level pin (`PluginBytecodeIsolationTest`) holds that `plugins.*` host code never references a vault-handle type. Tests: `PluginBytecodeIsolationTest` (20); `gradle testDebugUnitTest` 1001 green (0 failures) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope (documented): native `System.loadLibrary`, `sun.misc.Unsafe` and a separate `:remote` process option.
| phase-47 | `DONE` | B1-AUTH-02 (HIGH) fixed (see `workspace/phase-47/REPORT.md` + `docs/security-report.md` B1-AUTH-02 row). The lock is now enforced at the DATA LAYER. `NoteflowViewModel.lock()` (`NoteflowViewModel.kt:2543-2549`) — when a master password exists — now cancels the section/page observer jobs, calls `NoteflowDatabase.dispose()` (closes + forgets the Room/SQLCipher instance so NO keyed handle survives; previously it zeroized only `VaultKeyHolder`), marks `databaseDisposedByLock`, and resets `dataInitialized` so the next unlock re-establishes observers. The stale-handle/fresh-open exploits both fail closed: a stale handle hits a closed connection pool, and `NoteflowSqlcipherFactory.create` (`NoteflowDatabase.kt:343-371`) routes the `dek == null` open through the new pure-JVM `services/LockedOpenGuard.kt` — a password-protected vault with no in-memory DEK THROWS `IllegalStateException("Vault is locked: database key not available")` BEFORE any `getOrCreateDek()`/persisted-copy access, while a passwordless vault still re-reads its device-wrapped copy (the boot credential by design). Explicit unlocks (`verifyMasterPassword` `:2080`, `verifyBiometricsAndUnlock` `:2220`) reinstate the live connection via `reinstateDatabaseAfterLock()` (no-op unless lock() disposed it) BEFORE the dbGate flows flip on, and an open failure there is zeroized and returned — never counted as a wrong password. `onCleared()` also disposes. Tests: `LockedOpenGuardTest` (3) + `B1Auth02LockedOpenTest` (9, incl. a fake-repository model proving no key is ever re-created on a locked open + source-level wiring pins); `gradle testDebugUnitTest` 1013 green (0 failures) + `gradle assembleDebug` green + `gradle assembleRelease` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. Related-but-out-of-scope: in-flight save writes with zeroized key = B2-UI-1 (phase-49), plugin lifecycle hooks running before unlock = B1-AUTH-03 (phase-67). |
| phase-48 | `DONE` | B2-LOG-01 (HIGH) fixed (see `workspace/phase-48/REPORT.md` + `docs/security-report.md` B2-LOG-01 row). Crash logging is single-owner. `utils/AppStartupLogger.kt` is a startup-EVENT timer only: the crash handler it installed at `:38-42` (which ran OUTERMOST = FIRST because MainActivity registers PrivacyCrashReporter first) and the raw `logCrash`/`printStackTrace` → `Log.e` dump at `:55-77` are deleted, and the `Log.e(TAG, msg, e)` failure paths (which would print an exception's trace) no longer pass the exception object (`appendToFile` `:61`, `clearLogs` `:86`). `services/PrivacyCrashReporter.kt` is the SOLE `UncaughtExceptionHandler`; its `logUncaughtException` (`:53-55`) routes through the new pure-JVM `crashLogEntry` (`:64-71`, sanitized message + scrubbed `class.method(file:line)` frames, no raw trace) and writes to the local file only — never logcat. Repo-wide source pin: `setDefaultUncaughtExceptionHandler` appears only in `PrivacyCrashReporter.kt`. Tests: new `B2Log01CrashReportingTest` (6, incl. sanitization/scrub behavior + source pins); `gradle testDebugUnitTest` 1019 green (0 failures) + `gradle assembleDebug` green (first invocation had an unreproducible transient failure; all re-invocations fully `UP-TO-DATE` green with the 173.6 MB debug APK on disk). No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope (documented): `app_startup.log` size cap/rotation = B2-LOG-02 (phase-70); B1-PLAT-5 (path-regex namespace gap) was planned for phase-89 but CLOSED in this phase's review fix — `sanitizeMessage` now redacts any `/data/user/<uid>/...` or `/data/data/...` path (covers namespace + real applicationId dir), pinned by a new test. |
| phase-49 | `DONE` | B2-UI-1 (HIGH) fixed (see `workspace/phase-49/REPORT.md` + `docs/security-report.md` B2-UI-1 row). Post-lock editor saves can no longer persist PLAINTEXT into field-encrypted columns. The write path fails closed at the data layer: new pure-JVM `services/VaultWriteGate.kt` (`requireKey` throws `VaultLockedWriteException` on a zeroized DEK; `persistNow` = the persist-vs-defer decision) + `services/EditorFlushPolicy.kt` (latest-wins-per-page `DeferredSave` stash, `defer`/`drain`). `NoteRepository.kt` now uses `requireEncryptionKey()` (`:44`) in every encrypted-column write — `updatePageBody` `:409`, `createPage` `:547`, `renamePage` `:574`, `updatePageTitleAndTags` `:587`, `saveStrokesForPage` `:762`, `saveMediaEmbedsForPage` `:905`, `createNoteVersion` `:986` — and the `?: rawTitle/rawExtracted/rawText` and `else { rawText/pointsJson }` elvis/else plaintext fallbacks are grep-verified gone. `NoteflowViewModel.kt` routes every editor page-write through the gate (`flushEditorPageSave` `:2302`, `autosaveStrokes` `:2325`, `saveLayersGated` `:2341`, private `persistOrDefer` `:2362`), stashes locked flushes instead of dropping them, re-writes them ENCRYPTED via `flushPendingEditorSaves()` (`:2395`) in BOTH unlock paths (`:2120`, `:2242`), rejects `createNoteVersion` (`:2275`) while locked, and guards the page-create/rename/language-tag/daily/wiki paths. `EditorScreen.kt` has zero direct `viewModel.repository.save*` call sites left — dispose-flush `:430`, debounced autosave `:505`, layer/embed/sticky/insert-site changes, BackHandler `:857` and the top-bar back `:884` all route via the VM gate; `NonCancellable` import removed. Tests: new `B2Ui1LockedFlushTest` (15 after the review fix, behavior + source pins); `gradle testDebugUnitTest` 1035 green (0 failures) + `gradle assembleDebug` green (transient first-invocation failures in phases 47–49; the phase-49 review-fix run was green on first invocation). No schema change, no migration, no new deps, `.github/workflows/` untouched. Review fix (2026-08-15): the eight guarded page-write paths (`applyWorkspaceTemplate`/`addPage`/`createNoteFromSharedContent`/`renamePage`/`updatePageTitleAndTags`/`autoTagLanguageOnSave`/`openOrCreateDailyNote`/`openPageByTitle`) now route through `writeGuardedAgainstLock`/`isLockRacedWrite` so a lock racing them is a non-alarming snackbar, never a crash; `saveMarkdownNoteBody` defers lock-beaten bodies via `EditorFlushPolicy.DeferredBody` (`deferBody`/`drainBodies`) and re-writes them encrypted after unlock; `createNoteVersion` lock-rejection shows a notice; in-memory-deferral process-kill trade-off documented. Out-of-scope (documented): clipboard-scrub-on-lock = B2-UI-2, `lastSavedStrokeHash` concurrency = B2-UI-3, unlock state re-init = B2-UI-4, markdown non-atomic writes = B2-UI-5 — none touched here. |
| phase-50 | `DONE` | B2-DOS-01 (HIGH) fixed (see `workspace/phase-50/REPORT.md` + `docs/security-report.md` B2-DOS-01 row). Stroke geometry is now bounded at EVERY hop between DB bytes and the composable. New pure-JVM `services/StrokeGeometryPolicy.kt` owns the budgets (MAX_POINTS_PER_STROKE=20k, MAX_POINTS_PER_PAGE=200k, MAX_STROKES_PER_PAGE=2k, plaintext cap 2.5M chars/stroke, stored base64-ciphertext cap 3.4M chars/stroke — AES-GCM does not compress so ciphertext length is an exact proxy) + `applySaveGate`/`gateStroke`/`capLoadedPoints`/`storedPointsJsonOverBudget`/`plaintextPointsJsonOverBudget`. `NoteRepository.saveStrokesForPage` (`:793`, was `:533-587` in the finding) routes EVERY write through the gate and returns a `StrokeGeometryGateResult`; `getStrokesForPage` (`:636`) now pages through the new `StrokeDao.getStrokesForPageBounded` (`Daos.kt:181`, `WHERE length(pointsJson) <= :maxStoredChars … LIMIT :limit OFFSET :offset`), refuses over-budget plaintext before Gson, caps legacy over-specified strokes via `capLoadedPoints`, and stops once the page budget is exhausted — a 2M-point backup stroke can no longer OOM/ANR page-open. `EncryptionService.deserializeStrokes` (`:470`) refuses oversized plaintext pre-parse. `ImportExportService.sanitizeRestoredStrokeGeometry` (`:1674`) DELETEs over-budget stroke rows from a restored DB before re-key/migrate/transplant, so a crafted backup can never land a huge geometry (B1-DB-7 vector). `NoteflowViewModel` surfaces ONE non-alarming snackbar per page per session via `maybeNotifyGeometryCapped` (latch cleared on lock) on all three stroke-save paths. `AnnotationCanvas.kt:1415-1434` culls pages whose slab does not intersect the visible world rect `(screen − pan)/zoom` in paginated mode (paper/template/strokes of off-viewport pages are never drawn) — plus the existing `LayerBitmapCache` already provides the off-main raster for committed strokes. Tests: new `B2Dos01StrokeGeometryTest` (18, incl. source-level wiring pins); `gradle testDebugUnitTest` 1053 green (0 failures) + `gradle assembleDebug` green (first invocation had an unreproducible transient Gradle daemon failure; re-runs fully green. 173.6 MB debug APK on disk). No schema change, no migration, no new deps, `.github/workflows/` untouched.

| phase-51 | `DONE` | B1-NET-04 (MEDIUM) fixed (see `workspace/phase-51/REPORT.md` + `docs/security-report.md` B1-NET-04 row). SSRF blocklist for Web Capture + Citation title-fetch. New pure-JVM `services/SsrfHostPolicy.kt`: `blockedReason` (`:30`) blocks localhost/`*.local`/`*.localhost`, IPv4 literals in EVERY textual form (dotted-quad, short `127.1`, decimal `2130706433`, hex `0x7f000001`) across `0.0.0.0/8`, RFC-1918, `100.64/10` CGNAT, `127/8`, `169.254/16` (cloud metadata), IPv6 `::`/`::1`, `fe80::/10` link-local, `fc00::/7` ULA (incl. IMDSv2 `fd00:ec2::254`), and IPv4-mapped/compatible embedded forms (`::ffff:192.168.0.1`) — all structural, NO DNS. `WebPageFetchPolicy.validateUrl` (`:58-65`) refuses blocked hosts at entry + new `rejectHop` (`:80`) re-validates every redirect target; `WebPageFetcher.doFetch` (`:32-54`) applies it on EVERY hop AND fixes the pre-existing "redirect never assigned `cur`" bug (`cur = resolved`), keeping the 5-hop cap; `HttpsTitleFetcher.fetch` (rewritten `:39`) now manually follows up to 5 redirects (`instanceFollowRedirects=false`, `:69`), re-checks scheme+hostlist on every hop (HTTPS→HTTP hop refused under default `httpsOnly=true`), and refuses internal hosts/`ftp`/malformed before any socket; `CitationFormatterCore.validateUrl` (`:26-46`) switched to URI host extraction + the same blocklist at entry. Tests: new `B1Net04SsrfBlocklistTest` (19 — policy/entry/hop/fetch-refusal, all no-network); `gradle testDebugUnitTest` 1072 green (0 failures) + `gradle assembleDebug` green (transient first-invocation daemon flake; retry clean, 173.6 MB debug APK on disk). No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope (documented): recursive DNS-resolution/rebinding egress pinning (transport-level, future phase), `httpsOnly` optional HTTP kept for citation fallback parity, other B1-NET findings untouched. Review fixes (2026-08-15): ambiguous IPv4 textual encodings (`0x7f.0.0.1`, `0177.0.0.1`) are now refused outright via `SsrfHostPolicy.isOpaqueIpv4Literal`; bare `host:port` inputs are defanged in `normalize`; `WebCaptureEngine.captureWebPage` now fetches the validated/normalized `Validation.url` instead of the raw paste (bare-host captures reach the network). Residual name-based DNS-rebinding (`metadata.google.internal`, `*.nip.io`) documented as tracked out-of-scope in `docs/security-report.md`. `gradle testDebugUnitTest` 1075 green (0 failures) + `assembleDebug` green. |

| phase-52 | `DONE` | B1-NET-05 (MEDIUM) fixed (see `workspace/phase-52/REPORT.md` + `docs/security-report.md` B1-NET-05 row). HTTPS→HTTP redirect downgrades are closed at EVERY base `HttpURLConnection` transport. New pure-JVM `services/StrictRedirectPolicy.kt` is the single hop policy: `checkTlsHop` refuses any hop — entry URL AND every resolved 3xx `Location` — whose scheme is not `https` or whose host fails the B1-NET-04 `SsrfHostPolicy` blocklist; `resolveNextTlsHop` resolves RFC-3986, rejects loops/malformed/blank targets; `MAX_REDIRECTS = 5`. Previously auto-following transports are now `instanceFollowRedirects = false` + manual loop: `DuckDuckGoClient.search` (`DuckDuckGoClient.kt:163`), `OpenMeteoClient.get` (`WeatherClient.kt:104`), `DictionaryClient.lookup` (`DictionaryClient.kt:69`) and `AppFacadeHost.httpGet` (`AppFacadeHost.kt:67` — was `instanceFollowRedirects = true`). Connection factories are constructor-injectable (default = `openConnection`) so each transport is behavior-tested with a fake `HttpURLConnection`. Already-safe transports are source-pinned (`PinnedTlsConnector` plugin transports, `WebDavSyncService`, Web Capture/Citation fetchers) and `LocalSendSender.openConnection` (`LocalSendSender.kt:512`) got the same `= false` for its pinned payload connections. Out-of-scope (documented): `plugins/llm` `AssistantModelDownloader.kt:72` (`= true`) is a separate non-base downloadable-module artifact whose build is outside this phase's verification commands — same one-line treatment needed there before that module ships. Tests: new `B1Net05RedirectDowngradeTest` (28 — policy, per-transport downgrade/blocked/loop/cap, same-scheme https follow, facade, offline-fallback, source pins + repo-wide no-`= true` scan); `gradle :app:testDebugUnitTest` 1103 green (0 failures) + `gradle :app:assembleDebug` green. Review fixes (2026-08-15): the last base-app redirect-following hole was closed — `LocalSendSender.httpRegisterProbe` (`:235`) now sets `instanceFollowRedirects = false` (`:240`; it was missed by the initial sweep because the string-pin only proved the file contained *some* `= false`); the source-pin test now enforces a per-file count invariant (every `openConnection()` must be matched by an `instanceFollowRedirects = false`, class now 29 tests), closing the LocalSend gap and catching any future un-paired `openConnection()`; `AppFacadeHost` hop-cap wording unified to `"redirected too many times"` (`AppFacadeHost.kt:98`); trailing newline restored on `StrictRedirectPolicy.kt`. Re-run: `gradle :app:testDebugUnitTest` 1104 green (0 failures) + `gradle :app:assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-53 | `DONE` | B1-DB-2 (MEDIUM) fixed (see `workspace/phase-53/REPORT.md` + `docs/security-report.md` B1-DB-2 row). The plaintext→SQLCipher migration no longer deletes the original plaintext database on ANY failure. `NoteflowDatabase.migratePlaintextIfNeeded` (`NoteflowDatabase.kt:201-258`) now swaps atomically: the encrypted scratch file is verified (exists, non-empty, no plaintext header — `isPlaintextSqlite(tempFile) || tempFile.length() == 0L` → throw) and `tempFile.renameTo(dbFile)` replaces the original via `rename()` (atomic on bionic/Linux), removing the delete-then-rename window in which the user had NO database file; the stale `-wal`/`-shm` companions are removed only AFTER the verified encrypted file is in place. The catch block no longer deletes db+wal+shm — it routes through the new pure-JVM `quarantineMigrateFailed` (`NoteflowDatabase.kt:487-510`, `internal fun`, File-only): drops ONLY the scratch copy, preserves the original + `-wal`/`-shm`/`-journal` as `noteflow.sqlite.migrate-failed-<ts>` (bytes preserved, mirroring the phase-09 `*.corrupt-<ts>` quarantine), returns the timestamp for `DatabaseSecurityHelper.setCorruptionDetected`, and then `throw e` — the phase-43 `initializeData` catch spot-checks that flag in-session and surfaces the corruption-recovery screen (restore from backup / explicit start fresh). Tests: new `B1Db02MigrationFailureTest` (7 — behaviors: bytes preserved under `*.migrate-failed-*` incl. companions, missing companions/scratch handled, timestamp==suffix; source pins: catch routes through quarantine + sets the flag + rethrows, no `dbFile.delete()` in the catch, the only live-db delete is the empty-stub guard, swap-before-cleanup ordering). `gradle testDebugUnitTest` 1111 green (0 failures) + `gradle assembleDebug` green (first invocation had an unreproducible transient build failure; retry fully green with the 173.6 MB debug APK on disk). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-55 | `DONE` | B1-DB-5 (MEDIUM) fixed (see `workspace/phase-55/REPORT.md` + `docs/security-report.md` B1-DB-5 row). HTML/Obsidian ZIP imports now fail zebrabomb-like archives cleanly with bounded heap. New pure-JVM `services/ImportArchivePolicy.kt` owns the budgets (per-entry 50MB `MAX_IMPORT_ENTRY_BYTES`, total 200MB `MAX_IMPORT_TOTAL_BYTES`, 100× declared-vs-actual expansion ratio with 4KB floor, entry count 10k `MAX_IMPORT_ENTRY_COUNT`, 200MB archive input) and the exact single-settle accounting (`checkEntryChunk` never mutates; `settleEntryRead` once per completed entry — the first implementation double-counted the cumulative entryBytes per chunk and was caught by the new test) + `ImportSizeLimitException` (an `IllegalStateException`) so every rejection surfaces a clean non-alarming message. `ImportExportService.readUriBytes` (`:89-118`) reads under a hard stream cap and re-throws the dedicated exception (never swallowed by the null-on-error catch); `importHtmlZipOrFolder` (`:2063`) and `importObsidianVaultZip` (`:2250`, refactored to a SINGLE pass so accounting is exact and entries parse once) route every entry through `claimEntry`/`readEntryBounded` + `inputArchiveOverLimit`; `importHtmlFile` rethrows; the `zis.readBytes()` calls are gone repo-wide (source-pinned). `MAX_BACKUP_INPUT_BYTES` (`:1137`) is now public and the two restore callers (`HomeScreen.kt:148`, `NoteflowViewModel.kt:1805`) keep the 400MB backup cap so large legitimate vaults still restore; `Dialogs.kt:96` APK picker bounded to 400MB. HomeScreen `processImportedUris` surfaces `"Import skipped: …"` for oversized/bombed archives instead of a silent skip. Tests: new `B1Db05ImportZipBombTest` (13 — incl. a REAL 60MB-of-zeros zip that stops at ~50MB with `totalBytes <= 50MB+8192`, a 101-entry archive vs 100-budget, ratio triple, total, happy-path round-trip, source pins); `gradle testDebugUnitTest` 1142 green (0 failures) + `gradle assembleDebug` green (173.6MB debug APK, SHA-256 `ece3d77b…`). |

| phase-56 | `DONE` | B1-DB-7 (MEDIUM) fixed (see `workspace/phase-56/REPORT.md` + `docs/security-report.md` B1-DB-7 row). The legacy restore path can no longer accept an attacker-crafted PLAIN (unencrypted) zip nor open a backup's SQLCipher DB with the empty passphrase. Two pure-JVM gates in `ImportExportService.kt` (file-level `internal fun`s, both pinned by tests): `isPlainPkBackupBytes` classifies a raw `PK`-headed payload, and `backupRestoreOpenCandidates` returns ONLY `listOfNotNull(backupDekHex, currentDekHex).filter { it.isNotBlank() }.distinct()` — the historic `""` empty-key entry is gone AND stripped fail-closed even if a future caller passes it. `importBackup` (`:1595-1600`) REJECTS the plain zip outright with `"Restore rejected: this is an unencrypted (unsigned) backup."` BEFORE any decrypt/extract (the app has not produced keyless backups since the H4 fix, so nothing legitimate is lost); the authenticated device-DEK-encrypted legacy path (`:1601-1608`) and the NFLB2 password-v2 path are untouched. `validateAndPrepareRestoredDb` (`:1692`, candidate line `:1699`) now consumes only `backupRestoreOpenCandidates`, so an attacker's empty-passphrase `noteflow.sqlite` can never pass `PRAGMA integrity_check` and be re-keyed + HMAC-rearmed + swapped over the live vault. `HomeScreen.kt` picker (`:160-176`) refuses a PK zip with a non-alarming snackbar before any confirm dialog, and the remaining device-keyed legacy confirm dialog (`:1138-1147`) now explicitly warns the backup is UNTRUSTED/UNSIGNED. Tests: new `B1Db07PlainZipRestoreRejectedTest` (12 — PK classifier behavior, candidate-set fail-closed incl. `""`/blank inputs, no-duplicate/empty-set, source pins for the two gates + the picker refusal + the dialog warning); `gradle testDebugUnitTest` 1154 green (0 failures, 1142 + 12) + `gradle assembleDebug` green (full `--rerun-tasks` recompile with no `e:` errors, 173.7 MB debug APK SHA-256 `4b025ea4…`; the first invocation had a transient build failure, the forced clean re-run is fully green). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-59 | `DONE` | B1-PLAT-3 (MEDIUM) fixed (see `workspace/phase-59/REPORT.md` + `docs/security-report.md` B1-PLAT-3 row). Whole-vault/plaintext exports no longer auto-write to the world-readable public Downloads. Chose the finding's "better" fix (SAF rewrite): (1) the six `Environment.getExternalStoragePublicDirectory(DIRECTORY_DOWNLOADS)` copies in `ImportExportService.kt` and the one in `PsdExportService.kt` are deleted — every export wrapper keeps its output app-private in `cacheDir`; (2) every user-facing export routes through the new reusable `ui/components/SaFExporter.kt` (`rememberSaFExporter`) which delivers bytes via `ACTION_CREATE_DOCUMENT` (MIME + suggested name from the pure-JVM `services/ExportDestinationPolicy.kt` decision table) and deletes the staging copy on success (transfer-then-delete; API 19+, below minSdk 26, no fallback needed); (3) the whole-vault PLAINTEXT kinds (`OBSIDIAN_VAULT`/`HTML_SITE`/`VAULT_ZIP`) gate the picker behind a bold red "Export is NOT encrypted" consent dialog; (4) HomeScreen (plain backup, password backup, Obsidian, HTML site, notebook/section vault zips) + EditorScreen (PNG, WebP, page PDF, document PDF, note HTML, PSD, section vault zip) all rerouted through `exporter.export(...)` with the correct `ExportKind`; all "saved to Downloads" snackbars gone; LocalSend's cacheDir payload path intentionally unchanged. Tests: new `ExportDestinationPolicyTest` (11) + `B1Plat03ExportConsentTest` (5, source pins incl. a comment-stripped repo-wide no-`getExternalStoragePublicDirectory` scan). `gradle testDebugUnitTest` 1196 total with the only 2 failures pre-existing `B1Plat01ReleaseSigningTest` asserts on `docs/RELEASE.md`/`app/build.gradle.kts` (proven unrelated by failing identically on a clean stashed tree); `gradle assembleDebug` green (173.7 MB debug APK; the first invocation had an unrelated transient build failure — retry + final run green). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-60 | `DONE` | B1-PLAT-4 (MEDIUM) fixed (see `workspace/phase-60/REPORT.md` + `docs/security-report.md` B1-PLAT-4 row). Auto-lock is no longer OFF by default and no longer fires only on full backgrounding / next-touch. New pure-JVM `services/AutoLockPolicy.kt` owns `DEFAULT_AUTO_LOCK_TIMEOUT_SECONDS = 300` (SettingsManager reads it), `shouldAutoLock` (>= boundary, 0/negative = disabled) and `IDLE_CHECK_INTERVAL_MS`; `MainActivity` runs a continuous 1 s idle poll while authenticated (`LaunchedEffect(autoLockTimeoutSeconds, authenticated)`), stamps a fresh baseline at each unlock, keeps the pointerInput touch handler timestamp-only (never a lock decision), locks instantly on `ACTION_SCREEN_OFF` via a runtime receiver (register in onCreate, deregister in onDestroy; API 33+ uses the flagged registration, the plain system-broadcast registration below that), and applies FLAG_SECURE unconditionally (the debug clearFlags carve-out is deleted). `ON_STOP` → lock retained; `ON_PAUSE`→lock explicitly NOT chosen (breaks SAF pickers / biometric / share-sheet pauses). Tests: `B1Plat04AutoLockTest` (10 — pure-JVM decision + wiring source pins). `gradle testDebugUnitTest` 1206 total with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures (`docs/RELEASE.md`/build.gradle signing asserts, untouched); `gradle assembleDebug` green (173.7 MB debug APK, SHA-256 `d2c2eae7…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-61 | `DONE` | B1-PLAT-7 (MEDIUM) fixed (see `workspace/phase-61/REPORT.md` + `docs/security-report.md` B1-PLAT-7 row). Locally-present APKs are never a trusted update source and public Downloads are no longer scanned. New pure-JVM `services/UpdateTrustPolicy.kt` owns the decision table: `classifySource`/`hasOfficialChannel` → every local APK is `UNTRUSTED_LOCAL` (no official channel, no remote-verified signing key); `isPubliclyWritableDirectory` structurally refuses `/sdcard`, `/storage/emulated`, and `…/Android/data/…` external-files-dir mounts (case-insensitive, slash-normalized); `isScanSafeDirectory` gates every scan candidate; `mayInstall` fail-closed-gates UNTRUSTED installs behind explicit confirmation; trust-neutral announcement/confirmation copy (never "New update detected"). `UpdateService.checkForDownloadedUpdates` (`UpdateService.kt:128`) now scans ONLY app-private `filesDir`/`cacheDir` through the policy filter (the `getExternalFilesDir` + `/sdcard/Download` + `/storage/emulated/0/Download` scan dirs at `:104-137` are deleted); `inspectApkFile` (`:60`) classifies via the policy and drops the conditioning wording; `installApk` (`:175`) gained `trust` + `userConfirmedUntrusted` and its FIRST check is `UpdateTrustPolicy.mayInstall` — an untrusted file without confirmation is refused before any staging/FileProvider/launch. `AppUpdateDialog` (`ui/components/Dialogs.kt`) relabeled to "Scan App Storage for APK", never says "Downloads", and gates "Install Update" behind the strong untrusted-confirmation dialog (`Install anyway` / `Cancel`, error colour) invoking `installApk` only with `userConfirmedUntrusted = true`. Tests: `B1Plat07UpdateTrustTest` (11 test methods — pure-JVM decision table + source pins incl. repo-scan for the removed public-dir literals). `gradle testDebugUnitTest` 1217 total with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures (untouched `docs/RELEASE.md`/build.gradle asserts, documented in phases 55/59/60 too); `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. Scope: signature equality demoted to integrity-hint only (B1-PLAT-1 key rotation = phase-57); no official network update channel exists — when one ships it must use remote-verified pins, never a local APK feed. |
| phase-62 | `DONE` | B1-CRYPTO-03 (MEDIUM) fixed (see `workspace/phase-62/REPORT.md` + `docs/security-report.md` B1-CRYPTO-03 row). The salt + wrapped-DEK are no longer written as two independent SharedPreferences `.apply()` writes (`NoteflowViewModel.kt:1794-1795/1829-1830`) whose inter-write kill permanently bricked the vault. New pure-JVM `services/MasterPasswordCredential.kt` makes the pair ONE versioned blob (`MPB1|<saltB64>|<wrappedDek>`, `serialize`/`parse`/`fromLegacy`; `parse` fails closed on malformed/half values) — pure JVM (`java.util.Base64`) so the atomicity contract is unit-testable. `SettingsManager.commitMasterPasswordCredential` (`SettingsManager.kt:101`) persists blob + removes the two legacy keys in ONE synchronous disk-sync `commit()` (SharedPreferences' atomic temp-file+rename => a torn write leaves the PREVIOUS complete blob — a half pair is structurally impossible) and returns the disk-acked result; the read accessor `masterPasswordCredentialOrLegacy` (`:84`) prefers the blob and falls back to the legacy two-key pair, so pre-fix vaults keep unlocking until the next set/change migrates them (same atomic commit). `NoteflowViewModel.setMasterPassword` (`:2094`) / `changeMasterPassword` (`:2145`) round-trip-validate the wrapped DEK (`EncryptionService.decrypt(wrapped, derivedKek)`, zeroized check buffer) BEFORE committing and abort (`return false`) before any in-memory state flips when the commit fails; `verifyMasterPassword` (`:2211`) + `unwrapMasterDek` (`:2272`) read the credential through the single accessor. `clearSecuritySettings` also drops the blob. No DB schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: new `B1Crypto03MasterPasswordAtomicTest` (7 — blob round-trip + fail-closed parse, torn-write leaves old credential durable/unlockable, a store dying on the SECOND write (the audit fault injection) refuses the change and never bricks the vault, torn-position sweep invariant, legacy-pair resolution, source pins for single-commit/round-trip/no-two-pref-writes/accessor reads). `gradle testDebugUnitTest` 1224 total with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures (untouched `docs/RELEASE.md`/build.gradle asserts, documented in phases 55/59/60/61); `gradle assembleDebug` green (173.7 MB debug APK, SHA-256 `e463a944…`; first invocation hit the documented transient dex-merge failure — retry green). |
| phase-63 | `DONE` | B1-CRYPTO-04 (MEDIUM) fixed (see `workspace/phase-63/REPORT.md` + `docs/security-report.md` B1-CRYPTO-04 row). A NEW master password must now clear the pure-JVM `services/PasswordStrengthPolicy.kt` (the single decision table, KDoc documents the finding's "lockout is UI-only / vault only as strong as the password" caveat + Argon2id/TEE follow-ups): `PasswordStrengthVerdict` (ACCEPTED/TOO_SHORT/TOO_LONG/SEQUENTIAL/WEAK/LOW_DIVERSITY, each with a human-readable message) + `evaluate` = ≥ 8 NFKC-normalized graphemes (`MIN_STRENGTH_GRAPHEMES`, stronger than the old 6 floor, ≤ the 128 cap) + no sequential/keyboard-row/single-run-repeat patterns (`12345678`, `qwerty`, `1qaz2wsx`, `aaaaaaaa`, `abab…`) + ≥ 3 distinct graphemes + (for < 12 graphemes) ≥ 3 of 4 classes (upper/lower/digit/symbol); passphrases ≥ 12 pass on length alone so `correct horse battery staple` is accepted. Measured on `EncryptionService.normalizePassword` (the exact bytes `deriveKey` hashes, B2-CRYPTO-07) — full-width `１２３４５６７８` folds to `12345678` and is rejected as sequential. Enforced as the authoritative gate in `NoteflowViewModel.setMasterPassword` (`:2071`) + `changeMasterPassword` (`:2134`, NEW password only; the OLD password stays verify-only so pre-existing weaker vaults keep unlocking and rotating) and surfaced with `verdict.message` by both Dialogs.kt master-password dialogs (set `:682`, change `:631`). Repo pin: `PasswordStrengthPolicy` is referenced only by the policy file, the ViewModel gate and Dialogs.kt; unlock paths (`verifyMasterPassword`/`unwrapMasterDek`/`isMasterPasswordValid`) never strength-gate. No DB schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: new `B1Crypto04PasswordStrengthTest` (10 — rejected short/sequential/keyboard/repeated/low-diversity sets, NFKC folding, accepted set, passphrase exception, source pins incl. a repo-wide no-leak scan). `gradle testDebugUnitTest` 1239 total with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures (proven unrelated on a clean stashed tree, as in phases 55/59/60/61/62); `gradle assembleDebug` green. |
| phase-65 | `DONE` | B1-CRYPTO-07 (MEDIUM) fixed (see `workspace/phase-65/REPORT.md` + `docs/security-report.md` B1-CRYPTO-07 row). The vault-DEK biometric AndroidKeyStore key was only `AUTH_BIOMETRIC_STRONG`-bound on API 30+ (`SecurityService.kt:38-45`); on API 26-29 the bare `setUserAuthenticationRequired(true)` path left the key satisfiable by a screen PIN/pattern/password (AOSP pre-30 maps any non-(-1) validity, incl. default 0, to `HW_AUTH_PASSWORD | HW_AUTH_BIOMETRIC`). New pure-JVM `services/BiometricKeyBindingPolicy.kt` = single decision table (`MIN_API_FOR_STRONG_BIOMETRIC_BINDING = 30`, `strongBiometricKeyBindingSupported`, `refuseEnableMessage`, `PRE_30_BIOMETRIC_ONLY_VALIDITY_SECONDS = -1`). Enforcement: `NoteflowViewModel.setBiometricEnabled` (`:2531`) REFUSES enabling below API 30 (one-shot non-alarming `biometricRefusalMessage` StateFlow `:1106`) before the setting flips; `enforceDekAtRestPolicy` (`:2194`) DOWNGRADES a legacy enabled state below API 30 to password-only (setting off + `clearDek()`, never re-writes the weak-bound copy); `SecurityService.getOrCreateKey` (`:76-98`) binds any pre-30 auth key defensively with `setUserAuthenticationValidityDurationSeconds(-1)` (biometric-only per use) instead of the bare-required path; `getDecryptionCipher` (`:105-127`) + `NoteflowViewModel.getBiometricCipher` (`:2579`) return null below API 30 so LockScreen falls back to the master password + `disableBiometricFallback()`. The finding's explicit API-level marker: `storeDek` stamps `DekDeviceBlob.wrapperApiLevel = Build.VERSION.SDK_INT` (`dek_wrapper_api_level` in `SharedPrefsDekDeviceStore`) — informational/auditable only, NOT read-gated (pre-fix API-30+ blobs carry marker 0 and must still unlock). `BiometricAuthHelper` now distinguishes availability (`isBiometricAvailable`) from strong-bindability (`canCreateStrongBiometricBoundKey`); the settings dialog (`Dialogs.kt:502`) gates the switch on the latter. `DekAtRestPolicy.modeFor` gained `strongBiometricBindingSupported: Boolean = true` (default keeps 2-arg call sites compatible). Out of scope, documented in REPORT.md §8: `WebDavCredentialStore`'s positive-duration pre-30 binding (B1-NET-08 design) and the minSdk-30 bump (product decision). No DB schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: new `B1Crypto07BiometricKeyBindingTest` (20 — policy decision table, downgrade, source pins for the binding branches/API marker/cipher gates/UI wiring). `gradle testDebugUnitTest` 1275 total with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures (verified identical on a clean stash; documented in phases 55/59/60/61/62/63/64); `gradle assembleDebug` green (173.7 MB debug APK, SHA-256 `d571b695d77aaa1563817899d7d3f7ebcd99c96d071af38d122f2532cd05b66c`). |
| phase-67 | `DONE` | B1-AUTH-03 (MEDIUM) fixed (see `workspace/phase-67/REPORT.md` + `docs/security-report.md` B1-AUTH-03 row). Downloadable-plugin lifecycle hooks no longer run while the vault is locked: `NoteflowViewModel`'s init block now does ONLY `if (!settings.hasMasterPassword) startPluginLifecycle()` (`NoteflowViewModel.kt:258-272`) — a passwordless vault is authenticated from boot, a password-protected vault defers ALL plugin runtime loading + hooks to the first successful unlock. New `private fun startPluginLifecycle()` (`:285-312`, idempotent via `pluginLifecycleStarted` `:256`) owns the store re-materialization (`pluginEntryStore.all() → pluginRuntime.load → registerRemotePlugin`), the hook firing (`pluginRegistry.resumeLifecycle(appContext)`) and `refreshPluginStates()`; both unlock paths call it (`verifyMasterPassword` `:2489`, `verifyBiometricsAndUnlock` `:2643`). `lock()` tears down + quiesces via `pluginRegistry.pauseLifecycle(appContext)` + resets the flag (`:3204-3205`). `plugins/PluginRegistry.kt` gained the pure-JVM pause/resume gate: `lifecyclePaused` field (`:133`), `onProcessStart` early-return (`:184`), `pauseLifecycle` (fires `onDisable` for every live hook, clears `enabledNotified`, `:219-235`), `resumeLifecycle` (delegates to `onProcessStart`, `:238-241`), and the `setEnabled` enable-path guard `!lifecyclePaused` (`:279`) — the persisted opt-in is never rewritten, only the runtime hook is gated. Tests: `B1Auth03PluginLifecycleGateTest` (10 — 5 pure-JVM behavioral + 5 source-level wiring pins, incl. "locked: onEnable never fires, after unlock fires once"). `gradle testDebugUnitTest` 1304 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures, untouched asserts as in phases 55-66) + `gradle assembleDebug` green (173.7 MB debug APK SHA-256 `2a4bccfe…`; first invocation hit a transient `packageDebug` incremental-packaging failure, rerun fully green). No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope: post-lock capability-dispatch bounding (a separate layer from lifecycle hooks). **Review-fix (same commit): the gate now quiesces the WHOLE live-Context surface, not just hook firing** — `containedAvailability` returns `Unavailable` WITHOUT invoking `plugin.availability(context)` while paused (so every derived-state query + capability route incl. the previously out-of-scope post-lock dispatch fails closed on the LockScreen), `setEnabled` disable + `uninstallPlugin` fire `onDisable` only for a plugin whose `onEnable` ran this process (never `onDisable` without `onEnable`, never a live Context while locked), `notifyConfigChanged` early-returns while paused, VM `refreshPluginStates()`/`testPlugin()` no-op while `isLifecyclePaused` (`NoteflowViewModel.kt:371`/`395`), `pluginLifecycleStarted` is `@Volatile` + double-checked in `startPluginLifecycle()` (racing unlock paths can never double-boot). `B1Auth03PluginLifecycleGateTest` now 14 tests (8 behavioral incl. availability-gate-never-runs-while-locked / no-onDisable-without-onEnable / no-onConfigChanged-while-locked / nothing-routed-while-locked; 6 wiring pins hardened to brace-balanced body extraction). `gradle testDebugUnitTest` **1308** (only the same 2 pre-existing `B1Plat01ReleaseSigningTest` failures; one transient `WikiLinkParserCacheUnitTest` cancellation-timing failure appeared once and passed in isolation + on re-run — pre-existing flake) + `gradle assembleDebug` green. |
| phase-66 | `DONE` | B1-CRYPTO-08 (MEDIUM) fixed (see `workspace/phase-66/REPORT.md` + `docs/security-report.md` B1-CRYPTO-08 row). The artifact-signer pin now binds the FULL signer set, not a "last signed entry seen" cert: `ArtifactSignatureVerifier.collectSignerSet` (`ArtifactSignatureVerifier.kt:162`) force-verifies the JAR (`JarFile(verify=true)`) and (1) rejects ANY unsigned non-META-INF entry inside an otherwise-verified jar (`:173`, the attacker-key-on-`classes.dex` shape), (2) rejects any multi-signer entry, (3) rejects any archive whose entries mix different signers (`:189`), and (4) fails hard on an EMPTY verified signer set (`:199`) — the old `certs.firstOrNull()` last-entry fallback is gone (source-pinned). "One signer" is judged PER SIGNER CHAIN, not per certificate: `singleSignerChain` (`:235`) splits the JAR verifier's leaf-first chain list on certificate boundaries (issuer-DN match AND a verifiable signature), so a single CA-issued signer (leaf + issuers) is ACCEPTED while a second signer's chain is a hard boundary (review-fix for the pre-chain code's `certs.size != 1` over-rejection). New pure-JVM `plugins/runtime/SignerCertificatePolicy.kt` is the single decision table run by `verify()` (`:115`) — after the pin compare (`:107`, so a wrong key reports the accurate pin-mismatch reason): `checkValidity(now)` rejects an expired/not-yet-valid pinned cert (previously silently accepted) and a `KeyUsage` extension lacking the digitalSignature bit (bit 0) is rejected; an absent extension is unrestricted (RFC 5280). As defense in depth, a key-usage-invalid cert is ALSO refused by the signer-set gate when the platform JAR verifier surfaces such entries with `null` certificates. Pure JVM, runs unchanged on the API 26+ floor — no new platform requirement, no fallback needed. Tests: new `B1Crypto08SignerSetTest` (19 — single-pinned-signer passes; CA-chain-signed single signer passes; two-signer jar over every entry fails; signed+unsigned mixed jar fails; unsigned artifact fails; expired cert fails even with a matching pin hash; KeyUsage-excluded cert refused; policy table accepts absent/digitalSignature usage and rejects expired/not-yet-valid/non-signing certs; synthetic `singleSignerChain`/`sameChain` decision-table tests covering the multi-chain boundary and cross-entry chain equality; source pins). `gradle testDebugUnitTest` 1294 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures, untouched `app/build.gradle.kts`/`docs/RELEASE.md` asserts documented since phase-55) + `gradle assembleDebug` green (173.7 MB debug APK SHA-256 `41f94289…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-64 | `DONE` | B1-CRYPTO-05 (MEDIUM) fixed (see `workspace/phase-64/REPORT.md` + `docs/security-report.md` B1-CRYPTO-05 row). The vault DEK is no longer silently re-keyed when a stored device wrapper becomes undecryptable: new pure-JVM `services/DekReadResult.kt` = sealed `DekReadResult` (`NoBlob` / `Unlocked(dek)` / `AuthRequired` / `KeyLost(wrapperAlias)`) + typed `KeystoreKeyLostException(message, wrapperAlias)`. `SecurityService.readDekResult()` distinguishes "no blob stored" from "blob present but its AndroidKeyStore wrapping key is lost/unreadable" (the old `readDek()` collapsed both to `null`); `getOrCreateDek` THROWS `KeystoreKeyLostException` on `KeyLost` at every mint site (with and without `allowPasswordlessMint`) and mints only from `NoBlob`, so the stored wrapper can never be overwritten by a fresh DEK and the phase-43 quarantiner is never tripped for this cause. `storeDek` stamps a non-secret `wrapperAlias` (`noteflow_dek_key` / `noteflow_dek_key_auth`) + `wrapperVersion = 1` marker persisted by `SharedPrefsDekDeviceStore` (`dek_wrapper_alias`/`dek_wrapper_version`, dropped on `clear()`). `NoteflowViewModel`: new `_keystoreKeyLost` StateFlow gated into `dbGate` (`combine(_authenticated, _corruptionBlocked, _keystoreKeyLost)`); passwordless init routes through `readDekResult` (`Unlocked`→use, `NoBlob`→mint, `AuthRequired`/`KeyLost`→recovery state, the old `var dek = readDek(); if (dek==null) mint()` collapse is gone); `initializeData` catch surfaces key-lost (not corruption) when the corruption flag is clear; `setMasterPassword` throws `KeystoreKeyLostException` on `KeyLost`; new `attemptKeystoreKeyLostRecoveryFromBackup` (validates backup password BEFORE closing, mints a fresh DEK in memory, persists the device wrapper ONLY AFTER a successful restore) + `startFreshAfterKeystoreKeyLoss` (moves the old vault aside as `noteflow.sqlite.keystore-lost-<ts>`, bytes preserved) via `quarantineVaultFiles`. `MainActivity` renders the dedicated `KeystoreKeyLostScreen` between the corruption and restore screens (restore-from-backup with backup-password field + explicit two-step start-fresh). No DB schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: new `B1Crypto05SilentRekeyTest` (16 — behavioral via the `DekDeviceStore` seam + source pins). `gradle testDebugUnitTest` 1255 total (+16, no regressions) with only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures; `gradle assembleDebug` green (APK SHA-256 `8ff42d828428c055ee37fd93556636f869d1932bdf3584fde5b71c20be929599`). |
| phase-68 | `DONE` | B1-AUTH-04 (MEDIUM) fixed (see `workspace/phase-68/REPORT.md` + `docs/security-report.md` B1-AUTH-04 row). Markdown inline-image destinations resolve ONLY inside an allowlisted app-private subtree. New pure-JVM `services/InlineImagePathPolicy.kt` is the single resolver: `resolve(destination, baseDir)` refuses blank/null destinations, ABSOLUTE paths outright (a note never names files by absolute location), and ANY `..` path segment before file I/O (backslash `\` counted as a segment separator so Windows-style `..\..` sequences in imported Obsidian notes cannot hide traversal in a single filename); the candidate must EXIST, be a non-directory, and after canonicalization be a STRICT descendent of the canonical `baseDir` (symlink-plant escape refused); null/non-directory `baseDir` yields nothing. `MarkdownInlineImage` (`ui/components/ImageViewer.kt:129-131`) now routes through it — the old `file.isAbsolute && file.exists()` / `File(baseDir, dest).exists()` accept branches are deleted, so a crafted note (vault-import zip, WebDAV, share sheet, LocalSend) can no longer read-and-display arbitrary process-readable files (voice-note blobs, imports, exports) nor probe the existence of files outside the allowlisted subtree (a blocked absolute / `..` reference is classified by `InlineImagePathPolicy.isBlockedDestination` and shown a distinct non-alarming "Image location blocked" note — `ImageViewer.kt` — never echoed as "File not found", and the decode path re-canonicalizes before reading so a symlink swapped in after resolution is refused; in-subtree existence via the "File not found" fallback is an acknowledged low-value residual inside the allowlist); cover both markdown renderers (`MarkdownPreviewScreen.kt:1304/1312`, `MarkdownRenderer.kt:609/617`) via the single composable. Tests: new `B1Auth04InlineImagePathTest` (14 — absolute/traversal/symlink/root/baseDir-gating behavior, the blocked-destination classifier, and wiring pins: no `isAbsolute`/`File(baseDir, dest)` resolution survives in `ImageViewer.kt`, blocked-vs-missing message + re-canonicalization guard present; see REPORT.md Addendum for the review-fix details). `gradle testDebugUnitTest` 1322 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures, untouched asserts as in phases 55-67) + `gradle assembleDebug` green (173.7 MB debug APK SHA-256 `4735b13c…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope (documented): canvas photo embeds feeding `FullscreenImageDialog`/`decodeBoundedImage` from the stored `embed.contentUrlOrPath` column (B1-AUTH-05-like vector, separate phase) and the `sourceFilePath`→`baseDir` anchor itself (B1-AUTH-05 subject). |

| phase-69 | `DONE` | B1-AUTH-05 (MEDIUM) fixed (see `workspace/phase-69/REPORT.md` + `docs/security-report.md` B1-AUTH-05 row). A note's `pages.sourceFilePath` may only ever point inside the app-private imports root. New pure-JVM `services/SourceFilePathPolicy.kt` is the single confinement decision: `confine` refuses blank/null values, RELATIVE values outright (every legitimate stored value is an absolute path), any `..` segment in either `/` or `\` before any file I/O, and requires the canonical value to be a STRICT canonical descendant of the imports root (`ImportExportService.getImportsDir` = `File(filesDir,"noteflow/imports")`; canonicalization defeats symlink-plant escapes; null/non-directory root fails closed). Enforced at every boundary: the crafted-backup restore path — `ImportExportService.validateAndPrepareRestoredDb` now runs `sanitizeRestoredSourceFilePaths(db, getImportsDir(context))` (`ImportExportService.kt:1704-1708`) right after `sanitizeRestoredStrokeGeometry` and before the H3 schema read/re-key/migrate/transplant/swap, NULLing `sourceFilePath`+`sourceFileType` for every unconfined non-blank row, so a malicious reference never reaches the live vault; `NoteRepository` now owns `importsRoot` (constructor `:22`) and confines in `createPage`/`updatePageSource`/`migrateLegacyPlaintextNoteBodies`; `NoteBodyVaultPolicy.resolveBodyForDisplay`/`deleteLegacyNoteTextBody` and `WikiLinkParser.getFullTextForPage`/`readFullText` (full-text cache re-keyed to `(pageId, importsRootPath)`) read/delete a stored path only when confined; every caller passes the root — both `MainActivity.kt:438/539` body reads, `DocumentTextExtractor`, the VM's `updatePageSource` (also mirrors the confined value into `_selectedPage`) + both unlock-flush delete sites, KnowledgeGraph/BacklinksInspector/TagExplorer builders, and the command-palette index. The phase-44 evolution means no plaintext `.writeText()` source-path write survives; the phase closes every remaining read/delete surface. Tests: new `B1Auth05SourceFilePathTest` (17 — policy table incl. backslash-smuggled `..`, symlink-escape refusal, confined/unconfined body-read + delete gates, WikiLinkParser read gate, restore-sanitizer wiring), plus `NoteBodyVaultPolicyTest`/`WikiLinkParserCacheUnitTest` moved to the temp-imports-root model and `B2Ui1LockedFlushTest:270` updated for the split-argument confined delete. `gradle testDebugUnitTest` 1339 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` failures as in phases 55-68) + `gradle :app:assembleDebug --rerun-tasks` green (57/57 tasks; a first incremental `assembleDebug` hit a transient failure, the forced rebuild passed); `assembleRelease` refuses loudly per the B1-PLAT-1 keystore gate.  **Review-fix (2026-08-19): plugin route got a REAL production caller (HomeScreen `LocalSendSendDialog` sends through `sendFileWithPlugin` when the plugin is enabled; progress forwarded; disabled = unchanged direct send); seam/`LocalSendDevice` documented as host-only under the classloader sandbox (`FileTransferSender.kt` + `NoteflowPlugin.kt` KDocs); journal sanitizes the capability key on write + serializes read-to-record-to-write (`PluginManager.journalLock`) + a public `NoteflowViewModel.refreshPluginFlows()` wrapper (private `refreshPluginStates()` keeps its B1-AUTH-03 guard) is called on Settings-to-Plugins open so "Recent activity" never goes stale; `FileTransferKind` carries no embedded user-facing labels. Tests: `PluginInvocationJournalPolicyTest` +2 (key sanitize, concurrent-no-loss), `FileTransferPluginPolicyTest` +1 (progress forwarded).**No schema change, no new deps, `.github/workflows/` untouched. |
| phase-70 | `DONE` | B2-LOG-02 (MEDIUM) fixed (see `workspace/phase-70/REPORT.md` + `docs/security-report.md` B2-LOG-02 row). `app_startup.log` is no longer appended unboundedly: the append-only `FileWriter(logFile, true)` with no length check/rotation/delete is gone. New pure-JVM `services/StartupLogPolicy.kt` is the single budget + rotation decision table — `LOG_FILE_NAME`/`BACKUP_SUFFIX=".1"`, `MAX_LOG_BYTES = 500_000L` (same ~500KB budget `PrivacyCrashReporter.writeLogToFile` already caps its crash log at), `MAX_LOG_FILES = 2`, `wouldExceedCap` (:91, the BEFORE-write rotate decision), `rotateForAppend` (:101, keep-last-N: drop the oldest `.1`, promote the active file) and `pruneOnInit` (:117, clears any leftover over-cap file) — all plain `java.io.File`, testable on the pure JVM, API 26+ floor with no fallback. `AppStartupLogger.appendToFile` (`AppStartupLogger.kt:71-80`) now gates the write through the policy so the ACTIVE log can never pass the cap, `init` (:44-52) prunes on the background log executor (no main-thread I/O), and the dead `getLogs`/`clearLogs` accessors (old :65-88, and their unguarded `readText` fallback) are REMOVED — the KDoc notes any future export/share-logs UI must sanitize before leaving the device. Phase-48 posture retained: no uncaught-exception handler, no `printStackTrace`, no raw dump, no throwable to logcat (source-pinned). Tests: new `B2Log02StartupLogRotationTest` (14 — sustained-write cap invariance, keep-last-N total ≤ 2 × cap, generation retention, prune-on-init both-side behavior, 500KB-budget parity with PrivacyCrashReporter, wiring + dead-code-removal source pins). `gradle testDebugUnitTest` 1353 total (1350 green; only the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts on `docs/RELEASE.md`/`app/build.gradle.kts` — untouched — plus the documented `WikiLinkParserCacheUnitTest` cancellation-timing flake that passes in isolation) + `gradle :app:assembleDebug` green (57/57 tasks, debug APK SHA-256 `ce7542bc…`). No schema change, no migration (not required — retention policy only), no new deps, `.github/workflows/` untouched. |

| phase-71 | `DONE` | B2-LOG-03 (MEDIUM) fixed (see `workspace/phase-71/REPORT.md` + `docs/security-report.md` B2-LOG-03 row). Every import/export failure `Log.e("ImportExportService", ..., e)` call in `services/ImportExportService.kt` passed the exception OBJECT, so logcat received the full throwable — whose message embeds app-private file paths whose filenames ARE note titles (the sanitized `<note_title>_<ts>.md` under `filesDir/noteflow/imports/`, per B1-DB-4) — bypassing PrivacyCrashReporter's sanitizer. All 11 call sites (13 at audit time; 2 were removed by the phase-59 B1-PLAT-3 Downloads rewrite) now emit a 2-argument `Log.e` whose message is built by the new pure-JVM `services/FailureLogPolicy.kt` (`safeLogMessage(e, operation)` = FIXED operation label + `classNameToken(e)` = the exception's simple class name only; never `e.message`/`localizedMessage`/stack, so a path-carrying throwable message can never reach the log line). Audited sites before→after: `ImportExportService.kt:289/370/1096/2063/2098/2154/2223/2271/2343/2405/2462` → `:290/372/1099/2067/2103/2160/2230/2279/2352/2415/2473`. API 26+ floor (pure JVM, no platform calls, no fallback needed). Tests: new `FailureLogPolicyTest` (8 — path/message/cause leak-refusal, anonymous-class fallback, class-name-token purity, plus a mechanical source pin that every `Log.(e|w)` call in ImportExportService.kt has exactly TWO arguments (tag + sanitized message) and routes through `FailureLogPolicy`, and an operation-label presence pin). `gradle :app:testDebugUnitTest` 1361 total (1359 green in the final run; only the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts on `docs/RELEASE.md`/`app/build.gradle.kts` — untouched; the documented `WikiLinkParserCacheUnitTest` cancellation-timing flake passed in isolation and did not reproduce in the final run) + `gradle :app:assembleDebug` green (57/57 tasks, debug APK SHA-256 `0a1e30c6…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. |

| phase-72 | `DONE` | B2-UI-2 (MEDIUM) fixed (see `workspace/phase-72/REPORT.md` + `docs/security-report.md` B2-UI-2 row). In-app locks no longer leave decrypted note content on the system clipboard: `ClipboardGuard` was only scrubbed on ON_PAUSE, and the in-app lock paths (manual "Lock Vault Now" `Dialogs.kt:673`, idle auto-lock `MainActivity.kt:255`, ON_STOP `:149`, ACTION_SCREEN_OFF `:122`) call `viewModel.lock()` directly — the app stays foregrounded so ON_PAUSE may never fire. Now `NoteflowViewModel.lock()` (`NoteflowViewModel.kt:3218-3234`) calls `ClipboardGuard.scrubIfOwnCopy(appContext)` as its FIRST statement — before `repository.zeroizeKey()` and before the passwordless-vault gate, so every lock path + every vault kind scrubs. The decide → clear → forget logic is the new pure-JVM `services/ClipboardScrubPolicy.kt` (single decision table, `SCRUB_WINDOW_MS = 60_000`, `shouldScrub` refuses zero/expired timestamps) and `ClipboardGuard.scrubIfOwnCopy` now returns whether it cleared + forgets the copy timestamp after a scrub so a foreign (other-app) copy is NEVER wiped (a successful scrub leaves the next lock with nothing to clear). `clearPrimaryClipOverride` is an internal pure-JVM test seam; production still clears through the real system `ClipboardManager` (`clearPrimaryClip` API 28+ / empty `setPrimaryClip` API 26-27, best-effort so a platform failure never breaks the lock), and the ON_PAUSE scrub (`MainActivity.kt:145`) stays as defense-in-depth. Both note-content copy sources still stamp the guard before writing (`OcrResultDialog.kt:149-150`, `MediaEmbedComponents.kt:352-354`). No OS floor / fallback issue: `scrubIfOwnCopy` already handles API 26-27 (empty `setPrimaryClip`) vs API 28+ (`clearPrimaryClip`) and swallows any platform failure. Tests: new `B2Ui2ClipboardScrubTest` (13 — policy decision table incl. window boundary, recordCopy→scrub clears exactly once + forgets the timestamp, foreign/expired/no-app copies never wiped, failure-swallow, and source pins: lock scrubs before zeroize before the passwordless gate, all four lock entry points route through `viewModel.lock()`, production path still uses the real system service, both copy sources stamp the guard before writing). `gradle :app:testDebugUnitTest` 1374 total (1372 green — only the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts on `docs/RELEASE.md`/`app/build.gradle.kts`, proven unrelated by failing identically on a stashed clean tree) + `gradle :app:assembleDebug` green (57/57 tasks, debug APK SHA-256 `3c1cc734…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. |

| phase-73 | `DONE` | B2-UI-3 (MEDIUM) fixed (see `workspace/phase-73/REPORT.md` + `docs/security-report.md` B2-UI-3 row). The shared `NoteRepository.lastSavedStrokeHash` diff cache + overlapping debounce/dispose-flush serialization: `lastSavedStrokeHash` (`NoteRepository.kt:911-912`) was a plain access-order `LruBoundedMap` mutated from both the load path (`getStrokesForPage`, no transaction) and the save path (`saveStrokesForPage`, inside `withTransaction`) on different coroutines with no locking — two concurrent same-page saves could interleave the read-modify-write (older hash commit lands last ⇒ newer stroke silently not inserted), interleave the two `withTransaction` delete+upsert rounds (dropped rows), or corrupt the map (`ConcurrentModificationException`). Fix: the map is now `Collections.synchronizedMap(LruBoundedMap<…>)` (per-op atomic, B2-DOS-10 LRU bound preserved) and a new `private val pageSaveLocks = ConcurrentHashMap<String, Mutex>()` (`:944`) gives each page ONE fair/FIFO kotlinx `Mutex` that `saveStrokesForPage` (`:986-988`), `saveMediaEmbedsForPage` (`:1165-1167`) and `saveLayersForPage` (`:1244-1246`) ALL acquire via `lock.withLock { db.withTransaction { … } }` — the full strokes→embeds→layers snapshot is atomic per page, different pages stay concurrent, and a later-issued (newer) save commits last. `EditorScreen.kt:443-451` dispose now captures the pending debounce `saveJob`, nulls it, and routes through the new `NoteflowViewModel.disposeEditorPageFlush` (`NoteflowViewModel.kt:2875-2892`) which `cancel()`s the stale debounce → `join()`s any already-started write → flushes the FINAL newest snapshot; `autosaveStrokes` (`:2907-2920`) is `suspend fun` running the write inline in the debounce job so it is cancellable/awaitable. `MarkdownBodySaveCoordinator`'s coroutine-Mutex precedent reused; no new deps (stdlib/JDK + kotlinx Mutex already in tree). No OS-floor issue (API-agnostic primitives). Tests: new `B2Ui3StrokeSaveConcurrencyTest` (11 — a pure-JVM behavioral model driven by the SAME `synchronizedMap` + per-page-`Mutex` primitives: concurrent RMW never throws/loses/duplicates entries and never exceeds the cap, newer full snapshot always commits after an in-flight older one, no delete+upsert interleave, different pages not serialized, dispose cancels a pending debounce / awaits an in-flight one / no-pending flush, + source-level wiring pins for repository locks, EditorScreen dispose, and the VM helper). `gradle :app:testDebugUnitTest` 1447 total (0 failures / 0 errors) + `gradle :app:assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |

| phase-74 | `DONE` | B2-UI-5 (MEDIUM) fixed (see `workspace/phase-74/REPORT.md` + `docs/security-report.md` B2-UI-5 row). Markdown note-body saves + reads are serialized and latest-wins (the last non-atomic body path — dispose-flush `File.writeText` vs `produceState` `File.readText` truncate-race — is gone; bodies live only in the field-encrypted `pages.extractedText` column). New pure-JVM `services/MarkdownBodySaveCoordinator.kt`: every body write serialized through a per-page `kotlinx.coroutines.Mutex`, monotonic `seq` stamped at `issue(...)` time, `commitLatest` refuses (never runs the write; settles so awaiters release) any request no longer the latest issued one — latest-wins is issue-ORDER, never time-based, so touching a page before a slow write can never let a stale snapshot win; the deferred-body unlock flush re-`issue`s on the calling thread BEFORE any write (strictly older than a new edit); `awaitSettled(pageId)` bounded by `SETTLE_AWAIT_TIMEOUT_MS = 3000L` and chases any request issued while a reader waits. `NoteflowViewModel.saveMarkdownNoteBody` (`NoteflowViewModel.kt:2214-2278`) issues on the UI thread then `commitLatest { repository.updatePageBody(...); NoteBodyVaultPolicy.deleteLegacyNoteTextBody(...importsDir) }` on `Dispatchers.IO`; the new `readMarkdownNoteBody` (`:2291-2336`) awaits settle + RE-FETCHES `repository.getPageById` (never a stale flow snapshot; deflates to the composition's in-memory snapshot only on a transient decrypt failure, so a key-lost race can never surface as an empty editor that gets saved over the real body); `flushPendingEditorSaves` (`:2929-2988`) drains via issue→commitLatest. Both markdown `produceState` blocks (`MainActivity.kt:438/536`) read `viewModel.readMarkdownNoteBody(page.id, page.extractedText, page.sourceFilePath, page.sourceFileType)`; MainActivity no longer resolves note bodies or touches body files at all. B2-UI-1 lock semantics (defer, never drop, no plaintext) + B1-AUTH-05 imports-root confinement retained. Tests: new `B2Ui5MarkdownSaveSerializationTest` (13 — latest-wins supersede, a slow older write can never land after a newer one, in-flight serialization, a 24-way torn-write simulator leaves only a complete body + the last-issued body, a reader awaiting settle sees the committed value + chases a newer request issued while waiting, bounded-settle timing seam, per-page independence, + wiring source pins). `gradle :app:testDebugUnitTest` 1386 total (1383 green; the only failures are the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts on `docs/RELEASE.md`/`app/build.gradle.kts` + the documented `WikiLinkParserCacheUnitTest` cancellation flake — all three proven pre-existing by failing identically on a stashed clean tree) + `gradle assembleDebug` green (debug APK 173,751,590 bytes). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-75 | `DONE` | B2-DEPS-03 (MEDIUM) fixed (see `workspace/phase-75/REPORT.md` + `docs/security-report.md` B2-DEPS-03 row). Repo-local Gradle dependency verification is ON: `gradle/verification-metadata.xml` is committed — 519 components / 913 sha256-pinned artifacts covering `com.android.tools.build` (AGP, aapt2 incl. the `aapt2-*-linux.jar` platform classifier, lint), `org.jetbrains.kotlin` (Kotlin + compose compiler) and `com.google.devtools.ksp`, i.e. the build plugins named in the finding. Gradle 8.13 auto-enables STRICT checksum verification for EVERY resolved artifact (project deps, POM/module metadata, AND plugins) the moment that file is present — a compromised/MITM'd/poisoned-cache artifact now fails the build loudly instead of compiling into the signed APK. Bootstrap followed the documented incremental flow: `--write-verification-metadata sha256 help` then the REAL task set (`testDebugUnitTest assembleDebug`) so task-execution-time resolutions (platform-suffixed aapt2, lint/uast/play-sdk-proto) are pinned; round-trip proven by `gradle assembleDebug` passing with NO write flag (the pre-bootstrap run failed with `Dependency verification failed … aapt2 … checksum is missing from verification metadata`). `settings.gradle.kts` `dependencyResolutionManagement` (`:17-28`) mirrors the `pluginManagement` google() content filters so a polluted google index can never publish a fake org.jetbrains/io.coil-kt artifact; `mavenCentral()` retained. The attempt's `dependencyVerification { verify = "all" }` settings block — a Gradle-8.13 non-entity that broke settings compilation (`Unresolved reference: dependencyVerification`, proven in a minimal repro + bytecode scan of the distribution, which ships no such settings accessor) — was REMOVED; verification is file-driven, no settings DSL exists. Tests: new `B2Deps03DependencyVerificationTest` (5 — mirror filters, mavenCentral retained, broken DSL absent, metadata present + real, build-plugin groups pinned). `gradle testDebugUnitTest` 1391 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts; inputs untouched) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched (CI `distributionSha256Sum` + pinned opencode-install stay external notes — workflow edits prohibited). |
| phase-76 | `DONE` | B2-DEPS-04 (MEDIUM) fixed (see `workspace/phase-76/REPORT.md` + `docs/security-report.md` B2-DEPS-04 row). The downloadable-plugin signing identity is no longer a public default or an ephemeral build-bred keystore: `plugins/llm/build.gradle.kts` no longer hardcodes a default signing password and no longer runs the JDK keystore-generation CLI to mint a fresh self-signed JKS every build. The signing tasks (`:plugins:llm:signPlugin`/`verifyPluginSignature`/`pluginMetadata`) now FAIL LOUDLY — a `gradle.taskGraph.whenReady` gate (mirroring the `:app` phase-57 release gate) plus `requirePluginSigningKeystoreB64()`/`requirePluginSigningStorePass()` throw a `GradleException` when `PLUGIN_SIGNING_KEYSTORE_B64`/`PLUGIN_SIGNING_STORE_PASS` are unset (`PLUGIN_SIGNING_KEY_PASS` optional, defaults to the store password, never a committed constant). The dangling `:app:generateLlmPluginSeed` claim became a REAL task in `app/build.gradle.kts`: it depends on `:plugins:llm:pluginMetadata` (which fails without the signing env), validates the signed artifact's `sha256` (64-hex) + `pinnedCertHash` (`sha256/<base64>`), and rewrites the committed `GeneratedLlmPluginPin.kt` seed (`null` = fail-closed, no release pinned yet). `CompileTimePluginPins.RELEASES` now folds that seed in via `buildReleaseTable(*listOfNotNull(llmPluginSeedRelease).toTypedArray())`. A latent pre-existing bug (`dependsOn(pluginSigningKeystore)` passed a `Provider<RegularFile>` as a task dependency, so `signPlugin` could never run even with a keystore) was fixed. Positive path proven with a THROWAWAY `/tmp` keystore: seed task emitted the exact pin of the key that signed the artifact, then the pin was reverted (never committed). Tests: new `B2Deps04PluginSigningTest` (9 — source-pin + repo-scan, needle literals runtime-composed so the guard file can't trip itself). `gradle :app:testDebugUnitTest` 1456 green (0 failures/errors; the documented `WikiLinkParserCacheUnitTest` flake passed in isolation) + `gradle :app:assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |

| phase-77 | `DONE` | B2-DEPS-05 (MEDIUM) fixed 2026-08-17 (see `workspace/phase-77/REPORT.md` + `docs/security-report.md` B2-DEPS-05 row). The pre-fix `.done` (commit `c67707b`, junit-bom hashes) was a FALSE COMPLETION; this is the REAL fix. The downloadable-assistant GGUF now carries a PINNED identity: `plugins/llm/.../policy/AssistantStoragePolicy.kt` publishes the real git-LFS SHA-256 (`DEFAULT_MODEL_SHA256 = f0a42bb9…ab81a8`) + exact byte count (`DEFAULT_MODEL_SIZE_BYTES = 397_805_248` — the stale 398 MiB approximation gone; both re-verified against the HF repo-tree API), and `DEFAULT_MODEL_URL` is no longer overridable (the `plugins.<id>.model_url` setting + `LocalLlmPlugin`'s `settings` capture were deleted). New pure-JVM `policy/ModelDownloadPolicy.kt` = single decision table (`validateEntry` host==huggingface.co + https + no credentials, `validateHop`/`isAllowedDownloadHost` = huggingface.co + `*.huggingface.co` + `*.hf.co` CDN family, RFC-3986 `resolveNextHop` with loop/malformed refusal, `MAX_REDIRECTS = 5`, `verifyDownload` = size FIRST then constant-time full-length SHA-256). New pure-JVM `engine/AssistantModelDownloadRunner.kt` = manual `instanceFollowRedirects = false` loop (B1-NET-05 pattern, self-contained — plugins/llm can't import app services), per-hop re-validation before the next connection opens, body streamed into a `.part` temp while SHA-256 hashing (64 KiB, `yield()`, progress), accept ONLY on exact size+SHA-256 match; every failure deletes the temp with a typed message. Rewritten `engine/AssistantModelDownloader.kt`: `download(context, onProgress)` (url/size params gone), re-verifies an EXISTING model at every call (stale/poisoned file deleted + re-downloaded), StatFs free-space preflight, atomic rename, cancellation cleans temp. `LocalLlmPlugin.downloadModel` success text = "downloaded and verified". UI fallback literal `398L`→`379L` (`OnDeviceSmartAssistant.kt`). Tests: new `ModelDownloadPolicyTest` (21) + `AssistantModelDownloadTest` (12, scripted `HttpURLConnection` fake) + `AssistantPromptTest` pinned to real size/SHA-256. Also fixed PRE-EXISTING build blockers found at HEAD (not phase work, but they blocked every build: a missing `}` in `HomeScreen.kt` `processImportedUris` left the `for`/`try` unbalanced so the app module never compiled, and four broken test files — B2Crypto08 impossible random-bytes `assertArrayEquals`, B1Plat03 stale `rememberSaFExporter(scope)` pin vs `vaultScope`, B2Dos07 stale `GCM_IV_LENGTH` pin vs `EncryptionService.newIv()`, B2Dos08 count-only `DripInputStream` that could never deliver XML content plus a contradictory cap-pin assertion — plus the jsoup-CVE extractor actually DID drop the payload content for unknown elements, fixed in `WebToMarkdownExtractor.convert` else-branch). `gradle testDebugUnitTest` 1719 total green (0 failures) + `gradle assembleDebug` green. |
| phase-78 | `DONE` | B2-DOS-02 (MEDIUM) fixed (see `workspace/phase-78/REPORT.md` + `docs/security-report.md` B2-DOS-02 row). Vault search is bounded at every layer: new pure-JVM `services/VaultSearchPolicy.kt` owns the budgets (`SEARCH_CORPUS_CAP = 1500`, `DEEP_SCAN_BATCH_SIZE = 1500`, `exceedsCorpusCap`/`cachedWindowSize`/`isBlankQuery`/`pageMatches`/`refineNoticeMessage`). `NoteRepository.loadSearchCorpus` (`NoteRepository.kt:107-126`) now ALWAYS caches the decrypted window via the bounded DAO read `getAllActivePagesBounded` (`Daos.kt`, `LIMIT :limit`) — a keystroke search never re-decrypts the vault (pre-fix the >1500 vault dropped the cache, so every keystroke re-ran a full-vault AES-GCM); over-cap vaults are flagged via `NoteRepository.searchCorpusCapped` (recomputed per load). `searchPages` filters the cached window only; the EXPLICIT user-approved refine path is `deepSearchPages` (`:412-434`), paged via `getAllActivePagesPaged` (`LIMIT :limit OFFSET :offset`, only matches retained, cancellable). `NoteflowViewModel.searchVault` shares ONE cancellable `Job` (`searchVaultJob?.cancel()` before every launch + `if (isActive)` guard, `:1926-1934`) and `deepSearchVault` (`:1942-1948`) shares it, so concurrent keystrokes pre-empt in-flight decrypts. `HomeScreen` shows a one-time non-alarming "Search covers the most recent pages" banner + "Search all pages" action (`HomeScreen.kt`, gated on `viewModel.repository.searchCorpusCapped` + `refinedSearchDone`). Command palette indexes the same bounded window. Tests: `B2Dos02VaultSearchBoundedTest` (10 — policy decision table + wiring source pins). No schema change, no migration, no new deps, `.github/workflows/` untouched. |

| phase-79 | `DONE` | B2-DOS-03 (MEDIUM) fixed (see `workspace/phase-79/REPORT.md` + `docs/security-report.md` B2-DOS-03 row). Voice-note recording is bounded and the amplitude sampler is off-main. New pure-JVM `services/VoiceRecordingPolicy.kt` (decision table: `MAX_RECORDING_DURATION_MS` = 30 min, `MAX_RECORDING_BYTES` = 32 MB, `SAMPLER_TICK_MS` = 100, `MAX_STORED_WAVEFORM_ENTRIES` = 600, non-alarming limit messages) + `services/LiveWaveformBuckets.kt` (preallocated `FloatArray` accumulator; O(1)-amortized `append`; fold-on-full so `snapshot()` never exceeds `WaveformPeakMath.recordingLiveBuckets` = 160 and stays mean-preserving across the whole session). `VoiceNoteManager` sampler now runs on `Dispatchers.Default` (`VoiceNoteManager.kt:150`), appends `waveformBuckets.append(...)` and emits `waveformBuckets.snapshot()` (`:158`) — the pre-fix `_waveformAmplitudes.value + amp` full-list copy-on-write per 100 ms tick (~648M element copies after 1 h) on the main thread is gone — and aborts at the duration/file-size ceilings via `finalizeRecording(limitMessage)` (`:160-175`), which stops+encrypts (B1-DB-3 path preserved), surfaces a non-alarming `recordingError`, and publishes `completedRecordingResult` so `EditorScreen`'s `LaunchedEffect(completedVoiceRecording)` auto-attaches the audio embed through the shared `attachVoiceRecording` helper (never discarded, never orphaned). `startRecording`/`stopRecording`/`finalizeRecording` are serialized under a new `recorderLock` (`VoiceNoteManager.kt:75`) so a chip-tap stop racing a sampler ceiling-abort can never double-finalize. `NoteRepository.parseWaveformJson` (`:997-1009`) is bounded to 600 entries (both JSONArray + fallback paths). Tests: `LiveWaveformBucketsTest` (9) + `VoiceRecordingPolicyTest` (6) + `B2Dos03VoiceRecordingTest` (12, wiring source pins). `gradle :app:testDebugUnitTest` 1429 total (only the 2 pre-existing `B1Plat01ReleaseSigningTest` asserts on untouched `app/build.gradle.kts`/`docs/RELEASE.md`, pre-existing since phase-55) + `gradle :app:assembleDebug` green (APK SHA-256 `5cc83e79…`). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-80 | `DONE` | B2-DOS-04 (MEDIUM) fixed (see `workspace/phase-80/REPORT.md` + `docs/security-report.md` B2-DOS-04 row). `AppFacadeHost.httpGet` enforces the 10 MB response cap DURING the read, never after `readBytes()` already slurped the entire body into heap (the pre-fix `contentLengthLong` pre-check is -1/skipped for chunked/unknown responses, so a granted plugin pointing at a slow-chunked endpoint could pin hundreds of MB and OOM the process; the whole body arrived in heap BEFORE the `bytes.size > MAX` comparison). New pure-JVM `services/FacadeHttpGetPolicy.kt` is the single decision table (`MAX_FACADE_GET_BYTES` = 10 MB, `READ_BUFFER_BYTES` = 64 KiB): `readCapped` mirrors `WebPageFetcher` — a bounded streaming loop that throws `ResponseTooLargeException` mid-stream on the first chunk crossing the cap, so the heap accumulator can never exceed the budget + one read buffer. `AppFacadeHost.httpGet` (`AppFacadeHost.kt:91-93`) routes every body read through `FacadeHttpGetPolicy.readCapped`; the dead `readBytes()`-size post-check and the private `MAX_FACADE_GET_BYTES` companion are deleted; the `contentLengthLong` header pre-check (`:82-85`) stays as an early exit and the B1-NET-05 manual-redirect posture (`instanceFollowRedirects = false` `:67`, per-hop `StrictRedirectPolicy` re-validation `:71-76`) is retained so every redirect hop carries its OWN 10 MB budget. No schema change, no migration, no new deps, `.github/workflows/` untouched. API 26+ floor, pure java.io (no newer-API requirement, no fallback needed). Tests: new `B2Dos04FacadeGetStreamingCapTest` (7 — slow-chunked over-cap abort proven via a synthetic drip stream that never fully drains + bounded-yield assertion, exactly-at-cap accepted, small-body round-trip, header pre-check refuses before the stream opens, per-hop budget on a redirect chain, + source pins for readBytes-removal / readCapped-wiring / policy loop). `gradle testDebugUnitTest` 1436 total green (0 failures — the previously-failing `B1Plat01ReleaseSigningTest` asserts were repaired by the `b9a0b52` CI fix) + `gradle :app:assembleDebug` green (APK 173,760,926 bytes, SHA-256 `bc94479c…`). |
| phase-81 | `DONE` | B2-DOS-05 (MEDIUM) fixed (see `workspace/phase-81/REPORT.md` + `docs/security-report.md` B2-DOS-05 row). Attachment/import ingestion is bounded DURING the read, never post-slurp — a 500 MB picker/PDF/zip can no longer be `readBytes()`-ed wholly into heap to OOM the process (reachable via exported `ACTION_SEND */*` B1-PLAT-2 or any picker; repeatable as DoS). New pure-JVM decision table `services/AttachmentIngestPolicy.kt`: `MAX_ATTACHMENT_BYTES` = 25 MB, `READ_BUFFER_BYTES` = 64 KiB; `boundedReadBytes` streams over a fixed buffer and throws `ImportArchivePolicy.ImportSizeLimitException` mid-stream on the first chunk crossing the cap (heap never exceeds budget + one buffer); `readTextHead` is a head-bounded, prefix-preserving UTF-8 text head read (a multi-byte char split at the cap decodes lossily to a single replacement char, never an over-read past the budget; empty for missing/unreadable/empty). `EditorScreen` all 3 picker sites (`:236` custom-bg, `:263` paper-texture, `:829` photo embed) route through `boundedReadBytes` with a dedicated size-limit catch per site → truthful non-alarming snackbar ("Background image / Paper texture / Photo ... too large (max 25 MB)"), never silent; the pre-existing texture-path `inputStream?.close()` leak was removed (now `use`). `NoteflowViewModel.restoreEncryptedBackupFromZip` (`:3145-3147`) replaced `sourceZip.readBytes()` with `boundedReadBytes(it, ImportExportService.MAX_BACKUP_INPUT_BYTES)` — the 400 MB restore budget is preserved but enforced in-flight. `DocumentTextExtractor` rewritten: `MAX_EXTRACT_BYTES` = 25 MB (PDF operator scan head) + `MAX_TEXT_HEAD_BYTES` = 1 MB (text head, preserving the old `< 1_000_000` guard's spirit); `text` branch → `readTextBounded`, `extractPdfText` → `readFirstBytesBounded(file, MAX_EXTRACT_BYTES)`; no `file.readBytes()`/`file.readText()` left. Legacy plaintext-file-body reads switched to `AttachmentIngestPolicy.readTextHead` in `NoteBodyVaultPolicy.kt:64` and `WikiLinkParser.kt:274`. No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ pure java.io. Tests: new `B2Dos05AttachmentIngestTest` (14 — behavior via a synthetic `DripInputStream` proving mid-stream abort with bounded yield (never drains, never exceeds budget + buffer), exactly-at-cap accepted, small round-trip, empty stream, head-head read of a 1 MB file under a 64 KiB budget returns only a budget-length PREFIX (bounded + prefix-preserving), zero/missing-file degenerate cases, PDF-operator head found inside a giant PDF, + source pins: EditorScreen exactly 3 `boundedReadBytes` references with no raw `.readBytes()` after `openInputStream`, NoteflowViewModel no `sourceZip.readBytes()` + bounded read with backup budget, DocumentTextExtractor no raw readBytes/readText and uses `readFirstBytesBounded`, NoteBodyVaultPolicy/WikiLinkParser use `readTextHead`). `gradle testDebugUnitTest` 1470 total green (0 failures) + `gradle :app:assembleDebug` green. **Phase-81 review fixes** (same lineage): `NoteRepository.migrateLegacyPlaintextNoteBodies` (`:515`) — the last wholesale `file.readText()` on the legacy-body surface — now refuses oversized (> 25 MB) bodies (never read/deleted) and reads within-cap bodies head-bounded; WebDAV restore over-budget archives fail CLOSED with a truthful "Backup is too large to restore (max 400 MB)" message; `resolveBodyForDisplay` never returns a truncated head for oversized legacy bodies (falls through to the full column); `EditorScreen`'s 3 picker reads moved off the main thread; `boundedReadBytes` guards a contract-breaking zero-progress stream (throws after 16 idle reads). `B2Dos05AttachmentIngestTest` now 15 tests, 1471 total green + `assembleDebug` green. |
| phase-82 | `DONE` | B2-DOS-06 (MEDIUM) fixed (see `workspace/phase-82/REPORT.md` + `docs/security-report.md` B2-DOS-06 row). Multi-layer PSD export no longer materializes N full-page ARGB bitmaps + N per-layer channel buffers at once — a ~25-layer note export dropped from ~350 MB peak heap to a bounded ~125-132 MB, on every export. New pure-JVM decision table `services/PsdExportPolicy.kt`: `MAX_EXPORT_LAYER_COUNT` = 16, `capLayerCount`/`omittedLayerCount`/`isLayerCountCapped` (the layer-budget arithmetic) + `noticeMessage` (the one-time non-alarming wording, says which end of the stack was kept). `ImportExportService.exportPageToPsd` (`:2422`) clamps before rendering: it keeps the TOP 16 DATA layers of `layers.sortedBy { it.zOrder }` (highest zOrder = the visually front-most, most recently worked-on layers) in bottom→top PSD record order via `takeLast(exportedDataLayers)`/`dropLast(exportedDataLayers)` (the old unbounded `sortedBy { it.zOrder }.forEachIndexed` bitmap-every-layer loop is gone; a restored crafted vault can no longer raise the bitmap count), folds the omitted BOTTOM layers into ONE bounded merged-preview bitmap (`compositeExtras`) so the PSD's flattened composite still shows the full page, and returns the new `PsdExportService.PsdExportOutcome(file, exportedLayerCount, omittedLayerCount)`; `EditorScreen.kt:1407-1420` shows "PSD export included the top N of M layers — K omitted (max 16)" via `PsdExportPolicy.noticeMessage` AFTER a successful export (a cancelled picker can no longer hide the omission behind "Export cancelled"; never silent degradation). `PsdExportService.exportLayersToPsd` now writes the layer-and-mask section STREAMING: the info records go into a tiny bounded buffer (`layerRecordBytes`, few KB for 16 layers), the per-layer channel PIXEL data is written straight to the destination `DataOutputStream` one channel at a time (`writeLayerChannelData` → `writeChannelPixels`) — the pre-fix `layerPixelBlocks` full-size per-layer channel `ByteArray` accumulation AND the per-layer `IntArray(width*height)` are gone — and ONE `IntArray(width*height)` is reused for every layer and the composite, with a defensive `take(MAX_EXPORT_LAYER_COUNT + 1)` clamp plus a bounded `compositeExtras.take(MAX_EXPORT_LAYER_COUNT)` composite pass. The section-length arithmetic (`channelSizeFor`/`channelDataLength`/`layerSectionLength`) keeps the PSD layout byte-identical to the pre-fix materialized section. No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ (pure JVM; the psd layout helpers are unit-testable without Android). Tests: new `B2Dos06PsdExportLayerCapTest` (16 — policy behavior incl. cap/clamp/omission/notice, the streaming writer's channel-size/section-length/layer-record byte layout, single-pixel-buffer reuse, hidden-layer flag, dedup + source pins: no `layerPixelBlocks`/`chanBos`, exactly one `IntArray(width * height)`, ImportExportService uses `PsdExportPolicy.capLayerCount` + `takeLast(exportedDataLayers)` + returns `PsdExportOutcome`, EditorScreen surfaces `PsdExportPolicy.noticeMessage`, merged-preview `compositeExtras` folding + `MAX_EXPORT_LAYER_COUNT + 1` defensive clamp pins). `gradle testDebugUnitTest` 1487 total green (0 failures) + `gradle assembleDebug` green. |
| phase-83 | `DONE` | B2-DOS-07 (MEDIUM) fixed (see `workspace/phase-83/REPORT.md` + `docs/security-report.md` B2-DOS-07 row). Backup export can no longer materialize the ENTIRE vault zip in heap and then make a second full-size AES-GCM copy (or a ~1.37x Base64 expansion) — a few-hundred-MB vault OOM'd the IO thread on every "Create backup", making the backup feature itself an interactive DoS. `ImportExportService.exportBackup` (`ImportExportService.kt:1268-1369`) now streams the archive INCREMENTALLY into a transient app-private staging file (`File(context.cacheDir, BackupExportPolicy.stagingFileName(backupName))` `:1279`; zip written entry-by-entry via `ZipOutputStream(FileOutputStream)` through `BackupExportPolicy.zipVaultEntriesToStream` `:1281` — the pre-fix `ByteArrayOutputStream` accumulation + `baos.toByteArray()` are gone), then the v2 password path encrypts FILE-TO-FILE through `BackupExportPolicy.encryptStreamGcm` (`:1338-1345`): header write, then a bounded 64 KiB `Cipher.update` chunk loop, then `doFinal` tail+tag — byte-identical to the pre-fix single `doFinal` by the JCE stream-mode contract (pinned by an assertArrayEquals vs the one-shot reference), so the on-disk `NFLB2` layout is unchanged and legacy restores (`decryptBackupPayload`) read it unmodified; the device-keyed path streams through `BackupExportPolicy.encryptStreamDeviceKeyedBase64` (`:1359-1363`): GCM chunked + `java.util.Base64.Encoder.wrap(NonClosingSink)` — the same legacy wire format (`[version][iv][ciphertext+tag]`), never the in-heap Base64 expansion. Peak heap is now one `ENCRYPT_CHUNK_BYTES` (64 KiB) buffer + one `Cipher.update` output + the tag, never the archive; the plaintext staging zip is app-private (cacheDir) and deleted in `finally` (`:1366`); the KEK is still zeroized (`:1347`). Single pure-JVM decision table `services/BackupExportPolicy.kt` (`ENCRYPT_CHUNK_BYTES` = 64 KiB, `STAGING_SUFFIX = ".zip-staging"`, `stagingFileName`, `IDLE_READ_LIMIT` guard vs a zero-progress stream, mirroring `AttachmentIngestPolicy`); `EncryptionService` GCM constants (`GCM_IV_LENGTH`/`GCM_TAG_LENGTH`/`PAYLOAD_VERSION`/`FIELD_AAD`) flipped `private`→`internal` only (same app module). Callers unchanged (HomeScreen device-keyed + password paths, WebDAV C2b, LocalSend VAULT_BACKUP — all consume the returned cacheDir `File`). No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ (pure `java.io` + `java.util.Base64`), no fallback required. Tests: new `B2Dos07BackupExportStreamingTest` (8 — streamed-v2 payload byte-identical to the one-shot reference AND decrypts via the real restore decryptor; streamed device-keyed backup round-trips via real `EncryptionService.decrypt`; chunk-budget invariants: a 6 MiB input is pulled in ~96 ≤64 KiB reads and written in ≤64 KiB+16-byte writes, never the archive; vault zip streams incrementally to the staging file and stays a valid zip with every entry byte-complete; 6.7 MiB synthetic-vault end-to-end: streamed export decrypts back to EXACTLY the staged archive and every zip entry matches; source pins: no `val zipData`/`baos.toByteArray()`/`fos.write(cipherText)`/`val encryptedBase64`/`encrypt(zipData` in exportBackup + staging/deletion policy wiring, exactly 2 `ByteArray(ENCRYPT_CHUNK_BYTES)` in the policy, no readBytes/toByteArray/ByteArrayOutputStream, Base64 wrap + FIELD_AAD + versioned wire format; staging name never becomes a public download name). `gradle testDebugUnitTest` green (full suite, 0 failures) + `gradle assembleDebug` green (debug APK 173,773,954 bytes; one transient dex-merge OOM on the 2 GB default daemon retried clean with a 4 GB CLI heap — no `gradle.properties` change). |

| phase-84 | `DONE` | B2-CRYPTO-04 (MEDIUM) fixed (see `workspace/phase-84/REPORT.md` + `docs/security-report.md` B2-CRYPTO-04 row). Password-protected backups can no longer be created with a bare 6-char, no-complexity password, and the exported header no longer ships a cheap offline brute-force target. New pure-JVM policy `services/BackupPasswordPolicy.kt` (evaluate delegates to the single `PasswordStrengthPolicy` decision table used by the vault master password - >=8 NFKC-normalized graphemes, sequential/weak/low-diversity rejection, >=12-grapheme passphrase pass - plus `requireStrongBackupPassword` throwing `IllegalArgumentException` with verdict + `OFFLINE_BACKUP_NOTICE`). `ImportExportService.exportBackup` (`ImportExportService.kt:1346-1398`) replaces the old `normalizedGraphemeCount(backupPassword) >= MIN_PASSWORD_GRAPHEMES` bare-length require with `BackupPasswordPolicy.requireStrongBackupPassword(backupPassword)` and writes the new v3 `NFLB3` wire format: a fresh random 32-byte DEK-wrap key is split into [16B part1 (public header, `buildBackupHeaderV3` `:1173`)] + [16B part2 embedded at the start of the password-encrypted payload via `SequenceInputStream(ByteArrayInputStream(wrapKeyPart2), FileInputStream(stagingZip))` through `BackupExportPolicy.encryptStreamGcm` (`:1380-1390`)], so the public header alone holds HALF a key - an offline attacker can no longer test password guesses against a small wrapped-DEK blob; every crack attempt must fully decrypt the payload on top of PBKDF2. DEK wrap under `backup/dek-wrap` AAD, payload GCM under `backup/payload`+header AAD (B2-CRYPTO-03 domains), wrapKey + both halves + KEK all zeroized (`:1392-1397`). Restore/verify are format-agnostic over both magics (`parseBackupHeader` `:1540` reads `NFLB2`/`NFLB3`; `tryParseBackupV2`/`validateBackupPassword` now internal for tests) and NEVER strength-gate, so pre-fix weak-password backups still restore; v3 `validateBackupPassword` = ONE full payload decrypt + DEK unwrap (no cheap wrapped-DEK probe by design). The create-backup dialog (`HomeScreen.kt`) pre-checks `BackupPasswordPolicy.evaluate` and shows `OFFLINE_BACKUP_NOTICE` (restore dialog untouched). Also fixed a PRE-EXISTING latent zeroization-order bug the new tests exposed: `tryParseBackupV2` hex-encoded the unwrapped DEK AFTER `.fill(0)` (dekHex was all-zero - a password restore would rekey sqlcipher with a zero key); now hex-then-zeroize. Tests: new `B2Crypto04BackupPasswordTest` (11 - strength verdicts incl. old-accepted `123456`/`abcdef`/`A9fj2l!` now rejected, header carries only half the wrap key so the wrapped DEK cannot be opened from the header alone, wrong-password reject, v3 round-trip + v2 backward-compat, validate both magics never gates, export/parse/verify source pins); `B1Crypto04PasswordStrengthTest` whitelist updated for the sanctioned `BackupPasswordPolicy` reference. `gradle testDebugUnitTest` 1506 total green (0 failures) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched, pure-JVM/API-26 floor. |
| phase-85 | `DONE` | B1-NET-06 (LOW) fixed (see `workspace/phase-85/REPORT.md` + `docs/security-report.md` B1-NET-06 status). LocalSend no longer probes the whole Wi-Fi /24 or broadcasts the device model without explicit user consent. Two-thirds predate this phase and are re-verified + pinned here: (1) `Build.MODEL` was already stripped from the announce/identity in phase-110 (B1-NET-09) — `services/localsend/LocalSendProtocol.kt:106-114` sends `alias = "InkFlow"`, `deviceModel = null`; this phase re-wires both to the NEW single pure-JVM decision table `services/localsend/LocalSendDiscoveryPolicy.kt` (`SENDER_ALIAS`, `senderDeviceModel = null`, `:43-46`) so the identity can never diverge from one source, and (2) opening the Send dialog was already silent — `discover()` (`LocalSendSendDialog.kt`) fires ONLY from the explicit "Find nearby devices"/"Refresh" `onClick` handlers, never a `LaunchedEffect`. The remaining live gap (the actual finding) was the /24 `legacyHttpScan` HTTP register sweep (`LocalSendSender.kt:197-218` walking `1..254`, `:232-258` POSTing `/api/localsend/v2/register`) that ran BY DEFAULT: `discoverDevices` had `includeLegacyHttpScan: Boolean = true` and the dialog hard-coded `= true` (`LocalSendSendDialog.kt:113`) — one tap on "Find nearby devices" gave every LAN host the app's presence + local IP via 254 register POSTs. Fix, all behind the new decision table: `discoverDevices` (`LocalSendSender.kt:104-118`) defaults `includeLegacyHttpScan` to `LEGACY_HTTP_SCAN_ENABLED_BY_DEFAULT` (`false`) and the probe routes through `mayRunLegacyHttpScan(userOptedIn)` (fail closed; also only consulted when UDP discovery found nothing); the dialog seeds a "Also check every address on this Wi-Fi (slower; helps on networks that block discovery broadcasts)" Checkbox from that same `false` default and feeds the user's `legacyHttpScanOptIn` into the single discovery call — the sweep is now an explicit per-search user opt-in, a plain search emits ONLY a UDP announce/listen, and never a default silent subnet blast. Protocol stays pure-JVM/API-26; no new deps, `.github/workflows/` untouched; the only sender behavior change is on the LAN sweep path (`includeLegacyHttpScan=false` UDP-only search is the identical path to before-opt-in). Tests: new `B1Net06LocalSendDiscoveryGateTest` (7 — decision table: discovery requires explicit action + sweep off-by-default + fail-closed gate + identity wiring; source pins: sender default now reads the policy constant (never hard-coded `true`), dialog seeds opt-in from the policy and has no auto-discover effect, protocol identity wired to the policy constants, and a comment-stripped repo-wide scan proving no live `Build.MODEL` remains in `services/localsend/` main code — KDoc that merely documents the ban is handled, not a false positive). `gradle testDebugUnitTest` 1513 total green (0 failures, 0 skipped) + `gradle assembleDebug` green (debug APK 173,779,638 bytes). |
| phase-86 | `DONE` | B1-NET-07 (LOW) fixed (see `workspace/phase-86/REPORT.md` + `docs/security-report.md` B1-NET-07 status). The WebDAV remote-listing → download slice is now a pure-JVM decision table `services/WebDavRemoteListingPolicy.kt` (API-26 floor, no fallback needed): (1) `newestBackupHref` picks the href with the MAXIMUM filename timestamp across both name generations (`noteflow_vault_backup_<epochMillis>.nfb` legacy + `noteflow_vault_backup_<yyyy-MM-dd>_<token>.nfb` B2-CRYPTO-06) via `compareBy` ASCENDING + `maxWithOrNull` — a review-fix for the phase-86 initial commit's `compareByDescending`+`maxWithOrNull` inversion (which deterministically selected the OLDEST file, the exact rollback the finding is about) — unparseable names score lowest, same-timestamp ties break deterministically on the href, never XML document order (`WebDavSyncService.kt` `findBackupHrefs` + `newestBackupHref` replace `zipRegex`/`matches.last()` at `:290-297`); (2) the `.nfb` GET is streamed by `copyBounded` under `MAX_DOWNLOAD_BYTES` = 400 MB (mirrors the restore budget), checking the running total mid-loop so the target file never exceeds the cap, throwing the typed `DownloadTooLargeException` (whose message names the violation) and failing loudly on a contract-breaking zero-progress stream after `IDLE_READ_LIMIT` empty reads (`:337` wired; the too-large catch ALSO deletes the partial `webdav_download_import.nfb`); (3) `encodedRemoteFolderSegment` validates `remoteFolderName` as ONE path segment (blank/`.`/`..`/`/`/`\`/control chars rejected) and RFC 3986 percent-encodes it at all three URL interpolations (`:189/:226/:270`). Review-fix scoped the `copyTo` source pin to the download path only — the upload PUT keeps its genuinely-bounded local→server `input.copyTo(output)`. No schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: `B1Net07WebDavDownloadPolicyTest` (19 — out-of-order hrefs pick the newest day-stamp, legacy epoch-millis newer-vs-day names, deterministic same-day tie-breaks, unparseable-name scoring, both name generations parsed/rejected, empty listing safely null, over-cap mid-stream abort without draining, exactly-at-cap accepted, byte-for-byte small copy, stalled-stream failure, folder segment round-trip/encode, traversal/control/blank rejection, download-path wiring pins). `gradle testDebugUnitTest` 1532 total — the only failure is the known pre-existing `WikiLinkParserCacheUnitTest` cancellation-timing flake (passes in isolation, documented in phases 67/70/74/76/79/80/81); `gradle assembleDebug` green (debug APK 173,783,182 bytes, SHA-256 `0eecbb7e…`). |
| phase-87 | `DONE` | B1-DB-6 (LOW) fixed (see `workspace/phase-87/REPORT.md` + `docs/security-report.md` B1-DB-6 row). (1) WAL-aware tamper HMAC: the pre-fix main-file-only inline loop in `DatabaseSecurityHelper.computeDatabaseHmac` is replaced by the new pure-JVM `services/DatabaseHmacPolicy.kt` — `streamDbAndWal` streams `noteflow.sqlite` then its `-wal` companion through the same initialised `Mac`, so a WAL-only mutation committed between two checkpoints (the vault runs `JournalMode.WRITE_AHEAD_LOGGING`) is detected at the next verification; a cleanly-emptied WAL contributes byte-identical state to an absent WAL (no self-inflicted false positive), and every baseline-arming site already checkpoints first or reads a closed raw file (migration/export/restore stamps), so a freshly armed baseline covers `(main + empty/absent wal)`. (2) Per-session dismissal: `NoteflowViewModel.dismissDatabaseIntegrityWarning` (`NoteflowViewModel.kt:1106-1109`) no longer writes `databaseIntegrityCheckEnabled = false` and neither dismissal path touches the persisted `databaseIntegrityWarningDismissed` latch (zero `settings.databaseIntegrityWarningDismissed` references remain in the VM); the banner routes through the new pure-JVM per-session `services/IntegrityWarningDismissalGate.kt` (`integrityWarningDismissal.mayShow()` in the init verify `:1091`, `.onDismiss(dontShowAgain)` `:1107`, `.onReenable()` `:1115` in `setDatabaseIntegrityCheckEnabled`), re-armed on every launch so the vault's only tamper tripwire can never be permanently killed by one tap; the checkbox is honestly relabelled "Don't show again this session" (`MainActivity.kt:362`). B1-CRYPTO-06's fail-open missing-checksum re-baseline at `verifyDatabaseIntegrity` `:147-152` is deliberately UNTOUCHED (its own phase-91 finding). No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ pure java.io/javax.crypto (no fallback needed). Tests: `B1Db06WalCoverageAndDismissalTest` (16 — WAL-frame mutation / appended-WAL / WAL-removal / main-mutation detection at the next verification, empty-WAL≡absent-WAL sameness, identical-state clean verify, per-session dismissal lifecycle incl. a two-session never-permanently-disabled model, source pins for the policy wiring + no persistent-flag reads/writes in the VM + the session-scoped checkbox label). |

| phase-88 | `DONE` | B1-DB-8 (LOW) fixed (see `workspace/phase-88/REPORT.md` + `docs/security-report.md` B1-DB-8 row). Decrypt-failure fallbacks never render RAW CIPHERTEXT as note content: every display-field decrypt site in `NoteRepository` routes through ONE pure-JVM decision table `services/DecryptFailurePolicy.kt` whose `render(storedValue, decrypted, isCiphertext)` is the ONLY render outcome allowed — legacy plaintext verbatim, authenticated ciphertext's plaintext, or `UNREADABLE_MARKER` ("Unreadable (decryption failed)"), never the raw base64 AES-GCM blob. The four pre-fix sinks are rewired: `getStrokesForPage` (`:946-960`) via `decryptFieldForDisplay`/`decryptStoredGeometryOrBlank` (unreadable geometry yields an EMPTY payload — no phantom ink, nothing fed to `deserializeStrokes`), `getMediaEmbedsForPage`, `getNoteVersions`, and `decryptPageIfNeeded` (the pre-fix catch-all that returned the page unchanged is gone). The structural classifier (`isStructuralCiphertext`) keeps legacy plaintext rows rendering verbatim — never "fixed" into the marker. Each failed auth while a DEK is actually present (a locked vault never inflates it) is recorded in a per-session deduped ledger (`NoteRepository.kt:79-99`) keyed per NOTE (`note:<pageId>`, phase-88 review fix — the initial field-level keying let one corrupted note's strokes/embeds cross the threshold on their own, so a one-note corruption can now never block the vault, and `decryptPageIfNeeded` no longer early-returns the raw page when the DEK is zeroized); once `PERSISTENT_FAILURE_THRESHOLD` = 10 DISTINCT records fail, `decryptFailureListener` fires once and `NoteflowViewModel.initializeDataCore` (`:1330-1343`) escalates to the existing corruption/restore event: `DatabaseSecurityHelper.setCorruptionDetected` + `_corruptionBlocked` (recovery screen: restore-from-backup / re-key / start-fresh) + a non-alarming `PERSISTENT_DECRYPT_FAILURE_NOTICE` snackbar — never silent degradation. The ledger + escalation are reset at every legitimate session boundary (`lock()`, re-key `changeMasterPassword`, WebDAV restore, and every `initializeData`). No schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: `B1Db08DecryptFailureTest` (13 — real AES-GCM tampered/re-keyed/locked/legacy/authenticated reads + source pins for all four sinks and the VM escalation/reset wiring); `gradle testDebugUnitTest` 1561 total green (0 failures) + `gradle assembleDebug` green. |
| phase-89 | `DONE` | B1-PLAT-5 (LOW) **verify-only** (see `workspace/phase-89/REPORT.md` + `docs/security-report.md` B1-PLAT-5 row). The finding was already closed by the phase-48 review fix: `PrivacyCrashReporter.sanitizeMessage` (`services/PrivacyCrashReporter.kt:85-96`) no longer uses the hardcoded-namespace regex (`/data/user/\d+/com\.authorss81\.noteflow/\S+`) that could never match the runtime data dir `/data/user/0/com.aistudio.inkflow.app.bkxjrz/`; it now redacts ANY app-private path structurally via `/data/user/\d+/\S+` (modern layout, any uid/package) + `/data/data/\S+` (legacy alias) → `[PATH_REDACTED]`. Pinned by `B2Log01CrashReportingTest` `:68-78` (namespace form) and `:80-90` (`crash entry redacts the real runtime applicationId data dir too` — asserts the exact `com.aistudio.inkflow.app.bkxjrz` string never appears). This phase made NO code change (PROMPT is SUPERSEDED; the hardcoded regex was not re-introduced) and re-verified: `gradle testDebugUnitTest` 1561 tests green on re-run (the first run's single `WikiLinkParserCacheUnitTest` cancellation-timing flake passes in isolation — documented pre-existing flake since phase-67) + `gradle assembleDebug` green (debug APK 173,786,602 bytes, SHA-256 `1a076fe6…`; a first assembleDebug invocation hit the documented unreproducible transient build failure, re-run green). No schema change, no migration, no new deps, `.github/workflows/` untouched. Out-of-scope (documented): `namespace`→applicationId alignment is B1-PLAT-6 / ROADMAP 21.10 (major architectural change, needs user approval). |
| phase-90 | `DONE` | B1-PLAT-8 (LOW) fixed (see `workspace/phase-90/REPORT.md` + `docs/security-report.md` B1-PLAT-8 row). The NEW master-password floor is raised ≥ 10 NFKC-normalized graphemes and common/prefix-suffix words are rejected, with the "offline brute force is only mitigated by password entropy, never the on-device lockout" caveat documented in code + `docs/RELEASE.md`. `PasswordStrengthPolicy.MIN_STRENGTH_GRAPHEMES = 10` (`services/PasswordStrengthPolicy.kt:104`, was 8 after B1-CRYPTO-04 / 6 pre-fix B1-PLAT-8) + new `services/PasswordStrengthPolicy.kt` `isCommonPasswordVariant` (`:160`) / `PasswordStrengthVerdict.COMMON_PASSWORD` (`:64`) — a widely-leaked base word (`password`, `sunshine`, `letmein`, …) is refused whole or with only digit/symbol padding around it (`monkey5281`, `2026sunshine`, `letmein!!!`), structurally, so genuine passphrases that merely contain a word keep passing; sequential/repeated/class-diversity checks unchanged (`evaluate` `:141`, COMMON checked before LOW_DIVERSITY). **Phase-90 review fix (commit `llops: phase-90 review fixes`)**: `isCommonPasswordVariant` now runs FIRST in `evaluate` (after the 128 cap, before the 10 floor and before sequential), so bare `password`/`sunshine` and `password123`/`123password` report `COMMON_PASSWORD` — the review found the original build handed them a misleading `TOO_SHORT`/`SEQUENTIAL` verdict; accept/reject outcome is unchanged, only the reason string moved (pinned by new bare/sequential-pad assertions). Enforcement stays exactly at the set/rotate gate: `NoteflowViewModel.setMasterPassword` (`:2443`) + `changeMasterPassword` (`:2522`) route the NEW password through `PasswordStrengthPolicy.evaluate` (never at verify/unlock — pre-existing weaker vaults keep unlocking and rotating), the Dialogs.kt set/change dialogs surface `verdict.message`, and the backup gate `BackupPasswordPolicy` inherits the raised bar (a backup can never be protected by a password weaker than its vault). The exploit's premise (6-char + lockout gives false assurance) is closed on the CREATE side and documented on the OFFline side. No schema change, no migration, no new deps, `.github/workflows/` untouched, pure JVM / API-26 floor (no fallback needed). Tests: `B1Crypto04PasswordStrengthTest` 17 green (3 new B1-PLAT-8 cases: bare/prefix-suffix common-word rejection, passphrase-tolerance, min=10 + RELEASE.md documentation pin); `B2Crypto04BackupPasswordTest` updated to the 10 floor (9-char now TOO_SHORT, `sunshine123` COMMON_PASSWORD, "10 characters" message) — `gradle testDebugUnitTest` 1564 app tests green (0 failures, 0 skipped) + `gradle assembleDebug` green (debug APK 173,787,778 B, SHA-256 `232ad7013ba0d8337c79b0366829fcccf37076d40d85bc4c523cadf6d41a7cbc`; the first invocation hit the documented transient dex-merge failure — retry green). |

| phase-91 | `DONE` | B1-CRYPTO-06 (LOW) fixed (see `workspace/phase-91/REPORT.md` + `docs/security-report.md` B1-CRYPTO-06 row). The tamper baseline verification is FAIL-CLOSED and write-free. `DatabaseSecurityHelper.verifyDatabaseIntegrity` (`DatabaseSecurityHelper.kt:173-178`) now returns a sealed three-outcome `DatabaseIntegrityVerdict` from the single pure-JVM decision table `services/DatabaseIntegrityPolicy.kt` (API-26+ pure JVM, no fallback): `Verified` (stored baseline present + current main+`-wal` HMAC matches — compared via `ConstantTime.hexEqual`, B2-CRYPTO-01 full-length `MessageDigest.isEqual`), `Mismatch` (baseline present + differing bytes → genuine tamper), `CannotVerify` (baseline MISSING or current HMAC UN-COMPUTABLE → "cannot verify / possibly tampered"). Both pre-fix collapses are gone: the `stored==null → updateStoredChecksum(context); return true` silent re-baseline (an attacker deleting the `db_hmac_checksum` pref used to make the app bless a possibly-tampered file as "verified") and the `computeDatabaseHmac() ?: return true` trust-an-unverifiable-state. The function NEVER writes and NEVER re-baselines; `hasStoredChecksum` is the new read-only accessor and the pref stays write-only through `updateStoredChecksum`/`rearmBaselineFromFile` at the trusted arm sites (fresh-vault creation, migration, re-encrypt, backup, validate-then-arm restore). `NoteflowViewModel.verifyDatabaseIntegrityNow` (`NoteflowViewModel.kt:1188-1193`) maps the verdict onto two distinct UI states: `Mismatch` keeps the existing per-session tamper banner (B1-DB-6 gate), `CannotVerify` surfaces a DISTINCT non-alarming notice (`MainActivity.kt` tertiaryContainer banner, wording from `DatabaseIntegrityPolicy.CANNOT_VERIFY_NOTICE` — vault NOT locked, NOT proven compromised, only unverified; shared per-session dismissal, honest "Don't show again this session" checkbox), `Verified` clears both. The ONLY auto-arm is a brand-new vault: `initializeDataCore` (`:1509-1520`) arms the baseline iff `!vaultFilePresentAtStart && !DatabaseSecurityHelper.hasStoredChecksum(appContext)` (a first-run vault false-alarming forever would be wrong, and there is no prior state to tamper) — an EXISTING vault whose baseline is missing/unreadable is never re-baselined from a live-file verify. No schema change, no migration, no new deps, `.github/workflows/` untouched. Tests: `B1Crypto06DatabaseIntegrityPolicyTest` (11 — decision table incl. both fail-closed sides, constant-time compare pin, write-free helper, read-only `hasStoredChecksum`, distinct VM state + verdict mapping incl. fresh-unarmed suppression, per-session dismissal reuse, MainActivity notice pins); `gradle testDebugUnitTest` 1577 total green (0 failures, 0 skipped) + `gradle assembleDebug` green (debug APK 173,792,606 B, SHA-256 `8ecac53f…`). **Review fixes (commit `llops: phase-91 review fixes`)** (a) WAL-aware "fresh vault" probe (`vaultFilePresentAtStart` now counts a populated `-wal`, and `computeDatabaseHmac` stays computable for an empty main + populated WAL — a WAL-resident existing vault can never be silently re-baselined); (b) the first verification of a PASSWORDLESS vault is deferred past the initial data open (`firstDataInitDone` gate) so a mid-WAL-recovery hash can't false-Mismatch — locked vaults still verify at init; (c) `CannotVerify` honors the SAME per-session dismissal gate as `Mismatch`; (d) `MainActivity` banners deduplicated into one `IntegrityBannerCard` composable. No schema change, no new deps; `B1Crypto06DatabaseIntegrityPolicyTest` 14 green + `B1Db06` 16 green, `gradle testDebugUnitTest` 1580 total green (0 failures). |
| phase-92 | `DONE` | B1-AUTH-07 (LOW) fixed (see `workspace/phase-92/REPORT.md` + `docs/security-report.md` B1-AUTH-07 row). The create-backup dialog's pre-export master-password check (`NoteflowViewModel.isMasterPasswordValid`, sole caller `HomeScreen.kt:1316`) was a side-effect-free oracle — it ignored `lockoutActive()` and never bumped `failedUnlockAttempts`/`lockoutUntilEpochMs`, so an attacker with a brief window on an unlocked device could hammer the backup dialog with unlimited full-PBKDF2 guesses that never tripped the LockScreen's 5-attempt exponential lockout. BOTH master-password verification surfaces now share ONE persisted lockout. New `NoteflowViewModel.recordFailedMasterPasswordVerification()` (`:2867-2879`): bumps `_failedUnlockAttempts` + persisted `settings.failedUnlockAttempts`; at `MAX_FAILED_ATTEMPTS`=5 persists `settings.lockoutUntilEpochMs` via `computeLockoutDelayMs` (exponential backoff 30 s → 15 m cap, unchanged), starts the LockScreen countdown ticker, AND calls `lock()` so an in-app tripped lockout performs the same data-layer teardown as a real lock (B1-AUTH-02 posture — `dispose()` + no live keyed SQLCipher connection, `LockedOpenGuard` refuses fresh opens). New `resetMasterPasswordVerificationCounters()` (`:2886-2891`) zeroes `_failedUnlockAttempts` + both persisted counters + `_lockoutRemainingMs`. `verifyMasterPassword` (`:2806`) and `isMasterPasswordValid` (`:2917`/`:2921`) both delegate to the shared helpers — the old inline counter block in the verify catch (which read `settings.failedUnlockAttempts = newCount`) is GONE. `isMasterPasswordValid` now: `lockoutActive()` FIRST (refuses before any PBKDF2 work — the oracle is also closed under an active lockout), `masterPasswordCredentialOrLegacy == null` guard, failed attempt records via the shared helper, verified attempt still zeroizes the DEK (`dek.fill(0.toByte())`) then resets the counters. Deliberately still NO `PasswordStrengthPolicy` in `isMasterPasswordValid` (strength only gates set/rotate — weaker pre-existing vaults must keep unlocking; `B1Crypto04PasswordStrengthTest` asserts read). The backup dialog (`HomeScreen.kt:1316-1328`) still re-authenticates immediately before `ImportExportService.exportBackup(...)` (a tripped 5th attempt skips `checkpointWal`/`stampDatabaseChecksum`/`exportBackup` entirely) and now distinguishes a tripped/active lockout (`"Too many failed attempts. Try again after the lockout countdown."`) from a plain wrong password — never a silent degradation. No schema change, no migration, no new deps, `.github/workflows/` untouched, pure JVM + existing Android call paths on the API 26+ floor (no fallback needed). Tests: `B1Auth07IsMasterPasswordOracleTest` (11 — behavioral `LockoutModel` with injectable clock: both surfaces share ONE counter set, 5th failure via either locks + persists 30 s backoff + data-layer lock, an active lockout refuses both surfaces BEFORE any PBKDF2 work and never churns counters, a correct password is still refused while locked, a verified attempt clears the shared counters, the lockout survives a restart and lapses past the backoff, exponential table 30 s→15 m cap, a credential-less vault refuses both surfaces without counting; source pins that `isMasterPasswordValid` enforces the lockout FIRST, routes through both helpers, zeroizes the DEK, stays strength-gate-free, and that `verifyMasterPassword`'s inline counter block is gone + HomeScreen verifies before `exportBackup` + surfaces the lockout message); `B1Crypto04PasswordStrengthTest` comment/message updated for the throttled-not-strength-gated verifier. `gradle testDebugUnitTest` **1591 total green** (0 failures, 0 skipped; the documented pre-existing `WikiLinkParserCacheUnitTest` cancellation-timing flake appeared in one earlier full-suite run and passes in isolation + did not reproduce on the final run) + `gradle assembleDebug` green (debug APK 173,793,374 B, SHA-256 `881c83e3…`; first packageDebug invocation hit the documented transient incremental-packaging failure, retry green). **Phase-92 review fixes (commit `llops: phase-92 review fixes`)**: the two remaining inline success counter-resets — `verifyBiometricsAndUnlock` and `removeMasterPassword` — now delegate to the shared `resetMasterPasswordVerificationCounters()` so they cannot drift from the unlock/dialog path; `removeMasterPassword` additionally clears the stale `_lockoutRemainingMs` countdown its old inline block omitted (review FINDING #4). The `B1Auth07IsMasterPasswordOracleTest` `LockoutModel`'s backoff table is now source-pinned to production (`const val MAX_FAILED_ATTEMPTS = 5` + `computeLockoutDelayMs`'s `30_000L` seed / `(failures - MAX_FAILED_ATTEMPTS)` exponent / `exponent.coerceAtMost(5)` shift / `15 * 60 * 1000L` cap) so a model/production drift is caught (review FINDING #3). `workspace/phase-92/REPORT.md` now documents the unlock-path behavioral change from this phase: the pre-fix 5th UNLOCK failure only zeroized the key; post-fix a tripped threshold on ANY surface performs a full data-layer `lock()` (review FINDING #2 — deliberate B1-AUTH-02 strengthen, not a regression). Re-verified `gradle testDebugUnitTest` + `gradle assembleDebug` both green. |

| phase-93 | `DONE` | B2-LOG-04 (LOW) fixed (see `workspace/phase-93/REPORT.md` + `docs/security-report.md` B2-LOG-04 row). No plugin failure text can echo an attacker-controllable `downloadUrl` into logcat and no CR/LF can forge a logcat line. New pure-JVM decision table `plugin-sdk/src/main/kotlin/com/authorss81/noteflow/plugins/PluginLogPolicy.kt` (phase-71's `FailureLogPolicy` pattern applied to the plugin pipeline): `hasLineBreak`/`lineBreakError`/`stripLineBreaks`/`safeLine` — `safeLine` strips CR/LF AND redacts `https?://\S+` tokens to `<url>`. The one production sink `AndroidPluginLogger` (`app/.../plugins/PluginLogger.kt:34-48`) routes BOTH `lifecycle` and `error` lines through `safeLine` (defense in depth). Parse-time rejection: `PluginEntry.validationErrors` (`plugin-sdk/.../runtime/PluginEntry.kt`) and `HostedPluginVersion.validationErrors` (`app/.../runtime/HostedPluginManifest.kt`) refuse CR/LF in `id`/`name`/`downloadUrl` via the fixed-text `lineBreakError` (the hostile value is never echoed), so a hostile manifest leg, a hand-edited catalog blob (`PluginEntryCodec.decode` → `isValid`) or a return-to-pinned update offer is refused whole (`PluginManifestParser.parse` → `Invalid`). Call sites log FIXED tokens only: `DownloadablePluginInstaller.failCleanup` (`:103-117`) logs `code=<reasonCode>` (`DOWNLOAD_GUARD`/`MISSING_SHA256`/`MISSING_CERT_PIN`/`VERIFY_FAILED`/`VERIFY_NOT_IMPLEMENTED`/`REGISTRY_REFUSED`/`LOAD_FAILED`/`LOAD_NOT_IMPLEMENTED`) and the download-refused early return logs `code=DOWNLOAD_GUARD` — the pre-fix `message.substringBefore('.')` line is GONE; `PluginStoreController` download/delete refusals (`:211`/`:241`) log `code=REGISTRY_REFUSED` (never `result.reason`); `DownloadablePluginUpdater` logs the fixed `post-update reload failed`; `PluginUpdateEngine.failedUpdateKeepsPrevious` (`:258-279`) logs `update failed; stage=<stageCode>` (`download`/`verification`/`load-smoke-test`/`swap-persist`) — the B2-LOG-04 exploit's URL-prefix leak through `reason.substringBefore('.')` (a transport failure `network failure https://attacker.example/exfil?…` used to print `network failure https://attacker`) is GONE. The downloader's own guard refusals were already log-free, now pinned by a test. `message`/`reason` remain user-facing (store dialog) only. Tests: new `B2Log04PluginLogScrubbingTest` (10 — policy table, model rejection, codec/manifest refusal, downloader quiet-guard, engine fixed-stage log). `gradle :app:testDebugUnitTest` 1602 total green (0 failures, 0 skipped) + `gradle testDebugUnitTest` (all modules) green + `gradle assembleDebug` green (first invocation hit the documented transient dex-merge failure; retry green, debug APK 173,794,594 B, SHA-256 `4219405b…`). No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ pure JVM. |

| phase-94 | `DONE` | B2-LOG-05 (LOW) fixed (see `workspace/phase-94/REPORT.md` + `docs/security-report.md` B2-LOG-05 row). No WebDAV failure path can echo a raw exception message or a URL-derived string (and, via a `user:pass@` paste, the credentials) into the sync-status UI. New pure-JVM decision table `app/src/main/kotlin/com/authorss81/noteflow/services/WebDavFailurePolicy.kt` (phase-71 `FailureLogPolicy` pattern applied to UI text): fixed constants `CONNECT_FAILURE_MESSAGE`/`UPLOAD_FAILURE_MESSAGE`/`DOWNLOAD_FAILURE_MESSAGE`/`TOO_LARGE_DOWNLOAD_MESSAGE`/`INVALID_URL_MESSAGE`; `stripUrlUserInfo` (regex `([A-Za-z][A-Za-z0-9+.-]*://)[^/@\s]*@` → drops the userinfo + its `@`); `scrubForDisplay` (userinfo strip AND `scheme://host/path` collapse to `host/...` via `URL_TOKEN_REGEX`); `configFailureText(e, fallback)` (scrubbed `e.message.orEmpty()`, blank → fallback); `refusalReason(e)` (FIXED tokens classifying resolver defusals — network-path / outside-configured-server / not-HTTPS / malformed-unusable / other — never the href or URL). `WebDavSyncService.kt`: the pre-fix `"Invalid WebDAV server URL: ${e.message}"` malformed-catch is now `throw IllegalArgumentException(WebDavFailurePolicy.INVALID_URL_MESSAGE)` (the JVM `MalformedURLException` message dumps the user's RAW input — a `https://user:pass@…` paste exported its credentials through the exception text); `validateServerUrl` returns the userinfo-stripped normalized URL; `testAndPrepareConnection` → IllegalArgument = `configFailureText(e, INVALID_URL_MESSAGE)`, blanket = `CONNECT_FAILURE_MESSAGE` (was `"Connection failed: ${e.localizedMessage ?: e.message}"`); `uploadEncryptedVault` → IllegalArgument = `configFailureText(...)`, blanket = `UPLOAD_FAILURE_MESSAGE`; `downloadLatestEncryptedVault` → resolver IllegalArgument = `"Sync refused: ${refusalReason(e)}"`, origin-gate IllegalState = FIXED "Sync refused: ... configured WebDAV server." (dropped `${e.message}`), too-large = `TOO_LARGE_DOWNLOAD_MESSAGE` (was `e.message ?: "Remote backup archive is too large..."`), blanket = `DOWNLOAD_FAILURE_MESSAGE`. `WebDavSyncDialog.kt` scrubs all three status renders (upload `res.message`, restore `failureMessage`, download `res.message`) through `WebDavFailurePolicy.scrubForDisplay(...)` — defense in depth so even a future sink can't render raw text. Grep-pinned in the test: zero `localizedMessage`, zero `${e.message}`, zero bare `res.message`-assign in both production files; exactly 2 code reads of `e.message` (`configFailureText` + `refusalReason`), both only through `scrubForDisplay`. Auth/HTTP-status branches were already fixed strings and are left untouched. No schema change, no migration, no new deps, `.github/workflows/` untouched, API-26+ pure JVM. Tests: new `B2Log05WebDavFailureTextTest` (13 — userinfo stripping incl. the http-opt-in local URL, userinfo-free URLs unchanged, malformed-URL fixed message carrying NO secret echo, fixed constants never interpolate (`"\${"` absent) and none of the five category strings appears in the service, connect failure whose exception embeds credentials yields the fixed constant with scrubbed text free of secrets, scrubForDisplay/stripUrlUserInfo behavior, refusalReason token classification, configFailureText fallback, plus source pins on `WebDavSyncService.kt`/`WebDavSyncDialog.kt`/`WebDavFailurePolicy.kt`). `gradle :app:testDebugUnitTest` (WebDAV families incl. existing `WebDavSyncServiceTest`/`WebDavHrefResolverTest`/`B1Net07WebDavDownloadPolicyTest`) green, then `gradle testDebugUnitTest` 1619 total green (0 failures, 0 skipped) + `gradle assembleDebug` green. |
| phase-95 | `DONE` | B2-UI-4 (LOW) resolved as VERIFY-ONLY (see `workspace/phase-95/REPORT.md` + `docs/security-report.md` B2-UI-4 row, now `FIXED 2026-08-17`).
| phase-116 | `DONE` | Round-2 full source security audit (Phase-30 methodology repeated on the post-fix tree; see `workspace/phase-116/REPORT.md` + `docs/security-report-round2.md`). 2 batches × 5 parallel subagents, source review only, no code change (audit-only; fixes are phase-119's job). **Round-2 total: 43 findings (0 CRITICAL · 0 HIGH · 12 MEDIUM · 26 LOW · 5 INFO)**. Batch 1 (commit `cc04ed1`): 20 unique (crypto 2 · datarest 5 · net/plugins 5 · platform 5 · applogic 3). Batch 2 (commit `3134927`): 23 unique (UI+concurrency 6 · deps/CVE 4 · logging 4 · DoS 3 · phase-34-38 feature surface 6; FEA-07 folded into DOS-03). Batch-2 MEDIUMs: `R2-b2b1-UI-01` read-side pool-closed crash on lock, `R2-b2b2-DEP-01` unpinned CI Actions with keystore secrets, `R2-b2b4-DOS-01` unbounded note_versions, `R2-b2b4-DOS-02` uncapped live layer bitmaps, `R2-b2b5-FEA-01..03` knowledge-graph unculled edges + markdown quadratic scans + per-keystroke full-doc re-tokenization. Verification: `gradle testDebugUnitTest` green + `gradle assembleDebug` green (debug APK 173,838,046 B, SHA-256 `ceff4627…`; one transient first-invocation build flake, retry green). `.github/workflows/` untouched; no schema change, no migration, no new deps. The post-unlock state re-initialization gap was already closed end-to-end as a by-product of phase-47's B1-AUTH-02 fix (commit `23c8742`), verified + finding-pinned this phase with the phase-89 precedent (no production code change): `NoteflowViewModel.lock()` (`:3638-3694`, inside `if (settings.hasMasterPassword)` `:3668-3681`) resets `dataInitialized = false` (`:3673` — the audit's exact ask), cancels `sectionsJob`/`pagesJob` (`:3669-3670`), disposes the SQLCipher connection (`:3671`), and nulls the session StateFlows (`:3686-3690`), so `initializeData()` (`:1400-1402`, re-entry guard kept) can no longer be sticky; BOTH unlock paths reinstate the connection + boot the data layer before any dbGate flow re-subscribes — `verifyMasterPassword` `:2780`/`:2785`/`:2789` (`reinstateDatabaseAfterLock()` → `_authenticated=true` → `initializeData()`) and `verifyBiometricsAndUnlock` `:2985`/`:2990`/`:2995` — and `initializeDataCore()` (`:1526-1549`) restores `settings.activeNotebookId`/`settings.activeSectionId` from prefs (falling back to `ensureDefaultNotebookAndSection()` on a stale/deleted pair), re-arming BOTH `observeSections`/`observePages` (`:1541-1542`), closing the audit's `:1246-1253` "observePages only from selectSection" gap; `selectNotebook`/`selectSection` persist the ids (`:1661`/`:1682`); all six home-list flows (`notebooks`/`allSections`/`allActivePages`/`paletteItems`/`recentPages`/`trashedPages`, `:1272-1376`) are dbGate-gated on `_authenticated && !_corruptionBlocked && !_keystoreKeyLost`, so a locked vault emits `emptyList()` and an unlock re-subscribes + re-emits (the exploit's "re-shown without re-validation" half); `MainActivity.kt:221-239` re-selects the saved `activePageId` on unlock, rounding off the full lock→unlock state round-trip. Passwordless vaults skip tear-down by design (device-wrapped DEK is the boot credential — no lock boundary, no empty-list defect). New pure-JVM pin test `B2Ui4UnlockReinitializesStateTest` (10 tests: behavioral lock/unlock state-machine model — repopulation without manual nav, repeated lock/unlock cycles, passwordless no-op by design, re-entrant guard, stale-prefs fallback — plus source wiring pins incl. the dbGate predicate). No schema change, no migration, no new deps, `.github/workflows/` untouched. `gradle testDebugUnitTest` **1646 total green** (0 failures, 0 skipped; app 1629 + plugin modules 17) + `gradle assembleDebug` green (first invocation hit the documented transient dex-merge failure; retry green, debug APK 173,796,714 B, SHA-256 `d8d53906…`). |
| phase-117 | `DONE` | Release-APK build phase FOR phase-118 (Kali dynamic pentest). See `workspace/phase-117/REPORT.md` + `docs/security-report-round2.md` "Audit metadata → APK target". Build/verification only — NO app feature-code changes, no build-config changes (everything worked on the first attempt). `gradle assembleRelease` → **BUILD SUCCESSFUL in 8m 6s** (113 tasks, R8 minify + lintVitalRelease both ran). Artifact `app/build/outputs/apk/release/app-release.apk` (142,234,607 B), v2-signed by the real release keystore (CN=InkFlow Release, cert SHA-256 `69636edb9ee2487762e98f855f250ea1ec66233de13b61a4c014026b82c50196`; B1-PLAT-1 fail-closed held), SHA-256 `20849bcfcc72bfc649057464d17bd26f13bd74b76a89f1f0232244816ec85d3d`, versionCode 2 / versionName 1.0.0, built from commit `dd0c5f59`. No `.github/workflows/` edits. Review fixes 2026-08-17 (see `workspace/phase-117/REPORT.md` addendum + `logs/phase-117.review.log`): `docs/ARCHITECTURE.md` JDK-17→Temurin-21 claim corrected + stale "release falls back to debug keystore" note corrected to fail-closed B1-PLAT-1; `docs/security-report-round2.md` APK-target section now records `applicationId` `com.aistudio.inkflow.app.bkxjrz`, distinguishes source-audit commit `c813c99` from APK build source `dd0c5f5`, and instructs phase-118 to independently re-verify the artifact SHA-256 + signer cert before attacking (self-attested claims + best-effort `continue-on-error` upload caveat; v2-only signing = known INFO). No app code / no workflows edited. |
| phase-118 | `BLOCKED` | Kali full-environment dynamic pentest on the phase-117 APK (see `docs/security-report-round2.md` "Audit metadata → APK target" for the self-attested verification instructions the artifact failed). Marked `.blocked` (2026-08-17, commits `4dcb871`/`3e76a54` — only `.attempts` + `.blocked` marker, no REPORT.md/.done). No dynamic/APK findings were appended to the round-2 report; the 43 round-2 findings are therefore the SOURCE-audit set (phase-116) plus no phase-118 additions. Manual intervention (a device/AVD capable of apksigner/`adb` dynamic work) is required to unblock. |
| phase-119 | `NOT STARTED` | Round-2 Triage → generate new phases (see `workspace/phase-119/REPORT.md`). Generated 25 new phases starting at the pre-seeded max (133): fix phases **134-153** (all 43 findings bundled 2-3 per phase) + feature phases **154-158** (2-3 related features each, UI/UX + plugin ideas). Numbering decision documented: existing dirs 120-133 were pre-seeded user-requested UI/UX phases from commit `7a4b6e6` (renumbered by `c3a1789`), so the round-2 triage output starts at **max-existing-phase(133) + 1 = 134** per the PROMPT's "start at max+1" rule. |
| phase-120 | `DONE` | User-requested UI/UX feature (pre-seeded commit `7a4b6e6`): scrollable overflow menus (see `workspace/phase-120/REPORT.md`). Every real `DropdownMenu(`/`ExposedDropdownMenu(` in the app now passes `scrollState = overflowMenuScrollState()` + `modifier = overflowMenuScrollModifier()`: HomeScreen 7, EditorScreen 3, MarkdownPreviewScreen 1, UnifiedSidebar 3, MediaEmbedComponents 1, SpreadsheetTableView 1, Dialogs 1, KanbanBoardView 1, Phase16PluginDialogs 1, LocalSendSendDialog 2 (parity grep-verified 1:1). Fix: M3 `DropdownMenu` only scrolls when given a `scrollState` and its fixed 288 dp cap exceeds small/landscape/large-font usable heights (16-row Home maintenance ≈ 768 dp, 17-row Editor ⋮ ≈ 816 dp). New pure-JVM `services/OverflowMenuPolicy.kt` (`MAX_MENU_FRACTION_OF_SCREEN=0.60`, cap = 60 % of `screenHeightDp` coerced 120..560; ≥ 288 on roomy portrait phones so no visual change there) + shared `ui/components/OverflowMenuSupport.kt` helper composables. No contents/ordering/behavior changes, no deps, no schema, `.github/workflows/` untouched. Tests: new `OverflowMenuPolicyTest` (14: math + source pins); `gradle testDebugUnitTest` **1683 total, 0 failures/0 errors**; `gradle assembleDebug` green (first plain invocation had a transient daemon flake, forced `--rerun-tasks` run 57/57 executed green — non-reproducible). |
| phase-121 | `DONE` | User-requested UI/UX feature: rounder brush edges (see `workspace/phase-121/REPORT.md`). **Audit finding: the stroke render pipeline was already round** — `AnnotationCanvas.drawSingleStroke` uses `StrokeCap.Round`/`StrokeJoin.Round` for PEN/HIGHLIGHTER (AnnotationCanvas.kt:2943, :2985), FOUNTAIN_PEN (:3017), MARKER (:3199, :3212), DOTTED (:3262, :3278-3279), NEON (:3306-3316), FINELINER (:3339, :3364), LASER (:3416-3426); textured tools (PENCIL/OIL_PAINT/WATERCOLOR/OIL_PASTEL/GOUACHE/CHARCOAL/DRY_BRUSH/INK_WASH) route through `BrushTextureEngine.drawTexturedStrokePath` (`Paint.Cap.ROUND`+`Paint.Join.ROUND`, BrushTextureEngine.kt:136-137); CALLIGRAPHIC/CHISEL_MARKER use round-capped `drawRibbonStroke`. **The one intentional exception is the palette-knife flat smear** (`Paint.Cap.SQUARE`+`Paint.Join.BEVEL`, BrushTextureEngine.kt:329-330) — documented character (docs/brush-styles.md style 10), kept untouched. **Real defect fixed: the brush-palette previews misrepresented strokes** — `PenNibVisualPreview` hard-coded `StrokeCap.Square` for FINELINER/HIGHLIGHTER/OIL_PASTEL/GOUACHE (which render round) so the swatches showed sharp edges the actual stroke never has. New pure-JVM `services/BrushEdgePolicy.kt` is the single source of truth for cap/join roundness per tool (everything ROUND except the documented flat knife) and `PenNibVisualPreview` now derives its cap from the policy. No render-pipeline regressions, no deps, no schema, `.github/workflows/` untouched. Tests: new `Phase121BrushEdgeTest` (9: policy table + velocity-width smoothness + source pins for both render files + preview); `gradle testDebugUnitTest` **1692 total, 0 failures/0 errors**; `gradle assembleDebug` green. |
| phase-122 | `DONE` | User-requested UI/UX feature: rainbow colours (see `workspace/phase-122/REPORT.md`). The rainbow brush MODE already shipped in phase-27 (per-point render-time hue derivation, mode chips in the colour picker, per-stroke mode/seed in the stroke payload — no schema); this phase closed the three real gaps: (1) mode now PERSISTS across sessions via `SettingsManager.brushColorModeKey` (`SettingsManager.kt:203`) routed through the pure-JVM `services/ColorModePersistencePolicy.kt` (`PREF_KEY_COLOR_MODE="brush_color_mode_key"`, DEFAULT=SOLID, fail-closed `modeFromPref`), restored on editor open (`EditorScreen.kt:135-140`); (2) rainbow is now reachable from the width/quick picker via ONE shared `ui/components/ColorModeChipsRow.kt:40` (Solid/Rainbow/Gradient/Shimmer chips + gradient-end swatches) rendered in BOTH bottom sheets (`EditorScreen.kt:3133` + `:3451`), with every handler persisting (`:1829`/`:1878`/`:1845`/`:1894`/`:1821`/`:1610`); (3) hue math is allocation-free per point — `BrushColorModeMath.hueAdvance` (`BrushColorModeMath.kt:145`) = deterministic per-stroke-length full-360° sweep `normalizeHue(seedHueDeg + progress*360)`, `rainbowColorAt` (`:166`) uses scalar `valueOf` (no per-point `FloatArray`/`argbToHsv`), output byte-identical. Hue-advance policy = per-stroke-length (replay-deterministic), documented in KDoc. Textured tools keep the phase-27 mid-progress sampling (documented limitation). NO DB schema change, no new deps, `.github/workflows/` untouched. Tests: new `Phase122RainbowColorTest` (15: hue-advance determinism + 360° wrap + clamping, rainbow-agrees-with-policy, persistence round-trip + fail-closed to SOLID, non-rainbow strokes byte-identical, degenerate-stroke gamut, source pins for picker/persistence/canvas wiring); `gradle testDebugUnitTest` **1707 total, 0 failures/0 errors/0 skipped**; `gradle assembleDebug` green. |
| phase-123 | `DONE` | User-requested UI/UX feature: immediate colour/layer/tool application (see `workspace/phase-123/REPORT.md`). Fixes: (1) `AnnotationCanvas.kt:634` — added `activeLayerId`+`layers` to the drawing `pointerInput` restart-key list so a layer switch no longer leaves the stroke-commit closure capturing the PREVIOUS layer until a pen switch (was the "must switch pens first" bug); (2) `AnnotationCanvas.kt:247` — TEXT-tool colour `textSelectedColorInt` now `remember(currentColor)` so the next text stroke follows a newly picked brush colour; (3) `EditorScreen.kt:3011/3026/3041` — the three HSV sliders convert their just-changed channel inline (`HSVToColor(...)`) instead of applying the previous composition's `derivedColor`, so the final slider position reaches the stroke. Plain swatches/eyedropper/presets/tool/eraser-type were already live (verified). Tests: `Phase123ImmediateSelectionTest` (12 — restart-key model proving each selection is effective for the very next stroke + pre-fix reproducer + HSV immediate-apply model + source pins); `gradle testDebugUnitTest` 1721 app tests green (0 failures/0 skipped); `gradle :app:assembleDebug --rerun-tasks` green. No DB change, no new deps, `.github/workflows/` untouched. |
| phase-124 | `DONE` | Two eraser types — whole-stroke delete & smooth partial erase (see `workspace/phase-124/REPORT.md`). The two MODES already existed (phase-19: `EraserMode` enum `StrokeSegmenter.kt:14`, picker chips + persistence `EditorScreen.kt:1813` / `SettingsManager.kt:193`, split-based partial carry via `StrokeSegmenter.segment`, undo for both via `EditorScreen.handleStrokesChange` pre-state capture `:588`). Phase-124 closed the real gaps: (1) **pressure-aware smooth round edge** — new pure-JVM `EraserGeometryPolicy.kt` (`stampRadius(baseWidth,pressure)`, `coverageRadius` = stamp + half nib so the cut is always round, `previewRadius`, `legacyRadius`); `EraseSample(pos,pressure)` (`AnnotationCanvas.kt:85`) carries capture-time pressure, `applyEraser` (`:699`) stamps radius onto every sample (`:705`), and `StrokeSegmenter.segment`/`coverageRadiusFor` (`:52`) are per-sample radius-aware — heavier press carves a wider round swath, points outside the mask survive; (2) **cursor preview** — non-consuming pointer tracker (`:594`) mirrors the pointer into world coords; the canvas draws the live round mask (PARTIAL, `:1595`) or the whole-stroke highlight incl. symmetry mirror (STROKE, `:1604`); (3) **hit-test hardenings** — pure-JVM `StrokeSegmenter.hitStrokeAt` (`:68`, topmost + symmetry + start/end anchors) and `strokeContainsPoint` now also tests `stroke.end` (`AnnotationCanvas.kt:3753`) so shape-stroke tips erase. Tests: new `Phase124EraserTest` (17 — policy monotonicity/bounds, round-cut survivor geometry, pressure swath widening, merged stamp runs, topmost/symmetry/anchor hit-tests, source pins) + `StrokeSegmenterTest` (16) green; full `gradle :app:testDebugUnitTest` green + `gradle :app:assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-125 | `NOT STARTED` | User-requested UI/UX feature: enhanced tutorial. |
| phase-126 | `DONE` | All plugins OFF by default — every bundled/compiled plugin strictly opt-in (see `workspace/phase-126/REPORT.md`). **Audit finding: enablement already defaulted off** — `SettingsManager.isPluginEnabled` → `prefs.getBoolean("plugin_enabled_<id>", false)` (`services/SettingsManager.kt:340-341`); `PluginRegistry`/`defaultPlugins()` (`plugins/PluginRegistry.kt:855`) registers definitions only (opt-in is the sole write path, `setEnabled` `:273`); `onProcessStart` fires hooks only for enabled plugins (`:195`); capability routing refuses un-opted-in plugins with `NONE_ENABLED` (`plugins/PluginManager.kt:188-198`); store installs start REGISTERED (off) (`installPlugin` `:394-405`). Phase-126 work: the invariants are now PINNED by new pure-JVM `PluginOffByDefaultTest` (6 tests — fresh install every shipped plugin REGISTERED/off; explicit enable persists across registry reconstruction over the same store; upgrade keeps prior explicit choices + never-touched stay off; no onEnable fired before opt-in; no capability served before opt-in; store-wipe resets enable+ever-enabled so reinstall starts off) and the off-by-default policy is stated explicitly in `docs/PLUGINS.md` + `docs/ARCHITECTURE.md`. No auto-enable path was found anywhere in `app/src/main` (grep-verified: `setPluginEnabled`/`setEnabled` call sites = UI toggles + `PluginRegistry.setEnabled` only). CaseChangePlugin stays the store's OPTIONAL plugin (NOT in `defaultPlugins()`, downloaded → REGISTERED/off). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-127 | `NOT STARTED` | User-requested UI/UX feature: plugin store compact descriptions. |
| phase-128 | `DONE` | User-requested UI/UX feature: normal text style everywhere — no exaggerated typography (see `workspace/phase-128/REPORT.md`). Audit of every `Text(...)` in `ui/` + `MainActivity.kt` vs the `theme/TypeScale.kt` M3 ladder found typography was already standard except a handful of outliers, all normalised: `InteractiveTutorial.kt` — the ONLY `FontWeight.ExtraBold` + ONLY `letterSpacing=1.sp` in the app (Step-kicker `labelLarge+ExtraBold+tracking` → plain `labelLarge` `:210-214`), slide title `headlineSmall+FontWeight.Bold` → `titleLarge` default weight `:218-223`, section chip `.uppercase()` label → title case `:181`; `UnifiedSidebar.kt:124/164` — the ONLY hardcoded ALL-CAPS labels "QUICK NOTES"/"ALL NOTEBOOKS" → "Quick Notes"/"All Notebooks"; `EditorScreen.kt:3533` — "×" delete glyph `titleMedium`→`labelSmall` matching the sibling colour-picker pattern `:3179`. Excluded (documented): markdown H1/H2 heading ladders, the consistent `headlineMedium` lock/recovery screen-title family, canvas ink UI, emoji glyphs, capped data values. Also fixed a PRE-EXISTING phase-127 compile blocker that blocked every build: `PluginStoreDescriptionBlock.kt:12-13/72-74` imported nonexistent `Icons.AutoMirrored.Outlined.KeyboardArrowUp/Down` → plain `Icons.Outlined.*` (same as the working `UnifiedSidebar.kt:296/392`). Verification: `gradle testDebugUnitTest` 1818 tests 0 failures/errors/skipped (163 suites; the review pass corrected an earlier overstated `3536` count); `gradle assembleDebug` green (forced `--rerun-tasks` rerun; first hit the documented transient incremental-packaging failure). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-129 | `DONE` | User-requested UI/UX feature: restore the horizontal floating ink/tool bar + aspect-correct, draggable minimap (see `workspace/phase-129/REPORT.md`). The phase-35 (`7b0507b`) regression of the always-vertical `FloatingToolDock` + fixed 120×140dp minimap HUD is reverted: posture is orientation-only via pure-JVM `services/DockPosturePolicy.kt` (portrait → 56dp-tall horizontal capsule `InkBarPortraitBar`, landscape → 56dp side column `InkBarLandscapeBar`); ALL 9 pre-35 bar items restored exactly (tool selector w/ `displayTool` label + `primaryContainer` highlight, Scroll/Pan toggle, 40dp color swatch + outline ring, width badge, `VerticalDivider`/`HorizontalDivider`, Tune settings 40dp, 36dp Undo/Redo, `HIDDEN_DRAWING` auto-hide + tap restore, bottom-centre/end-centre default anchors @ `EditorScreen.kt:2498/2657`); drag/snap/session-persist are opt-in settings default OFF (`FloatingWidgetDragPolicy.kt`; session-scoped offset hoisted in `EditorScreen`, persisting only on opt-in). Minimap: `services/MinimapGeometryPolicy.kt` + `AnnotationCanvas.kt` — size proportional to the canvas world aspect fitted in the pre-35 120×140dp max box (single uniform map scale for pan/viewport agreement incl. seamless/infinite), default bottom-right anchor, draggable only when `minimapDraggable` (default OFF); visibility default reverted to OFF (`SettingsManager.kt:317` reads `MinimapGeometryPolicy.VISIBLE_BY_DEFAULT=false`, the phase-35 persisted default-true is gone). Settings sheet gained the 4 phase-129 toggles. Tests: `Phase129InkBarMinimapPolicyTest` 23 green; `gradle testDebugUnitTest` green (0 failures), `gradle assembleDebug` green (`--rerun-tasks`; first run hit the documented transient packaging flake, retry clean). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-130 | `DONE` | User-requested UI/UX feature: FLAG_SECURE gated on `!BuildConfig.DEBUG` (see `workspace/phase-130/REPORT.md`). New pure-JVM decision `SecureWindowPolicy.shouldApplySecureFlag(debug)` (`app/src/main/kotlin/com/authorss81/noteflow/services/SecureWindowPolicy.kt`); `MainActivity.onCreate` (`app/src/main/kotlin/com/authorss81/noteflow/MainActivity.kt:144-146`) applies FLAG_SECURE ONLY under `if (SecureWindowPolicy.shouldApplySecureFlag(BuildConfig.DEBUG))` — the phase-60 unconditional `window.addFlags` is gone (repo-wide source grep: the ONLY `window.addFlags(...FLAG_SECURE)` in `app/src` is the guarded one; no `clearFlags` FLAG_SECURE anywhere; the only other `.addFlags` is the unrelated `ExportEnginePlugin` `FLAG_GRANT_READ_URI_PERMISSION`). `buildConfig = true` verified (`app/build.gradle.kts:89`), so `BuildConfig.DEBUG` is generated in the namespace. R8/minify release posture confirmed: `onCreate` is an activity entry point (kept by AGP default rules) and `BuildConfig.DEBUG`=false folds the branch to always-true, so release keeps the flag. Tests: `B1Plat04AutoLockTest` now 11 (new `SecureWindowPolicy decision is debug-absent and release-present` + updated `FLAG_SECURE is applied only under the non-debug BuildConfig guard` source pin). `gradle testDebugUnitTest` 1842 total green (0 failures/0 errors/0 skipped) + `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-131 | `DONE` | Project metadata & LLM plugin build-script alignment (see `workspace/phase-131/REPORT.md`). New repo-root `metadata.json` (name InkFlow / namespace `com.authorss81.noteflow` / applicationId `com.aistudio.inkflow.app.bkxjrz` / version 2 "1.0.0" with VERSION_CODE/VERSION_NAME env overrides / SDK 36-26-36 / Gradle 8.13 no-wrapper / AGP 8.7.3 / Kotlin 2.0.21 / KSP 2.0.21-1.0.25 / JVM 17 / modules app+plugin-sdk+plugins:llm / capability buckets = exact `PluginCapability.ALL` partition (18 compile-time, assistant downloadable, file_transfer unserved) / downloadable-LLM record with `inBaseApk:false`). Pure-JVM `services/ProjectMetadata.kt` (Gson, test source set) parse+`validate()` (fail-closed fields; `usesGradleWrapper` must be false, `inBaseApk=true` rejected, API-26+ SDK floor; unknown-key/gap/dup detection). `plugins/llm` build script fully catalog-aligned: last raw coordinate `junit:junit:4.13.2` → `libs.junit` (`gradle/libs.versions.toml:32-34,88`; `plugins/llm/build.gradle.kts:50`; `app/build.gradle.kts:225`); standalone `packagePlugin` green under system Gradle; B2-DEPS-04 fail-closed signing gate re-verified (no env → loud refusal) AND positive signed pipeline proven with a throwaway `/tmp` keystore (`signPlugin`→`verifyPluginSignature` `jar verified`→`pluginMetadata` emitted `sha256`+`pinnedCertHash`+`version=1.0.0`), then reverted (no keystore material / no signed pin committed; `GeneratedLlmPluginPin.kt` still fail-closed `null`). Tests: `Phase131MetadataAlignmentTest` (11 — metadata/build-file/version/SDK/toolchain/module/capability/LLM-record cross-checks + parser/validator negatives); `gradle testDebugUnitTest` 1853 total **0 failures/0 errors/0 skipped** (`--rerun-tasks`) + `gradle assembleDebug` green (debug APK 173,945,158 B SHA-256 `7c1d7366…`); base APK binary-verified 0 mediapipe classes / 0 native libs. No schema change, no new deps (junit coordinate unchanged, so no `verification-metadata.xml` update), `.github/workflows/` untouched. |
| phase-132 | `DONE` | User-requested UI/UX feature: command-palette header squish fix (see `workspace/phase-132/REPORT.md`). Header re-architected into a nested Column (`CommandPaletteOverlay.kt:188-204`) — title on its own full-width `maxLines=1`/ellipsized line with the 45-char shortcut hint rendered beneath it (also single-line/ellipsized); the pre-fix weight(1f)-title vs unconstrained-hint squeeze is gone. Header strings moved to the pure-JVM single-source-of-truth `services/CommandPaletteHeaderPolicy.kt` (`TITLE`/`SHORTCUT_HINT`); truncation is left to Compose's pixel-based `TextOverflow.Ellipsis`. **Review fixes**: the initial `shortcutHint`/`truncate`/`DEFAULT_HINT_MAX_CHARS`/`ELLIPSIS` char-budget decision table was dead production code (only tests referenced it; Compose renders the full hint and ellipsizes by pixels) and was REMOVED, with its 7 tests — surrogate-pair/UTF-16 split risk gone with it; "49-char" overstatement corrected to 45 in docs; missing final newline added (`.editorconfig` `insert_final_newline`). Tests: `Phase132CommandPaletteHeaderTest` (3); `gradle testDebugUnitTest` 1856 total green (app 1806 + plugins:llm 50; 0 failures/0 errors/0 skipped) + `gradle :app:assembleDebug` green (`--rerun-tasks`: 57/57 tasks; debug APK SHA-256 `da287f67…`; first plain invocation hit the documented transient packaging flake, retry clean). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-133 | `DONE` | User-requested UI/UX feature: Add-Page FAB & Daily Journal — new pages open immediately (see `workspace/phase-133/REPORT.md`). Root cause: active page resolved only via `pages.find { it.id == activePageId }` against the section-filtered async Room flow → null on the creation frame → transition dropped. New pure-JVM `services/ActivePageResolution.kt` = `ActivePageResolution.resolve(activePageId, synchronous, allActivePages, sectionPages)` with precedence synchronous → `allActivePages` → section `pages`, plus `ActivePageTracker`/`ActivePageTrackerState` (synchronous capture on `open`, `onAuthoritative` refresh that drops confirmed-but-absent pages while keeping the unconfirmed creation-frame copy, `restore` re-arms the saved id). `MainActivity.kt:226-289` collects `allActivePages`, keeps the synchronous copy in a `remember`-ed tracker, resolves through the helper, and re-arms via `LaunchedEffect(allActivePages, pages)`; a second effect drops id/editor when a confirmed page disappears. `NoteflowViewModel.openOrCreateDailyNote` (`:2324`) / `openPageByTitle` (`:2397`) — and the same latent fix in `createNoteFromSharedContent` (`:1914`) — return the page, re-synchronize `observePages(sec.id)`, and dispatch `onOpen` via `withContext(Dispatchers.Main)`. Tests: `Phase133ActivePageResolutionTest` (19 — null-immediate-frame → fallback wins, precedence order, stale-sync ignored, tracker lifecycle); `gradle testDebugUnitTest` green (app 1825 + plugins:llm 50 = 1875 total; 0 failures/0 errors/0 skipped) + `gradle :app:assembleDebug` green (`--rerun-tasks`: 57/57; debug APK SHA-256 `919a2d32…`; first plain invocation hit the documented transient packaging flake, retry clean). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-134 | `NOT STARTED` | Fix R2-B1A-01 + R2-B1A-02 + R2-b2b1-UI-01 (MEDIUM×3): lock-vs-inflight — every write/search/read path guarded against the disposed SQLCipher pool (`writeGuardedAgainstLock`, `searchVaultJob?.cancel()` in `lock()`, shared read-side guard). |
| phase-135 | `NOT STARTED` | Fix R2-B1D-02 + R2-b2b1-UI-03 + R2-b2b1-UI-06: restore hardening — reject structurally-invalid/empty backups before swap, one-in-flight re-entrancy gate on all 4 restore entry points, `rememberSaveable` recovery state. |
| phase-136 | `DONE` | Fix R2-B1D-01 (MEDIUM) fixed (see `workspace/phase-136/REPORT.md` + `docs/security-report-round2.md` R2-B1D-01 row). The WAL-aware tamper baseline (`noteflow.sqlite` + `-wal`, B1-DB-6) is now re-armed at EVERY session end instead of only at event-driven mutations — ordinary note edits (committed-but-uncheckpointed WAL frames) no longer verify against a pre-edit baseline and raise a FALSE "Database integrity check failed" banner at the next start. `NoteflowDatabase.dispose()` (`NoteflowDatabase.kt:482-520`) — the single session-end funnel: master-password `lock()` (`NoteflowViewModel.kt:4056-4060`), app-exit `onCleared` (`:4089`), restore `NoteRepository.closeDatabase` (`:501`) and reopen-after-lock `reopenDatabase` (`:511`) — now FULL-checkpoints the WAL on the still-live keyed connection (fully-stepped `PRAGMA wal_checkpoint(FULL)`, `:489-497`), closes the vault (`:500`, best-effort) and re-arms the stored baseline against the quiescent file via `DatabaseSecurityHelper.updateStoredChecksum` (best-effort `runCatching` so a keystore/prefs failure never breaks lock/restore); the checksum-prefs Context is cached at build time (`cachedAppContext`, `getDatabase` `:437`). **Review fixes:** the full-file HMAC + checksum-prefs commit runs on a single-thread daemon executor (`REARM_EXECUTOR`, `:69-74`) so `lock()`/`onCleared()` never block on it; `getDatabase` joins the pending re-arm (`:444-447`) before rebuilding and `onCleared` awaits it at app exit (`awaitPendingRearm()` `:529-531`); the baseline helpers persist with `commit()` not `.apply()` so a hard kill cannot drop the re-arm; and clean app-exit re-arms passwordless vaults too (intended — they already hold a baseline from first-run/migration/backup). Lock teardown stays master-password-only (the passwordless boot DEK is the credential; no session boundary) and `verifyDatabaseIntegrity` STILL never re-baselines (B1-CRYPTO-06 invariant kept; post-exit tampering of main/`-wal` still detected; a hard process-kill without lock/onCleared remains the one documented residual false-positive window). Tests: new `Phase136TamperBaselineCadenceTest` (8 — arm→edit→re-arm→Verified vs arm→edit→Mismatch control, WAL-fold + empty-WAL invariance, source pins for checkpoint→close→schedule ordering, the getDatabase-join, the master-password-gated lock funnel, the onCleared-await, the restore/reopen funnel, the commit-not-apply helpers and the verify-never-rebaselines pin); `gradle testDebugUnitTest` 1869 total green (app 1819 + plugins:llm 50; 0 failures/0 errors/0 skipped) + `gradle assembleDebug` green (transient first-invocation dex-merge flake, retry clean). No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-137 | `DONE` | Fix R2-B1D-05 + R2-B1D-03 (LOW×2) (see `workspace/phase-137/REPORT.md`). Every DB-file copy producer now runs checkpoint-then-copy. `ImportExportService.exportBackup` (`ImportExportService.kt:1308-1312`) is the SINGLE disciplined producer: it runs a full `repository.checkpointWal()` + HMAC re-stamp (`repository.stampDatabaseChecksum`, `:1335-1336`) BEFORE copying the main file, then copies through the new pure-JVM `services/VaultSnapshotCopyPolicy.kt` verified snapshot (`checkpointThenCopy`, `:1337`) — the source is SHA-256 digested before AND after the copy plus the staging digested; the snapshot is accepted only when all three match, retried up to `MAX_VERIFY_ATTEMPTS=3` when a concurrent WAL auto-checkpoint mutates the source mid-copy, and FAILS CLOSED (loud `IllegalStateException`, torn staging deleted, `:1337-1341`) when the source never stabilizes. The DB zip entry now reads the verified `stagedDb` (`:1346`), never the raw live file (`FileInputStream(dbFile).copyTo(zos)` gone). `exportBackup` REQUIRES the repository, and all four producers pass it — HomeScreen plain (`HomeScreen.kt:613-617`) + password (`:1419-1424`) backups, WebDAV (`NoteflowViewModel.kt` `exportEncryptedBackupToZip`), and the previously-dead path LocalSend `VAULT_BACKUP` (`LocalSendSendDialog.kt:147-152`, R2-B1D-03 — no longer ships a WAL-stale archive). The now-redundant explicit checkpoint/re-stamp were removed from HomeScreen/WebDAV (single owner, no double HMAC). Tests: `Phase137BackupCopyConsistencyTest` (7 — verified-copy behavior with a fake checkpoint/copy seam incl. a mid-copy auto-checkpoint race retry + fail-closed, checkpoint-before-copy ordering, + source pins for exportBackup ordering and all four producers). `gradle testDebugUnitTest` 1927 total (app 1877 + plugins:llm 50), 0 failures/0 errors/0 skipped; `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-138 | `DONE` | Fix R2-B1D-04 (LOW) (see `workspace/phase-138/REPORT.md`). v2/v3 restore now decrypts FILE-TO-FILE (never ~800MB in-heap peak), export/restore budgets are aligned, and ANY post-restore-failure reopens the vault. (1) `BackupExportPolicy` gained the streaming decrypt mirrors + `skipFully` (`BackupExportPolicy.kt:150-253`); `ImportExportService` decrypts v2/v3 into a transient staging file (`decryptPayloadToFile` `:1528`, `tryParseBackupV2File` `:1661`, `validateBackupPasswordFile` `:1779`), streams the legacy device-keyed Base64+GCM decrypt (`decryptDeviceKeyedToFile` `:1992`), stages picked URIs to a cache file (`stageBackupUriToFile` `:1879`), `importBackup` takes a `File` (`:1910`), and the v3 prefix is skipped at extract (`restoreFromZip(zipFile, offsetBytes…)` `:2053`). (2) New pure-JVM `services/BackupBudgetPolicy.kt` is the single budget source: 100MB/entry, total = 400MB wire cap (GCM out = in+16 ⇒ a decryptable archive is always accepted), 40k entry belt, 100x ratio seal; the export packer refuses at pack time (`claimPackFile`, `:118`) exactly what the extractor refuses (`claimRestoreChunk` `:136`) — no more "exportable-unrestorable" backups (was 200MB/50MB asymmetric). (3) New pure-JVM `services/RestoreFailSafe.kt` (`guaranteeReopenAfterRestore` `:28`) closes+runs+reopens on ANY `Throwable` incl. Error/OOM; wired into `NoteflowViewModel` recovery (`:2313`), keystore-lost (`:2403`), WebDAV restore (`:3772`), and `HomeScreen.performRestore` (`:172`); HomeScreen picker now classifies by FILE (`isNflbBackupFile`/`isPlainPkBackupFile`, `HomeScreen.kt:224/:234`) — v3 backups finally get the password dialog, not the legacy UNTRUSTED dialog. Tests: `Phase138RestoreStreamingTest` (new, 12 — stream file-to-file round trip, zero-AAD rescue, v2 parse/verify factory + staging cleanup, cap-parity pack/restore, reopen model incl. OOM); updated `B2Crypto04` (file API), `B1Db07`, `RestoreHardeningWiring`, `B2Dos05`, `B2Dos07`, `Phase137`. `gradle testDebugUnitTest` 1939 total (app 1889 + plugins:llm 50), 0 failures/0 errors/0 skipped; `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. **Review fixes (2026-08-18):** staged restore files deleted on every HomeScreen outcome; 400MB wire cap enforced on the encrypted EXPORT output; restore count-belt charges leaf entries only; keystore-lost recovery defers the repository DEK swap until after the swap (no wrong-key reopen); dead "corrupted" rethrow removed — see `workspace/phase-138/REPORT.md`. |
| phase-139 | `DONE` | Fix R2-B1P-01 (MEDIUM) fixed (see `workspace/phase-139/REPORT.md` + `docs/security-report-round2.md` R2-B1P-01 row). The clipboard-scrub-on-lock gap is closed by choosing the finding's UNCONDITIONAL-clear option: the untracked NATIVE note-content copy surfaces (markdown-editor `OutlinedTextField` selection Copy, `HybridMarkdownEditor.kt:219`, and the OCR dialog's `SelectionContainer` Copy, `OcrResultDialog.kt:121`) stamp no `ClipboardGuard.recordCopy()` and are not interceptable reliably, so `lock()` (`NoteflowViewModel.kt:4057`, scrub at `:4073`) now calls the new `ClipboardGuard.scrubUnconditionally(appContext)` (`ClipboardGuard.kt:84`, shared private `clearPrimaryClip` `:88`) — the whole primary clip is cleared on EVERY lock path, no stamp check, no 60s window (were an "own copy in window" only, denying on a 0L stamp). A decrypted note body copied via the editor's native selection menu can no longer survive a lock. The windowed own-copy decision (`ClipboardScrubPolicy.shouldScrub`, `ClipboardScrubPolicy.kt:32`) stays on the ON_PAUSE hook (`MainActivity.kt:154`) as defense-in-depth so a brief app switch never wipes a foreign copy; API 26-28 `setPrimaryClip(ClipData.newPlainText("",""))` fallback retained (no floor regression). Both stamped copy sites keep `recordCopy()` for that windowed path. Grep-pins in `B2Ui2ClipboardScrubTest` (18 tests, 13 old + 5 new): the enum artifact-copy pin now walks ALL `ui/` `clipboardManager.setText(` call sites and requires each to be preceded by `ClipboardGuard.recordCopy()`, plus a new pin that raw `{set,clear}PrimaryClip` exists ONLY inside `ClipboardGuard.kt` (no bypass writer); the lock-block pin asserts `scrubUnconditionally` runs before `repository.zeroizeKey()` and before the `hasMasterPassword` gate and that `lock()` does NOT call the windowed `scrubIfOwnCopy(appContext)`; ON_PAUSE pin asserts `scrubIfOwnCopy(this)` is the windowed path and `scrubUnconditionally` never appears before `ON_STOP`. `B1Db08DecryptFailureTest` lock-region boundary token updated to match. `gradle testDebugUnitTest` 1944 total (app 1894 + plugins:llm 50), 0 failures/0 errors/0 skipped; `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-140 | `DONE` | Fix R2-B1A-03 + R2-b2b1-UI-02 + R2-B1P-05 (1 LOW + 1 LOW + 1 INFO, see `workspace/phase-140/REPORT.md` + `docs/security-report-round2.md` rows) — the three windows where decrypted content or attacker text can sit above/around a locked or paused vault are closed. **R2-B1A-03 (ON_PAUSE cover):** new pure-JVM `services/OnPauseCoverPolicy.kt` (decision table — cover only `hasMasterPassword && authenticated`, ANY resume dismisses) drives an opaque last-child full-screen `Surface` raised by `MainActivity` ON_PAUSE (`MainActivity.kt:170-179`) and cleared on ON_RESUME; the pause also dismisses the share-confirm dialog (attacker preview text) and the Command Palette (decrypted note-title list), both separate windows that would float above the activity cover. Locking on ON_PAUSE stays rejected (phase-60 picker/biometric/share-sheet breakage) — covers never break those returns. **R2-b2b1-UI-02 (dialog FLAG_SECURE):** new `@Composable secureDialogProperties(...)` (`ui/components/SecureDialogProperties.kt`) with `securePolicy` driven by pure-JVM `services/SecureDialogPolicy.kt` (reuses `SecureWindowPolicy.shouldApplySecureFlag(BuildConfig.DEBUG)` — `SecureOn` release-only, `Inherit` debug/emulator) wired into EVERY finding-listed dialog window: `CommandPaletteOverlay.kt`, `OcrResultDialog.kt`, `MarkdownPreviewScreen.kt` (transform-confirm + TextTools + LanguageDetection), `WebSearchDialog.kt`, `Phase16PluginDialogs.kt` (Dictation/ReadAloud/Translation), `Phase26PluginDialogs.kt` (Dictionary/Weather/UnitConverter/Outline/Citation) — compose-ui 1.7.6 `DialogProperties.securePolicy` is stable (no ExperimentalComposeUiApi), material3 1.3.1 `AlertDialog` accepts `properties`. **R2-B1P-05 (share-confirm state):** both states hoisted into the ViewModel (`NoteflowViewModel.pendingShareConfirm`/`pendingShare`, `:1368-1376`; pure-JVM models + `PendingSharePolicy` in `services/PendingShareState.kt`) so rotation (singleTask, no configChanges) cannot re-prompt an answered confirm (`MainActivity.readShareIntent` bails in-flight, `:787`); the confirm `AlertDialog` renders ONLY under `authenticated` (`:681`) so it can never float above `LockScreen`; `lock()` drops BOTH states via `PendingSharePolicy.clearOnLock` (master-password vaults only) so a pre-lock "Clip" never auto-applies at the next unlock (`NoteflowViewModel.kt:4154-4163`). No bytes move anywhere in the flow (B1-PLAT-2 staging untouched). Tests: `Phase140OnPauseCoverPolicyTest` (4) + `Phase140DialogSecurityTest` (7, comment-stripped source pins per file) + `Phase140ShareStateLockTest` (7) + `B1Plat02ShareConfirmationTest` (5, updated to the new wiring). `gradle testDebugUnitTest` 1963 total (app 1913 + plugins:llm 50), 0 failures/0 errors/0 skipped; `gradle assembleDebug` green (173.9 MB debug APK, SHA-256 `b17afc59…`). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-141 | `DONE` | Fix R2-B1P-02 + R2-B1P-03 + R2-b2b3-LOG-04 (3 LOW, see `workspace/phase-141/REPORT.md` + `docs/security-report-round2.md` rows) — export/share hygiene. **R2-B1P-02 (staging lingers / destroy-on-copy-failure):** new pure-JVM `services/ExportStagingPolicy.kt` (`cleanupAfterSaF` `:46-56`) — delivered → DELETE, copy-FAILED → KEEP (a dropped write never destroys the fresh export), no-data → DELETE, cancel → DELETE; `SaFExporter.kt:92-102` feeds every picker outcome through it (single branched callback, `:78-104`), and the plaintext-warning consent dismiss/Cancel also delete the staged decrypted file (`:118`,`:152`). A cancelled whole-vault PLAINTEXT export can no longer accumulate under `cacheDir/_exports`, and the success path no longer deletes on a failed SAF write. **R2-B1P-03 (chooser-less share + grantable-root retention):** `ExportShareHelper.shareFile` → `chooserForExport` (`ExportEnginePlugin.kt:72-91`) always wraps `ACTION_SEND` in `Intent.createChooser(send, CHOOSER_TITLE)`, so a default MIME handler can never auto-receive decrypted note bytes; `EditorScreen.kt:203-209` launches the chooser via an ActivityResult launcher and deletes the plaintext staging file when the chooser dismisses (delivered or dismissed, transfer-then-delete `:207`) and on a no-receiver launch failure (`:1497`). **R2-b2b3-LOG-04 (title echo in subject):** share subject is the generic `SHARE_SUBJECT = "Exported note"` (`ExportEnginePlugin.kt:67`,`:81`) — the filename-derived `EXTRA_SUBJECT, file.name` is gone and production-code-pinned as the ONLY `EXTRA_SUBJECT` writer. Tests: `ExportStagingPolicyTest` (8, decision table + full cartesian sweep through the fake ActivityResult seam) + `Phase141ExportHygieneTest` (7, source pins). `gradle testDebugUnitTest` 1978 green (0 fail/err/skip) + `gradle assembleDebug` green (debug APK 173,976,410 B, SHA-256 `3b93f592…`). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-142 | `DONE` | Fix R2-B1N-01 + R2-B1P-04 + R2-B1N-05 (see `workspace/phase-142/REPORT.md`): stream/network I/O — capped mid-stream LocalSend readers, `copyBounded` idle-progress bailout, (scheme,host,port) allow-lists. LocalSend peer bodies now read CAPPED mid-stream through the new pure-JVM `services/localsend/LocalSendBodyReadPolicy.kt` (register probe 2048, `httpPost` success 8192 + errorStream 512; `ResponseTooLargeException` on the first over-cap window, never slurp-then-truncate, `LocalSendSender.kt:264/:454-463`). `BoundedStreamCopier.copyBounded` gained the 16-consecutive-idle-reads bailout (`MAX_CONSECUTIVE_IDLE_READS`, mirroring the phase-81 `AttachmentIngestPolicy` sibling) so a hostile ContentProvider stream returning 0 forever can't hot-spin the IO thread. Plugin allow-lists normalized to `(scheme, host, effective-port)` triples via the new pure-JVM `services/HostPortAllowList.kt` (reuses `WebDavHrefResolver.Origin`; bare `host`/`host:port` → `https://host:443`, full `http(s)://` entries keep their own scheme/port; unparseable → null, additive-only) — wired into `CompileTimePluginPinStore` (`:112`), the shared `isHostAllowListed` gate (`:216`), `HttpsManifestTransport` (`PluginManifestFetcher.kt:130`) and `PluginDownloader` (`:147`). **Review fixes**: `WebDavHrefResolver.originOf` rejects empty-host + non-http(s) schemes (`WebDavHrefResolver.kt:50-58`) so `originOfOrNull`/`normalizeEntry` fail closed; the slurp-pin regex and the interleaved-zero-stream test fixture were corrected; REPORT.md created. Tests: `LocalSendBodyReadPolicyTest` (11), `HostPortAllowListTest` (16), `BoundedStreamCopierTest` (9), +1 each in `CompileTimePluginPinStoreTest`/`PluginDownloaderTest`/`HttpsManifestTransportTest`. `gradle :app:testDebugUnitTest` 1954 total 0 fail/err/skip + `gradle :app:assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-143 | `DONE` | Fix R2-B1N-04 (LOW) (see `workspace/phase-143/REPORT.md` + `docs/security-report-round2.md` R2-B1N-04 row): Web Capture is HTTPS-by-default — `WebPageFetchPolicy.ALLOWED_SCHEMES` is `{"https"}` only (`WebPageFetchPolicy.kt:22`), `validateUrl`/`rejectHop` gained the WebDAV-style `allowInsecureHttp` boolean (default false) so an explicit `http://` entry or https→http redirect is REFUSED unless the user ticks the per-fetch one-time "allow insecure HTTP" checkbox in `WebCaptureDialog` (HomeScreen.kt, same UX as WebDAV `allowInsecureHttp` `WebDavSyncService.kt:134-142`); bare/host-only input still defaults to `https://`; the flag threads `WebCapturePlugin.captureWebPage` (interface `NoteflowPlugin.kt`) → `WebCaptureEngine` → `WebPageFetcher.fetch(url, allowInsecureHttp)` with per-hop re-validation at `WebPageFetcher.kt:28`/`:49`. Tests: `WebCaptureExtractorTest` http-refused + http-with-opt-in cases, `B1Net04SsrfBlocklistTest` https-only entry/list + https→http redirect refused/opted cases. `gradle testDebugUnitTest` 2008 green (app 1958, llm 50; 0 fail/err/skip) + `gradle assembleDebug` green (first invocation hit the known transient DexArchiveMerger flake; immediate re-run succeeded). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-144 | `DONE` | Fix R2-B1N-02 + R2-B1N-03: deep network boundary (see `workspace/phase-144/REPORT.md`). **R2-B1N-02 — DNS-rebinding mitigation via resolve-and-pin**: new pure-JVM `services/DnsRebindingPolicy.kt` (resolves the hop ONCE via an injectable resolver, validates EVERY returned A/AAAA against the `SsrfHostPolicy` ranges fail-closed — any internal/mapped answer refuses the whole hop — and pins the actual CONNECT to the checked addresses via a layered `PinnedSslSocketFactory` whose `createSocket(Socket,…)` discards+rebuilds a platform pre-connected socket that reached a non-pinned address, plus a `PinnedHostnameVerifier` that re-checks the hop host). Wired per hop into ALL six reachable user-influenced/plugin transports: `HttpsTitleFetcher`, `WebPageFetcher`, `AppFacadeHost`, `DuckDuckGoClient`, `WeatherClient`, `DictionaryClient` (each gained an injectable `dnsResolver` param defaulting to `InetAddress.getAllByName`; resolve runs BEFORE the connection factory/socket). **R2-B1N-03 — egress/exec static-scan + classloader hardening**: `PluginFrameworkClassLoader` now refuses `java.net.*`/`javax.net.*` + exact `java.lang.Runtime`/`java.lang.ProcessBuilder` at resolution time (name-based, so string-built `Class.forName("java.net." + "Sock" + "et")` lands there too) while `java.lang.String` etc. still delegate; `ArtifactStaticScan` additionally rejects any parsed string/type carrying a dot-form `java.net.`/`java.lang.`/`javax.net.ssl.`/`javax.net.` prefix fragment at a word boundary (advisory layer; the classloader is the decisive gate). Tests: new `B1Net08DnsRebindingPinTest` (15) + new classloader/scan tests in `PluginBytecodeIsolationTest` (24 total) + stub DNS resolvers added to the existing `B1Net05`/`B2Dos04` transport tests. `gradle testDebugUnitTest` 1978 total green (0 failures — 1 pre-existing `WikiLinkParserCacheUnitTest` cancellation-timing flake passes on re-run, unrelated) + `gradle assembleDebug` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-145 | `DONE` | R2-B1C-02 + R2-B1C-03 fixed (see `workspace/phase-145/REPORT.md`). **R2-B1C-02 (LOW):** auth-bound WebDAV credentials are no longer silently deleted on `UserNotAuthenticatedException`/`KeyStoreException("user not authenticated")` — `WebDavCredentialStore.load()` is now a convenience over the classified `loadDetailed()` sealed result (`None`/`Credentials`/`AuthRequired`/`Corrupt`); auth-expiry = `AuthRequired` (blob INTACT, NEVER cleared), only a genuine AEAD/tag/decrypt failure clears. The biometric re-auth the finding said was missing is real: `prepareReauthCipher()` (decrypt cipher over the stored auth-bound blob) → `BiometricPrompt.CryptoObject` → `decryptWithReauthCipher(cipher)` after biometric success, mirroring `SecurityService.getDecryptionCipher`; a failed re-auth returns null WITHOUT clearing. `WebDavSyncDialog` classifies on launch (`WebDavSyncDialog.kt:123-139`): `AuthRequired` keeps remember-me checked, shows a non-alarming notice + "Unlock with biometrics" button (`:82-119`); the saved copy survives every path. No new permission/dependency (reuses `BiometricAuthHelper`; `MainActivity` is a `FragmentActivity`). **R2-B1C-03 (INFO):** DEK hex is eliminated from the immutable-String surfaces — `NoteflowDatabase` builds the SQLCipher passphrase directly as ASCII-hex BYTES (`ByteArray.toSqlcipherPassphraseBytes`, byte-identical to the old lowercase hex) and zeroizes them in the same try/finally after `SupportOpenHelperFactory(passphraseBytes)` + the plaintext-migration byte[] open; the restore pipeline carries backup+current DEKs as zeroizable ByteArrays end-to-end (`BackupV2Payload.dek` = bytes, `importBackup` zeroizes `v2.dek` in `finally`), hex Strings exist ONLY inside `validateAndPrepareRestoredDb` (DEKs copied first so the repo's live array is never touched, both owned copies zeroized in `finally`), and the SQLCipher String-API touches (`rekeySqlcipherDb`, `migrateFieldCiphertexts`, candidate opens) all feed zeroized ASCII bytes via `String.toAsciiBytes()`. Tests: `R2B1C02WebDavCredentialHygieneTest` (6: auth-expiry AuthRequired + blob survives, AEAD-tamper Corrupt + clears, CryptoObject re-auth round-trip, failed re-auth never clears, source pins for no-clear paths + UI wiring) + `R2B1C03DekHexScopingTest` (3: byte[] factory/open, exactly 3 `toHexString` sites confined to the validator, owned-copy zeroize pairs); updated old-signature pins in `B2Crypto04BackupPasswordTest`, `Phase138RestoreStreamingTest`, `B1Db07PlainZipRestoreRejectedTest`, `RestoreHardeningWiringTest`. `gradle testDebugUnitTest` green (0 failures) + `gradle assembleDebug` green. No schema change, no migration, no new deps. |
| phase-146 | `DONE` | R2-b2b2-DEP-02 + R2-b2b2-DEP-04 + R2-b2b2-DEP-03 fixed (see `workspace/phase-146/REPORT.md`). **DEP-02 (LOW):** committed wrapper — `gradlew`/`gradlew.bat`, `gradle/wrapper/gradle-wrapper.jar`, `gradle/wrapper/gradle-wrapper.properties:7` pins `distributionSha256Sum=20f1b117…aed78` (verified against the live gradle.org `.sha256`; wrapper executes + enforces it). Residual deferred to phase-147 (workflow edits need approval): CI still provisions `gradle-version: "8.13"` with no checksum (`android.yml:43`, `release.yml:24`, `llops.yml:137,312`) and the opencode installer is still `curl | bash` — `Phase146BuildIntegrityTest` guards CI from drifting off 8.13 until then. **DEP-04 (LOW):** `gradle/verification-metadata.xml` now `<verify-signatures>true</verify-signatures>` with a 64-key `<trusted-keys>` block + committed local keyring (`verification-keyring.gpg`/`.keys`, 73 master keys / 155 fprs, binary==armored, covers ALL trusted keys — offline verification, no key-server dependency) with a review-fix regeneration (`--export-keys`) that promoted 7 previously-unfetchable signer keys (incl. the androidx+com.android umbrella) into trusted+exported (trusted 57→64, `<pgp>` 10→11, keyring 65→73 masters; pre/post in `workspace/phase-146/REPORT.md`); `mavenCentral()` is one-way allow-listed in `dependencyResolutionManagement` AND `pluginManagement` (full-match regexes; unknown groups fail fast); regeneration provenance documented in `settings.gradle.kts`. **DEP-03 (INFO):** `dependencyInsight` confirms `okhttp 3.0.0 -> 4.12.0` losing candidate (POM-only, jars never resolve → CVE-2021-0341 unreachable); POM-only entries kept + documented; LLM-graph lines tracked for the tasks-genai bump. Tests: `Phase146BuildIntegrityTest` (6) + updated `B2Deps03DependencyVerificationTest` (5) + shared `CentralAllowlist` source. `gradle testDebugUnitTest` green (full suite, --rerun-tasks) + `gradle assembleDebug` green + `:app:compileReleaseKotlin` green. No schema change, no migration, no new deps, `.github/workflows/` untouched. |
| phase-147 | `DONE` | Fix R2-b2b2-DEP-01 (MEDIUM, see `workspace/phase-147/REPORT.md` + `docs/CI_PINNING.md`). **NOT-APPROVED PATH** — editing `.github/workflows/` needs explicit user approval per AGENTS.md; none was granted in this phase's context, so the workflow files are untouched and the edit is documented PENDING USER APPROVAL. Committed: `docs/CI_PINNING.md` gap report (all 19 `uses:` across `release.yml`/`android.yml`/`llops.yml` inventoried; mutable-tag → commit-SHA mapping computed via the GitHub REST API 2026-08-18 — `actions/checkout@v4`→`11d5960a…7262` (2026-07-16), `setup-java@v4`→`cf277c60…6c3` (2026-08-04, an upstream **deprecation commit** — operator should pin `v5` `b6effb05…bf961`), `gradle/actions/setup-gradle@v3`→`d9c87d48…ef70` (2024-07-15), `upload-artifact@v4`→`ea165f8d…a02` (2025-03-19), `android-actions/setup-android@v3`→`9fc6c4e9…5407` (2024-11-09) — plus newer majors, the Dependabot `# vX` comment form for maintainable pins, and a permission-scope reduction plan for `llops.yml:20-23` (job-level `permissions:`, drop `pull-requests: write`, `select-phase` read-only)); `.github/dependabot.yml` (github-actions ecosystem, weekly, grouped — GitHub's own infra applies it; also the home for DEP-02's deferred `distribution-sha256-sum`/opencode-installer wiring). Approved edit must leave `git grep "#v[0-9]" .github/workflows` empty. Constraints honored: no app code, no schema, no new deps, release fail-closed keystore gate untouched. Verification: `gradle testDebugUnitTest` 1993 tests 0 fail/err/skip + `gradle assembleDebug` green (debug APK 173,999,374 B, SHA-256 `beb0b00f…78c9`). |
| phase-148 | `DONE` | Fix R2-b2b3-LOG-01 + R2-b2b3-LOG-02 + R2-b2b3-LOG-03 (see `workspace/phase-148/REPORT.md`): no raw `e.message`/attacker-carried text reaches a user error surface or logcat. New pure-JVM `services/UiFailureTextPolicy.kt` fixed-text decision table (`restoreFailureMessage`/`recoveryMessage`/`backupFailureMessage`/`importSkippedMessage` + defensive `scrubForUi`), wired into `HomeScreen.kt:200-360/673/1496`, `NoteflowViewModel.kt:2407/2498/3843/3863`, `Dialogs.kt:86`, `EditorScreen.kt:968`, `LocalSendSender.kt:384/606-611`; `ImportExportService.kt:2148/2159` now throw a FIXED "unsafe relative path" refusal (no `$entryName` echo); `VoiceNoteManager.kt` 8 logcat sites + `ProtobufBrushLoader.kt` 3 catches log only `FailureLogPolicy.classNameToken(e)`. Tests: new `Phase148UiFailureTextScrubTest` (18) + updated `B1Db05ImportZipBombTest` source pin. `gradle testDebugUnitTest` 2012 green (0 failures) + `gradle assembleDebug` green. |
| phase-149 | `DONE` | Fix R2-b2b4-DOS-01 (MEDIUM, see `workspace/phase-149/REPORT.md`): `note_versions` retention cap + paged/batched decrypt + capped backup writer. **Retention cap**: new pure-JVM `services/NoteVersionRetentionPolicy.kt` (`MAX_VERSIONS_PER_PAGE`=20, `DECRYPT_BATCH_SIZE`=20, `REENCRYPT_BATCH_SIZE`=100, shared prune/paged SQL — single literal wired into the DAO `@Query` AND the raw sanitizers, `timestampMs DESC, rowid DESC` tie-break); `NoteRepository.createNoteVersion` (NoteRepository.kt:1436) inserts AND prunes the oldest past-cap rows inside ONE transaction (`NoteVersionDao.pruneVersionsForPage`, Daos.kt, count-gated via `exceedsCap`) so save/autosave/translate loops never accumulate an unbounded table. **Paged decrypt**: `getNoteVersions` (NoteRepository.kt:1461) reads ONLY the newest bounded window through `NoteVersionDao.getVersionsForPagePaged` (`LIMIT :limit OFFSET :offset`) — the pre-fix whole-history loop + `getVersionsForPage` whole-table read are gone — and `VersionHistoryBottomSheet` (pre-fix `:38-42` decrypted every body at once) now materializes lazily: keeps the pinned guarded `viewModel.getNoteVersions(page.id)` for the first window and streams further windows via new `viewModel.getNoteVersionsPaged` (`NoteflowViewModel.kt:3711`) as the list scrolls. Both whole-table re-key sweeps (migrateFieldRecordAad + reencryptPlaintextFields) page via `getVersionsForReencryptPaged`. **Capped backup writer**: `ImportExportService.exportBackup` prunes the STAGED snapshot (`pruneStagedSnapshotVersions`, DEK-opened) AFTER its checkpoint-then-copy — never the live vault, so a failed export can't delete history and an export never serializes oversized legacy history; `validateAndPrepareRestoredDb` runs new `sanitizeRestoredNoteVersions(db)` under the candidate key (raw SQL sharing the policy's PRUNE_KEEP_NEWEST_SQL) BEFORE re-key/field-migration, so a crafted ~5,000-row × ~50 KB-body archive (~250 MB heap) can never OOM on history open or restore. Schema-compatible — no migration. Tests: new `Phase149NoteVersionsRetentionTest` (16 after phase-149 review fixes — fake-DAO insert+prune model, `rowid` tie-break determinism, initial-window bound, staged-snapshot export prune pin; policy + source pins). `gradle testDebugUnitTest` 2029 tests: 2028 pass, 1 PRE-EXISTING failure `Phase148UiFailureTextScrubTest` (UNC-path redaction — reproduced on a clean stash of this branch, untouched by phase-149) + `gradle assembleDebug` green. |
| phase-150 | `DONE` | Fix R2-b2b4-DOS-02 + R2-b2b4-DOS-03 + R2-b2b5-FEA-04 (see `workspace/phase-150/REPORT.md` + `docs/security-report-round2.md` rows): canvas memory & render budget. **R2-b2b4-DOS-02 (MEDIUM — live-layer cap + resident-raster byte budget):** new pure-JVM `services/LayerRenderBudgetPolicy.kt` owns the single set of numbers the whole stack wires to — `MAX_LIVE_LAYER_COUNT`=16 (deliberately the SAME as `PsdExportPolicy`'s export cap), `MAX_RESIDENT_BITMAP_BYTES`=64 MB + `byteSize`, and the top-`zOrder`-`rowid` ordering SQL (`BOUNDED_TOP_LAYERS_ROOM_SQL` wired verbatim into `LayerDao.getTopLayersForPageBounded` + `KEEP_HIGHEST_Z_LAYERS_RAW_SQL` into the raw restore sanitizer). `NoteRepository.getLayersForPage` now materializes only the TOP-16 read (pre-fix: EVERY `layers` row, so a crafted 40-layer 1080x2400 page = ~416 MB native → OOM on open); `getLayerCountForPage` + `NoteflowViewModel.maybeNotifyLayersCapped` raise the ONE-TIME non-alarming notice on load (cleared at lock with `geometryCappedNotifiedPages`); `EditorScreen.onAddLayer`/`onDuplicateLayer` fail CLOSED at the cap with `layerLimitNotice()` (no silent limit). The renderer's unbounded `layerBitmapCache` map is replaced by `ui/components/LayerBitmapLruCache.kt` — a byte-budgeted LRU (`LinkedHashMap` access-order) whose `put` evicts least-recently-UNUSED full-page ARGB bitmaps back to `BitmapPool` and whose `clear` releases on invalidation/unmount. Restore path: `sanitizeRestoredLayerCounts(db)` (raw `pruneLayerPagesToLiveCap`, per-page `DELETE ... id NOT IN (top 16)`) runs under the candidate key BEFORE re-key/field-migration, and `pruneStagedSnapshotLayers` trims the export's STAGED snapshot (never the live vault), mirroring phase-149. **R2-b2b4-DOS-03 (LOW — minimap work budget):** `MinimapGeometryPolicy` gained `MAX_MINIMAP_POLYLINE_SEGMENTS`=400 + `MAX_MINIMAP_SAMPLED_STROKES`=120 + `strokeStepFor`/`pointStepFor`/`maxLineDraws` — the minimap's pre-fix fixed-1/2/4-point re-walk (~50,000 `drawLine`/frame at the phase-50 ~200k-point cap) is replaced by budgeted global strides; worst case ≤ 520 draw calls. **R2-b2b5-FEA-04 (LOW — unbounded page count):** `services/CanvasPageBudgetPolicy.kt` clamps end-of-stroke Y (`clampMaxStrokeY`, non-finite→0) to a sizable `MAX_DYNAMIC_PAGES`=2000 world ceiling and `clampCalculatedPages` bounds the derived render loop — a crafted `"y":1e9` stroke (short JSON, passes length gates) previously derived ~628k pages; the per-page `activeStrokeList.filter{ pdfPage==pageIdx }` is hoisted to one `groupBy` per frame (map lookup per page). Tests: `Phase150CanvasRenderBudgetTest` (23, pure-JVM fake-store + source pins). `gradle :app:testDebugUnitTest` 2052 total, 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (unrelated baseline, reproduced on clean stash per phase-149) + `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-151 | `DONE` | Fix R2-b2b5-FEA-02 + R2-b2b5-FEA-03 (see `workspace/phase-151/REPORT.md`): markdown main-thread perf. **R2-b2b5-FEA-02 (MEDIUM):** `MarkdownInlineMath.kt` no longer scans to end-of-string per backtick opener — `findBacktickRuns` pre-computes every maximal run in ONE left-to-right pass, `closingPositionIndex` buckets closer positions by run length (binary-search `findClosingBacktick`), and each code-span membership test goes through the `CodeRangeIndex` interval index (O(log R)) instead of a linear `codeRanges.any { index in it }`. Adversarial distinct-length backtick runs parse linearly. **R2-b2b5-FEA-03 (MEDIUM):** `HybridMarkdownEditor` holds the document as ONE one-pass-tokenized `MarkdownDocument` (`MarkdownBlockTokenizer.tokenize` = lines + blocks + checkbox candidates + candidates-by-block preindex); the per-keystroke path is `replaceBlock` — cached lines reused (no full `content.lines()`), only the edited window re-classified (`classifyWindow`), untouched blocks shifted by the line delta, and checkbox candidates recomputed only around the window (`incrementalCandidates` reuses unchanged before/after candidates — no full-document rescan); `toggleCheckbox(doc,…)` flips a marker with zero re-tokenization; candidate order/cursor preindex replace the per-block O(blocks×candidates) `filter`. Byte-identical output (tokenizer must still produce the exact same blocks/candidates — enforced by reference-equivalence tests). Tests: `Phase151MarkdownMainThreadPerfTest` (18 — equivalence vs the old scanner, length-scaling linearity, 5k-line typing bound, source pins). `gradle testDebugUnitTest` 2075 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. |
| phase-152 | `DONE` | Fix R2-b2b5-FEA-01 + R2-b2b5-FEA-05 + R2-b2b5-FEA-06 (see `workspace/phase-152/REPORT.md`): feature-data bounds. **R2-b2b5-FEA-01 (MEDIUM):** new pure-JVM `KnowledgeGraphEdgePolicy.kt` (`edgeCapFor` 300/600/1000, `cullEdgesToSurvivors` keeps edges whose BOTH endpoints survived node culling, dedups deterministically, top-K by max(endpoint updatedAt) + sourceId→targetId tie-break, fails closed); `KnowledgeGraphScreen` culls nodes first then feeds the SAME culled edge list to both `graphEdges` and physics `edgeRefs` (drawn edges ≤ physics edges, bounded ≤ 1000); Canvas tag-filter verdict memoized in `filteredById`/`pageFiltered` (no per-edge re-split per frame); `WikiLinkParser` gains `MAX_LINKS_PER_PAGE`=200 (`extractWikiLinks` caps the scan) + `MAX_TOTAL_EDGES`=100_000 (inline HashSet dedup, break-on-cap) and the whole-edge-set `edgeList.distinct()` materialization is gone. **R2-b2b5-FEA-05 (LOW):** `PaletteDoc` precomputes `lowerTitle`/`lowerBody`/`lowerTags` once at index build; `score()`/`rank()`/`matchesTagFilter` consume the cached lowercase and the query is lowercased once per `rank()`; the per-keystroke `lowerLog` HashMap is deleted; `makeSnippet(body, lowerBody, lq)` keeps original-case highlight. **R2-b2b5-FEA-06 (LOW):** `WaveformPeakMath.finiteOrZero` + `renderAmp` gate non-finite samples at parse time (both `parseWaveformJson` paths in `NoteRepository`) AND decimation AND render clamp (`AudioPlaybackCard` uses `renderAmp`; NaN/±Inf → 0.1f, never NaN bar geometry). Tests: `KnowledgeGraphEdgePolicyTest` (15), `Phase152FeatureDataBoundsWiringTest` (6 source pins, comment-stripped), plus FEA-05/FEA-06 additions to `CommandPaletteMathTest`/`WaveformPeakMathTest`. `gradle testDebugUnitTest` 2105 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (reproduced on a clean stash, untouched); `gradle assembleDebug` green. |
| phase-153 | `DONE` | Fix R2-b2b1-UI-04 + R2-b2b1-UI-05 (see `workspace/phase-153/REPORT.md` + `docs/security-report-round2.md` rows): post-lock UI channels. **R2-b2b1-UI-04 (LOW):** new pure-JVM `services/SnackbarLockPolicy.kt` (`VOICE_RECORD_DISCARDED_NOTICE`, `mayBufferWhileLocked(isAuthenticated, text)` decision table, `messageSurvivesLock`, `MAX_PENDING`=16); `NoteflowViewModel`'s snackbar channel is a bounded `StateFlow<List<SnackbarMessage>>` FIFO (clearable; `showSnackbar` emission API unchanged for ~140 call sites) — `lock()` CLEARS the queue in the hasMasterPassword teardown and the emission gate DROPS every non-survive message while locked (restore/import outcomes, note titles); `MainActivity`'s root collector is `LaunchedEffect(authenticated)` — locked = no rendering over the LockScreen + boundary dismiss + FIFO drain via `nextSnackbarMessage`/`consumeSnackbar`. The pre-fix host stayed composed outside the LockScreen branch (host at `MainActivity.kt:690-693`); the fix gates the collector, not the host. **R2-b2b1-UI-05 (LOW):** `VoiceNoteManager.release()` now returns a one-shot discard flag (set only in the DEK-null `finalizeRecording` fail-closed branch, `:268`; plaintext-temp sweep byte-intact); `EditorScreen` teardown (`:255`) republishes `VOICE_RECORD_DISCARDED_NOTICE` through the persistent pipeline — the sole survive-lock message — instead of the dead per-editor `recordingError` banner. Tests: `Phase153LockedSnackbarPolicyTest` (12 — decision table, notice honesty, source pins). `gradle testDebugUnitTest` 2117 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-154 | `NOT STARTED` | Feature: knowledge graph & wiki-navigation power-ups — node peek/quick-preview + click-to-open, per-notebook/cluster subgraph filter, backlinks breadcrumb + unlinked-mentions jump. |
| phase-155 | `DONE` | Canvas & brush workshop implemented + review-fixed (see `workspace/phase-155/REPORT.md`): two-finger undo/redo + quick-color ring gestures (pure-JVM `GestureRedoUndoClassifier.kt`/`QuickColorRingMath.kt`, pinch-guard, opt-in toggles) and `.inkbrush` brush-preset import/export (`BrushPresetFileCodec.kt` JSON bundle + raw-protobuf pass-through to `ProtobufBrushLoader`, `BrushPresetImportPolicy` caps, bounded read, SAF export via `ExportKind.BRUSH_PRESET`). Review fixes: fixed the two compile breakers (stdlib has no `kotlin.math.TWO_PI`; `LocalViewConfiguration.current`/`kotlinx.coroutines.withTimeoutOrNull` inside the restricted pointer-input scope), added 44 tests, routed the picker ingest through the bounded reader, updated docs. `gradle assembleDebug` green; full suite 2164 tests / 2 pre-existing failures (Phase148 UNC-path scrub, WikiLinkParser cache-cancel timing flake). |
| phase-156 | `DONE` | Feature: onboarding, empty states & first-run polish (see `workspace/phase-156/REPORT.md`). **Feature 1 — first-run onboarding:** new pure-JVM `services/OnboardingPolicy.kt` (`shouldAutoShow`, 3 `OnboardingStep`s, privacy-stance copy "Your vault stays on this device"); `SettingsManager.onboardingCompleted` (default false, distinct from `isFirstRun`/`tutorialCompleted`); `FirstRunOnboardingSheet` (`ui/components/FirstRunOnboarding.kt`) — non-blocking `ModalBottomSheet`, 3 instant steps (reduce-motion satisfied by construction — no animated transitions), per-step CTAs (`onCreateNote`/`onOpenPluginStore`/`onOpenWebDav`), Skip/Close/Get-started all one-time-dismiss. `HomeScreen` auto-shows it only when `OnboardingPolicy.shouldAutoShow(isFirstRun, hasMasterPassword, onboardingCompleted)`; the phase-125 tutorial is disarmed at that instant (they never stack); "Show help again" added to the ⋮ `MaintenanceMenu` so the guide stays reachable even after a master password is set. **Feature 2 — empty states everywhere:** `EmptyStateKit.kt` gained kinds `RECENT`/`KNOWLEDGE_GRAPH`/`VERSION_HISTORY`/`WEB_SEARCH` + a nullable `actionLabel` on `EmptyStateDecision`; `PLUGIN_STORE` is now query-aware ("No plugin matches" + "Clear filter" vs "Nothing in the store") and `HOME_GRID` returns CTAs ("Clear search"/"Create your first note"/"Create a note"). Wired: Recent + Trash tabs in `HomeScreen.kt`, knowledge-graph zero-nodes overlay with wikilink teaching copy + "Create a note" CTA (`KnowledgeGraphScreen.kt` `graphLoaded` flag — never flashes pre-load), tag vault (already present, `TagExplorerView.kt:88` `TAG_VAULT`), version history (`VersionHistoryBottomSheet.kt` informational, no CTA), web search (`WebSearchDialog.kt` new honest `SearchStage.NoResults` + "New search"), plugin store filter (`PluginStoreDialog.kt` `storeFilter` + "Clear filter"). No content renders while the vault is locked (arms-empty + auth re-check on every surface). **Feature 3 — home glanceable stats:** new pure-JVM `services/HomeStatsMath.kt` (`countDistinctWikiLinks` bounded by `WikiLinkParser.MAX_LINKS_PER_PAGE`, `daysSinceBackup`, `backupChip`, `chips`); `NoteflowViewModel` exposes `onboardingCompleted`/`lastBackupTimestamp` `StateFlow`s + `countCachedWikiLinks()` on the cached search corpus (no new DB reads); `HomeScreen` renders `n notes · n links · backup` chips + a "No backup yet" nudge above ⋮ "Backup to File"; `ImportExportService.exportBackup` records `lastBackupTimestamp` at the single success chokepoint (covers all producers incl. WebDAV + LocalSend), WebDAV push refreshes it. Tests: `OnboardingPolicyTest.kt` (gate matrix + step/privacy copy), `HomeStatsMathTest.kt` (link counting/dedupe, day math, chips), `EmptyStateResolverTest.kt` extended (new kinds + CTAs). `gradle testDebugUnitTest` 2187 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-157 | `DONE` | Feature: plugin ecosystem & store UX (see `workspace/phase-157/REPORT.md`). **Feature 1 — capability browser + store filter:** new pure-JVM `services/PluginCapabilityDirectory.kt` — `rows(entries, isInstalled)` maps every `PluginCapability` to its serving catalog plugins with an honest `Coverage` verdict (`INSTALLED` / `AVAILABLE_ON_STORE` / `UNSERVED`); the still-unserved capabilities (today: FileTransfer; Assistant until the downloadable LLM is installed) are surfaced in the store BEFORE a request would fail loudly. `PluginStoreDialog` gains a "Plugins | What can plugins do?" view-mode toggle + a `StoreCapabilityRow` per capability (installed vs store-available lists, "No plugin yet" copy for unserved) + a compact horizontal per-capability `FilterChip` row (with "All"), combined with the phase-156 text filter (match-less + capability-filtered empty state has ONE "Clear filter" CTA that resets both). **Feature 2 — update UX with notes + Update all:** new pure-JVM `services/PluginUpdatePromptPolicy.kt` — `notesForDisplay` (control-char collapse, 240-char bound, then `UiFailureTextPolicy.scrubForUi` — R2-b2b3-LOG-03: hosted release notes never reach the approval dialog raw), `versionDeltaText`, `updateAllPlan` (deterministic, deduped, per-download `UpdateAllItem`s) and `batchSummary`. The approval dialog now renders scrubbed "What changed: …" notes; `PluginStoreDialog` adds an "Update all" button; `NoteflowViewModel.updateAll()` checks, then walks each offered update through ITS OWN approval dialog (`openNextPendingUpdate`) — declining any approval ends the batch, and nothing updates without a per-download "Approve & install" (compile-time pins + TLS pinning intact). **Feature 3 — plugin diagnostics:** new pure-JVM `services/PluginDiagnosticsRowPolicy.kt` (`servedCapabilitiesLabel`, `optInLabel`, `lifecycleLabel`, `scrub`, `reasonLine`, `lastInvocationLine`, `footer`); `PluginSettingsDialog` per-plugin rows now show the capabilities/opt-in/lifecycle footer, and `state.reason` + last-invocation summaries are scrubbed (phase-148 rule) before rendering. Tests: `PluginCapabilityDirectoryTest` (9), `PluginUpdatePromptPolicyTest` (10), `PluginDiagnosticsRowPolicyTest` (9). `gradle testDebugUnitTest` 2216 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-158 | `DONE` | Feature: reading/focus mode + share-sheet capture polish + lightweight home widget (deferred ROADMAP 22.5, see `workspace/phase-158/REPORT.md`). **Feature 1 — reader/focus mode:** new pure-JVM `services/ReaderModePolicy.kt` (`MAX_COLUMN_WIDTH_DP`=680f, `BODY_LINE_HEIGHT_MULTIPLIER`=1.35f, `readerLineHeightSp` proportional to the ALREADY-SCALED theme font, no absolute `.sp` in the policy, `defaultReaderForCapturedNote`, `READER_TOGGLE_LABEL`); `MarkdownPreviewScreen` gains `initialReaderMode`/`onConsumeReaderMode` (one-shot, `LaunchedEffect(page.id)`), a reader `FilterChip` with INSTANT no-animation swap (reduce-motion satisfied by construction), a read-only render path that NEVER composes `HybridMarkdownEditor` (long-press-safe) with a centered `widthIn(max=680.dp)` capped column, widened leading threaded via file-local `LocalReaderMode` CompositionLocal into headings+paragraphs, and editing chrome stripped in reader mode (Save/Smart-Assistant/Plugins hidden; History+Backlinks stay). Captured notes open in reader mode by default (`readerModeRequestedFor` `rememberSaveable`, consumed by both MarkdownPreviewScreen call sites; `.txt`/`sourceFileType=="text"` pages route there too). **Feature 2 — share-sheet capture polish (22.5a):** `ShareCaptureMode` (`NEW_NOTE`/`APPEND_TO_ACTIVE`, `fromToken` fails closed), `PendingShareConfirmState.stagedAtMs`, `PendingShareState.captureMode`; `PendingSharePolicy` gains `CONFIRM_HOLD_EXPIRY_MS`=10min + `isExpired`, `resolveAppendTarget` (append ONLY text-only clip + active page, else honest `CREATE_NEW_NOTE`), `deferredAppliesNow`, and the NON-SECRET captured marker (flag+stamp, never content; `SettingsManager.capturedSharePending/AtMs`). `stagePendingShare` stamps+marks, `confirmPendingShare(captureMode)`, `consumePendingShare`/`cancelPendingShareConfirm` clear the marker, `lock()` drops states+marker for password vaults (fail-closed). Dialog: new-vs-append radio choice under auth (disabled+explained when no active note / image clip), 10-min hold expiry auto-cancel (re-checked so answered confirms survive), choice `rememberSaveable`. Apply effect routes through `resolveAppendTarget` → `appendSharedContentToPage` (reads latest committed body via `readMarkdownNoteBody`, writes via `saveMarkdownNoteBody` — B2-UI-1 lock-gated) or `createNoteFromSharedContent`. **Feature 3 — home widget (22.5b, IN-BASE launcher-only):** `WidgetLaunchPolicy.kt` (pure-JVM extras contract) + `ui/widget/QuickCaptureWidget.kt` (`AppWidgetProvider`, RemoteViews single PendingIntent → MainActivity with `EXTRA_QUICK_CAPTURE=true`, `updatePeriodMillis=0`, NO vault/IO/permission in widget process) + `res/xml/quick_capture_widget_info.xml` (1x1/2x1, `home_screen`, resizeable) + layout/drawable/strings + manifest receiver `exported="false"`. `MainActivity.handleQuickCaptureIntent` (typed extras map, true-only) in `onCreate`+`onNewIntent`; `LaunchedEffect(authenticated, quickCaptureRequested)` fires `addPage` ONLY when unlocked (locked tap creates nothing; flag consumed). Tests: `Phase158ReaderModePolicyTest` (6), `Phase158ShareCapturePolicyTest` (8), `Phase158WidgetLaunchPolicyTest` (6); updated `B1Plat02ShareConfirmationTest` + `Phase140ShareStateLockTest` for the new `confirmPendingShare(clipMode)` wiring. `gradle testDebugUnitTest` 2240 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new base-APK deps, `.github/workflows/` untouched. |
| phase-159 | `DONE` | Release-APK build + verification for the Kali round-2 dynamic pass (see `workspace/phase-159/REPORT.md` + `docs/kali-report-round2.md` "APK target"). `gradle assembleRelease` (Gradle 8.13, NO wrapper) → **BUILD SUCCESSFUL in 7m 8s, 113 tasks (113 executed)** from commit `a9d8918c933ce498ed4ad7c2780218ad2b606392`; workflow-restored env keystore `/home/runner/work/_temp/release.keystore` (PKCS12, alias `inkflow`, cert SHA-256 `69:63:6E:DB:…:01:96`) passed `keytool -list` + `validateSigningRelease`; release build ran `:app:minifyReleaseWithR8` (`isMinifyEnabled = true`, `app/build.gradle.kts:70`) + `lintVitalRelease`. Artifact `app/build/outputs/apk/release/app-release.apk` = 142,339,635 B ($>$1 MB), **apksigner verify: `Verifies`, v2 scheme**, 1 signer (DN `CN=InkFlow Release, OU=Dev, O=Authorss81, L=Unknown, ST=Unknown, C=US`, cert SHA-256 `69636edb…c50196` matching the keystore, public-key SHA-256 `0328af28…e7030`), versionCode `2` / versionName `1.0.0` (env VERSION_CODE/VERSION_NAME unset ⇒ `app/build.gradle.kts:18-19` defaults), applicationId `com.aistudio.inkflow.app.bkxjrz`, **APK SHA-256 `54feb16c3533c6966f071414095c2256966c69161d845d9a67f7224d82bb455a`**. NO build-config fixes were needed (clean first-run build); NO app feature code touched; fail-closed B1-PLAT-1 signing (`app/build.gradle.kts:133-153`) intact; `docs/RELEASE.md` untouched. Full unit-test suite NOT run (owned by other phases); no lint/test failures surfaced inside assembleRelease's lifecycle. APK uploaded as the `noteflow-release-apk` artifact (binary not pushed to git) — **READY FOR KALI (phase-160)**. |
| phase-160 | `DONE` | Kali round-2 **STATIC** security analysis of the release APK (re-unblocked by `b1f533f`; dynamic-only checks DECLARED `DYNAMIC-DEFERRED`, see `workspace/phase-160/REPORT.md` + `docs/kali-report-round2.md`). Artifact identity CROSS-CHECKED: at-hand APK SHA-256 `9ce99c1b3dbcbdb9fa6080961b9e043c8ead6e9828a4e1286958cd331743233f` (142,344,579 B, versionCode 2 / 1.0.0, signer cert `69636edb…c50196` **matches** phase-159) — NOTE the SHA differs from phase-159's recorded `54feb16c…` (the pre-downloaded artifact is a newer post-phase-159 build; HEAD `82c11f6`). **Findings (27 rows `R2-KS-01..07`, `R2-KS-10..19`, `R2-KS-20..29`, all evidence-backed):** 2×MEDIUM — lingua `language-models/` 207.6 MB/75 langs + ML Kit OCR/translate native+assets BAKED into the base APK (base-APK-size plugin constraint violated; round-1 Phase-32-NEW-01 now larger); 1×LOW no ABI splits (re-confirmed), 1×LOW release hygiene (`DebugProbesKt.bin`/`kotlin-tooling-metadata.json`/`firebase-*.properties` in-payload); rest INFO PASS-verified: allowBackup/backup-rules off, only MainActivity exported + share-sheet SEND filters, FileProvider restricted (`apk/`+`exports/` only), QuickCaptureWidget exported=false + immutable PI, FLAG_SECURE `:389`, WebDAV HTTP-gated-allowlist + cross-host auth-strip, DB quarantine `*.corrupt-<ts>` + HMAC recompute-on-restore, plugin sandbox loader + static scan + pin compare constant-time, placeholder plugin cert-pin `sha256/AAECAwQFBgc…Hh8=` **fails closed** (update path inert until real pin ships), no hardcoded secrets in 28,409 dex strings, sqlcipher.so RELRO+BIND_NOW. MobSF 4.5.2 installed/boots but 142 MB scan exceeded the 2-core runner's command timeout (documented WHY; evidence over tooling). 12 dynamic checks D1–D12 declared with operator reproducers (PBKDF2 timing, DEK read hooks, zeroization proof, `run-as` DB forensics, quarantine/restore attacks, FLAG_SECURE, biometric invalidation, pin fail-closed, WebDAV MITM, LocalSend TOFU, voice-note at-rest, WebDAV blob AEAD). All 5 batches committed+pushed (`8b02d43`, `ebe14ac`, `07e5c0f`, `50b07d1`, final). |
| phase-162 | `DONE` | Build/code failure fixes + verify decryption-safety edge cases (see `workspace/phase-162/REPORT.md`). **Part A:** (A1) the merge-leftover duplicate `renameTag`/`deleteTag` in `NoteflowViewModel.kt` are NOT in the current tree — single implementations at `:2312`/`:2345` (phase-134 `viewModelScope` + `writeGuardedAgainstLock` dispatch), 178 top-level funs all unique, braces balanced (744/744) — VERIFIED-OK. (A2) `plugins/llm/build.gradle.kts` `project.exec` (deprecated, breaks Gradle 9 config-cache isolation) replaced in BOTH `signPlugin` and `verifyPluginSignature` by a `ProcessBuilder` inside `doLast` via the new `runExternal(cmd, failureMessage)` helper (list form = no shell, args exact; output streamed+discarded so keystore passwords never reach the log; `GradleException("… exit code $exitCode")` on non-zero) — FIXED and proven BOTH ways: positive path signed/verified/metadata'd with a throwaway `/tmp` keystore, and the unset-env run still FAILS CLOSED at the phase-76 `whenReady` gate. (A3) `gradle/verification-metadata.xml` gained the `<trusted-artifacts>` allow-list for `com.android.tools.build` / `org.jetbrains.kotlin` / `androidx.databinding` after the verify flags (verification itself untouched) — FIXED. `gradle :plugins:llm:packagePlugin :app:assembleDebug` BUILD SUCCESSFUL; `testDebugUnitTest` 2240 green / 1 pre-existing `Phase148UiFailureTextScrubTest:234` UNC-path failure (untouched). **Part B (all VERIFIED-OK, no rewrites):** B1 `DecryptFailurePolicy` marker + per-note dedup ledger (`NoteRepository.kt:108-113`) → `setCorruptionDetected` + `_corruptionBlocked` → `CorruptionRecoveryScreen` (`NoteflowViewModel.kt:1705-1712`, `MainActivity.kt:555-560`); threshold is 10 distinct notes (not ~3). B2 `readDekResult` sealed `NoBlob/Unlocked/AuthRequired/KeyLost` (`SecurityService.kt:200-224`), `getOrCreateDek` THROWS `KeystoreKeyLostException` on `KeyLost` (`:254`), `_keystoreKeyLost` in `dbGate` (`NoteflowViewModel.kt:1369`) → `KeystoreKeyLostScreen` (`MainActivity.kt:561-568`). B3 AAD = `table|recordId|fieldName` (`EncryptionService.kt:340-341`) and every write binds fresh ciphertext to the target id (`createPage`/`createNoteVersion`/`saveStrokesForPage`/`saveMediaEmbedsForPage`); `movePage` mutates only `sectionId` (not in AAD) (`Daos.kt:158-159`); duplicate-layer copies strokes in memory with new UUIDs and re-encrypts on the next save (`EditorScreen.kt:861-884`). B4 `firstDataInitDone` gate (`NoteflowViewModel.kt:172`, verify at `:1240`) + WAL-aware `vaultFilePresentAtStart` + session-end FULL checkpoint + re-arm (`NoteflowDatabase.kt:518-536`) jointly rule out a cold-start mid-read WAL false Mismatch. B5 `StrokeGeometryPolicy` caps + `saveStrokesForPage` save-gate + paged DAO read + plaintext pre-Gson guard + `capLoadedPoints` + `deserializeStrokes` guard + `AttachmentIngestPolicy.boundedReadBytes`/`readTextHead` at all picker/text sites — constants are 20k/200k (not 2k/50k). B6 per-page `pageSaveLocks` Mutex (`NoteRepository.kt:1113`) serializes autosave (`autosaveStrokes`), dispose/on-pause (`disposeEditorPageFlush`→`flushEditorPageSave`), and background unlock-flush (`flushPendingEditorSaves`) since ALL full-page writes funnel through the three lock-taking functions. |
| phase-163 | `DONE` | "Don't show again" now persists for the data-recovery screens, keyed to the recovery EVENT (see `workspace/phase-163/REPORT.md`). New pure-JVM `services/RecoveryDismissalPolicy.kt` decision table (blocking ∧ same event timestamp ⇒ suppressed; new timestamp / un-keyable legacy event ⇒ MUST show, fail closed); `DatabaseSecurityHelper` gains event-keyed dismissal prefs (`corruption_dismissed_timestamp`, `keystore_lost_{event_alias,event_timestamp,dismissed_timestamp}`) via `.commit()`; `NoteflowViewModel.dismissCorruptionRecovery`/`dismissKeystoreKeyLostRecovery` persist the keyed dismissal, and every gating site (ctor, open-failure handler, decrypt-fail escalation, both keystore-lost detection branches) is recomputed through `RecoveryDismissalPolicy.mayShow`. Review fixes (2026-08-19): the missing `import` that broke `assembleDebug` was added (build green); the "Don't show again" checkbox + Dismiss button were actually WIRED into `CorruptionRecoveryScreen`/`KeystoreKeyLostScreen` (`MainActivity.kt`, calling the ViewModel dismiss methods — the original commit left them as unreachable dead code); keystore restore + start-fresh call `clearKeystoreLostDismissal` (`.commit()` for the pre-`exitProcess` exit) so a fixed vault is never nagged; `DekReadResult.AuthRequired` now carries `wrapperAlias` so ALL keystore-lost detection sites use ONE single-read result identity (`security.currentWrapperAlias()` removed — no re-reading the blob); `corruptionTimestamp` is now a computed getter (a mid-session fresh stamp shows on the screen). The phase-87 DB-integrity tripwire banner stays per-session (untouched). Tests: new `Phase163RecoveryDismissalTest` (policy decision table + wiring pins). `gradle assembleDebug` green; full suite green except the 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-164 | `DONE` | Tag vault/explorer is scoped to the CURRENTLY selected notebook (see `workspace/phase-164/REPORT.md`). Pre-fix `TagExplorerView.kt:48` loaded `viewModel.loadAllActivePages()` (WHOLE vault) and `WikiLinkParser.buildTagHierarchy` aggregated every notebook's tags globally. Now `NoteflowViewModel.loadScopedTagHierarchy` (`:4064`, guarded by `withLockedPoolGuard`) queries only the selected notebook's pages via `repository.getPagesForNotebookOnce` (page→section→notebookId; NO schema change) + the notebook's own CSV tag list; new `WikiLinkParser.buildScopedTagHierarchy` (`:526`) scans only those pages (text `#tag`s + CSV `tags` field) plus `notebookTags`, reusing the shared bounded/cancellable `collectTextTags` + depth-bounded `buildTagTree` (whole-vault `buildTagHierarchy` behavior byte-identical), cached per unlock epoch + scope fingerprint (new `scopedTagRecomputes` metric). `TagExplorerView` keys its LaunchedEffect on `(notebookId, notebook tags)` and runs the build in-effect (mid-switch scans cancelled, B2-DOS-11); `HomeScreen` clears a stale tag filter on notebook switch. Tests: new `Phase164TagVaultScopingTest` (8: A-tags-never-in-B, notebook switch re-vaults + re-caches, moved page's tags follow it, CSV+notebook tags included, case-insensitive tag-identity merge (review-fix), empty state, source pins), `Phase134LockVaultInflightTest` tag-explorer pin updated. **Review fixes (2026-08-19):** notebook-tag list normalized identically to page tags (lowercase + `/`-trim) so mixed-case inputs merge into one node; CSV-tag pass bounded to the same `MAX_SCAN_PAGES` scan set as text tags; `loadScopedTagHierarchy` scoped by the caller's captured `notebookId` (the effect key) not re-read `selectedNotebook.value`; `TagExplorerView` clears stale tag state on a lock-race auth re-check; `HomeScreen` treats zero-member tag pickups as no-ops and filters the active tag across the whole notebook's pages (`allActivePages`) instead of only the current section. `gradle :app:testDebugUnitTest` 2260 green / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle :app:assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-165 | `DONE` | Gallery thumbnails beautified (see `workspace/phase-165/REPORT.md`). `ui/components/GalleryView.kt` `GalleryCardItem` redesigned: flat 180 dp square box → Material 3 cards — 20 dp rounded corners, tonal `surfaceVariant` container with a subtle `primaryContainer` wash fading from the top, 3 dp elevation, fixed portrait 10:16 aspect ratio (so `GridCells.Adaptive(minSize=168.dp)` + 12/14 dp spacing stays balanced on phones AND tablets), rich preview (type badge, overflow-ellipsized `titleSmall` title, first ~2-3 preview lines, pinned `PushPin`, ≤3 tag chips + "+N", updated date with `Schedule` icon). Empty-text pages show a graceful tonal circular-icon placeholder ("Ink & canvas page" / "PDF page" / "Image page" / "Empty page"). Normal typography only (phase-127 — no `10.sp` overrides; the old `fontSize = 10.sp` uses are gone), ripple from the clickable `Card` (no multi-select in the app yet, so no separate selection state). Public `GalleryView(pages, viewModel, onOpenPage, modifier)` API unchanged — `HomeScreen.kt:1333` (pageViewMode=1) keeps working. No canvas rasterization, no image generation, no new deps, no schema change, `.github/workflows/` untouched. `gradle :app:compileDebugKotlin` green; full `assembleDebug` green. |
| phase-166 | `DONE` | Compact-screen overflow pass (see `workspace/phase-166/REPORT.md`): six live fixed-width-Row clip sites at 360dp fixed. (1) `WebDavSyncDialog` primary actions moved from the `AlertDialog` confirm row to stacked full-width body buttons (`WebDavSyncDialog.kt:305-388`; `confirmButton` = Close only, `:391`). (2) `CalendarView` stacks the date summary above the "New Note for Date" button (`CalendarView.kt:188-212`). (3) HomeScreen import orientation chip row is horizontally scrollable (`HomeScreen.kt:1711-1714`). (4) HomeScreen filtered-by-tag banner text is `weight`+`maxLines`+ellipsis (`HomeScreen.kt:1233-1245`). (5) `InteractiveTutorial` puts "Skip Tutorial" on its own line and end-aligns the Back/Skip-step/Next row (`InteractiveTutorial.kt:338-402`). (6) `MarkdownPreviewScreen` app bar decluttered: title row = title + Reader chip only; view-mode / split-orientation / Serif chips moved to a full-width horizontally scrollable sub-bar beneath the app bar (`MarkdownPreviewScreen.kt:564-635`; Serif reachable in reader/preview modes, phase-158 behavior preserved). Also the KnowledgeGraph selected-node card title wraps/ellipsizes (`KnowledgeGraphScreen.kt:674-680`). Regression guard: `Phase166LayoutOverflowTest` (8 source-pinning tests, `:29`). Review-fixes (2026-08-19): the WebDav overflow pin was strengthened to assert the actions are full-width BODY buttons with the confirm slot holding only Close, and the Serif toggle kept its phase-158 gate (reader/preview/split, hidden in the plain editor). `gradle testDebugUnitTest` 2268 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-167 | `DONE` | Bottom system-nav bar no longer overlays messages + calendar rows — dynamic insets, no hard-coded heights (see `workspace/phase-167/REPORT.md`). Diagnostic: there is NO bottom `NavigationBar` in the app and no second in-app bottom bar — the reported "back/home + another button at the bottom" is the OS 3-button/gesture **system navigation bar** under edge-to-edge rendering. **FIX 1** — root `SnackbarHost` (`MainActivity.kt:798-809`; the app's entire transient-message channel is snackbar-only — no Toasts exist anywhere) sat at root Box bottom with no inset → added `.navigationBarsPadding()` (kept `align(BottomCenter)`; deliberately NO `safeDrawingPadding()` to avoid double top-inset at screen level). **FIX 2** — the three recovery screens render edge-to-edge with NO Scaffold: `RestoreBlockedScreen` (`:1285-1295`), `CorruptionRecoveryScreen` (`:1366-1376`), `KeystoreKeyLostScreen` (`:1489-1499`) — their scroll modifiers gained `.navigationBarsPadding()` + `.statusBarsPadding()` (review-fix: these Scaffold-less screens draw under the transparent status bar at top too). Verified NOT broken: the four content screens already apply Scaffold `innerPadding` (m3 `ScaffoldDefaults.contentWindowInsets` = `systemBarsForVisualComponents`, decompile-confirmed) — HomeScreen `:778-783`, EditorScreen `:1810-1814`, MarkdownPreviewScreen `:552-556` (+`.imePadding()` `:562`), KnowledgeGraphScreen `:385-389` — untouched; CalendarView pages list is `weight(1f)`-bounded inside that padded column (`CalendarView.kt:229-231`), so the last row scrolls above the bar. Result: works on 360dp small screens AND large screens (pure runtime insets, no pixels). Regression guard: `Phase167BottomNavOverlayTest` (8 source-pinning tests). `gradle testDebugUnitTest` 2276 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. **Triage of Kali round-2** (`docs/kali-report-round2.md`) added phases **175 + 176** (R2-KS-21 ML Kit OCR/translation move-out → phase-175; R2-KS-27 release hygiene + R2-KS-24 R8 mapping → phase-176). |
| phase-168 | `DONE` | Cold start opens the LAST-USED notebook (see `workspace/phase-168/REPORT.md`). Root causes: `lastNotebookId` (`SettingsManager.kt:94-96`) was NEVER WRITTEN (grep-verified), and `initializeDataCore()` (`NoteflowViewModel.kt:1866-1889` pre-fix) only restored the notebook when `activeNotebookId` AND a valid `activeSectionId` matched — a stale/deleted section pref dropped the notebook too and fell through to `ensureDefaultNotebookAndSection()` (first/`default_nb`). Fix: `selectNotebook()` now persists `settings.lastNotebookId` on EVERY selection change (the notebook-switcher chokepoint); `onCleared()` persists on exit; `initializeDataCore()` reads `lastNotebookId ?: activeNotebookId`, restores the notebook independently of section validity (stale section → first-section via `observeSections`), falls back to the FIRST existing notebook when the last was deleted (and persists it), or default_nb+default_sec on a brand-new vault. Prefs only, no schema change, security model untouched. Tests: `Phase168LastNotebookRestoreTest` (10, behavior model + source pins). `gradle testDebugUnitTest` 2285 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. `.github/workflows/` untouched. |
| phase-169 | `DONE` | "Pages become Unreadable (decryption failed) after export/import" fixed + fail-closed hardening (see `workspace/phase-169/REPORT.md`). Diagnosis: the export is a faithful checkpoint→verify→stage snapshot (no AAD/id/DEK mutation — `ImportExportService.exportBackup`), so export itself never corrupts a row; the reported symptom was the cross-key RESTORE path silently stranding un-re-keyable rows under the old DEK. Fix (real import bug): `reencryptFieldValue` → pure-JVM `reencryptFieldOutcome` (`FieldReencryptOutcome.{Migrated,LeavePlaintext,AuthFailed}`); `migrateTable` counts `AuthFailed` rows and throws `RestoreReEncryptionException` — restore is REJECTED before any swap, temp DB quarantined, fixed `UiFailureTextPolicy.RESTORE_REENCRYPT_FAIL_TEXT` surfaced (never the raw count). Data-loss guard: `NoteRepository.updatePageBody`/`updatePageTitleAndTags`/`renamePage` refuse to persist the literal `UNREADABLE_MARKER` (typed `UnreadableContentWriteException`) — an editor/rename pre-filled with the marker could permanently overwrite the recoverable encrypted original — every `NoteflowViewModel` save/rename/flush site catches it → `DecryptFailurePolicy.UNREADABLE_ROW_GUIDANCE`; `autoTagLanguageOnSave` skips it silently. Round-trip: `Phase169ExportImportRoundTripTest` (encrypt→re-key→decrypt for all 7 `fieldEncryptedColumns`, legacy global-AAD migration, same-DEK identity, missed-re-key marker control, classification + source pins). Reviewer-found gaps closed: `renamePage` guard + catch, `autoTagLanguageOnSave` crash, cursor `try/finally`, quarantine on reject, quarantined-copy evidence. `gradle testDebugUnitTest` 2296 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-170 | `DONE` | Base-APK size: lingua corpus trimmed to the 24 used languages + release ABI splits (see `workspace/phase-170/REPORT.md`). **Phase-32-NEW-01 (MEDIUM) FIXED**: `app/build.gradle.kts:125-127` strips the 51 unused `language-models/<iso>/**` resource dirs via `packaging.resources.excludes` (driven by the `LINGUA_UNUSED_LANGUAGE_ISOS` constant `:13`, source-pinned by new `Phase170LinguaTrimTest` against `LanguageDetectionCore.SUPPORTED`); debug APK 174,184,662 → 128,717,267 B and release universal 142.0 MB → 96,878,100 B (language-models packed 80,227,557 → 34,791,639 B, 75 → 24 dirs; the 24 kept dirs are byte-identical to the lingua JAR, `LanguageDetectionTest` green). **Phase-32-NEW-02 (LOW) FIXED**: release-only ABI splits (`splits { abi { } }`, `:140-147`, gated on the requested-task list so `assembleDebug` stays monolithic) emit `app-arm64-v8a/-armeabi-v7a/-x86/-x86_64-release.apk` (53.3–56.3 MB each, single-ABI 6 native libs) + `app-universal-release.apk` (all 4 ABIs, sideload/emulator channel); every split passes `apksigner verify` (v2, `CN=InkFlow Release` — B1-PLAT-1 fail-closed intact). `gradle assembleDebug` + `assembleRelease` green; `gradle testDebugUnitTest` green except the 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched); `WikiLinkParserCacheUnitTest` cancellation flake reproduced-then-green on re-run. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-171 | `DONE` | Release signing v3 + plugin-channel operator runbook (see `workspace/phase-171/REPORT.md`), closing the two remaining INFO packaging findings from phase-161 triage. **Phase-32-NEW-03 FIXED**: `enableV3Signing = true` added to `signingConfigs.create("releaseConfig")` (`app/build.gradle.kts` ~`:57`) — root cause was minSdk=26 (< AGP 8.7.3's automatic-v3 threshold of 28), now empirically proven: baseline release APKs verified `v2:true/v3:false`, post-fix every ABI split + universal verifies `v2:true` **and** `v3:true` (`apksigner verify --print-certs -v`, real `CN=InkFlow Release` identity `69636edb…c50196`; v1/v4 untouched, minSdk NOT bumped, B1-PLAT-1 fail-closed gate verified: keystore-less `assembleRelease` still fails with "Release build refused"). **Phase-32-NEW-04 RUNBOOKED**: new `docs/PLUGIN_CHANNEL.md` (file/line `HostedPluginManifest.kt:242-243`, correct LEAF-cert-DER `openssl` pin recipe — the RFC-7469 `-pubkey`/SPKI variant is rejected by this app, verified — go-live checklist, cert-renewal procedure), referenced from `docs/RELEASE.md` + `docs/PLUGINS.md`; the placeholder itself intentionally unchanged. `PinnedCertHashTest` extended 4→9 with the fail-closed contract (`matches` true for known-good, FALSE for placeholder/wrong/near-miss/wrong-prefix/blank pins) + an exact-value placeholder source pin. `gradle assembleDebug` + `assembleRelease` green; `B1Plat01ReleaseSigningTest` (8) + `HttpsManifestTransportTest` (9) + `PinnedCertHashTest` (9) green; `gradle testDebugUnitTest` 2306 with only the 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched). No schema change, no new deps, `.github/workflows/` untouched, placeholder pin unchanged (no fake pins). |
| phase-172 | `DONE` | Editor & canvas productivity (see `workspace/phase-172/REPORT.md`): (1) **persistent recents + favorites** — pure-JVM `services/ColorRecentsPolicy.kt` (MAX_RECENT_COLORS=16, MAX_FAVORITE_COLORS=12, dedupe + most-recent-first + fail-closed comma-joined prefs wire format), persisted in `SettingsManager.recentColors`/`favoriteColors`, surfaced as VM StateFlows (`NoteflowViewModel` `recentColors`/`favoriteColors`/`recordRecentColor()`/`toggleFavoriteColor()`), and wired into `ColorPickerBottomSheet` — the old volatile `remember(palette, …)` recents derivation is replaced by the persisted list (padded with session custom colors), a Favorites star (48dp `minimumInteractiveComponentSize` target) toggles the current color, swatch taps `onRecordRecent` + `onColorSelect`, eyedropper samples record too. (2) **minimap quick-actions** — pure-JVM `services/CanvasNavigationPolicy.kt` (budgeted `computeContentBounds` reusing `MinimapGeometryPolicy` strides above `EXACT_BOUNDS_POINT_CAP=20k`, world-clamped `zoomToFit` in the 0.5×..4× zoom window with `FIT_PADDING_PX=48`, `jumpHome`=(1,0,0), degenerate fit falls back home, `shouldAnimate`/reduce-motion gate); `AnnotationCanvas` adds Fit + Home chips to the minimap (scrollable row — phase-166 discipline) animating through the existing `updateZoomAndPan` transform pipeline with a `SpringCanvasPan` spring and SNAPPING under reduce-motion. (3) **layer blend presets** — pure-JVM `services/LayerBlendPresetPolicy.kt` (Normal/Multiply/Screen/Overlay/Soft-Light chips, all renderer-supported, `blendApiQ`/PorterDuff parity test-pinned; the dropdown's 12-mode literal now reads `RENDERER_SUPPORTED_MODES`); the chips drive the SAME `onUpdateLayer(layer.copy(blendMode=…))` → `handleLayersChange` → `saveLayersGated` path, so per-layer opacity/blend persist on the existing `LayerEntity` fields (no schema change). Tests: `ColorRecentsPolicyTest` (13) + `CanvasNavigationPolicyTest` (13) + `LayerBlendPresetPolicyTest` (7). `gradle testDebugUnitTest` 2339 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched, reproduced); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched, base-APK-size book unchanged. |
| phase-173 | `DONE` | Feature: plugin ecosystem — FileTransfer served over LocalSend + invocation journal + honest store metadata (see `workspace/phase-173/REPORT.md`). **Feature 1 — FileTransfer REAL:** `plugins/filetransfer/LocalSendFileTransferPlugin.kt` (id `plugins.filetransfer`, capability `FileTransfer`) sends note-HTML / encrypted vault backup / Obsidian+HTML export via the EXISTING Protocol v2.2 `LocalSendSender` — REUSE not fork — through the new `FileTransferSender` seam (`services/localsend/FileTransferSender.kt`, pure-JVM testable) which `LocalSendSender` now implements; the production sender is built by a host `LocalSendSenderFactory` (`services/localsend/`, so `plugins.*` never references a vault handle — `PluginBytecodeIsolationTest` pin). Fail-closed: typed `FileTransferOutcome` (Sent/Rejected/Error), all descriptions `UiFailureTextPolicy`-scrubbed, opt-in/off by default, discovery never auto-enables the B1-NET-06 `/24` sweep (`includeLegacyHttpScan=false` extended to the seam default sourced from `LocalSendDiscoveryPolicy`). Routing: `NoteflowViewModel.sendFileWithPlugin(...)` → `withPluginAsync(FileTransfer, …)`. **Feature 2 — invocation journal:** `services/PluginInvocationJournal.kt` (bounded 20/plugin, wire `epoch\u0001cap\u0001ok|fail\u0001detail`, `Store` + `NoOpStore`), persisted via `plugin_invocation_journal_<id>` (outside `plugins.<id>.*` so a plugin can't forge its own history; store Delete wipes with the rest), scrubbed end-to-end; `PluginManager` records every invocation + self-check through `SettingsPluginInvocationJournalStore`; `PluginSettingsDialog` renders "Recent activity (last 20)". **Feature 3 — store row metadata:** `services/PluginStoreRowPolicy.kt` — capabilities label (bounded fold, exclusive-first then alphabetical for determinism), shipping bucket, download size / "needs the hosted channel" honesty; wired into `PluginStoreDialog` rows. `metadata.json`: `file_transfer` moved to `servedByCompileTimePlugins`, `unserved` now `[]`. Tests: `FileTransferPluginPolicyTest` (9), `PluginInvocationJournalPolicyTest` (8), `PluginStoreRowPolicyTest` (7), updated `PluginFrameworkTest` (unserved probe now `Assistant`), `Phase131MetadataAlignmentTest`, `B1Net06LocalSendDiscoveryGateTest`. `gradle testDebugUnitTest` 2363 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched; `WikiLinkParserCacheUnitTest` + `Phase151MarkdownMainThreadPerfTest` cancellation/perf flakes re-verified green in isolation); `gradle assembleDebug` green. No schema change, no new deps, `.github/workflows/` untouched. |
| phase-174 | `DONE` | Feature: reading & authoring UX — note stats bar, outline quick-jump, wiki-link autocomplete + slash-menu insert (see `workspace/phase-174/REPORT.md`). **(1) Note-stats footer:** pure-JVM `services/NoteStatsFormatPolicy.kt` (locale-safe `NumberFormat` counts, `~N min read` ceil-to-minute + 1-min floor, blank-note → no line, `MIN_MATERIAL_LENGTH_DELTA=8` recompute guard + `STATS_DEBOUNCE_MILLIS=250`); `MarkdownPreviewScreen` runs `snapshotFlow{contentText}` → `debounce(250)` → `TextToolsAnalyzer.analyze` (single O(n) pass) → `statsLabel` = "1,234 words · ~6 min read · 5,678 chars", static non-animated `Text` footer in every view mode, hidden under reduce-motion + blank notes — never a per-keystroke re-tokenize. **(2) Outline quick-jump rail:** pure-JVM `services/HeadingScrollIndex.kt` (unique occurrence-suffixed labels `Notes (2)`, registered content-offset px); in reader mode the ALREADY-parsed CommonMark doc is DFS-collected into `Heading` order (`collectHeadingNodes`, same order RenderBlocks renders) and indexed ONCE; an anchored collapsible `ReaderOutlineRail` (fixed 168dp — phase-166 360dp-safe; nested scroll ≤300dp) taps a label → `scrollState.scrollTo`/`animateScrollTo` (reduce-motion instant) via the file-local `HeadingMeasureScope`/`LocalHeadingMeasure` that converts each heading's `boundsInRoot().top`-vs-viewport + current scroll into a scroll offset on layout. **(3) Wiki-link `[[` autocomplete:** pure-JVM `services/WikiSuggestionPolicy.kt` (prefix-then-substring ranking, case-insensitive dedup, cap 6, `[`/`]`/`|` syntax-breaking titles excluded, `[[Raw.md|Clean]]` alias snippet, `locateQuery` bounds for the unterminated region); candidates = cached bounded search corpus TITLES ONLY via `NoteflowViewModel.cachedWikiLinkTitles()` (`repository.cachedCorpus()`, no per-keystroke DB reads, empty + fail-closed when locked); `RawBlockEditor` (`HybridMarkdownEditor.kt`) shows an in-field popup for an unterminated `[[` and replaces the whole region on pick; slash menu gains a first-entry "Insert Wiki Link" (`SlashCommandMenu.kt` `onInsertWikiLink`) opening the FLAG_SECURE `WikiLinkPickerDialog` (`ui/components/WikiLinkSuggestions.kt`), both feeding `WikiSuggestionPolicy.wikilinkSnippet`. No new deps, no schema change, `.github/workflows/` untouched. Tests: `Phase174NoteStatsFormatPolicyTest` (10), `Phase174HeadingScrollIndexTest` (7), `Phase174WikiSuggestionPolicyTest` (13) = 30 new; `gradle testDebugUnitTest` 2396 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched, reproduced on clean stash); `gradle assembleDebug` green. |
| phase-175 | `NOT STARTED` | Kali R2-KS-21 (MEDIUM): move ML Kit OCR `assets/mlkit-google-ocr-models/` + `libmlkit_google_ocr_pipeline.so` + ML Kit translate `libtranslate_jni.so` + `res/raw/translate_models_metadata.json` out of the BASE APK into the signature-verified downloadable-plugin runtime (per approved plugin architecture); OCR + translation re-served through the existing capability facades; honest plugin-not-installed fallback. DoD incl. release-APK inventory diff. (Generated by phase-167 triage.) |
| phase-176 | `NOT STARTED` | Kali R2-KS-27 (LOW) + R2-KS-24 (INFO): release packaging hygiene — exclude `DebugProbesKt.bin` / `kotlin-tooling-metadata.json` / `firebase-*.properties` from the release APK payload; retain R8 `mapping.txt` (`app/build/outputs/mapping/release/`) so forensic/crash passes can back-map obfuscated symbols; `extractNativeLibs` decision documented in REPORT; mapping archival in CI deferred (workflow edit needs user approval). (Generated by phase-167 triage.) |
| phase-177 | `DONE` | Plugin ecosystem full review — wiring, opt-in defaults, accurate on/off state, enable/disable/delete correctness (see `workspace/phase-177/REPORT.md`). Complete file:line inventory of the whole surface (`plugins/` core, `plugins/store/`, UI dialogs, `SettingsManager` keys) committed first (step 1, `INVENTORY.md`). **Off-by-default VERIFIED + pinned**: `plugin_enabled_<id>` defaults false (`SettingsManager.kt:447-448`), only the user-facing toggles write it, `installPlugin` flips install-only (`PluginRegistry.kt:428`), routing requires `enabled==true` (`PluginManager.kt:197`), bootstrap/resume/refresh never auto-enable — no plugin ships on by default (`STEP2-VERIFY.md`). **On/off state accuracy VERIFIED**: store rows + settings switch derive from the SAME enable store the router reads; installed ⇔ state ⇔ plugin, off/on mutually exclusive (`STEPS-3-4-5-VERIFY.md`). **Delete** is confirmation-gated on the ONLY delete path (`PluginStoreDialog.kt:535-558`); rejected plugins show Delete/no-Enable; store Delete runs `deleteDownloadedAssets` + wipes opt-in/ever-enabled/journal/consent/entry (`PluginRegistry.kt:441-469`). **FIX (F1)**: stale R2-b2b2-DEP-03 pin red-lined after phase-175 — `:plugins:mlkit` now resolves okhttp-3.0.0/okio-1.6.0 jars (verified, `mlkit:translate`), so the "POM-only, jar never resolves" assertion was updated to require a verified jar or retained POM-only entry (`Phase146BuildIntegrityTest.kt`, `settings.gradle.kts` comment). **New pins**: `Phase177PluginEcosystemReviewTest` (3) — row-state single source of truth over 6 lifecycle stages, `deleteDownloadedAssets` exactly-once, rejected-enable refusal. `gradle assembleDebug` green; `gradle testDebugUnitTest` 2384 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched; `Phase151MarkdownMainThreadPerfTest` + `WikiLinkParserCacheUnitTest` timing/concurrency flakes pass in isolation + in the second full run). No schema change, no new deps, `.github/workflows/` untouched. |
| phase-178 | `DONE` | Reference-image layer (ROADMAP Phase-07 encouraged item, tracked absent in `docs/phase-status-gaps.md`) shipped — see `workspace/phase-178/REPORT.md`. Insert a photo as a dimmed (30–50%, default 40%), non-inking underlay that strokes draw OVER but never modify. Per-page single underlay persisted as a `media_embeds` row (type `REFERENCE_IMAGE`, field-encrypted `textContent` opacity config via `ReferenceImagePolicy`, plain geometry columns) — **no DB schema change**. Excluded from the draggable embed set (`NoteRepository.getCanvasItemsForPage`), carried forward RAW across every delete+reinsert page save (`saveMediaEmbedsForPage`), rendered above paper/template/page-bitmap yet below the ink pass in single/seamless/paginated modes (`AnnotationCanvas`), artwork stored RELATIVE and resolved on read+delete through `InlineImagePathPolicy` (B1-AUTH-05 app-private confinement), ingestion bounded via `AttachmentIngestPolicy`. UI: overflow menu Insert/Remove Reference Image, SAF picker, opacity slider + removal card (`EditorScreen`), `insertPage` shifts the underlay's page index/y. Exports unaffected — the only embed→render export surface draws `PHOTO` only. Tests: `Phase178ReferenceImageUnderlayTest` (6 policy + 5 source-pins); `B2Dos05AttachmentIngestTest` source pin updated 4→5 (the new picker is the 5th bounded ingest site). `gradle assembleDebug` green; `gradle testDebugUnitTest` 2396 total / 1 pre-existing `Phase148UiFailureTextScrubTest` UNC-path failure (untouched). No schema change, no new deps, `.github/workflows/` untouched, base-APK-size rule intact. |

## ROADMAP.md phases (the app's own feature roadmap, 1-35)

| ROADMAP phase | Status | Verified evidence / notes |
|---------------|--------|---------------------------|
| PHASE 1 | `DONE` | Collapsible/floating toolbars, search buttons in panels, sticky notes, minimap, tutorial all present and reachable. |
| PHASE 2 | `DONE` | 21-tool brush suite + stylus guard/pressure sensitivity verified in code. |
| PHASE 3 | `DONE` | Wikilinks, force-directed graph, backlinks/unlinked mentions, tag explorer, daily notes verified. |
| PHASE 4 | `PARTIAL` | Pressure/tilt/timestamp capture FIXED via official `pointerInteropFilter` bridge (truth-table row 4). Sync-badge + tap-stroke-to-jump claim still unverified; image embeds fixed in 21.6. |
| PHASE 5 | `PARTIAL` | Encryption/zeroization/backup rows fixed. Row 16 "master key rotation" still re-wraps the same DEK (MED); biometric non-auth fallback path remains (row 10). |
| PHASE 6 | `DONE` | Cornell template (`EditorScreen.kt:3012`, `AnnotationCanvas.kt:1841`, `ImportExportService.kt:403`), batch vault/section export (`NoteflowViewModel.kt:1072,1082`, `HomeScreen.kt:618,656`). |
| PHASE 7 | `PARTIAL` | 20.1-20.4/20.6-20.9 done (R8 on, RDP, bitmap pool, layer cache, off-main-thread). Baseline profiles deferred (20.5). |
| PHASE 8 | `DONE` | Kanban/Calendar/Gallery/Spreadsheet all wired (`HomeScreen.kt:981-984`). |
| PHASE 9 | `DONE` | Slash command menu (`MarkdownPreviewScreen.kt:544`), callout banners, collapsible headings verified. |
| PHASE 10 | `PARTIAL` | On-device assistant exists; multi-workspace isolation NOT present (no workspaceId/isolation grep hits). |
| PHASE 11 | `DONE` | `FloatingBottomToolbarPill` verified (`EditorScreen.kt:1456,1820`). |
| PHASE 12 | `DONE` | Pressure/tilt captured via official MotionEvent bridge (20.7). |
| PHASE 13 | `DONE` | androidx.ink deps present (`app/build.gradle.kts:129-133`). |
| PHASE 14 | `DONE` | Palette manager + brush presets verified. |
| PHASE 15 | `DONE` | Serialization, export compatibility, fallback rendering verified. |
| PHASE 16 | `DONE` | Layer system + cached layer bitmaps verified (`LayerBitmapCache` wired in 20.3). |
| PHASE 17 | `DONE` | `ShaderCapabilityHelper.isAgslSupported` gating verified. |
| PHASE 18 | `DONE` | `WetBrushEngine` + `WetMixingEffect` wired into AnnotationCanvas (gated by capability + setting). |
| PHASE 19 | `DONE` | Security/honesty recovery shipped (phases 2/3/9): field encryption, real WebDAV sync, FLAG_SECURE, quarantine + corruption recovery, honest relabeling. |
| PHASE 20 | `PARTIAL` | 20.1-20.4, 20.6-20.9 shipped; **20.5 (FTS5 search) deferred — needs user approval (new DB schema)**. |
| PHASE 21 | `PARTIAL` | 21.1/21.2/21.4-21.7/21.9 shipped. **21.3 baseline profiles, 21.8 syntax highlighting (highlighted-kt), 21.10 applicationId/namespace unification deferred — each needs user approval/dependency.** |
| PHASE 22 | `PARTIAL` | 22.1-22.3, 22.6 shipped. **22.4 adaptive 3-pane layout deferred (needs tablet device); 22.5 share-sheet capture + widget still to do.** |
| PHASE 23 | `PARTIAL` | 23.1/23.2/23.4/23.5 verified (continuous canvas branch, shared coordinate space). **23.3 persisting mode choice in `NotePageEntity` deferred — needs Room migration.** |
| PHASE 24 | `PARTIAL` | PDF import fixes + HTML/import + image paths shipped. **24.6 Notion interop explicitly not prioritized/deferred.** |
| PHASE 25 | `PARTIAL` | 25.1-25.6, 25.9-25.10 shipped. **25.7 LaTeX/katex NOT present** (zero katex/MathView hits); **25.8 multi-workspace isolation NOT present.** |
| PHASE 26 | `PARTIAL` | 26.2-26.6 shipped (live markdown, transcription, WebDAV, version history, shape snap). **26.1 handwriting-to-text removed (stub deleted in phase-03) — real impl deferred; 26.7 FTS+OCR deferred.** |
| PHASE 27 | `DONE` | All 27.x bug-fix queue items shipped/refuted (incl. 27.1 refuted by Kali pentest). |
| PHASE 28 | `DONE` | Release engineering shipped: signing config, CI gates, schema export, test foundation, PrivacyCrashReporter, privacy policy docs, targetSdk 36. |
| PHASE 29 | `DONE` | UI bug-fix queue shipped (graph loop, back handler, edge-to-edge, imePadding, 48dp targets, debounce, layer cache, undo O(N²), confetti, polish). |
| PHASE 30 | `DONE` | Theme v2, dynamic color, typography/shapes, motion, adaptive panes, component upgrades, micro-interactions shipped (30.8 is a process note, not a feature). |
| PHASE 31 | `DONE` | Real AGSL wet-mixing wired (see PHASE 18 + phase-04 workspace evidence). |
| PHASE 32 | `PARTIAL` | 32.1-32.8, 32.10 shipped. **32.9 baseline profiles not wired (deferred; old dead file deleted in phase-03).** |
| PHASE 33 | `DONE` | `docs/COMPATIBILITY.md` + `docs/TUTORIAL.md` exist, tier system + fallback rules + config-changes cleanup shipped. |
| PHASE 34 | `PARTIAL` | 34.0-34.8 shipped (restore hardening, re-key, lock-zeroization, relabel, zip-bomb, FileProvider, ClipboardGuard, HMAC re-arm). **34.9 dynamic re-run with device/emulator still open.** |
| PHASE 35 | `CANCELLED` | Shelved then removed in the phase-03 honesty audit (HardwareProfiler/RenderingEngineTier/BrushEngine facade/JNI stub all deleted). |
| PHASE 36 | `DONE` | Micro-interactions & fluid motion shipped: shared-element card→editor morph (`ui/components/FluidPageReveal.kt`, `HomeScreen.kt` card-bounds capture, `MainActivity.kt:350`), milestone haptics gated by new `SettingsManager.hapticsEnabled` + reduce-motion (`services/MotionPolicy.kt`, AnnotationCanvas snap tick, `ColorSwatch` detent, width-slider notch), tuned springs (`MotionSystem.SpringReveal/SpringDismiss/SpringCanvasPan/SpringSheet`). `MotionPolicyTest` = 10 tests. See `workspace/phase-36/REPORT.md`. |

## Cross-cutting facts

- All `.done` markers in `workspace/` live in `phase-01`..`phase-16` and `phase-18`..`phase-114` (verified by directory listing). The phase-77 `.done` was a FALSE COMPLETION (commit `c67707b`, junit-bom hashes only) that the phase-115 sweep removed so the pipeline could re-select it — **phase-77 is now genuinely `DONE`** (2026-08-17, B2-DEPS-05, real fix + tests + `REPORT.md`). No `.deferred`/`.blocked`/`.attempts`/`.no_work`/`.session` markers exist for the DONE phases; the only `.blocked`/
`.no_work` markers live in phases that genuinely could not run: **phase-118** (dynamic PIE/anti-debug verification, no rooted Android env). **phase-160** originally belonged to this list (its Kali dynamic scope, `.attempts`=3, `.timeout`=360) but was RE-UNBLOCKED (commit `b1f533f`) as a STATIC Kali pass and is now `DONE` with 27 evidence-backed findings — see the phase-160 row and the `DYNAMIC-DEFERRED` D1-D12 list in `docs/kali-report-round2.md` (the earlier "APK-metadata-only / no dynamic findings" wording is superseded).
  - **2026-08-15 (phase-32 review fix): `workspace/phase-32/.done` was removed — a false completion (marked `.done` with no code/docs/findings; commits `1ce5a44` reset + `6b17422` re-added). `scripts/phase_runner.sh` now gates `.done` on a working-tree evidence check (changes outside `logs/` + the phase's own markers), so a phase can no longer self-certify DONE with only logs; a zero-work run is counted as a failed attempt and leaves a `.no_work` marker for diagnosis.**
  - **2026-08-15 (phase-32 already-done guard): commits `44a7210`+`27b93fd` re-selected the completed phase-32 and left contradictory `.no_work`/`.deferred`/`.deferred_attempts`/`.session` alongside its legitimate `.done`. `phase_runner.sh` now short-circuits normal-run mode when `.done` exists and clears those stale markers; the stale phase-32 markers were removed here so select-phase stops re-selecting it.**
  - **2026-08-15 (phase-42 review fix): commit `65bf35e` re-added stale `.no_work`/`.deferred`/`.deferred_attempts`/`.session` markers alongside phase-42's legitimate `.done` (the phase-32 scenario again). Removed here; only `.done` remains. See `workspace/phase-42/REPORT.md` Addendum 2.**
- `phase-17` is the only phase whose directory no longer exists (deleted 4112eb8) and is therefore marked `CANCELLED` and excluded from heading marking.
- ROADMAP/workspace numbering differ: workspace `phase-NN` (audit pipeline) ≠ ROADMAP `PHASE N` (app feature roadmap). Do not confuse them.
- `phase-20` (the earlier doc-fix) is `DONE`. `phase-21`..`phase-38` and every fix phase `39`..`114` are all `DONE` — verified in the **phase-115 doc sweep** (2026-08-17) against git commits + REPORT.md + test classes; their 86 PROMPT headings were updated from `[NOT STARTED]` to `[DONE]` in that sweep. **`phase-77` was the one genuine false completion** — its `.done` (commit `c67707b`) only added `gradle/verification-metadata.xml` junit-bom hashes (no code/test/REPORT; `AssistantModelDownloader.kt:72` still `instanceFollowRedirects = true`, no SHA-256 pin, `expectedSizeBytes` never compared) and was REMOVED in that sweep; it is now re-executed for REAL (2026-08-17, B2-DEPS-05 — pinned GGUF identity + testable download pipeline, see the phase-77 row above and `workspace/phase-77/REPORT.md`).
- Sources: `workspace/phase-14/AUDIT_REPORT.md`, `workspace/phase-08/PERF_REPORT.md`, `ROADMAP.md` POST-AUDIT TRUTH TABLE + per-phase checklists, git history (`git log -S` / `git show --stat`), direct source inspection.
